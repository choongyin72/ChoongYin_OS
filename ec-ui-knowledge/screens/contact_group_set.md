# Screen: Contact Group Set

- **Type:** OV-GM (manage-object, groupmodel), date-effective (`CLASS_TYPE=OBJECT`/`TIME_SCOPE_CODE=VERSIONED`).
- **BF_CODE:** CO.0225 - **Treeview:** Configuration > Messaging > Maintain Contact Group Set
- **DB base/version:** `CONTACT_GROUP` / `CONTACT_GROUP_VERSION`
- **DB view:** `OV_CONTACT_GROUP_SET` (key `CODE`)
- **Last verified:** 2026-08-03 - EC 14.2.4 - local sandbox - `verify_screen.py` OVERALL PASS
  (RF 4/4 pass + Playwright 8/8, DB-verified, self-clean, full I-U-D). First live run passed cleanly,
  no fixes needed.

## Selectors
| Purpose | Selector |
|---|---|
| Open | search `Maintain Contact Group Set` -> `label.tv-link` "Maintain Contact Group Set" **(NOT** "Contact Group Set" - see Quirks) |
| Navigator | ONE mandatory Functional Area dropdown (`nav:form:G:0:R:1:C:1:dd`, first-available resolves to "Administration") + GO |
| Grid | `manageObject:form:T_data` |
| Insert form | Contact Group Set Code (mandatory) + Name (mandatory) + Start Date + Functional Area (parent_dd - must equal the nav-captured value) |
| Delete | `objectdates` End Date = Start Date |

## Quirks
- **The class's own `class_property_cnfg` LABEL ("Contact Group Set") does NOT match the live
  treeview screen title ("Maintain Contact Group Set").** A search for the exact LABEL text finds no
  clickable screen. Confirmed the real title via a live partial-match menu search first, then
  verified via `scan_ec_screen.py` with that real title. Worth checking for any future screen
  discovered via a class-name-first (not screen-name-first) audit.
- Otherwise a completely standard, low-risk OV-GM build - same family (Functional Area nav +
  parent_dd binding) as Message Group (CO.0236), same folder (Configuration > Messaging). The
  `gen_ovgm.py` template + Message Group's proven config transferred directly with zero fixes needed.

## Automation (code in ec-automation)
- **Playwright:** `py/contact_group_set_iud.py` (generated by `gen_ovgm.py`, `parent_dd="Functional
  Area"`, `nav_levels=1`, `start_date="2020-01-01"`).
- **RF:** T3 `pageobjects/Configuration/Messaging/contact_group_set_page.resource` + suite
  `tests/Configuration/Messaging/contact_group_set_iud.robot`.
- **Gate:** `verify_screen.py` -> OVERALL PASS.
