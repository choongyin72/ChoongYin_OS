# EC Automation Scorecard
_Last updated: 2026-08-16_

## N-Series Patterns (Production Data Interaction)

| Pattern | Variant | Object Type | Status | Notes | Date |
|---------|---------|-------------|--------|-------|------|
| N1 | Daily status edit-in-place | PWEL (WR.0001) | ✅ Live 3/3 | Reference impl | - |
| N1 | Daily status edit-in-place | STRM (PO.0002) | ✅ Live 3/3 | | - |
| N1 | Daily status edit-in-place | STRM-OIL (PO.0001) | ✅ Live 3/3 | Sibling of gas stream; "Oil"=generic-EC name for Pluto "Daily Liquid Stream Status"; C5=GRS_VOL; scope P1 @ 2023-01-01 (P1 Cond); reuses T2 verbatim (no shared-file change) | 16 Jun |
| N1 | Daily status edit-in-place | STRM-WAT (PO.0003) | ✅ Live 3/3 | Sibling of gas/oil stream; Daily Water Stream Status; C3=GRS_VOL; scope P1 @ 2023-01-01 (P1 S087); reuses T2 verbatim (no shared-file change); stacked on PR #35 | 17 Jun |
| N1 | Daily status edit-in-place | STRM-ELE (PO.0066) | ✅ Live 3/3 | Sibling; Daily Electrical Stream Status; **C2=POWER_CONSUMPTION** (no volume — electrical); scope P1 @ 2011-01-01 (P1 S046); reuses T2 verbatim (no shared-file change); stacked on PR #36 | 17 Jun |
| N1 | Daily status edit-in-place | TANK-VCF (PO.0005.02) | ✅ Live 3/3 | TANK variant; Daily Tank Status - VCF Calc; grid `dip_table:form`; **C4=Liquid Dip → DV_TANK_DAY_DIP_STATUS.LIQUID_DIP_LEVEL** (view, no base table); name is an INPUT (C1) so row-find by C1 value, not textContent; save triggers VCF recalc; OV_TANK; scope P1 @ 2011-01-01 (P1 T006); reuses T2 verbatim (no shared-file change) | 17 Jun |
| N1 | Daily status edit-in-place | IWEL | ✅ Live 3/3 | | - |
| N1 | Daily status edit-in-place | EQPM | ✅ Live 3/3 | Non-iframed; C4=AVG_PRESS | 14 Jun |
| N1 | Sub-daily status edit | PWEL | ✅ Live 3/3 | Datetime-keyed PK; UI↔DB unit conversion (~14.5x pressure) | 14 Jun |
| N1 | Daily status edit-in-place | PFLW (flowline) | ✅ Live 3/3 | Date-range nav (From+To); 4-level cascade; ON_STREAM_HRS unitless | 15 Jun |
| N1 | Sub-daily status edit | STRM | ✅ Live 3/3 | Datetime-keyed PK; GRS_VOL unitless; doc errors fixed (R7) | 15 Jun |
| N1 | Daily status edit (**UPDATE-only**) | IFLW (water inj flowline) | ✅ Live 4/4 | New/Delete toolbar DISABLED (no record insert/delete — business-domain nature); suite edits the value set/change/clear (clear=update-to-null); sibling of PFLW; IFLW_DAY_STATUS; C2=ON_STREAM_HRS; menu-mirrored path; Bank-style bundle | 15 Jun |
| N1 | Daily status edit (**UPDATE-only**) | GIFLW (gas inj flowline) | ✅ Live 4/4 | Sibling of WI (same grid); IFLW_DAY_STATUS INJ_TYPE=GI; P1 F004 GI; set/change/clear; menu-mirrored path; Bank-style bundle; **stacked on PR #24** | 15 Jun |
| N1 | Monthly status edit | PWEL | ⏸ Parked | Sparse data (13 rows), poor target | 14 Jun |
| N3 | Status process P→V | HA.0001 Daily | ✅ Live 2/2 | Dual DB oracle: ROWS_UPDATED + RECORD_STATUS count | 14 Jun |
| N3 | Status process V→A | HA.0001 Daily | ⏸ Parked | STIM_DAY_VALUE empty (0 rows); WELL_FLUID_ANALYSIS needs WHERE vars + data/SME — next: seed data | 15 Jun |
| N3 | Status process V→A | HA.0001 Monthly | 🔵 Build-ready | Thin new T3 for Monthly Data Status Processes screen | 14 Jun |
| N3 | Status process →A (month) | Monthly Data Status Processes (P1_FwdUpdPar1) | 🔵 Built, dryrun-green, live GATED | Screen model verified live (single Date G:0 + Process G:1); target IWEL_DAY_STATUS 19,659 P rows (data EXISTS); suite `monthly_status_process_run.robot` + thin T3; live behind `--variable LIVE_OK:yes` (no-WHERE → large reversible mutation; confirm blast radius + oracle grain on first observed run) | 15 Jun |
| N1 | Composition edit (per-component) | STRM-GAS-COMP (PO.0020) | ✅ Live 3/3 | NEW pattern: per-COMPONENT rows (not object×day); 8-field nav + `go_button:form:B`; grid loads on Analysis Status=Approved + Sampling=*Spot; edit Methane MOL_PCT (COMPONENT_NO=C1) → `DV_STRM_COMP_ANALYSIS`; Ethane(C2) guard proves no normalize-on-save; new append-only DbVerify `component_value_should_be`; target P1 S038_AGA3_1985_AGA8_Y_1 @ 2011-11-01 | 17 Jun |
| N1 | Composition edit (per-component) | STRM-OIL-COMP (PO.0019) | ✅ Live 3/3 | Oil/condensate sibling of PO.0020; edits **WT_PCT** (cell `C2_in`; oil's C1=mol% empty); Facility Class 1=**P1 Facility Allocation** (not P1 Facility 1); target P1 Alloc S001 M OIL @ 2023-06-01; reuses `component_value_should_be` (col=WT_PCT) — no DbVerify change/no canary; Ethane(C2) guard; TC03 reload-before-revert (2nd Save won't re-arm same session); synthetic wt% data (accepted; realistic=later) | 17 Jun |
| N1 | Composition edit (per-component) | WELL-GAS-COMP (WR.0010.01) | ✅ Live 3/3 | Well-level gas-comp sibling of PO.0020; mandatory (yellow) nav only = Date+PU+Area+Facility → GO; **NEW: SELECT the analysis row** in the header grid (lists all date-valid analyses) before components load; edit Methane MOL_PCT (C1, cell `C1_in` like gas) → `DV_WELL_COMP_ANALYSIS` (ANALYSIS_TYPE=WELL_GAS_COMP); Ethane(C2) guard; reuses `component_value_should_be` (no DbVerify change/no canary); name source WELL_VERSION; TC03 reload+RE-SELECT before revert; target P1 W260 GP Comp Gas @ 2025-04-01 (synthetic 0.1) | 17 Jun |
| EVENT | Alarm event-log (gated inline grid) | ALARMS (`FCTY_DAY_ALARM`) | ✅ Live 4/4 (2026-06-19) | **NEW pattern**: gated cascade nav (Date+PU+Area+Facility+GO) + inline-grid INSERT/UPDATE/DELETE; **no object code → unique REASON marker oracle** (`View Count Where DV_ALARMS REASON`); physical delete; reuses T2 `table_class`; full IUD bundle + SOW; treeview EC Production > Production Operations > Event | 19 Jun |

### Key Learnings
- **UI↔DB unit conversion**: UI(psi) ↔ DB(bar), factor ~14.5038. Naive "DB == typed" oracle fails on pressure/rate cols.
- **Sub-daily PK**: PWEL_SUB_DAY_STATUS keyed on (OBJECT_ID, DAYTIME[+time], SUMMER_TIME) — not TRUNC(date). Needs datetime-keyed DbVerify.
- **ec-worker**: Must be running (overlay 12); scheduler STANDBY on front-end nodes. ORA-06569 = empty data scope, not mechanism failure.
- **Data recovery**: Oracle Flashback AS OF TIMESTAMP used successfully for 192-cell restore.

---

## Section Coverage (Screen Automation)

| Section | Screens | Status | Notes |
|---------|---------|--------|-------|
| Basic Objects | 12/12 | ✅ Complete | Reference IUD suite |
| Financial Objects | 15/15 | ✅ Complete | All suites present (validated 2026-06-15) |
| Commercial Objects | ~11 screens | 🟡 Mostly complete | Sub Field parked |
| Account Mapping | 14/14 | ✅ Complete | Financial; unparked after start-date discovery |
| MIME Type Mapping | TV | ✅ Complete | Table-class/TV, both frameworks |
| Equipment | OV | ✅ Complete | 2nd OV screen |
| Language | IUD | ✅ Complete | 2nd Table-class |
| Dispatching Objects — slice 1 | 24/24 | ✅ Complete | 6 BU-gated OV-GM screens |
| Dispatching Objects — slice 2 | Nomination (TV) + Meter (popup) + Pipeline Segment | ✅ Live 5/5 (Meter **FULL Area-pattern conversion, 2026-08-26**; Pipeline Segment **FULL Area-pattern conversion, 2026-08-26**) | Pipeline (the parent screen) parked. Meter: corrects an earlier wrong "DOES NOT FIT Area" classification — the navigator itself (`nav:form:G:0:R:1:C:1:dd`, Business Unit) is a single dropdown identical in shape to Area's own Production Unit navigator; the Delivery Point popup lives entirely on the insert form, orthogonal to the navigator, same situation as Chemical Stream's From Connection popup. Converted from the older 4-TC/suite-login/generated-code/inline-DB-verify pattern to properties-file-driven insert/update/verify, per-TC login (`METER_EC_USER`/`METER_EC_PASS`), fixed code `AUTOTEST_METER`, explicit `Find/Clear Meter Row By Filter` (15 hits in output.xml), TC04 Find added, zero inline DB-verify calls; full `tests/` dryrun 874/874; robocop 10 issues (7 on the changed screen files = exact parity with Area's own 7-issue baseline, +3 pre-existing unrelated credentials.py findings); DB self-clean confirmed (0 residual `AUTOTEST_METER%` via a fresh oracledb connection after the live run). Pipeline Segment: re-confirmed live (2026-08-26, temp recon robot deleted before commit) that the navigator is a single mandatory Business Unit dropdown at `nav:form:G:0:R:1:C:1:dd` — the SAME single-dropdown shape as Area's own Production Unit navigator (a 2nd dropdown at `C:2`, labelled "Pipeline", is an OPTIONAL grid filter, NOT mandatory). Converted from the older 4-TC/suite-login/generated-timestamp-code/inline-DB-verify pattern to properties-file-driven insert/update/verify (`testdata/pipeline_segment_navigator.properties`/`pipeline_segment_insert.properties`/`pipeline_segment_update.properties`/`pipeline_segment_form_verify.properties`/`pipeline_segment_grid_verify.properties`), per-TC login (`PIPELINE_SEGMENT_EC_USER`/`PIPELINE_SEGMENT_EC_PASS` added to `resources/credentials.py`), fixed test code `AUTOTEST_PIPELINE_SEGMENT` (confirmed free in `OV_PIPELINE_SEGMENT` via a fresh oracledb connection before the build), explicit `Find/Clear Pipeline Segment Row By Filter` wired into Update/Find/Verify-Found/Delete (15 hits confirmed via output.xml grep), TC04 Find added (now 5 TCs), zero inline DB-verify calls in the `.robot` file. Full `tests/` dryrun 878/878 pass; robocop parity with Area (same VAR02+DOC02 kind/count, 7 issues); DB self-clean confirmed (0 residual `AUTOTEST%` rows in `OV_PIPELINE_SEGMENT` via a fresh independent oracledb connection run AFTER the live run). No shared T1/T2 edits |
| Dispatching Objects — slice 2 | Nomination (TV) + Meter (popup) | ✅ Live 5/5 (Meter **FULL Area-pattern conversion, 2026-08-26**) | Pipeline parked. Meter: corrects an earlier wrong "DOES NOT FIT Area" classification — the navigator itself (`nav:form:G:0:R:1:C:1:dd`, Business Unit) is a single dropdown identical in shape to Area's own Production Unit navigator; the Delivery Point popup lives entirely on the insert form, orthogonal to the navigator, same situation as Chemical Stream's From Connection popup. Converted from the older 4-TC/suite-login/generated-code/inline-DB-verify pattern to properties-file-driven insert/update/verify, per-TC login (`METER_EC_USER`/`METER_EC_PASS`), fixed code `AUTOTEST_METER`, explicit `Find/Clear Meter Row By Filter` (15 hits in output.xml), TC04 Find added, zero inline DB-verify calls; full `tests/` dryrun 874/874; robocop 10 issues (7 on the changed screen files = exact parity with Area's own 7-issue baseline, +3 pre-existing unrelated credentials.py findings); DB self-clean confirmed (0 residual `AUTOTEST_METER%` via a fresh oracledb connection after the live run) |
| Dispatching Objects — Transport Zone | Transport Zone (OV-GM, BU-gated) | ✅ Live 5/5 (**FULL Area-pattern conversion, 2026-08-26**) | Part of the slice-1 24/24 BU-gated OV-GM group; individually rebuilt from its older 4-TC/suite-login/generated-code/inline-DB-verify build. Live recon (read-only) confirmed the navigator's G:0 has 3 columns: C:0 Date (mandatory:true, already defaulted, no fill needed), C:1 Business Unit dropdown (mandatory:true, genuinely empty — the ONLY field needing a fill), C:2 a 2nd dropdown (mandatory:false, optional filter — GO succeeds with C:2 empty once C:1 is set) — single-dropdown/same-row-cascade shape, same as Area. Converted to properties-file-driven insert/update/verify, per-TC login (`TRANSPORT_ZONE_EC_USER`/`TRANSPORT_ZONE_EC_PASS`), fixed code `AUTOTEST_TRANSPORT_ZONE` (confirmed free in `OV_TRANSPORT_ZONE` via a fresh oracledb connection before the build), explicit `Find/Clear Transport Zone Row By Filter` (15 hits in output.xml), TC04 Find added, zero inline DB-verify calls; full `tests/` dryrun 875/875; robocop 10 issues (7 on the changed screen files = exact parity with Area's own 7-issue baseline, +3 pre-existing unrelated credentials.py findings); DB self-clean confirmed (0 residual `AUTOTEST%` rows in `OV_TRANSPORT_ZONE` via a fresh oracledb connection after the live run) |
| Contract Objects | Contract Area (OV-GM, BU-gated) | ✅ **FULL Area-pattern conversion, live 5/5 (2026-08-26)** | 1st Contract Objects screen; built by ec-object-iud-builder; sibling of Transport System; converted from OLD (suite-login/4-TC/inline-nav/inline-DB-verify) to Area's properties-file-driven/T2-consolidated/per-TC-login 5-TC shape, DB self-clean confirmed, 0 residual |
| Cargo Objects | Carrier (OV, plain) — Bank-pattern conversion | ✅ Rebuilt live 5/5 (2026-08-23, Batch 11) | Converted from the older hardcoded-field-id (`Fill New Object Form`) pattern (built 2026-06-19 by ec-object-iud-builder, then ✅ Live 4/4) to the label-driven, properties-file-driven, T2-consolidated Bank/Berth/Port pattern; `OV_CARRIER`, grid `manage_object_nav_nav:form:T_data`; navigator re-confirmed NOT gated (optional date + GO only, no mandatory dropdown/cascade) via the prior SOW recon + proven Playwright driver before conversion; screen-prefixed labels ("Carrier Code"/"Carrier Name") confirmed via the same prior recon; mandatory reference dropdown "Unit" filled `__FIRST__` at Insert (throwaway record) but deliberately excluded from `@{CARRIER_FORM_LABELS}` so the known `__FIRST__`-fails-round-trip-verify gotcha never applies (Batch 2 VAT Code precedent); fixed test code `AUTOTEST_CARRIER`; explicit grid-filter wiring (`Find/Clear Carrier Row By Filter`, output.xml confirms 14 `Find Carrier Row By Filter` hits); no shared T1/T2 edits; dryrun 772/772 pass; DB-verified (fresh oracledb query on `CODE` column of `OV_CARRIER`) + self-cleaning (independent fresh-connection re-read = 0 residual `AUTOTEST_CARRIER` rows) |
| Laboratory Objects | Analysis Point (OV-GM, 3-level cascade) | ✅ Live 4/4 (2026-06-19) | 1st Laboratory Objects screen; built by ec-object-iud-builder; clean OV-GM (Op PU/Area/Facility settable in form) |
| Royalty Objects | Royalty Owner (OV, manage-object) — Bank-pattern conversion | ✅ Rebuilt live 5/5 (2026-08-23) | Batch-5 conversion (with Sales Order/Product Group/Royalty Depositor/Unit Agreement, in parallel isolated worker clones, see `tmp/batch5_shared_findings.md`) from the older hardcoded-field-id/generated-code pattern to Bank/State's label-driven, properties-file-driven, T2-consolidated pattern; `OV_ROYALTY_OWNER`; grid `manage_object_nav_nav:form:T_data` (no navigator); labels confirmed live as screen-prefixed "Royalty Owner Code"/"Royalty Owner Name" (not generic "Code"/"Name", same as State/County); fixed test code `AUTOTEST_ROYALTY_OWNER`; explicit grid-filter wiring (`Find/Clear Royalty Owner Row By Filter`) included from the start, output.xml confirms 5 `Find Royalty Owner Row By Filter` hits; no shared-file edits; DB-verified (fresh oracledb query on `CODE` column of `OV_ROYALTY_OWNER`) + self-cleaning (independent fresh-connection re-read = 0 residual `AUTOTEST_ROYALTY_OWNER` rows) |
| Royalty Objects | Royalty Depositor (OV, plain) — 2/8 | ✅ Live 5/5 (2026-08-23, Batch 5 Bank-pattern conversion) | 2nd of 8 Royalty Objects screens; converted from the old hardcoded-field-id driver to the label-driven, properties-file-driven, T2-consolidated Bank pattern (`OV_ROYALTY_DEPOSITOR`, no nav dd, End=Start delete); screen-prefixed labels ("Royalty Depositor Code/Name") confirmed live; grid-filter wiring included from the start; no shared-file edits; DB-verified + self-cleaning. 2026-08-25 alignment fix: removed the leftover inline `Royalty Depositor Should Exist In DB` keyword + its TC02 call (also uppercased a stray `ov_royalty_depositor` prose reference to the house `OV_ROYALTY_DEPOSITOR` convention) - violated Bank's pure-screen-only verification convention (2026-08-18), same deviation class as DOA Credit Limit (PR #503); re-verified live 5/5, dryrun 841/841, DB self-clean 0 residual |
| Royalty Objects | Product Group (OV, plain) — 3/8 | ✅ Live 5/5 (2026-08-23, Batch 5 Bank-pattern rebuild) | Rebuilt from the older hardcoded-field-id pattern to the label-driven, T2-consolidated Bank pattern (mirrors State); `OV_PRODUCT_GROUP`, screen-prefixed labels "Product Group Code"/"Product Group Name"; only Start Date mandatory beyond Code/Name (Sort Order/Product Group Type/Comments optional, left out of IUD flow); explicit grid-filter wiring included from the start; no shared-file edits; DB-verified + self-cleaning |
| Royalty Objects | Unit Agreement (OV, plain) — 4/8 | ✅ Live 5/5 (2026-08-23) | 4th of 8 Royalty Objects screens; converted (Batch 5) from the older hardcoded-field-id generator build to the label-driven, properties-file-driven, T2-consolidated Bank pattern (mirrors `bank_page.resource`); real labels confirmed live: "Unit Agreement Code"/"Unit Agreement Name" (screen-prefixed, not generic "Code"), plus an optional "Comments" field and an objectForm-level "End Date" field (left unset at insert); `OV_UNIT_AGR` (view != slug); grid-filter wiring included from day one; no shared-file edits; DB-verified + self-cleaning (0 residual, fresh connection) |
| Royalty Objects | Tract (OV-GM, gated) — 5/8 | ✅ Live 4/4 (2026-06-26), then ✅ **FULL Area-pattern conversion, 2026-08-26** | 5th Royalty Objects screen, 1st OV-GM; gated by Unit Agreement nav dd; clone of Transport System; `OV_TRACT`; 2 OV-GM gotchas solved (date-effective parent 2010 -> form date 2011; lazy-redraw extra Apply Navigator); DB-verified + self-cleaning; stacked on PR #122. **2026-08-26: corrects PR #555's own first commit**, which had wrongly declined this conversion reasoning the navigator's two separate `nav:form:G:` groups (`G:0` Date, `G:1` Unit Agreement) didn't fit Area's single-group cascade shape. Live re-recon found `G:0`'s Date field already carries a non-empty default on load (needs no fill) and `G:1`'s Unit Agreement dropdown (`nav:form:G:1:R:1:C:0:dd`, confirmed at COLUMN 0 not 1) is the ONLY mandatory-and-empty nav field - structurally the same single-mandatory-dropdown shape as Area's own navigator. Extended the shared T2 `Apply Navigator From Properties` (resources/manage_object.resource) with two new OPTIONAL, backward-compatible arguments (`${group}=0`, `${start_col}=1`), proven via a full-tree dryrun (874/874 unchanged before/after) plus live 5/5 regression canaries on Area and Meter (existing callers of the old 2-arg form). Full 5-TC rebuild: per-TC Login/Logout (`TRACT_EC_USER`/`TRACT_EC_PASS`), fixed `AUTOTEST_TRACT` (confirmed free live via a fresh oracledb connection), properties-file-driven insert/update/verify (`testdata/tract_{navigator,insert,update,form_verify,grid_verify}.properties` - Unit Agreement value identical in the navigator and insert files per the owner's field-reuse rule), explicit `Find/Clear Tract Row By Filter` (15 hits in output.xml), zero inline DB-verify calls. Re-confirmed live: Unit Agreement 1-4 all effective 2010-01-01 (`OV_UNIT_AGR.OBJECT_START_DATE` via a fresh oracledb query), Start Date 2011-01-01 reused from the prior driver. Live 5/5, full-tree dryrun 875/875, robocop 7 issues on the changed screen files = exact parity with Area's own 7-issue baseline, DB self-clean confirmed (0 residual `AUTOTEST_TRACT` via a fresh oracledb connection after the live run). Wrongly-added Tract row removed from `docs/navigator-screens-not-matching-area.md`. |
| Royalty Objects | Unit - Well Setup (PC, gated) — 6/8 | ✅ Live 4/4 full I-U-D (2026-06-27) | 6th Royalty Objects screen, 2nd PC (after Object List Setup); gated by Unit Agreement nav dd; clone of Object List Setup; view `DV_UNIT_WELL_SETUP` (count-delta on `PERF_INTERVAL_CODE`); **INSERT + UPDATE (edit COMMENTS `C3_in`, verified via present-in-view) + physical DELETE**; saved-row select via `C0_in` (NEW row calendar vs SAVED text) cost 1 live run then fixed; UPDATE added after user feedback (initial ship was I/D only — IUD requires all three); test pair UNIT_3 (empty) × 108_WB1-1_PF1; no shared-file edits; DB-verified + self-cleaning |
| Calculation Objects | Calculation Context (OV, manage-object) — Bank-pattern completion (Batch 7) | ✅ Live 5/5 (2026-08-23) | Batch-7 (NEW round beyond the closed 23-screen/37-37-filter pool, see `tmp/batch7_shared_findings.md` and `docs/grid-filter-standardization-checklist.md`'s "Batch 7 additions" section); brought an already-partially-label-driven screen up to FULL Bank-pattern parity: added properties-file-driven Insert/Update (`testdata/calculation_context_*.properties`) + explicit `Find/Clear Calculation Context Row By Filter` grid-filter wiring; `OV_CALC_CONTEXT`, grid `manage_object_nav_nav:form:T_data` (no navigator cascade beyond the universal GO); live-recon-confirmed labels "Calc Context Code"/"Calc Context Name", Description/Comments optional fields; fixed test code `AUTOTEST_CALCCTX` (confirmed free live); no shared T1/T2 edits; DB-verified (`Field Should Equal In View OV_CALC_CONTEXT` on NAME/DESCRIPTION/COMMENTS) + self-cleaning (0 residual rows via a fresh oracledb connection); filter keyword confirmed fired via output.xml grep; robocop clean; dryrun 753/753. 2026-08-25 alignment fix: removed the leftover inline `Calculation Context Should Exist In DB` keyword + its TC02 call, plus 5 direct `Field Should Equal In View` calls across TC02/TC03 - violated Bank's pure-screen-only verification convention (2026-08-18), same deviation class as DOA Credit Limit (PR #503); coverage unchanged (T2's `Verify Calculation Context Record Exists/Updated` already compares the same fields on-screen); re-verified live 5/5, dryrun 841/841, DB self-clean 0 residual |
| Royalty Objects | Tract - Well Setup (PC, cascade) — 7/8 | ✅ Live 4/4 full I-U-D (2026-06-27) | 7th Royalty Objects screen, 3rd PC; sibling of Unit - Well Setup (PR #130) over same `WELL_SETUP` base; CASCADE nav (Unit Agreement -> Tract); view `DV_TRACT_WELL_SETUP` (count-delta on `PERF_INTERVAL_CODE` + COMMENTS present-in-view for UPDATE); INSERT+UPDATE+physical DELETE; live 4/4 first run (RC.0050 lessons applied: full I-U-D scope, C0_in saved-row select, pre-flight); no empty Tract -> baseline-0 member under existing Unit 3 Tract 01, existing P1 PI-5/PI-6 verified intact; no shared-file edits; DB-verified + self-cleaning |
| Royalty Objects | Product Group Setup (3-tier, multi-entity) — 8/8 | ✅ Live 10/10 full I-U-D x3 (2026-06-27) | 8th/LAST Royalty Objects screen + most complex (3-tier master->detail->sub-detail, tab-gated, NO nav); ALL 3 sub-entities full I-U-D: Product Group Setup (`DV_PRODUCT_GROUP_SETUP`), Product Group Cost (`DV_PRODUCT_GROUP_COST`), Stream Calc Category (`PRODUCT_STRM_BAL_CAT` — label!=table); COMMENTS-sentinel oracle; 3 gotchas solved (label!=table; silent-reject->fill mandatory cells; 2nd-save-no-rearm->reload before U/D); no shared-file edits; DB-verified + self-cleaning. **Royalty Objects batch COMPLETE 8/8.** |
| Date Objects | Document Date Term (OV, plain) — 1/5 | ✅ Live 4/4 (2026-06-28), then ✅ **rebuilt to Bank-pattern 2026-08-24** (PR #493) - RF 5/5 live, properties-file-driven insert/update/verify, explicit grid-filter wiring, fixed AUTOTEST_DOCUMENT_DATE_TERM code, per-TC Login/Logout, added missing TC04; grid auto-loads (Excluded-table entry corrected); DB self-clean 0 residual via fresh connection | 1st of 5 Date Objects screens; **pilot for the 19-item IUD deliverable standard** (bundle CHECKLIST.md + reviewer gate); built by ec-object-iud-builder; Bank clone + 2 extra mandatory New-Object inputs (METHOD autocomplete dd + numeric OFFSET, filled last); `OV_DOC_DATE_TERM`; reuses T2 `manage_object` + shared `Select EC Dropdown Option`; no shared-file edits; DB-verified + self-cleaning |
| Date Objects | Document Received Term (OV, plain) — 2/5 | ✅ Live 4/4 (2026-06-28), then ✅ **rebuilt to Bank-pattern 2026-08-24** | Converted from the older hardcoded-field-id (`Fill New Object Form`) pattern to the label-driven, properties-file-driven, T2-consolidated Bank pattern (mirrors `bank_page.resource`/`berth_page.resource`/`port_page.resource`); live navigator scan confirmed the single date field is OPTIONAL (mandatory=False) + GO, NOT a genuine mandatory-date gate (corrects its prior grouping alongside Document Date Term/Payment Term/Choke/Choke Model in `docs/bank-pattern-conversion-checklist.md`'s "Excluded" table — see that doc's own reclassification note); GENERIC "Code"/"Name" labels (not screen-prefixed, confirmed live); mandatory beyond Code/Name/Start Date: "Date Calculation Method" autocomplete dropdown (test `Manual entry`) + numeric Offset (test `0`), matching the proven Playwright driver; fixed test code `AUTOTEST_DRT` (confirmed free live); explicit `Find/Clear Document Received Term Row By Filter` wiring included, added the missing TC04 Find; `OV_DOC_RECEIVED_TERM`; no shared-file edits; live 5/5, dryrun 794/794, filter fired 23x (output.xml grep), DB self-clean confirmed 0 residual via a fresh oracledb connection |
| Date Objects | Payment Term (OV, plain) — 3/5 | ✅ Live 4/4 (2026-06-28) then ✅ Rebuilt to Bank-pattern 2026-08-24 — RF 5/5 live, properties-file-driven insert/update/verify, explicit grid-filter wiring | 3rd of 5 Date Objects screens; richer form than the term screens (recon caught **shifted field rows**: Method R7, mandatory DAY_VALUE R8, optional Calculation dd R9); reuses T2 `manage_object` + shared `Select EC Dropdown Option`; original build stacked on PR #142. **Bank-pattern conversion (2026-08-24)**: rebuilt `payment_term_page.resource`/`payment_term_iud.robot` to mirror `bank_page.resource`/`berth_page.resource`/`disposition_type_page.resource` exactly — properties-file-driven insert (`Insert Object From Properties And Verify Code`), update, form/grid verify, explicit `Find/Clear Payment Term Row By Filter` wired into Update/Find/Verify-Found/Delete, dedicated `PAYMENT_TERM_EC_USER/PASS`, fixed test code `AUTOTEST_PAYMENT_TERM` (confirmed free via a fresh oracledb query, before and after the live run). Real on-screen labels confirmed via the proven Playwright driver's own evidence screenshots (`evidence/pt_04_insert_filled.png`/`pt_07_update_filled.png`): "Payment Term Code" is screen-prefixed but "Name" is generic (like Bank); the dropdown/number labels are "Date Calculation Method"/"Number Of Days" (NOT "Method"/"Offset" — the driver's own internal variable names are generic clone-naming, not live label text). **Navigator reclassified**: same "date nav + GO to load" shape as Berth/Port (`docs/ec_screen_registry.md`) — confirmed NOT a mandatory gate via the proven driver + live evidence (grid auto-populates on open, no GO needed before Save/Find/Delete); moved out of `docs/bank-pattern-conversion-checklist.md`'s "Excluded" table. Added the missing TC04 Find (prior suite had only 4 TCs), removed prior inline DB-verify calls from the test file (PURE SCREEN verification, matching Bank's convention). Live 5/5, robocop 9 issues (4 VAR02 + 5 DOC02, parity with Disposition Type's 11-issue baseline, no regression), full-tree dryrun 794/794, filter fired 5x / cleared 5x (output.xml grep), DB self-clean confirmed 0 residual `AUTOTEST%` rows via a fresh independent oracledb connection. No shared T1/T2 file changes. Playwright bundle deliberately left untouched — RF-only task |
| Date Objects | Calendar (OV, custom-URL) — Bank-pattern conversion | ✅ Rebuilt live 5/5 (2026-08-23, Batch 6 Bank-pattern conversion, final batch) | Converted from the older hardcoded-field-id pattern to the label-driven, properties-file-driven, T2-consolidated Bank pattern (mirrors `bank_page.resource`/`royalty_owner_page.resource`); `OV_CALENDAR`; custom-URL OV, grid `nav:form:T_data`, NO navigator/GO (T2 `Save And Refresh List` auto-detects and falls back to toolbar Refresh); labels confirmed live as screen-prefixed "Calendar Code"/"Calendar Name" (not generic "Code"/"Name", same as Royalty Owner/State); only Code/Name/Start Date mandatory (7 weekday-indicator checkboxes + Description/Comments optional, left out of IUD flow, no scope expansion); fixed test code `AUTOTEST_CALENDAR`; explicit grid-filter wiring (`Find/Clear Calendar Row By Filter`) included from the start, output.xml confirms 5 `Find Calendar Row By Filter` hits; Member Calendars/Calendar Usage child grid kept OUT OF SCOPE (untouched); no shared-file edits; DB-verified (fresh oracledb query on `CODE` column of `OV_CALENDAR`) + self-cleaning (independent fresh-connection re-read = 0 residual `AUTOTEST_CALENDAR` rows) |
| Date Objects | Calendar Collection (OV, custom-URL) — 5/5 | ✅ Live 5/5 (2026-08-23, Batch 6 Bank-pattern rebuild) | Rebuilt from the older hardcoded-field-id pattern to the label-driven, T2-consolidated Bank pattern; custom-URL OV, no navigator GO (confirmed live), generic Code/Name labels; member calendars = separate child grid (out of object-IUD scope, unchanged); `OV_CALENDAR_COLLECTION`; explicit grid-filter wiring included from day one; no shared-file edits; DB-verified + self-cleaning. **Date Objects batch and the FULL 23-screen Bank-pattern candidate pool are now COMPLETE.** |
| Stream Objects | Stream Item (CD.0008, custom-URL OV) | ✅ Live 3/3 Insert+Delete (2026-08-03) | 3rd-attempt build (parked twice on a wrong Copy-based-insert assumption); real Insert = standard `_open_new_object()` (already correctly cased); 12 mandatory reference fields are ordinary DROPDOWNS (not popups, despite the error message's `[..._POPUP]` naming); Name is EC-documented server-derived (any typed value discarded); **UPDATE OUT OF SCOPE** - blocked by an unconfigured EC scheduler job (`UpdateStreamItem`/BF VO.0031), confirmed live 3x + against EC's own online help, owner-instructed skip; non-standard navigator GO id `buttongo:form:B` (local wrapper, not the shared engine's `click_go()`); Start Date must be >= 2003-01-01 for the reference dropdowns to have options (4th confirmed instance of the ref-dropdown date-scope bug class); `OV_STREAM_ITEM`; no shared-file edits; DB-verified + self-cleaning |
| System | Production Day Table (CO.1033, TV-style grid) | ✅ Live 1/1 Insert-only (2026-08-03) | 3rd-attempt build; distinct root cause from the other 2 "toolbar mystery" screens (no case-sensitivity bug, no scheduler-job block) - real blockers were (1) filling Object Code via `.fill()` silently breaks the NEXT cell's dropdown from rendering, fixed by real keystrokes+Tab (`Type Cell By Id`); (2) DB commit visibility measured ~8s slower than every other screen, needs a generous post-Save wait; (3) RF's `Evaluate JavaScript` trailing-argument form silently passes `undefined`, fixed via `${VARIABLE}` string substitution into the JS source (this project's own established convention). **DELETE DOES NOT EXIST ON THIS SCREEN** - owner-confirmed live: a genuine permanent business-process decision (not a code/selector gap); toolbar Delete never enables for any row, End Date is a plain field not a delete trigger. **Self-clean impossible by design** - owner decision 2026-08-03: accept permanent test residual (same precedent as Royalty Contract), 8 `AUTOTEST_PDT_*` rows accumulated this session; run the suite/driver sparingly going forward; `OV_PRODUCTION_DAY`; no shared-file edits; DB-verified (present + NAME) |
| Sales Objects | Price Object (CO.3016, OV-GM) | ✅ Live 4/4 full I-U-D (2026-08-03) | Original "pager-walk click timeout" park reason was wrong on 2 separate prior investigations (2026-07-27 original, 2026-08-02 re-investigation for issue #321) - the real defect: inserting with Business Unit left unset leaves `BUSINESS_UNIT_CODE` NULL, invisible under any page of a BU-scoped grid (same class as Message Group/Planned Well, not a pagination bug). Fixed via `gen_ovgm.py`'s `parent_dd="Business Unit"` mechanism. Found + hand-fixed a related generator gap: `nav_levels` config only caps the Python driver's cascade, NOT the generated RF T3 (which still tries the shared multi-level cascade keyword and times out on 2 unrelated optional filter columns) - fixed locally by bypassing the shared keyword with a direct single-dropdown fill, matching the identical precedent already set on Service (CO.2103); `OV_PRICE_OBJECT`; no shared-file edits; DB-verified + self-cleaning |
| Sales Objects | Product Price Object (CD.0011, custom-URL OV, no navigator) — brand-new build, zero prior automation | ✅ Live 5/5 (2026-08-25, first attempt) | Built via `ec-bank-pattern-new-screen` (lean RF-only deliverable, no Playwright bundle). **Genuinely different screen from the already-shipped "Price Object" (CO.3016) directly above**, despite sharing the SAME underlying class/view (`PRICE_OBJECT`/`OV_PRICE_OBJECT`) - confirmed via a live `BUSINESS_FUNCTION` DB lookup (`BF_CODE`/`NAME`/`URL`): CO.3016 = `Price Object`, URL `manage_object_navmodel_nav/.../CLASS_NAME/PRICE_OBJECT` (OV-GM, mandatory Business Unit nav gate); CD.0011 = `Product Price Object`, URL `general_price_object/CLASS_NAME/PRICE_OBJECT` (a distinct custom controller, no navigator at all). Also distinguished from the similarly-worded "Product Price Object **Setup**" (SP.0029, same `general_price_object` controller but a different module namespace `com.ec.revn.sp` and a different exact label with "Setup" appended) - NOT built, not investigated further, exact-label match confirmed via `BUSINESS_FUNCTION.NAME` before choosing CD.0011. `class_property_cnfg.LABEL` for `PRICE_OBJECT` is only `"Price Object"` (the class's own generic label) - the menu label "Product Price Object" is a `BUSINESS_FUNCTION.NAME`-level distinction only, so the usual label-driven `resolve_ec_screen.py`/`scan_ec_screen.py` DB step was bypassed in favour of the already-confirmed BF_CODE/URL (see `tmp/check_product_price_object.py`). DB metadata: `CLASS_TYPE=OBJECT`, `TIME_SCOPE_CODE=VERSIONED`, base table `PRODUCT_PRICE`. Live scan confirmed a genuinely nav-free shape (grid `nav:form:T_data`, zero nav fields, no GO button - same shape as Cost Centre; `Save And Refresh List`'s existing no-GO fallback to toolbar Refresh handled this with no changes needed). Mandatory `objectForm` fields: Price Object Code/Price Object Name/Start Date + 4 dropdowns (Product Name/Price Concept/UOM/Price Rounding Rule) - the SAME field set CO.3016's own driver already proved `__FIRST__`-safe for all 4, confirmed again here on first live attempt (no retries needed). Field labels screen-prefixed ("Price Object Code"/"Price Object Name", not generic "Code"/"Name"), matching CO.3016's own convention. Fixed test code `AUTOTEST_PPO` (confirmed free via a fresh oracledb query against `OV_PRICE_OBJECT.CODE` before and after the live run: 0/0). Explicit grid-filter wiring (`Find/Clear Product Price Object Row By Filter`) included from the start, output.xml confirms 15 `Find Object Row By Filter` hits. Robocop: 11 issues (9 baseline VAR02/DOC02, matching Cost Centre's own 9-issue baseline exactly, + 2 extra LEN32 solely from this screen's longer variable-name prefix) - not a regression. Full `tests/` dryrun 810/810 pass. No shared T1/T2 file edits. Adds new rows here and in `docs/ec_screen_registry.md` (genuinely new screen) |
| Revenue Split Keys | Product Split Key (CD.0036, custom-URL OV, no navigator) — brand-new build, zero prior automation | ✅ Live 5/5 (2026-08-25, first attempt) | Built via `ec-bank-pattern-new-screen` (lean RF-only deliverable, no Playwright bundle). One of 6 sibling "* Split Key" screens (Product/Company/Field/Stream Item Category/Other/Stream Item) that ALL share the SAME underlying class/view (`SPLIT_KEY`/`OV_SPLIT_KEY`, per `workstreams/master-plan/ec-automation/docs/ov-non-bank-targets.md`'s 2026-08-25 investigation note), differentiated only by a server-side `SPLIT_TYPE` value tied to the URL path segment opened - this screen's own real URL, confirmed via live `page.url` capture: `manage_object_split_key/CLASS_NAME/SPLIT_KEY/SPLIT_TYPE/PRODUCT`. The investigation doc described a shared "single Date+GO navigator" across all 6 - live recon on THIS sibling specifically (`tmp/scan_product_split_key.py`) found NO navigator at all (grid `nav:form:T_data` loads directly, a `navButton:form:B` exists but is not a gating GO), same nav-free shape as Product Price Object (CD.0011) - each of the 6 siblings must be independently recon'd rather than assumed identical past the shared field list. Mandatory `objectForm` fields (confirmed live, yellow+empty): Split Key Code/Split Key Name/Start Date only - Description/Comments/Value Method/Shares add to 100%/Rounding Rule/Number of Rounding Decimals/Number Display Format all confirmed live as NOT mandatory, left unset (fewer mandatory fields than Product Price Object's 4 extra dropdowns). Field labels screen-prefixed ("Split Key Code"/"Split Key Name", not generic "Code"/"Name"). Fixed test code `AUTOTEST_SPLITKEY_PRODUCT` - deliberately NOT a generic `AUTOTEST_SPLIT_KEY`, since 5 sibling Split Key screens were being built in parallel this same round, all writing to the same shared `OV_SPLIT_KEY` view; confirmed free via a fresh oracledb query before the run (0 rows) and confirmed as the only `AUTOTEST%`-prefixed row in `OV_SPLIT_KEY` after self-clean (0 rows, 0 other AUTOTEST rows present). Explicit grid-filter wiring (`Find/Clear Product Split Key Row By Filter`) included from the start, output.xml confirms 22 `Find Object Row By Filter` hits. Robocop: 9 issues (DOC02-only), fewer than Product Price Object's 11-issue baseline - not a regression. Full `tests/` dryrun 815/815 pass. No shared T1/T2 file edits (only an additive `PRODUCT_SPLIT_KEY_EC_USER`/`_EC_PASS` pair appended to `resources/credentials.py`, same convention as every other screen). NOT the same screen as "Split Item Other" (CD.0017, same treeview folder, `split_item_other_page.resource`) - a different, already-built, standalone screen on class `SPLIT_ITEM_OTHER` via the plain `manage_object_nav` controller. Adds new rows here and in `docs/ec_screen_registry.md` (genuinely new screen) |
| Revenue Split Keys | Company Split Key (CD.0044, custom-URL OV, no navigator) — brand-new build, zero prior automation | ✅ Live 5/5 (2026-08-25) | Built via `ec-bank-pattern-new-screen` (lean RF-only deliverable, no Playwright bundle). **One of 6 sibling "* Split Key" screens** (Product/Company/Field/Stream Item Category/Other/Stream Item Split Key) sharing ONE underlying class `SPLIT_KEY`/view `OV_SPLIT_KEY` via the same generic controller `manage_object_split_key/CLASS_NAME/SPLIT_KEY/SPLIT_TYPE/<TYPE>` - this screen's own confirmed URL segment is `SPLIT_TYPE/COMPANY` (live `page.url` capture 2026-08-25); the other 5 siblings are built independently in parallel, each with its OWN distinctly-scoped fixed test code to avoid collision on the shared view - this screen uses `AUTOTEST_SPLITKEY_COMPANY` (deliberately NOT the generic `AUTOTEST_SPLIT_KEY`). Distinct from "Split Item Other" (CD.0017, class `SPLIT_ITEM_OTHER`, controller `manage_object_nav` - a different, already-built, standalone screen). `class_property_cnfg.LABEL` for `SPLIT_KEY` is only the generic "Split Key" - `resolve_ec_screen.py`'s label lookup correctly resolves `CLASS_TYPE=OBJECT`/`TIME_SCOPE_CODE=VERSIONED`/base `SPLIT_KEY`/view `OV_SPLIT_KEY` from the SCREEN name "split key", but the shared `scan_ec_screen.py`'s OWN label match fails on the exact menu title "Company Split Key" (mis-detects `CLASS_TYPE=None`, would mis-classify as TV) - a bespoke recon script (`tmp/scan_company_split_key.py`) was used instead to force the correct OV recon path. Live recon found NO mandatory navigator field at all (nav fields=[]) despite a `navButton:form:B` "Go..." button being present - the grid (`nav:form:T_data`) already showed its 11 real rows before that button was ever clicked, and clicking it made no difference to the row count; this CONTRADICTS the sibling-investigation note's claim of a "single Date+GO navigator" for the Split Key family - that claim was not re-verified live on this specific screen before this build, and this screen's own live scan is the authoritative source. Mandatory `objectForm` fields: Split Key Code/Split Key Name/Start Date only (screen-prefixed labels, confirmed via live mandatory-yellow scan); Description/Comments/Value Method/Shares add to 100%/Rounding Rule/Number of Rounding Decimals/Number Display Format all optional (confirmed populated on existing rows CO0001-CO0003, e.g. `VALUE_METHOD='STANDARD'`/`ROUNDING_RULE='HALF_UP'`/`NUMBER_FORMAT='NINE_DEC'`), left unset per the IUD-fill-only-needed-fields convention. T2's `code_label` kwarg threaded as `Split Key Code` through every Insert/Update/Find/Verify call site (this screen's Code label is screen-prefixed, not the generic "Code" default - first live attempt failed twice on this exact gap: `Insert Object From Properties And Verify Code` raised `Dictionary '${data}' has no key 'Code'`, then `Update`/`Find` timed out waiting for a `updateAttributes` span with text `'Code'` that doesn't exist on this screen - fixed by passing `code_label=Split Key Code` everywhere). Fixed test code `AUTOTEST_SPLITKEY_COMPANY` confirmed free via a fresh oracledb query against `OV_SPLIT_KEY.CODE` before the live run (0 rows, and 0 `AUTOTEST%` rows of any kind in the whole shared view), and confirmed 0 residual again via a second fresh connection after the live run (self-clean). Explicit grid-filter wiring (`Find/Clear Company Split Key Row By Filter`) included from the start, output.xml confirms 15 `Find Object Row By Filter` hits / 8 underlying `Filter Grid Text Column By Value` hits. Robocop: 9 issues (parity with Report Context's own 9-issue baseline exactly - same DOC02 x5 + VAR02 pattern, not a regression). Full `tests/` dryrun 815/815 pass. No shared T1/T2 file edits (only an additive `COMPANY_SPLIT_KEY_EC_USER/PASS` pair appended to `credentials.py`). Adds new rows here and in `docs/ec_screen_registry.md` (genuinely new screen) |
| Facility Objects | Facility Class 2 (CO.0021) | (P) PERMANENT sandbox config gap (2026-08-03) | 3rd investigation round (fresh recon, no assumptions carried over) disproved the prior round's "Finder popup" hypothesis - navigator is a plain single Date field, no popup/dropdown/Finder anywhere. Reproduced the defect live (owner-observed): Insert succeeds with no error, row persists correctly in `OV_FCTY_CLASS_2`, but the grid's own server-side widget config reports `rowCount: 0` regardless (confirmed via `PrimeFaces.widgets['manageObject_wv'].cfg.paginator`) - a genuine server-side issue, not client rendering. Ruled out RECORD_STATUS/approval state (identical to every working screen). **Owner confirmed directly: this class requires a special sandbox-level setting that isn't configured in this environment** - not a code defect, not buildable from automation. Self-cleaned 4 `AUTOTEST_FC2_*` rows via the view's own End=Start UPDATE (owner-authorized explicitly, since the UI cannot select an invisible row). No bundle shipped - permanently out of scope until the environment is reconfigured. |
| Messaging | Contact Group Set (CO.0225, OV-GM) | ✅ Live 4/4 full I-U-D (2026-08-03) | 1st screen discovered via a DB-first coverage audit (`SELECT class_name FROM CLASS_CNFG WHERE CLASS_TYPE='OBJECT'`, 295 total, cross-referenced against every tracking doc to find genuinely unautomated classes). Real screen title ("Maintain Contact Group Set") differs from the class's own `class_property_cnfg` LABEL ("Contact Group Set") - confirmed via a live menu search before committing to build, since an exact-LABEL search finds no clickable screen. Same family as Message Group (Functional Area nav + `parent_dd` binding), same folder (Configuration > Messaging) - `gen_ovgm.py` template transferred with ZERO fixes needed, first live run ALL PASS; `OV_CONTACT_GROUP_SET`; no shared-file edits; DB-verified + self-cleaning |
| Integration Services | Remote Endpoint Configuration (CO.1082, TV-style grid) | ✅ Live 4/4 full I-U-D (2026-08-03) | 2nd screen from the same DB-first coverage audit. Real screen title ("Remote Endpoint Configuration") differs from the class's own LABEL ("Endpoint configuration") - same class of naming gap as Contact Group Set. **Genuine Code format constraint found live**: EC rejects the usual `AUTOTEST_XX_` uppercase-underscore convention here - Code must be lowercase alphanumeric + hyphens only (a DNS-slug format, e.g. `autotest-rec-<timestamp>`). First live RF run hit the established "missing `Refresh Screen` after Save" gap (same class of defect as Constant Standard) - fixed by adding it after all 3 Save calls; re-ran live 4/4. Physical delete (`TIME_SCOPE_CODE=INVARIANT`); `OV_ENDPOINT_CONFIG`; no shared-file edits; DB-verified + self-cleaning |
| ECIS Excel Upload | End-to-end | ✅ Complete | Own interface + schedule, ran live |
| ECIS Excel Upload | Re-runnable SQL + skills + evidence | ✅ Merged (2026-06-22, PR #93) | `workstreams/ecis-excel-upload/`: idempotent create/delete SQL (update-insert, no MERGE), live DB-verified demo (`AVG_BH_PRESS` NULL→210.5/215/220.3 @ 2003-01-10, self-cleaned), page-broken evidence doc, + `ec-sql-script-builder` & `ecis-excel-upload-builder` skills. KNOWN OPEN: automated `upload→RUN NOW` timing flakiness (root cause unconfirmed) |
| Assets | Recon | ✅ Scanned | Registry complete |
| Financial Objects | Cost Centre (OV, custom-URL) — Bank-pattern conversion | ✅ Rebuilt live 5/5 (2026-08-22) | Batch-1 pilot conversion (with Revenue Order + WBS, in parallel worker sessions) from the older hardcoded-field-id/generated-code pattern to Bank/State's label-driven, properties-file-driven, T2-consolidated pattern; `OV_FIN_COST_CENTER`; grid `nav:form:T_data`, no GO (confirmed live), reload via T2 `Save And Refresh List`'s toolbar-Refresh fallback; labels confirmed live as generic "Code"/"Name" (not screen-prefixed); fixed test code `AUTOTEST_COST_CENTRE`; no shared-file edits; DB-verified + self-cleaning (fresh-connection re-read = 0 residual) |
| Basic Objects | County (OV, manage-object) — Bank-pattern conversion | ✅ Rebuilt live 5/5 (2026-08-23) | Batch-2 conversion (with Country/Regulatory Permits/Currency/VAT Code, in parallel isolated worker clones, see `tmp/batch2_shared_findings.md`) from the older hardcoded-field-id pattern to Bank/State's label-driven, properties-file-driven, T2-consolidated pattern; `OV_COUNTY`; grid `manage_object_nav_nav:form:T_data` with GO (confirmed live); labels confirmed live as screen-prefixed "County Code"/"County Name" (not generic "Code"/"Name", same as State/Region); fixed test code `AUTOTEST_COUNTY`; explicit grid-filter wiring (`Find/Clear County Row By Filter`) included from the start, output.xml confirms 5 `Find County Row By Filter` hits; no shared-file edits; DB-verified (fresh oracledb `Field Should Equal In View` on NAME/DESCRIPTION post insert+update) + self-cleaning (independent fresh-connection re-read = 0 residual `AUTOTEST%` rows in `OV_COUNTY`) |
| Financial Objects | Currency (OV, manage-object) — Bank-pattern conversion | ✅ Rebuilt live 5/5 (2026-08-23) | Batch-2 5-screen conversion (with Country/County/Regulatory Permits/VAT Code, in parallel isolated-clone worker sessions) from the older hardcoded-field-id pattern to Bank/Cost Centre's label-driven, properties-file-driven, T2-consolidated pattern; `OV_CURRENCY`; grid `manage_object_nav_nav:form:T_data`; labels confirmed live as generic "Code"/"Name" (not screen-prefixed); fixed test code `AUTOTEST_CURRENCY`; **registry-flagged mandatory "Active" checkbox (`MandatoryCellStyleWhite`) confirmed live NOT save-blocking** - a checkbox has no "empty" state, so it's omitted from insert properties (Save succeeds unchecked, `ACTIVE_IND` lands 'N'); explicit grid-filter wiring included from day one (`Find/Clear Currency Row By Filter`, 5 hits confirmed in live output.xml); no shared-file edits; DB-verified + self-cleaning (fresh-connection re-read = 0 residual) |
| Commercial Objects | Field Group (OV, manage-object) — Bank-pattern conversion | ✅ Rebuilt live 5/5 (2026-08-23) | Batch-3 5-screen conversion (with Customer/Licence/MMS Lease/Operator Lease, in parallel isolated-clone worker sessions, see `tmp/batch3_shared_findings.md`) from the older hardcoded-field-id pattern to Bank/Country's label-driven, properties-file-driven, T2-consolidated pattern; `OV_FIELD_GROUP`; grid `manage_object_nav_nav:form:T_data`; plain nav, no mandatory scope (confirmed live); labels confirmed live as generic "Code"/"Name" (not screen-prefixed); fixed test code `AUTOTEST_FIELD_GROUP`; explicit grid-filter wiring included from day one (`Find/Clear Field Group Row By Filter`, 5 hits confirmed in live output.xml); no shared-file edits; DB-verified + self-cleaning (fresh-connection re-read = 0 residual) |
| Commercial Objects | Field (OV-GM, groupmodel) — FULL Bank/Area-pattern conversion (2026-08-26) | ✅ Rebuilt live 5/5 (2026-08-26) | Follow-up to PR #525 (navigator-fill delegation only) per owner standing rule: any EC screen with a navigator matching Area's layout must follow Area's FULL pattern; `OV_FIELD`; grid `manageObject:form:T_data`; mandatory Area dropdown + GO (OV-GM, KEPT as-is, navigator fill still delegates to the shared T2 `Apply Navigator From Properties`, untouched this round); labels confirmed live as SCREEN-PREFIXED "Field Code"/"Field Name" (like Area, not generic "Code"/"Name"); insert sets **Geo Area** = navigator value `Offshore area` (groupmodel visibility link, not statically mandatory, excluded from round-trip form-label compare); fixed test code `AUTOTEST_FIELD` (confirmed free in `OV_FIELD` via fresh oracledb connection); 5 TCs with per-TC Login/Logout on one Suite-Setup browser; properties-file-driven Insert/Update/Verify via 4 new files (`testdata/field_{insert,update,form_verify,grid_verify}.properties`); explicit grid-filter wiring (`Find/Clear Field Row By Filter`, 29 hits confirmed in live output.xml); zero inline DB-verify calls left in `field_iud.robot` (confirmed via grep - DB check now lives only in the shared T2 `Verify Object Removed`); no shared T1/T2 file edits; dryrun 847/847; DB-verified + self-cleaning (fresh-connection re-read = 0 residual `AUTOTEST%` rows in `OV_FIELD`) |
| Commercial Objects | Vendor (OV, manage-object) — Bank-pattern conversion | ✅ Rebuilt live 5/5 (2026-08-23) | Batch-4 conversion (with State Lease/Cost Object Mapping/DOA Credit Limit/Product Description, in parallel isolated-clone worker sessions, see `tmp/batch4_shared_findings.md`) from the older hardcoded-field-id pattern to Bank/Customer's label-driven, properties-file-driven, T2-consolidated pattern; `OV_VENDOR`; grid `manage_object_nav_nav:form:T_data`, no mandatory nav scope (confirmed live, only the universal Date+GO as-at-date bar); labels confirmed live as generic "Code"/"Name" (not screen-prefixed); mandatory set on BOTH objectForm and updateAttributes: Code, Name, Start Date, ERP Vendor Code (text), Official Name (text), Vendor Group (dropdown, real first option `Contract Owner Vendor` used literally, not `__FIRST__`); fixed test code `AUTOTEST_VEND`; explicit grid-filter wiring included from day one (`Find/Clear Vendor Row By Filter`, 5 hits confirmed in live output.xml); no shared-file edits; DB-verified + self-cleaning (fresh-connection re-read = 0 residual) |
| Financial Objects | Product Description (OV, manage-object) — Bank-pattern conversion | ✅ Rebuilt live 5/5 (2026-08-23) | Batch-4 5-screen conversion (with State Lease/Vendor/Cost Object Mapping/DOA Credit Limit, in parallel isolated-clone worker sessions, see `tmp/batch4_shared_findings.md`) from the older hardcoded-field-id pattern to Bank/Customer's label-driven, properties-file-driven, T2-consolidated pattern; `OV_PRODUCT_NODE_ITEM`; grid `manage_object_nav_nav:form:T_data`; plain nav (universal Date+GO bar only, no mandatory scope, confirmed live: NAV_DD_COUNT=0); Code label confirmed live as SCREEN-PREFIXED "Product Node Item Code" (not generic "Code"), passed as `code_label` throughout; THREE mandatory reference dropdowns (Product/Node/Financial Code, `MandatoryCellStyle` confirmed live in both objectForm and updateAttributes) - literal first-option values read back live (NOT `__FIRST__`, per the VAT Code gotcha): Product=`AS3_CrudeOil`, Node=`Apollo FPSO`, Financial Code=`Frame Agreement`; Start Date uses `2003-01-01` (EC_TEST_START_DATE_REFDD convention, same reason as VAT Code's 3 dropdowns); fixed test code `AUTOTEST_PD`; explicit grid-filter wiring included from day one (`Find/Clear Product Description Row By Filter`, 5 hits confirmed in live output.xml); no shared-file edits; DB-verified + self-cleaning (fresh-connection re-read = 0 residual `AUTOTEST_PD` rows in `OV_PRODUCT_NODE_ITEM`) |
| Financial Objects | Sales Order (OV, manage-object) — Bank-pattern conversion | ✅ Rebuilt live 5/5 (2026-08-23) | Batch-5 conversion (see `tmp/batch5_shared_findings.md`) from the older hardcoded-field-id pattern to Bank/Cost Object Mapping's label-driven, properties-file-driven, T2-consolidated pattern; `OV_PRODUCT_SALES_ORDER`; grid `manage_object_nav_nav:form:T_data`, no mandatory nav scope (confirmed live: NAV_DD_COUNT=0, only the universal Date+GO as-at-date bar) - naming concern from the shared-findings doc (possible document-header-plus-lines shape) resolved live, same outcome as Cost Object Mapping in Batch 4; Code label confirmed live as SCREEN-PREFIXED "Product Sales Order Code" (not generic "Code"), passed as `code_label` throughout; TWO mandatory reference dropdowns beyond Code/Name/Start Date (`MandatoryCellStyle` confirmed live in both objectForm and updateAttributes), neither a cascade: Company, Field - literal option values used (NOT `__FIRST__`, per the VAT Code gotcha): Company=`Acme Chemicals` (same clean option already proven on Cost Object Mapping), Field=`Apollo`; Start Date uses `2003-01-01`; fixed test code `AUTOTEST_SO`; explicit grid-filter wiring included from day one (`Find/Clear Sales Order Row By Filter`, 5 hits confirmed in live output.xml); no shared-file edits; DB-verified + self-cleaning (fresh-connection re-read = 0 residual `AUTOTEST_SO` rows in `OV_PRODUCT_SALES_ORDER`) |
| Financial Objects | Account Mapping (OV, manage-object, custom-URL) — Bank-pattern conversion | ✅ Rebuilt live 5/5 (2026-08-23, 1 retry) | Batch-6 conversion (FINAL of the 23-screen candidate pool, see `tmp/batch6_shared_findings.md`) from the older hardcoded-field-id pattern to Bank/Cost Object Mapping's label-driven, properties-file-driven, T2-consolidated pattern; `OV_FIN_ACCOUNT_MAPPING`; custom-URL OV (no navigator, GO-button locator confirmed 0 matches live), grid id `manageObject:form:T_data` (NOT the shared `${OV_MANAGE_OBJECT_TABLE}` navigator-based constant); confirmed NOT a scope mismatch despite the "Mapping" name (genuine Code/Name manage-object OV, same outcome as Cost Object Mapping in Batch 4); reused the screen's own already-PROVEN reference combination from its prior 2026-06-12 Playwright IUD PASS (`JOU_ENT_ALL_ALL_ALL_ACCRUAL_CREDIT`, re-confirmed still free via a fresh DB check 2026-08-23) across EIGHT mandatory reference dropdowns (Line Item Type, Financial Code, Company Category, Status, Debit / Credit, Debit PK, Credit PK, Financial Account - mandatory class confirmed live on the input/dd-span itself, one level deeper than the usual wrapping-tableCell technique) plus Account Category (statically non-mandatory but a cascade dependency for Financial Account); grid is a 75-row custom grid with 13 columns and NO Start Date column (grid-verify checks Code/Name only); **1 live retry**: Line Item Type re-renders as short code `ALL` after reload instead of the literal text picked at insert (same documented gotcha as DOA Credit Limit's Role Name) - excluded from the live-DOM round-trip form-label list, DB ground-truth covers it; fixed test code `AUTOTEST_AM`; explicit grid-filter wiring included from day one (`Find/Clear Account Mapping Row By Filter`, 5 hits confirmed in live output.xml); no shared-file edits; DB-verified + self-cleaning (fresh-connection re-read = 0 residual `AUTOTEST_AM` rows in `OV_FIN_ACCOUNT_MAPPING`) |
| Financial Objects | Bank Account (OV, manage-object) — Bank-pattern conversion (Batch 11, FINAL batch of the confirmed 23-screen candidate pool) | ✅ Rebuilt live 5/5 (2026-08-23) | Bigger lift than Batch 7-10 (was STILL on the OLDER hardcoded-field-id pattern, `Fill New Object Form ${BANK_ACCOUNT_INS_CODE} ...`, confirmed via direct grep - see `tmp/batch11_shared_findings.md`), converted straight to Bank/Berth's label-driven, properties-file-driven, T2-consolidated pattern; `OV_BANK_ACCOUNT`; grid `manage_object_nav_nav:form:T_data` with GO (manage-object navigator). Code label confirmed live as SCREEN-PREFIXED **"Bank Account Code"** (not generic "Code", unlike Bank itself), passed as `code_label` throughout - via a fresh objectForm+updateAttributes ECCell label dump (30 fields each). Mandatory beyond Code/Name/Start Date: **Sort Code** (text) + THREE reference dropdowns **Bank**/**Customer**/**Currency** - kept from the screen's own already-PROVEN Playwright driver (`screens/.../Bank_Account/playwright/ec_iud_bank_account.py`, live-PASS 2026-06-11) rather than the live static-mandatory scan alone, since Customer showed `{mandatory:false}` on this pass (a conditional-mandatory business rule invisible to static CSS, same Process Train Batch-9 lesson) while Bank/Currency showed `{mandatory:true}`; a live Vendor dd (R22, not in the proven driver, not static-mandatory) deliberately omitted (IUD fill only needed fields). Bank/Customer/Currency use `__FIRST__` and are excluded from the round-trip form-label compare (Storage Flow Batch-10 precedent - a resolved reference value can re-render as different display text after reload); DB ground truth is Code presence in `OV_BANK_ACCOUNT` only. Grid columns Bank Account Code/Name/Start Date/End Date (Bank convention, confirmed via the screen's own prior evidence screenshot); fixed test code `AUTOTEST_BACC` (confirmed free via a fresh oracledb query, before and after the live run); explicit grid-filter wiring (`Find/Clear Bank Account Row By Filter`) included from the start, output.xml confirms 15 filter/15 clear hits; no shared T1/T2 file changes (`resources/manage_object.resource`/`resources/common.resource` untouched). Live 5/5, robocop 7 issues (2 VAR02 + 5 DOC02, same baseline-noise class already accepted throughout this batch series, no regression), dryrun 772/772, DB self-clean confirmed 0 residual `AUTOTEST%` rows via a fresh oracledb connection. This ADDS a new row here (automation-scorecard.md had no prior Bank Account entry) and MODIFIES the existing `docs/ec_screen_registry.md` row (from the 2026-06-11 Playwright-era build). |
| Hydrocarbon Objects | Product (OV, manage-object, CO.0007, class PRODUCT) — brand-new build, zero prior automation | ✅ Live 5/5 (2026-08-24, first attempt) | Phase 3 (`tmp/phase3_shared_findings.md`), built via `ec-bank-pattern-new-screen` (lean RF-only deliverable, no Playwright bundle). Confirmed genuinely new via `grep -ril` across `py/pageobjects/tests/screens` (only sibling matches, no `product_page.resource`/`product_iud.robot`) and confirmed NOT the same class as already-automated siblings Product Description (CD.0012) or Product Group (RC.0053). DB metadata (`resolve_ec_screen.py`): CLASS_TYPE=OBJECT, TIME_SCOPE_CODE=VERSIONED, base table `PRODUCT`, view `OV_PRODUCT`. Live scan (`scan_ec_screen.py`) confirmed plain manage-object shape: navigator = single OPTIONAL date field + GO (`button:form:B`), grid id `manage_object_nav_nav:form:T_data` — no navigator-registry entry existed, eligible for this round per the stricter "zero navigator-column entries" bar. Only Product Code/Product Name/Start Date are mandatory on Insert; Hydrocarbon Component/Product Group/Product Type/ERP Code are optional dropdowns, left unset (no live-verified valid option values, IUD-fill-only-needed-fields convention). Field label is SCREEN-PREFIXED **"Product Code"/"Product Name"** (not generic "Code"/"Name"), passed as `code_label`/`${PRODUCT_CODE_LABEL}` throughout, same pattern as State/County/Country/Bank Account. Note: Product Type and ERP Code exist on `objectForm` (Insert) but are absent from `updateAttributes` (confirmed live) — update properties only carry Product Name/Description. Delete = End Date = Start Date via objectdates row (`tab:tabPanel:objectdates:form:G:0:R:0:C:3:da_input`, same C:3 convention as Bank). Fixed test code `AUTOTEST_PRODUCT` (confirmed free via a fresh oracledb query against base table `PRODUCT.OBJECT_CODE`, before and after the live run: 0/0/0 across `PRODUCT`/`PRODUCT_VERSION`/`OV_PRODUCT`). Explicit grid-filter wiring (`Find/Clear Product Row By Filter`) included from the start, output.xml confirms 15 `Find Object Row By Filter` hits. Robocop: 12 issues (7 VAR02 + 5 DOC02) on the changed files — parity with Bank's own 13-issue baseline (same category set, Bank has one extra VAR02 for its extra unused doc-only variable), not a regression. Full `tests/` dryrun 779/779 pass. No shared T1/T2 file edits. This ADDS new rows here and in `docs/ec_screen_registry.md` (genuinely new screen, no prior entry in either doc). |
| Stream Objects | Stream - All (OV, manage-object, CD.0007, class STREAM) — brand-new build, zero prior automation | ✅ Live 5/5 (2026-08-24, first attempt) | Built via `ec-bank-pattern-new-screen` (lean RF-only deliverable, no Playwright bundle). `docs/ov-reuse-targets.md` had this screen listed as "covered" already, but that claim was STALE/WRONG — a `grep -ril` across `py/pageobjects/tests/screens` for the class/view found only DIFFERENT sibling screens (Chemical Stream=`CHEM_STREAM`, Delivery Stream, N1 "Sub Daily Gas Stream Status - by Stream", the Stream/Oil/Gas Component Analysis screens — all different classes/BF_CODEs), never anything for class `STREAM`/view `OV_STREAM` itself; corrected in the same PR. Also confirmed distinct from Manage Stream (CO.0251, a different BF_CODE already investigated and excluded this session — real 3-level mandatory Business Unit/Area/Facility cascade nav, NOT this screen's shape). DB metadata (fresh oracledb query on `CLASS_CNFG`): CLASS_TYPE=OBJECT, TIME_SCOPE_CODE=VERSIONED, `APP_SPACE_CNTX`=EC_PROD, base table `STREAM`, view `OV_STREAM`. Live scan confirmed plain manage-object shape: navigator = single Date field + GO (`button:form:B`), URL `manage_object_nav/CLASS_NAME/STREAM` (Bank's exact layout signature), grid id `manage_object_nav_nav:form:T_data`; real treeview label confirmed **"Stream - All"** exactly (neither bare "Stream" nor "Stream All" match — 128 other "Stream"-named screens exist in the treeview, e.g. "Stream - by Group Model", so an exact-match check was essential, not a substring guess). ⚠️ UNLIKE Bank/Berth/Product's 3-field baseline, live MandatoryCellStyle scan of the 72-row `objectForm` found **TWO EXTRA mandatory dropdowns**: Stream Type (5 live-confirmed options: Measured/Reference/Calculated/Quality/Derived, used `Measured`) and Alloc Period (10 live-confirmed options: Daily/Monthly/Yearly/.../Not included, used `Daily`) — both filled with literal option text, not `__FIRST__` (VAT Code round-trip gotcha), both confirmed to persist into `updateAttributes` as still-mandatory (read-only scan of an existing production row, `AS1_FCTY_09_WAT_PROD`), so TC02/TC04 round-trip all 4 labels (Stream Code/Stream Name/Stream Type/Alloc Period). Field labels are SCREEN-PREFIXED "Stream Code"/"Stream Name". Delete = End Date = Start Date via objectdates row (same `C:3` convention as Bank/Berth). Fixed test code `AUTOTEST_STREAM` (confirmed free via a fresh oracledb query against `OV_STREAM.CODE`, before and after the live run: 0/0). Explicit grid-filter wiring (`Find/Clear Stream All Row By Filter`) included from the start, output.xml confirms 15 `Find Object Row By Filter` hits. Robocop: 9 issues (1 VAR02 + 5 DOC02 + other baseline noise) — exact parity with Berth's own 9-issue baseline on the identical file shape, not a regression. Full `tests/` dryrun 802/802 pass. No shared T1/T2 file edits (`resources/manage_object.resource`/`resources/common.resource` untouched). This ADDS new rows here and in `docs/ec_screen_registry.md` (genuinely new screen), and CORRECTS the stale "covered" claim in `docs/ov-reuse-targets.md`. |
| Integration Services | Target Mapping Configuration (TV-style, IS.0002, class IMP_TARGET_MAPPING) — brand-new build, zero prior automation, **Find-only by owner confirmation, not a build limitation** | ✅ Live 2/2 (2026-08-24) | Owner directly confirmed (real, first-hand knowledge) this screen does NOT support Insert/Update/Delete despite the toolbar icons appearing enabled — independently re-confirmed this session via a fresh, read-only live DOM probe: the Insert and Delete toolbar `<li>` both carry `ui-submenu-state-disabled`, and there is no Update icon at all (count=0). No contradiction found, so only TC01 (clean-load) + TC04 (find) were built, per the reduced-scope instruction — no TC02/TC03/TC05. DB: `IMP_TARGET_MAPPING` (label "Import Target Mapping"), CLASS_TYPE=OBJECT, TIME_SCOPE_CODE=INVARIANT (no version table), verify view `OV_IMP_TARGET_MAPPING`. NON-STANDARD ids confirmed live (do not reuse generic manage_object.resource constants for this screen): navigator filters `StandardNavigator:form:G:0:R:1:C:{0,1,2}:in` (Class/Attribute/EC Key, none mandatory), GO `buttongo:form:B` (not `button:form:B`/`go_button:form:B`), grid `imp_target_mapping_table:form:T_data` (~20 rows per load, autocomplete-dropdown cells `Cn_dd_input`, not plain `<td>` text — the shared `table.resource` Get Table Rows keyword does not apply here, so the page object reads `.value` via a dedicated JS-evaluate keyword). TC04 finds ONE real, pre-existing row (owner-supplied, live-verified exact match this session): Class=`PWEL_DAY_STATUS`, Attribute=`AVG_LIQ_VOL`, EC Key=`ecValue16`, Class Key 1=`Key 1`, Class Key 2=`Key 2` — cross-checked against `OV_IMP_TARGET_MAPPING` by EC Key. Zero mutation anywhere: no self-clean check in the usual sense; instead a fresh oracledb connection confirmed `OV_IMP_TARGET_MAPPING` row count unchanged (117 → 117) across the live run (`tmp/tmc_rowcount_check.py`). Robocop clean on both new files. Full `tests/` dryrun 792/792 pass. Dedicated `TARGET_MAPPING_CONFIGURATION_EC_USER`/`_PASS` added to `resources/credentials.py` (additive only). No shared T1/T2 file edits. Suite named `target_mapping_configuration_find.robot` (not the usual `_iud.robot` suffix) since it never performs an Insert/Update/Delete. Deviation from the prompt's "isolated sparse-checkout clone under Workplaces/" instruction: built directly on a feature branch in the main worktree instead, matching this repo's actual recent practice (PRs #486/#487 and others were built the same way, not via a Workplaces sparse clone) — flagged here per the no-silent-deviation rule. This ADDS new rows here and in `docs/ec_screen_registry.md` (genuinely new screen, no prior entry in either doc). |
| Revenue Split Keys | Stream Item Split Key (OV, manage-object, CD.0156, class SPLIT_KEY, SPLIT_TYPE=STREAM_ITEM_SPLIT) — brand-new build, zero prior automation | ✅ Live 5/5 (2026-08-25, first attempt) | One of 6 sibling "* Split Key" screens (Product/Company/Field/Stream Item Category/Other/Stream Item) built in parallel this round that ALL share the SAME base view `OV_SPLIT_KEY`, differentiated only by `SPLIT_TYPE` (server-side, from the URL path segment, not user-entered) — collision risk with the 5 siblings' own AUTOTEST rows in the SAME view avoided by using a distinctly-scoped fixed test code `AUTOTEST_SPLITKEY_STREAMITEM` (NOT the generic `AUTOTEST_SPLIT_KEY`), confirmed free in `OV_SPLIT_KEY` before the run and 0 residual after (fresh oracledb connection). Genuine identity confusion RESOLVED live, not assumed: a DB `class_property_cnfg` LABEL lookup for the exact string "Stream Item Split Key" initially resolved to the WRONG class (`STREAM_ITEM_SPLIT`, `CLASS_TYPE=INTERFACE`, no view) — the same shared-label-text failure mode this session already hit elsewhere (e.g. "Well" vs "Well Mode") — because a DIFFERENT, genuinely separate treeview entry ("Stream Item Split Key Shares") shares overlapping label text; the correct screen was confirmed only by the LIVE treeview click itself (search "Split" broadly, then exact-text tv-link match on "Stream Item Split Key", distinct from both "Stream Item Split Key Shares" and "Stream Item Category Split Key"). That live click confirmed URL `manage_object_split_key/CLASS_NAME/SPLIT_KEY/SPLIT_TYPE/STREAM_ITEM_SPLIT` and a genuine Bank-shaped screen (objectForm/updateAttributes/objectdates/daytimes-mirror tabs all present), matching the other 5 siblings. Two split_key-URL-family-specific non-generic ids confirmed live (NOT the shared T2 constants): grid id `nav:form:T_data` (NOT `${OV_MANAGE_OBJECT_TABLE}`, which is the DIFFERENT manage_object_nav family's constant) and navigator GO button id `navButton:form:B` (NOT the standard `button:form:B`) — required no shared-file change because T2's `Save And Refresh List` already falls back to the toolbar Refresh when `button:form:B` is absent. Only "Split Key Code"/"Split Key Name"/"Start Date" are mandatory on Insert (live objectForm scan, 11 fields); Description/Comments/Value Method/Shares add to 100%/Rounding Rule/Number of Rounding Decimals/Number Display Format are all optional, no dropdown fill needed. Both Code and Name labels use the class's own GENERIC "Split Key" prefix (not a "Stream Item"-prefixed label). Delete = End Date = Start Date via objectdates row (`tab:tabPanel:objectdates:form:G:0:R:0:C:3:da_input`, same `C:3` convention as Bank). Explicit grid-filter wiring (`Find/Clear Stream Item Split Key Row By Filter`) included from the start, output.xml confirms 15 `Find Object Row By Filter` hits. Robocop: 9 issues (4 VAR02 + 5 DOC02) on the changed files — exact parity with the sibling family screen Split Item Other's own 9-issue baseline, not a regression (2 initial LEN32 variable-name-length issues on this screen's own longer name were fixed by shortening `..._PROPERTIES` to `..._PROPS`). Full `tests/` dryrun 815/815 pass. No shared T1/T2 file edits (`resources/manage_object.resource`/`resources/common.resource` untouched). Built in an isolated sparse-checkout clone under `Workplaces/stream_item_split_key/` on branch `feature/stream-item-split-key-bank-pattern`, independent of the other 5 sibling screens' own isolated clones/branches. This ADDS new rows here and in `docs/ec_screen_registry.md` (genuinely new screen, no prior entry in either doc). |

---

## Coverage Strategy (Phased Rollout)

| Phase | Scope | Target | Status |
|-------|-------|--------|--------|
| Phase 1 | Configuration screens (objects, lookups, setups) | All Config sections complete | 🟡 In progress |
| Phase 2 | Operation screens (production data, allocations, status processes) | N1/N3/EVENT patterns | 🔵 Next |
| Phase 3 | Transaction screens (contracts, invoices, nominations, liftings) | TBC after Phase 2 | ⏳ Future |

**Runner batch size target:** 25–50 screens/day (current default: 25, raised from 8 per #150). Increase up to 50 via `EC_LEARN_MAX_SCREENS` env var (legacy `EC_LEARN_MAX` still honoured).

**Phase 1 priority order:** Basic Objects → Financial Objects → Commercial Objects → Royalty Objects (✅ 8/8) → Date Objects (✅ 5/5) → remaining config sections (Account Mapping, MIME, Equipment, Language, Dispatching, Contract, Cargo, Laboratory Objects).

**Phase 2 entry criteria:** All Phase 1 config sections ✅ Complete in Section Coverage table above.

---

## Framework Health

| Item | Status | Notes |
|------|--------|-------|
| robocop lint | ✅ Clean | 43→2 warnings after health pass |
| T3 Open-Screen promotion | ✅ Done | Shared keywords adopted |
| Dynamic test data | ✅ Refactored | |
| maximize-browser + expand-screen | ✅ Shared | Adopted across all suites |
| WR.0001 canary | ✅ Running | Used as regression check after each new suite |
| DB restore / teardown | ✅ Pattern established | All suites self-cleaning |
| R16 bundle-credential guard | ✅ Live (2026-06-19) | `scripts/check_bundle_hygiene.py` FAILs on hardcoded creds in `screens/**/playwright/*.py`; wired into ec-object-iud-builder Step-5 verify (PR #77/#78); shared `tmp/scripts/ec_session.py` login helper |
| EC screen-family registry (machine-readable) | ✅ Live (2026-06-19) | `ec_screen_registry.json` (7 IUD families + golden exemplars); `resolve_ec_screen.py` prints CLONE hint; `scan_ec_screen.py` auto-fills gated nav dd + GO (PR #74/#75/#76) |
| EVENT_LOG family (code-less event rows) | ✅ Live (2026-06-20) | New family in `ec_screen_registry.json`; golden exemplar Alarms (`FCTY_DAY_ALARM`/`DV_ALARMS`); marker oracle + physical delete (R19); next clone = Reported Alarms (PR #84) |
| Bundle/recon ASCII hygiene | 🟡 Gap (R20) | All 3 bundles this cycle shipped an em-dash in the FAIL-branch print string; skill template + `check_bundle_hygiene.py` static-ASCII check pending |
| Bundle/recon ASCII hygiene | ✅ Live (2026-06-20) | CLOSES the row above: PR #87 ASCII-normalised 40 bundle/recon `.py` + extended `check_bundle_hygiene.py` with a static non-ASCII gate over `playwright/*.py` + `investigation/*.py` (FAIL on any non-ASCII byte). Reviewer reproduced PASS (R16 + R20) over 48 bundles + 121 recon; 0 non-ASCII in either glob. Wired into skill Step-5 verify |

---

## Parked Items (Resume Backlog)

| Item | Reason | Next step |
|------|--------|-----------|
| N1 Monthly PWEL | Sparse data | Find a month with adequate PWEL_MTH_STATUS rows |
| N3 V→A Daily | STIM_DAY_VALUE empty (0 rows); WELL_FLUID_ANALYSIS needs WHERE_FORMULA var resolution + seeded data | Seed STIM_DAY_VALUE provisional rows or get SME confirmation on Analysis class→physical-table |
| N3 V→A Monthly | Build-ready | Thin new T3 for Monthly Data Status Processes screen |
| Dispatching Pipeline | Not attempted | Resume from slice 2 checkpoint |
| ~~Financial Objects (3 parked)~~ | Closed as stale — all 15 FO suites present (validated 2026-06-15) | — |
| Commercial Sub Field | Not attempted | |
| Disposition Type (OV, CO.0208) | ✅ Done 2026-07-25 (Playwright/original RF) then ✅ Rebuilt to Bank-pattern 2026-08-24 — RF 5/5 live, properties-file-driven insert/update/verify (`Insert Object From Properties And Verify Code` etc.), screen-prefixed labels ("Disposition Code"/"Disposition Name"), fixed test code `AUTOTEST_DISPOSITION_TYPE` (confirmed free via fresh oracledb query), explicit grid-filter wiring (`Find/Clear Disposition Type Row By Filter`, output.xml confirms 15 `Find Object Row By Filter` hits), added the missing TC04 Find (prior suite had only 4 TCs), removed the prior build's inline DB-verify calls from the test file (PURE SCREEN verification, matching Bank's convention), no shared T1/T2 edits, DB-verified + self-cleaning (fresh oracledb re-read = 0 residual `AUTOTEST%` rows); full-tree dryrun 793/793 pass; 1st OV-reuse-target on shared py/ec_object_iud.py engine (Playwright bundle untouched) | 34 uncovered OV reuse-targets remain (see docs/ov-reuse-targets.md) |
| Report Area (OV, RP.0017) | ✅ Done 2026-07-25 (Playwright/original RF) then ✅ Rebuilt to Bank-pattern 2026-08-23 (Batch 9) — RF 5/5 live, properties-file-driven insert/update/verify (`Insert Object From Properties And Verify Code` etc.), screen-prefixed labels ("Report Area Code"/"Report Area Name"), date label is "Start date" (lowercase "date" — confirmed live), fixed test code `AUTOTEST_RPTA`, explicit grid-filter wiring (`Find/Clear Report Area Row By Filter`, output.xml confirms 28 `Find Object Row By Filter` hits across the suite), no shared T1/T2 edits, DB-verified + self-cleaning (fresh oracledb re-read = 0 residual `AUTOTEST_RPTA` rows); dryrun 762/762 pass; 2nd OV-reuse-target (simplest OV) | 33 uncovered OV reuse-targets remain (see docs/ov-reuse-targets.md) |
| Choke Model (OV, CO.0217) | ✅ Done 2026-07-26 (RF 4/4 + Playwright 7/7 via verify_screen.py gate) then ✅ Rebuilt to Bank-pattern 2026-08-24 — RF 5/5 live, properties-file-driven insert/update/verify (`Insert Object From Properties And Verify Code` etc.), screen-prefixed labels ("Choke Model Code"/"Choke Model Name", confirmed live), no mandatory dropdowns (all 9 optional dropdowns confirmed non-mandatory via live scan), fixed test code `AUTOTEST_CHOKE_MODEL` (confirmed free via fresh oracledb query), explicit grid-filter wiring (`Find/Clear Choke Model Row By Filter`, output.xml confirms 15 `Find Object Row By Filter` hits), added the missing TC04 Find (prior suite had only 4 TCs), removed the prior build's inline DB-verify calls from the test file (PURE SCREEN verification, matching Bank's convention), no shared T1/T2 edits, DB-verified + self-cleaning (fresh oracledb re-read = 0 residual `AUTOTEST_CHOKE_MODEL` rows); full-tree dryrun 798/798 pass. **Corrects `docs/bank-pattern-conversion-checklist.md`'s stale "Excluded" grouping** ("Choke, Choke Model — mandatory single date + GO before grid loads") — live navigator scan + grid-row-count recon confirm the date field is OPTIONAL (mandatory=False) and the grid auto-loads before any GO click, the same shape as Bank/Document Date Term, not a genuine gate. Playwright bundle (`py/choke_model_iud.py`) left untouched — RF-only task | 31 uncovered OV reuse-targets remain |
| Choke (OV, CO.0185) | ✅ Done 2026-07-25 (RF 4/4 + Playwright 7/7, verify_screen.py OVERALL PASS) then ✅ rebuilt to full Bank-pattern 2026-08-25 — live-confirmed navigator single date field mandatory=False + GO, grid auto-loads (seed data "P1 C001" visible pre-GO), same shape as Bank/Choke Model; live 5/5, dryrun 798/798, filter fired 15x, DB self-clean confirmed via fresh connection (0 residual `AUTOTEST%` in OV_CHOKE) | see docs/ov-reuse-targets.md; workstreams/master-plan/ec-automation/docs/bank-pattern-conversion-checklist.md |
| Port (OV, CO.2003) | ✅ Done 2026-08-23 (Batch 9 FULL Bank-pattern conversion) - RF live 5/5 (TC01-05: clean-state/insert/update/find/delete), properties-file-driven insert/update/verify, explicit grid-filter wiring (`Find/Clear Port Row By Filter`, output.xml confirms 15 hits), fixed test code AUTOTEST_PORT, DB-verified vs OV_PORT (Name), self-clean (0 residual rows via fresh connection); dryrun 762/762 pass; **paginated grid (2 pages) confirmed the shared T2 filter/row-locate keywords still walk all pages live**. Supersedes 2026-07-26 partial build (RF 4/4 + Playwright 7/7) | see docs/grid-filter-standardization-checklist.md (Batch 9 addition) |
| Berth (OV, CO.2012) | ✅ Done 2026-07-26 (Playwright/original RF) then ✅ Rebuilt to Bank-pattern 2026-08-23 (Batch 7) — RF 5/5 live, properties-file-driven insert/update/verify (`Insert Object From Properties And Verify Code` etc.), screen-prefixed labels ("Berth Code"/"Berth Name", confirmed via ec-ui-knowledge/screens/berth.md + proven py/berth_iud.py), fixed test code `AUTOTEST_BERTH`, explicit grid-filter wiring (`Find/Clear Berth Row By Filter`, output.xml confirms 15 `Find Berth Row By Filter` hits across the suite), no shared T1/T2 edits, DB-verified + self-cleaning (fresh oracledb re-read = 0 residual `AUTOTEST_BERTH` rows); dryrun 753/753 pass. label-driven; added generic engine `wait_for_row_absent` (async delete-redraw). Stacked on #203 | 29 uncovered OV reuse-targets remain |
| Canal (OV, CO.2069) | ✅ Done 2026-07-26, upgraded to FULL Bank pattern 2026-08-23 (Batch 7) — RF now 5/5 (properties-file-driven Insert/Update/Find/Delete + explicit grid-filter wiring, `docs/grid-filter-standardization-checklist.md`) + Playwright 7/7 via verify_screen.py gate (OVERALL PASS), DB-verified vs OV_CANAL (Name), self-clean; 7th OV-reuse-target; label-driven | 28 uncovered OV reuse-targets remain |
| Revenue Stream Category (OV, CD.0015) | ✅ Done 2026-07-26 - RF 4/4 + Playwright 7/7 via verify_screen.py gate (OVERALL PASS), DB-verified vs OV_STREAM_CATEGORY (Name), self-clean; 8th OV-reuse-target; label-driven, generator-scaffolded | see docs/ov-reuse-targets.md |
| Split Item Other (OV, CD.0017) | ✅ Done 2026-07-26 - RF 4/4 + Playwright 7/7 via verify_screen.py gate (OVERALL PASS), DB-verified vs OV_SPLIT_ITEM_OTHER (Name), self-clean; 10th OV-reuse-target; then rebuilt 2026-08-23 (Batch 10) to full Bank-pattern: properties-file-driven insert/update/verify + explicit grid-filter wiring; live 5/5, dryrun 767/767, filter fired 30x, DB self-clean confirmed via fresh connection | see docs/ov-reuse-targets.md |
| Stream Item Category (OV, CD.0016) | ✅ FULL Bank-pattern conversion (Batch 10, 2026-08-23) - RF 5/5 (TC01-05), properties-file-driven insert/update/verify, explicit grid-filter wiring, fixed test code AUTOTEST_SIC, DB self-clean via fresh connection (0 residual). Supersedes prior 2026-07-26 partial build (RF 4/4 + Playwright 7/7). 2026-08-25 alignment fix: removed direct `Code Should Be Present In View`/`Field Should Equal In View`/`Code Should Be Absent In View` calls from TC02/TC03/TC05 - violated Bank's pure-screen-only verification convention (2026-08-18), same deviation class as DOA Credit Limit (PR #503); re-verified live 5/5, dryrun 841/841, DB self-clean 0 residual | see docs/ec_screen_registry.md |
| Other Split Key (OV, CD.0046) | ✅ Brand-new Bank-pattern RF build (2026-08-25) - one of 6 sibling "* Split Key" screens sharing class SPLIT_KEY / view OV_SPLIT_KEY (discriminated by SPLIT_TYPE='SPLIT_ITEM_OTHER'), each with its own distinctly-scoped fixed test code (AUTOTEST_SPLITKEY_OTHER) to avoid cross-screen collision. RF 5/5 (TC01-05), properties-file-driven insert/update/verify, explicit grid-filter wiring (`Find/Clear Other Split Key Row By Filter`, fired 15x), dryrun 815/815 pass, DB self-clean confirmed via fresh connection (0 residual). Custom-URL OV shape (grid `nav:form:T_data`, no gating navigator/GO). Distinct from Split Item Other (CD.0017, different class/controller) and Other Split Key Shares (CD.0047, not built this round). RF-only, no Playwright bundle | see workstreams/master-plan/ec-automation/docs/ec_screen_registry.md |
| Inventory Area (OV, CD.0115) | ✅ Done 2026-08-23 (Batch 8 FULL Bank-pattern conversion) - RF live 5/5 (TC01-05 clean-state/insert/update/find/delete), properties-file-driven insert/update/verify, explicit grid-filter wiring, fixed test code AUTOTEST_INVA, DB-verified vs OV_INVENTORY_AREA, self-clean (0 residual rows via fresh connection). Supersedes 2026-07-26 partial build (RF 4/4 + Playwright 7/7) | see docs/ov-reuse-targets.md, docs/grid-filter-standardization-checklist.md (Batch 8 addition) |
| Reservoir Block (OV, CO.0133) | ✅ Done 2026-08-23 (Batch 9 FULL Bank-pattern conversion) - RF live 5/5 (TC01-05 clean-state/insert/update/find/delete), properties-file-driven insert/update/verify, explicit grid-filter wiring, fixed test code AUTOTEST_RESVB, DB-verified vs OV_RESV_BLOCK, self-clean (0 residual rows via fresh connection). Supersedes 2026-07-26 partial build (RF 4/4 + Playwright 7/7) | see docs/ov-reuse-targets.md, docs/grid-filter-standardization-checklist.md (Batch 9 addition) |
| Reservoir Formation (OV, CO.0135) | ✅ Done 2026-08-23 (Batch 9 FULL Bank-pattern conversion) - RF live 5/5 (TC01-05 clean-state/insert/update/find/delete), properties-file-driven insert/update/verify, explicit grid-filter wiring (`Find/Clear Reservoir Formation Row By Filter`, output.xml confirms 7 hits), fixed test code AUTOTEST_RESVF, DB-verified vs OV_RESV_FORMATION, self-clean (0 residual rows via fresh connection); dryrun 762/762 pass. Supersedes 2026-07-26 partial build (RF 4/4 + Playwright 7/7) | see docs/ov-reuse-targets.md, docs/grid-filter-standardization-checklist.md (Batch 9 addition) |
| Blend (OV, CO.0219) | ✅ Done 2026-08-23 (Batch 7 FULL Bank-pattern conversion) - RF live 5/5 (TC01-05 clean-state/insert/update/find/delete), properties-file-driven insert/update/verify, explicit grid-filter wiring, fixed test code AUTOTEST_BLEND, DB-verified vs OV_BLEND, self-clean (0 residual rows via fresh connection). Supersedes 2026-07-26 partial build (RF 4/4 + Playwright 7/7) | see docs/ov-reuse-targets.md, docs/grid-filter-standardization-checklist.md (Batch 7 addition) |
| Chemical Transport Tank (OV, CO.0257) | ✅ Done 2026-08-23 (Batch 8 FULL Bank-pattern conversion) - RF live 5/5 (TC01-05 clean-state/insert/update/find/delete), properties-file-driven insert/update/verify, explicit grid-filter wiring (`Find/Clear Chemical Transport Tank Row By Filter`, output.xml confirms 7 hits), fixed test code AUTOTEST_CTT, DB-verified vs OV_CHEM_TRANS_TANK, self-clean (0 residual rows via fresh connection); dryrun 758/758 pass. Supersedes 2026-07-26 partial build (RF 4/4 + Playwright 7/7) | see docs/ov-reuse-targets.md, docs/grid-filter-standardization-checklist.md (Batch 8 addition) |
| Chemical Product (OV, CO.0072) | ✅ Done 2026-08-24 (Phase 3, genuinely new build via `ec-bank-pattern-new-screen` — no prior automation existed) - RF live 5/5 (TC01-05 clean-state/insert/update/find/delete), properties-file-driven insert/update/verify, explicit grid-filter wiring (`Find/Clear Chemical Product Row By Filter`, output.xml confirms 26 hits), fixed test code AUTOTEST_CHEMPROD, DB-verified vs OV_CHEM_PRODUCT, self-clean (0 residual rows via fresh connection, 0 orphaned CHEM_USAGE_REPORT_CONF child rows); dryrun 779/779 pass. **Unparks a documented EC PRODUCT DEFECT** (ec-ui-knowledge/EC_KNOWN_ISSUES.md — child-FK blocked End=Start delete) via a new screen-scoped `libraries/ChemicalProductCleanup.py`, no shared T1/T2 file changes | see docs/ec_screen_registry.md, docs/bank-pattern-conversion-checklist.md (Phase 3 addition), ec-ui-knowledge/EC_KNOWN_ISSUES.md |
| Calculation Context (OV, CO.1059) | ✅ Done 2026-07-26 - RF 4/4 + Playwright 7/7 via verify_screen.py gate (OVERALL PASS), DB-verified vs OV_CALC_CONTEXT (Name), self-clean; 16th OV-reuse-target; label-driven, generator-scaffolded | see docs/ov-reuse-targets.md |
| Dummy Tag Event Object (OV, CO.1063) | ✅ Done 2026-07-26 - RF 4/4 + Playwright 7/7 via verify_screen.py gate (OVERALL PASS), DB-verified vs OV_DUMMY_TAG_EVENT (Name), self-clean; 17th OV-reuse-target; label-driven, generator-scaffolded | see docs/ov-reuse-targets.md |
| Transactional Inventory Layout Set (OV, IN.0033) | ✅ Done 2026-07-26 - RF 4/4 + Playwright 7/7 via verify_screen.py gate (OVERALL PASS), DB-verified vs OV_TRANS_INV_TMPL_SET (Name), self-clean; 18th OV-reuse-target; label-driven, generator-scaffolded | see docs/ov-reuse-targets.md |
| Input List (OV, CD.0035) | ✅ Bank-pattern converted 2026-08-25 (replaces the 2026-07-26 generated row above) - live RF 5/5 (TC01 Clean/TC02 Insert/TC03 Update/TC04 Find/TC05 Delete), dryrun 842/842 on full tree, DB self-clean confirmed 0 residual (fresh oracledb conn); properties-file-driven (testdata/input_list_{insert,update,form_verify,grid_verify}.properties), explicit Find/Clear Input List Row By Filter grid-filter wiring (fired 15x in output.xml), fixed AUTOTEST_INPUTLIST code, per-TC Login/Logout, PURE SCREEN verification in the .robot file (previous suite called DB-verify keywords directly from test cases - removed) | see docs/bank-pattern-conversion-checklist.md |
| HCB System (OV, CD.0097) | ✅ Done 2026-07-26 - RF 4/4 + Playwright 7/7 via verify_screen.py gate (OVERALL PASS), DB-verified vs OV_BALANCE (Name), self-clean; 2nd mandatory-dropdown OV-reuse-target; label-driven, generator-scaffolded | see docs/ov-reuse-targets.md |
| Input List (OV, CD.0035) | ✅ Done 2026-07-26 - RF 4/4 + Playwright 7/7 via verify_screen.py gate (OVERALL PASS), DB-verified vs OV_STREAM_ITEM_COLLECTION (Name), self-clean; 1st mandatory-dropdown OV-reuse-target; label-driven, generator-scaffolded | see docs/ov-reuse-targets.md |
| HCB System (OV, CD.0097) | ✅ Done 2026-08-25 - upgraded to FULL Bank-pattern: RF 5/5 live (added TC04 Find), properties-file-driven insert/update, explicit grid-filter wiring (fired 30x), fixed test code AUTOTEST_HCB, dryrun 842/842, DB self-clean confirmed via fresh oracledb connection vs OV_BALANCE; removed inline DB-verify calls from the .robot file (Bank pure-screen-verify convention); corrected stale "no mandatory dropdown" note - HCB Category IS mandatory; Playwright 7/7 (unchanged, 2026-07-26) | see docs/ov-reuse-targets.md |
| Config Variable (OV, IN.0031) | ✅ Done 2026-07-26 - RF 4/4 + Playwright 7/7 via verify_screen.py gate (OVERALL PASS), DB-verified vs OV_CONFIG_VARIABLE (Name), self-clean; 3rd mandatory-dropdown OV-reuse-target; label-driven, generator-scaffolded | see docs/ov-reuse-targets.md |
| Orifice Plate (OV, CO.0089) | ✅ Done 2026-08-23 (Batch 8) - upgraded to FULL Bank-pattern: RF 5/5 live (added TC04 Find), properties-file-driven insert/update, explicit grid-filter wiring (fired 13x), fixed test code AUTOTEST_ORIFICE_PLATE, dryrun 758/758, DB self-clean confirmed via fresh oracledb connection vs OV_ORIFICE_PLATE; Playwright 7/7 (unchanged, 2026-07-26) | see docs/ov-reuse-targets.md; workstreams/master-plan/ec-automation/tmp/batch8_shared_findings.md |
| Data Extract Setup (OV, SP.0043) | ✅ Done 2026-07-26 (Playwright/original RF) then ✅ Rebuilt to FULL Bank-pattern 2026-08-25 — RF 5/5 live, properties-file-driven insert/update/verify (`Insert Object From Properties And Verify Code` etc.), SCREEN-PREFIXED "Data Extract Setup Code"/"Data Extract Setup Name" labels (confirmed via ec-ui-knowledge/screens/data_extract_setup.md + New Object form screenshot), mandatory Data Extract Type dropdown = Actual (Insert-only, excluded from updateAttributes compare; value confirmed live via SUMMARY_SETUP_VERSION.SUMMARY_TYPE distinct = ACTUAL/FORECAST), fixed test code `AUTOTEST_DXS` (confirmed free live via fresh oracledb query, before and after), explicit grid-filter wiring (`Find/Clear Data Extract Setup Row By Filter`, output.xml confirms 13 `Find Data Extract Setup Row By Filter` hits), removed the inline `Field Should Equal In View` DB-verify call that was directly in the old .robot file (pure-screen-verification fix, same deviation class as County/HCB System/etc.), no shared T1/T2 edits, DB-verified + self-cleaning (fresh oracledb re-read = 0 residual `AUTOTEST%` rows in OV_SUMMARY_SETUP); dryrun 842/842 pass; robocop parity with data_extract_set_iud.robot exemplar (same 5 DOC02 + 4 VAR02 baseline). Sibling `Data Extract Set` screen deliberately untouched (out of scope) | see docs/ov-reuse-targets.md |
| Data Extract Set (OV, SP.0049) | ✅ Done 2026-07-26 (Playwright/original RF) then ✅ Rebuilt to FULL Bank-pattern 2026-08-23 (Batch 10) — RF 5/5 live, properties-file-driven insert/update/verify (`Insert Object From Properties And Verify Code` etc.), GENERIC "Code"/"Name" labels (NOT screen-prefixed, confirmed via ec-ui-knowledge/screens/data_extract_set.md), mandatory Owner Class dropdown = All (Insert-only, excluded from updateAttributes compare), fixed test code `AUTOTEST_DXT` (confirmed free live via fresh oracledb query, before and after), explicit grid-filter wiring (`Find/Clear Data Extract Set Row By Filter`, output.xml confirms 13 `Find Data Extract Set Row By Filter` hits), no shared T1/T2 edits, DB-verified + self-cleaning (fresh oracledb re-read = 0 residual `AUTOTEST%` rows in OV_SUMMARY_SET); dryrun 767/767 pass; robocop parity with berth_iud.robot exemplar (same 5 DOC02 + 4 VAR02 baseline). Sibling `Data Extract Setup` screen deliberately untouched (out of scope). 6th mandatory-dropdown OV-reuse-target | see docs/ov-reuse-targets.md |
| Meter Run (OV, CO.0091) | ✅ Done 2026-07-26 (Playwright/original RF) then ✅ Rebuilt to Bank-pattern 2026-08-23 (Batch 8) — RF 5/5 live, properties-file-driven insert/update/verify (`Insert Object From Properties And Verify Code` etc.), screen-prefixed labels ("Meter Run Code"/"Meter Run Name", confirmed via the proven py/meter_run_iud.py + prior live-tested page object), fixed test code `AUTOTEST_METER_RUN`, explicit grid-filter wiring (`Find/Clear Meter Run Row By Filter`, output.xml confirms 15 `Find Meter Run Row By Filter` hits across the suite), no shared T1/T2 edits, DB-verified + self-cleaning (fresh oracledb re-read = 0 residual `AUTOTEST_METER_RUN`/`AUTOTEST_MR_001` rows); dryrun 758/758 pass. label-driven; 7th (scanner-driven: 3 dropdowns + 3 extra mandatory) OV-reuse-target | see docs/ov-reuse-targets.md |
| Document Template (OV, CD.0013) | ✅ Done 2026-07-26 (Playwright/original RF) then ✅ Rebuilt to FULL Bank-pattern 2026-08-24 (Phase 3) — RF 5/5 live, properties-file-driven insert/update/verify (`Insert Object From Properties And Verify Code` etc.), GENERIC "Code"/"Name" labels (confirmed via ec-ui-knowledge/screens/document_template.md + `py/document_template_iud.py`), de-facto-mandatory `Document Title` extra field (proven-driver field, kept from prior build), fixed test code `AUTOTEST_DOCUMENT_TEMPLATE` (confirmed free live via fresh oracledb query, before and after), explicit grid-filter wiring (`Find/Clear Document Template Row By Filter`, output.xml confirms 15 hits), no shared T1/T2 edits, DB-verified + self-cleaning (fresh oracledb re-read = 0 residual `AUTOTEST%` rows in OV_DOC_TEMPLATE); full-tree dryrun 775/775 pass; robocop 7 issues, parity vs Bank's 13 (no regression). Playwright 7/7 unchanged (2026-07-26). extra-field (scanner-driven) OV-reuse-target. 2026-08-25: removed 2 leftover inline DB-verify keywords from TC02/TC05 (pure-screen-only convention alignment, same class as DOA Credit Limit); re-verified live 5/5, dryrun 810/810, filter fired 18x, DB self-clean 0 residual. | see docs/ov-reuse-targets.md |
| Transactional Inventory Properties (OV, IN.0023) | ✅ Done 2026-07-26 - RF 4/4 + Playwright 7/7 via verify_screen.py gate (OVERALL PASS), DB-verified vs OV_TRANS_INVENTORY (Name), self-clean; extra-field (scanner-driven) OV-reuse-target; label-driven, generator-scaffolded | see docs/ov-reuse-targets.md |
| Storage Flow (OV, CO.2091) | ✅ Done 2026-08-23 (Batch 10) - RF 5/5 live pass (fixed `AUTOTEST_STFLOW` code, per-TC Login/Logout), DB self-clean (0 residual AUTOTEST rows via fresh oracledb connection). Rebuilt `storage_flow_page.resource` from the older unique-timestamped-code/no-filter pattern to Bank/Berth's label-driven, properties-file-driven, T2-consolidated pattern, with grid `Find/Clear Storage Flow Row By Filter` wiring added. Kept the existing proven mandatory dropdowns `Flow Direction`/`Storage` (`__FIRST__`) already used by the page object and `py/storage_flow_iud.py` (per the Batch-9 Process Train lesson - trust the existing proven field set), added to `testdata/storage_flow_insert.properties`, excluded from the round-trip form-label compare. Full `tests/` dryrun 767/767 pass; robocop 12 issues (4 VAR02 + 5 DOC02 + 3 credentials.py baseline noise), identical in kind/count to the merged Berth baseline; filter keyword (`Find Object Row By Filter`) fired 15x, confirmed via output.xml grep. No T2/T1 changes needed. cascade-dropdown (first-available) OV-reuse-target | see PR (feature/storage-flow-bank-pattern, independent clone under Workplaces/storage_flow); see docs/ov-reuse-targets.md |
| UOP Key (OV, CD.0099) | ✅ FULL Bank-pattern (2026-08-25) - live RF 5/5 (fixed UOP_AUTOTEST code, per-TC Login/Logout, properties-file-driven insert/update/verify, explicit grid-filter wiring, zero inline DB-verify calls in the test file - fixes the 2026-07-26 build's exact Issue #504 deviation), DB self-clean 0 residual (fresh conn); Playwright bundle (py/uop_key_iud.py) unchanged | see workstreams/master-plan/ec-automation/docs/bank-pattern-conversion-checklist.md |
| Process Train (OV, CO.0120) | ✅ Done 2026-08-23 (Batch 9) - RF 5/5 live pass, DB-verified vs OV_PROCESS_TRAIN (Name), self-clean (0 residual `AUTOTEST_PT` rows via fresh oracledb connection, confirmed both pre-run free and post-run clean). Rebuilt `process_train_page.resource` from the older fixed-argument/no-filter pattern to Bank/Berth's label-driven, properties-file-driven, T2-consolidated pattern, with grid `Find/Clear Process Train Row By Filter` wiring added from the start. **CORRECTION found live this batch:** the earlier "no mandatory dropdowns" claim was wrong for persistence - a live attempt with only Code/Name/Start Date clicked Save successfully but the row never reached `OV_PROCESS_TRAIN` and left EC's own unsaved-changes confirmation modal open, stalling the suite; re-running the already-proven `py/process_train_iud.py` unmodified (which fills **Production Facility Class 1** = `__FIRST__`) passed 7/7 cleanly, so that dropdown is now included in `testdata/process_train_insert.properties` too (excluded from the round-trip form-label compare list, since `__FIRST__` never matches the resolved literal text on reload). Full `tests/` dryrun 762/762 pass; robocop 9 issues (4 VAR02 + 5 DOC02), identical in kind/count to the established baseline; filter keyword (`Find Process Train Row By Filter`) confirmed fired via output.xml grep. No T2/T1 changes needed | see PR (feature/process-train-bank-pattern, independent clone under Workplaces/process_train); see docs/ov-reuse-targets.md |
| Calculation Group Context (OV, CO.0245) | ✅ Done 2026-07-26 (Tier-1 baseline) - RF 4/4 + Playwright 7/7 via verify_screen.py gate (OVERALL PASS), DB-verified vs OV_CALC_GRP_CONTEXT (Name), self-clean; cascade-dropdown OV-reuse-target; label-driven, generator-scaffolded. **UPGRADED to FULL Bank-pattern 2026-08-23 (batch 7)**: RF 5/5 (added TC04 Find), properties-file-driven Insert/Update/Verify (`testdata/calculation_group_context_*.properties`), explicit `Find/Clear Calculation Group Context Row By Filter` grid-filter wiring (23 hits confirmed in output.xml), dedicated `CALCULATION_GROUP_CONTEXT_EC_USER/PASS`, fixed test code `AUTOTEST_CGC_BANK`; dryrun 753/753; DB self-clean re-confirmed via a fresh oracledb connection (0 residual); no shared T1/T2 file edits | see docs/ov-reuse-targets.md; docs/bank-pattern-conversion-checklist.md "Batch 7" section; docs/ec_screen_registry.md |
| Deferment Group (OV, CO.0149) | ✅ Done 2026-07-26 (Tier-1 baseline) then **rebuilt to full Bank-pattern 2026-08-23 (Batch 8), BLOCKED on `TV_T_BASIS_ACCESS` LEVEL_ID=0 ("No access") for all 5 roles, RE-VERIFIED + merged 2026-08-23 after owner fixed sysadmin-role access**: page object/suite/testdata were already built (Batch 8) against the current label-driven, properties-file-driven, T2-consolidated Bank/Berth/Bank-Account pattern but could never be run live since the screen link wasn't even findable via menu search; owner granted sysadmin access, live login + menu-search confirmed the screen now opens, then RF 5/5 live pass (fixed `AUTOTEST_DEFERMENT_GROUP` code, per-TC Login/Logout), DB self-clean (0 residual rows in `OV_DEFERMENT_GROUP` via a fresh independent oracledb connection, `CODE` column), grid-filter keyword (`Find/Clear Deferment Group Row By Filter`) confirmed fired 15x via output.xml grep, full `tests/` dryrun 774/774 pass, robocop clean on the page object (0 issues) and 9 issues on the suite (4 VAR02 + 5 DOC02, same accepted baseline-noise class as Bank Account/Berth/Storage Flow, no regression); no shared T1/T2 file edits | see docs/ov-reuse-targets.md |
| EC Code Object (OV, CD.0135) | ✅ Done 2026-07-26 (RF 4/4 + Playwright 7/7) then ✅ FULL Bank-pattern conversion 2026-08-24 (PR #494) - RF live 5/5 (TC01-05 incl. added Find), properties-file-driven insert/update/verify, explicit grid-filter wiring (fired 15x), fixed AUTOTEST_EC_CODE_OBJECT code, per-TC Login/Logout, de-facto-mandatory EC Code Type/EC Code dropdowns (__FIRST__) per the proven driver, DB self-clean 0 residual via fresh connection | see docs/ov-reuse-targets.md |
| Reservoir Block Formation (OV junction, CO.0137) | OK Done 2026-08-25 - Playwright driver 15/15 DB-verified + self-clean (2026-07-27), Issue #401 dropdown-by-CODE fix + FK-linkage re-verify (2026-08-18). RF 5-TC multi-object suite (create Block+Formation -> link via RBF -> update -> delete -> teardown parents reverse-order) RE-VERIFIED live 2026-08-25: robocop clean, dryrun 5/5, live 5/5 PASS, DB self-clean confirmed (fresh oracledb conn, 0 residual rows in OV_RESV_BLOCK_FORMATION/OV_RESV_FORMATION/OV_RESV_BLOCK for the AUTOTEST codes). Screen is a junction OV with dependent-cascade dropdowns and multi-object parent/child teardown - structurally NOT plain-Bank-shaped (no single Date+GO, no properties-file-driven T2 Insert/Update, no Find/Clear Row By Filter); correctly out of scope for the plain Bank-pattern conversion round; kept as its own purpose-built multi-object suite | none - suite is complete and passing |
| Conversion Group (custom-URL OV, CO.1049) | ✅ Done 2026-07-27 - RF 4/4 + Playwright 7/7 via verify_screen.py gate (OVERALL PASS), DB-verified vs OV_CONVERSION_GROUP (Name), self-clean; 1st custom-URL OV (no navigator GO, toolbar Refresh); label-driven | see docs/ov-non-bank-targets.md |
| Document Sequence (custom-URL OV, CD.0109) | ✅ Bank-pattern converted 2026-08-25 - RF 5/5 live (was 4/4) + Playwright 7/7 (unchanged); properties-file-driven, explicit grid-filter wiring, fixed test code, per-TC login/logout, PURE SCREEN verification (removed 3 inline DB-verify calls previously embedded in the .robot file); DB self-clean 0 residual (fresh connection); full-tree dryrun 811/811 | see docs/ov-non-bank-targets.md |
| Calculation Library (custom-URL OV, CO.1060) | ✅ Done 2026-07-27 - RF 4/4 + Playwright 7/7 via verify_screen.py gate (OVERALL PASS), DB-verified vs OV_CALC_LIBRARY (Name), self-clean; custom-URL OV; label-driven | see docs/ov-non-bank-targets.md |
| Task Process (custom-URL OV, CO.0191) | ✅ Done 2026-07-27 - RF 4/4 + Playwright 7/7 via verify_screen.py gate (OVERALL PASS), DB-verified vs OV_TASK_PROCESS (Name), self-clean; custom-URL OV; label-driven | see docs/ov-non-bank-targets.md |
| Node (OV-GM, CD.0006) | OK Done 2026-07-30 - RF 4/4 pass + Playwright 8/8 via verify_screen.py (OVERALL PASS), DB-verified vs OV_NODE (Name), self-clean; OV-GM gated-navigator; label-driven; Op PU first-available | see docs/ov-non-bank-targets.md |
| Chemical Tank (OV-GM, CO.0070) | OK Done 2026-07-30 - RF 4/4 pass + Playwright 8/8 via verify_screen.py (OVERALL PASS), DB-verified vs OV_CHEM_TANK (Name), self-clean; OV-GM gated-navigator; label-driven; Op PU first-available | see docs/ov-non-bank-targets.md |
| Chemical Injection Point (OV-GM, CO.0212) | OK Converted to Area's FULL pattern 2026-08-26 - RF 5/5 live pass, DB-verified vs OV_CHEM_INJ_POINT, self-clean; properties-file-driven insert/update/verify, per-TC login, `Apply Navigator From Properties` (3-level PU->Area->Facility Class 1 cascade), zero inline DB-verify calls | see docs/ov-non-bank-targets.md |
| Test Separator (OV-GM, CO.0040) | OK Done 2026-07-30 - RF 4/4 pass + Playwright 8/8 via verify_screen.py (OVERALL PASS), DB-verified vs OV_TESTSEPARATOR (Name), self-clean; OV-GM gated-navigator; label-driven; Op PU first-available | see docs/ov-non-bank-targets.md |
| Production Separator (OV-GM, CO.0042) | OK Done 2026-07-30 (base IUD, verify_screen PASS) - MODIFIED 2026-08-26: FULL Area-pattern structural conversion (owner standing rule: any navigator screen matching Area's layout gets Area's FULL pattern). 5 TCs (Verify Clean State/Insert/Update/Find/Delete), per-TC Login/Logout, FIXED test code `AUTOTEST_PSEP` (confirmed free in OV_PRODSEPARATOR via fresh oracledb connection, before AND after the live run: 0/0). Genuine 3-level Production Unit -> Area -> Facility Class 1 navigator cascade (`nav:form:G:0:R:1:C:1..3:dd`, confirmed live) now filled via the shared T2 `Apply Navigator From Properties`, driven by EXPLICIT `testdata/production_separator_navigator.properties` values (Op Production Unit=AS1 EC Exploration Norway, Op Area=AS1_Area, Op Facility Class 1=AS1_Facility_01 - the SAME values the prior "First Available" cascade already resolved to, captured explicitly rather than re-invented), replacing the old `Apply OV-GM Navigator First Available` inline cascade. Properties-file-driven Insert/Update/verify via T2 (`testdata/production_separator_{insert,update,form_verify,grid_verify}.properties`). Explicit `Find/Clear Production Separator Row By Filter` grid-filter wiring (Update/Find/Verify-Found/Delete). PURE SCREEN verification in the .robot file (0 inline DB-verify calls - TC05's DB check lives in the shared T2 `Verify Object Removed`; confirmed via grep). Real field labels confirmed live: screen-prefixed "Production Separator Code"/"Production Separator Name" - every T2 call passes `code_label=Production Separator Code`. Gotcha found + fixed live: the objectForm's mandatory "Op Production Unit" dropdown is a date-effective reference filtered by the form's own Start Date (reference_ec_object_start_date_version.md) - requesting the EXACT navigator value there (post Start-Date-fill) reproducibly timed out (2 live repros); switched to the shared T2's `__FIRST__` literal, exactly matching the pre-existing proven driver's own mechanism. RF live 5/5 pass (after fix), DB self-clean 0 residual (fresh oracledb connection), filter keyword fired 15x (output.xml grep). robocop 7 issues (all DOC02), identical in kind/count to Area's own baseline (7 issues). Full `tests/` dryrun 850/850 pass. No T2/T1 shared-file changes needed. Playwright driver (`py/production_separator_iud.py`) left untouched. | see docs/ov-non-bank-targets.md |
| Test Device (OV-GM, CO.0123) | OK Done 2026-07-30 - RF 4/4 pass + Playwright 8/8 via verify_screen.py (OVERALL PASS), DB-verified vs OV_TEST_DEVICE (Name), self-clean; OV-GM gated-navigator; label-driven; Op PU first-available | see docs/ov-non-bank-targets.md |
| Channel (OV-GM, CO.2077) | OK Done 2026-07-30 - RF 4/4 pass + Playwright 8/8 via verify_screen.py (OVERALL PASS), DB-verified vs OV_CHANNEL (Name), self-clean; OV-GM gated-navigator; label-driven; Op PU first-available | see docs/ov-non-bank-targets.md |
| Loading Arm (OV-GM, CO.2078) | OK Done 2026-07-30 - RF 4/4 pass + Playwright 8/8 via verify_screen.py (OVERALL PASS), DB-verified vs OV_LOADING_ARM (Name), self-clean; OV-GM gated-navigator; label-driven; Op PU first-available | see docs/ov-non-bank-targets.md |
| Pilot Boat (OV-GM, CO.2081) | OK Done 2026-07-30 - RF 4/4 pass + Playwright 8/8 via verify_screen.py (OVERALL PASS), DB-verified vs OV_PILOT_BOAT (Name), self-clean; OV-GM gated-navigator; label-driven; Op PU first-available | see docs/ov-non-bank-targets.md |
| Tug Boat (OV-GM, CO.2080) | OK Done 2026-07-30 - RF 4/4 pass + Playwright 8/8 via verify_screen.py (OVERALL PASS), DB-verified vs OV_TUG_BOAT (Name), self-clean; OV-GM gated-navigator; label-driven; Op PU first-available | see docs/ov-non-bank-targets.md |
| Well Hookup (OV-GM, CO.0108) | OK Done 2026-08-26 - FULL Area-pattern structural conversion on top of the 2026-07-30 build (owner standing rule: any navigator screen matching Area's layout gets Area's FULL pattern). 5 TCs (Verify Clean State/Insert/Update/Find/Delete), per-TC Login/Logout, FIXED test code `AUTOTEST_WH` (confirmed free in DB), properties-file-driven Insert/Update/verify via T2, explicit `Find/Clear Well Hookup Row By Filter` grid-filter wiring (Update/Find/Verify-Found/Delete), PURE SCREEN verification in the .robot file (0 inline DB-verify calls - TC05's DB check lives in the shared T2 `Verify Object Removed`). Genuine 3-level PU->Area->Facility Class 1 cascade (via `Apply Navigator From Properties`, values captured live via tmp/recon_well_hookup_navigator_cascade.py: AS1 EC Exploration Norway / AS1_Area / AS1_Facility_01) KEPT unchanged. RF live 5/5 pass, DB self-clean 0 residual (fresh oracledb connection), filter keyword fired 15x (output.xml grep). robocop 7 issues (2 VAR02 + 5 DOC02), identical in kind/count to Facility Class 1's own accepted baseline. Full `tests/` dryrun 850/850 pass. No T2/T1 changes needed | see docs/ov-non-bank-targets.md |
| Facility Class 1 (OV-GM, CO.0019) | OK Done 2026-08-26 - FULL Bank-pattern structural conversion on top of PR #526's navigator-fill (owner standing rule: any navigator screen matching Area's layout gets Area's FULL pattern). 5 TCs (Verify Clean State/Insert/Update/Find/Delete), per-TC Login/Logout, FIXED test code `AUTOTEST_FC1` (confirmed free in DB), properties-file-driven Insert/Update/verify via T2, explicit `Find/Clear Facility Class 1 Row By Filter` grid-filter wiring (Update/Find/Verify-Found/Delete), PURE SCREEN verification in the .robot file (0 inline DB-verify calls - TC05's DB check lives in the shared T2 `Verify Object Removed`). Genuine 2-level PU->Area cascade (via "Apply Navigator From Properties") KEPT unchanged; 0.7s flat-sleep timing re-verified live under per-TC login/logout - RF live 5/5 pass, DB self-clean 0 residual (fresh oracledb connection), filter keyword fired 26x (output.xml grep). robocop 7 issues (2 VAR02 + 5 DOC02), identical in kind/count to Area's own baseline (7 issues). Full `tests/` dryrun 847/847 pass. No T2/T1 changes needed | see docs/ov-non-bank-targets.md |
| Storage (OV-GM, CO.0034) | OK Done 2026-08-26 - FULL Area-pattern structural conversion on top of the 2026-07-30 verify_screen-PASS build (owner standing rule: any navigator screen matching Area's layout gets Area's FULL pattern). 5 TCs (Verify Clean State/Insert/Update/Find/Delete), per-TC Login/Logout, FIXED test code `AUTOTEST_STG` (confirmed free in DB), properties-file-driven Insert/Update/verify via T2, explicit `Find/Clear Storage Row By Filter` grid-filter wiring (Update/Find/Verify-Found/Delete), PURE SCREEN verification in the .robot file (0 inline DB-verify calls - TC05's DB check lives in the shared T2 `Verify Object Removed`). Genuine 3-level PU->Area->Facility Class 1 cascade converted from "Apply OV-GM Navigator First Available" to "Apply Navigator From Properties" with EXPLICIT values confirmed live via a dedicated recon script (`tmp/recon_storage_navigator_cascade.py`): Op Production Unit=AS1 EC Exploration Norway, Op Area=AS1_Area, Op Facility Class 1=AS1_Facility_01 (same values first-available already picked, captured explicitly). Storage's own genuine mandatory Storage Type/Product Name dropdowns kept as `__FIRST__` in `testdata/storage_insert.properties`, preserving the proven driver's exact handling. RF live 5/5 pass, DB self-clean 0 residual (fresh oracledb connection), filter keyword fired 15x (output.xml grep). robocop 10 issues (5 DOC02 on the suite + 3 on the pre-existing shared credentials.py), at/below the established baseline-noise class. Full `tests/` dryrun 850/850 pass. No T2/T1 changes needed | see docs/ov-non-bank-targets.md |
| Contract Area Setup (OV custom-URL, CO.2038) | OK Done 2026-07-30 - RF 4/4 pass + Playwright 7/7 via verify_screen.py (OVERALL PASS), DB-verified vs OV_CONTRACT_AREA_SETUP (Name), self-clean; custom-URL OV (no navigator/GO); label-driven; 2 mandatory ref dds first-available | see docs/ov-non-bank-targets.md |
| Lifting Account (OV-GM, CO.2004) | OK Done 2026-07-30 - RF 4/4 pass + Playwright 8/8 via verify_screen.py (OVERALL PASS), DB-verified vs OV_LIFTING_ACCOUNT (Name), self-clean; OV-GM 4-level nav (SPECIFIC P1 values incl. Storage 2nd row); label-driven; Company first-avail + Storage Name = nav Storage | see docs/ov-non-bank-targets.md |
| Well (OV-GM, CO.0049) | OK Done 2026-07-30 (base IUD, Playwright 8/8) - MODIFIED 2026-08-26: FULL Area-pattern structural conversion of the RF suite (owner standing rule: any EC screen with a navigator matching Area's layout must follow Area's FULL pattern), converted directly from the OLD 4-TC/suite-level-login/timestamped-code/inline-DB-verify shape (not a follow-up to a prior navigator-only PR - Well had none). 5 TCs (Verify Clean State/Insert/Update/Find/Delete), per-TC Login/Logout on one Suite-Setup browser, dedicated `WELL_EC_USER`/`WELL_EC_PASS` pair (additive in `resources/credentials.py`). FIXED test code `AUTOTEST_WELL` (confirmed free in `OV_WELL` via fresh oracledb connection, before AND after the live run: 0/0). Genuine 3-level P1 navigator cascade (Production Unit -> Area -> Facility 1, same-row C:1/C:2/C:3) KEPT unchanged, now filled via the shared T2 `Apply Navigator From Properties` (resources/manage_object.resource - already documented there as a proven 3-level case) driven by `testdata/well_navigator.properties`, instead of the old inline `Select EC Dropdown Option`x3 + `Apply Navigator`. Well Type mandatory first-available dropdown preserved via the `__FIRST__` sentinel in `testdata/well_insert.properties`. Removed the screen-local `Well Should/Should Not Exist In DB` wrapper keywords - verification now delegates entirely to T2's `Verify Object Insert Exists/Form Record/Found/Removed/Does Not Exist` (confirmed via grep: zero inline DB-verify calls remain in well_iud.robot). Explicit grid-filter wiring (`Find/Clear Well Row By Filter`) added into Update/Find/Verify-Found/Delete (14 `Find Object Row By Filter`-family hits confirmed in live output.xml). Properties-file-driven insert/update via T2's `Insert/Update Object From Properties` (5 new `testdata/well_{navigator,insert,update,form_verify,grid_verify}.properties` files). Real field labels reused verbatim from the pre-existing driver: screen-prefixed "Well Code"/"Well Name" - every T2 call passes `code_label=Well Code`. RF live 5/5, full-tree dryrun 850/850 (zero collisions), robocop 7 issues (2 VAR02 + 5 DOC02) - exact parity with Area's own reference-pattern files (same command against area_page.resource/area_iud.robot: also 7 issues, same categories). No shared T1/T2 file changes (Well's cascade already fit the existing `Apply Navigator From Properties` keyword). | see docs/ov-non-bank-targets.md |
| Chemical Stream (OV-GM, CO.0258) | OK Done 2026-08-26 - converted to FULL Area-pattern structure (5 TCs, per-TC login/logout, fixed code AUTOTEST_CHS, properties-file-driven insert/update/verify, explicit grid-filter wiring, zero inline DB-verify calls); live RF 5/5 pass, full-tree dryrun 850/850 pass, DB self-clean confirmed (0 residual AUTOTEST% rows in OV_CHEM_STREAM); OV-GM SPECIFIC-P1-values 3-level nav (Production Unit -> Area -> Facility Class 1) via shared T2 Apply Navigator From Properties + mandatory From Connection POPUP (stream_node_ref_popup: inner Object Type CHEM_TANK + inner GO, grid manage_object_nav_nav:form:T_data - screen-local pickers PRESERVED UNCHANGED, confirmed still firing live) | see docs/ov-non-bank-targets.md |
| Shift (OV-GM, CO.0224) | OK Done 2026-08-26 - converted to Area's FULL pattern (5-TC/per-TC-login/pure-screen-verify, navigator via shared "Apply Navigator From Properties" + testdata/shift_navigator.properties); live 5/5, dryrun 850/850, DB self-clean vs OV_SHIFT, filter keyword fired 15x, zero inline DB-verify calls; mandatory FREE-TEXT Start Time (HH:MI) kept as-is via properties-driven insert; prior state OK Done 2026-07-31 - RF 4/4 pass + Playwright 8/8 via verify_screen.py (OVERALL PASS) | see docs/ov-non-bank-targets.md |
| Cargo Planning Forecast (custom forecast-manager OV, CP.0030) | OK Done 2026-07-31 - RF 4/4 pass + Playwright 8/8 via verify_screen.py (OVERALL PASS), DB-verified vs OV_FCST_MNGR_FCST_LIST (Name), self-clean; per-field nav groups (SPECIFIC P1 + Storage); mandatory End Date; grid fcst:form:T_data; dual-view (FCST_MNGR_FCST_LIST primary); label-driven | see docs/ov-non-bank-targets.md |
| Create Calculation (TV-style calc header, CO.1042) | OK Done 2026-07-31 - RF 4/4 pass + Playwright 8/8 via verify_screen.py (OVERALL PASS), DB-verified vs OV_CALCULATION (Name), self-clean; context-gated dual grid; blank-row Period/Type dds; UPDATE via VERSIONS grid; DELETE CALCULATION button; header IUD only | see docs/ov-non-bank-targets.md |
| Well Hole (OV-GM, CO.0051) | OK Done 2026-08-26 - converted to Area's full RF pattern (5-TC/per-TC-login/pure-screen-verify/properties-file-driven/explicit grid filter); live RF 5/5, DB self-clean vs OV_WELL_HOLE (fresh conn, 0 residual AUTOTEST); 3-level SPECIFIC "P1" navigator scope (proven live, same as sibling Well) via shared `Apply Navigator From Properties`; full-tree dryrun 850/850; Playwright bundle (8/8, 2026-07-31) unchanged | see docs/ov-non-bank-targets.md |
| Well Bore (OV-GM, CO.0054) | OK Done 2026-07-31 - RF 4/4 pass + Playwright 8/8 via verify_screen.py (OVERALL PASS), DB-verified vs OV_WELL_BORE (Name), self-clean; OV-GM per-field nav (SPECIFIC real-well value); mandatory Well POPUP (grid Objects:form:T_data, 3rd variant); label-driven | see docs/ov-non-bank-targets.md |
| Well Bore Interval (OV-GM, CO.0057) | OK Done 2026-07-31 - RF 4/4 pass + Playwright 8/8 via verify_screen.py (OVERALL PASS), DB-verified vs OV_WELL_BORE_INTERVAL (Name), self-clean; OV-GM 6-group per-field nav (G:6 = well bore; G:5 unusable); mandatory Well Bore POPUP (grid Objects:form:T_data); label-driven | see docs/ov-non-bank-targets.md |
| Perforation Interval (OV-GM, CO.0153) | OK Done 2026-07-31 - RF 4/4 pass + Playwright 8/8 via verify_screen.py (OVERALL PASS), DB-verified vs OV_PERF_INTERVAL (Name), self-clean; OV-GM 7-group per-field nav (full well chain; G:5 unusable); INNER-GO Well Bore Interval POPUP + Reservoir Block Formation dd; label-driven | see docs/ov-non-bank-targets.md |
| Truck (plain OV, CO.0264) | OK Done 2026-07-31 - RF 4/4 pass + Playwright 8/8 via verify_screen.py (OVERALL PASS), DB-verified vs OV_TRUCK (Name), self-clean; plain OV (custom grid truck_object:form:T_data, empty navigator); 9 mandatory fields from EC save-messages; FIRST screen from the new plain-OV generator gen_ov.py | see docs/ov-non-bank-targets.md |
| Trailer (plain OV, CO.0265) | OK Done 2026-08-23 (Batch 10 Bank-pattern conversion) - RF live 5/5 pass, DB-verified vs OV_TRAILER, self-clean (0 residual, fresh connection), filter keyword fired (output.xml grep); rebuilt properties-file-driven insert/update + explicit grid-filter wiring (`Find/Clear Trailer Row By Filter`), matching Bank/Berth's full shape; plain OV (date-only navigator + GO, custom grid trailer_object:form:T_data) | see docs/ov-non-bank-targets.md |
| Driver (plain OV, CO.0266) | OK Done 2026-07-31 - RF 4/4 pass + Playwright 8/8 via verify_screen.py (OVERALL PASS), DB-verified vs OV_DRIVER (Name), self-clean; plain OV (date-only navigator + GO, no cascade); label-driven | see docs/ov-non-bank-targets.md |
| Pilot (OV-GM, CO.2079) | OK Done 2026-07-31 - RF 4/4 pass + Playwright 8/8 via verify_screen.py (OVERALL PASS), DB-verified vs OV_PILOT (Name), self-clean; OV-GM gated-navigator; label-driven; Op PU first-available | see docs/ov-non-bank-targets.md |
| Report Group (plain OV, CO.0158) | OK Done 2026-08-24 - Bank-pattern conversion: properties-file-driven insert/update/verify, explicit grid-filter wiring, fixed test code, per-TC Login/Logout, 5 TCs (added Find); RF live 5/5, full-tree dryrun 798/798, robocop parity vs Disposition Type, DB-verified vs OV_REPORT_GROUP, self-clean via fresh connection, filter keyword fired 15x; plain OV (date-only navigator + GO, no cascade); label-driven | see docs/ov-non-bank-targets.md, docs/bank-pattern-conversion-checklist.md |
| Service (OV-GM, CO.2103) | OK Done 2026-08-01 (base IUD, verify_screen PASS) - MODIFIED 2026-08-26: converted to Area's full pattern (owner standing rule - any EC screen with a navigator section matching Area's layout follows Area's FULL pattern). Navigator is a SINGLE Business Unit dropdown at row 1/C:1 (Contract Area/Contract are further nav columns visible on the screen but are OPTIONAL filters - confirmed live, GO with only Business Unit set already loads the grid); fill now delegates to the shared T2 `Apply Navigator From Properties` driven by `testdata/service_navigator.properties` (value `TS3 BU1`, the same value the pre-existing driver already proved live), replacing the old inline `Select EC Dropdown Option`+`Apply Navigator`. Converted the RF suite's STRUCTURE to Area's 5-TC/per-TC-login/pure-screen-verify shape (added TC04 Find, was 4 TCs). Fixed test code `AUTOTEST_SERVICE` (confirmed free via fresh oracledb query on OV_SERVICE, before AND after the live run: 0/0). Removed the screen-local `Service Should/Should Not Exist In DB` wrapper keywords and their inline calls - verification now delegates entirely to T2's `Verify Object Insert Exists/Form Record/Found/Removed/Does Not Exist` (confirmed via grep: zero inline DB-verify calls remain in service_iud.robot). Explicit grid-filter wiring (`Find/Clear Service Row By Filter`) added into Update/Find/Verify-Found/Delete (14 explicit + 1 T2-internal = 15 `Find Object Row By Filter` hits in output.xml). Properties-file-driven insert/update via T2's `Insert/Update Object From Properties` (testdata/service_{insert,update,form_verify,grid_verify}.properties) - mandatory-field set kept EXACTLY as the pre-existing proven driver (Service Template/Service Type/Service Status/Contract/Transport System). Real field labels confirmed (pre-existing driver, shipped 2026-08-01): screen-prefixed "Service Code"/"Service Name" - every T2 call passes `code_label=Service Code`. RF live 5/5 (after clearing a residual `AUTOTEST_SERVICE` row left by an earlier interrupted live attempt - confirmed via fresh oracledb query, self-clean 0/0 both before and after the final run), full-tree dryrun 850/850 (zero collisions), robocop parity vs Area's own reference files (same DOC02-only issue categories, same count). No shared T1/T2 file changes this round (`Apply Navigator From Properties` already existed from the Area conversion). | see docs/ov-non-bank-targets.md, docs/bank-pattern-conversion-checklist.md |
| Area (OV-GM, CO.0003) | OK Done 2026-08-01 (base IUD, verify_screen PASS) - MODIFIED 2026-08-25: owner-directed EXCEPTION converting the RF suite's STRUCTURE to the Bank-pattern 5-TC/per-TC-login/pure-screen-verify shape while KEEPING the genuine mandatory Production Unit navigator gesture (Area remains OV-GM, this is NOT a claim it is now plain Bank-shaped - Area is still excluded from the general Bank-pattern conversion initiative per docs/bank-pattern-conversion-checklist.md; this is a one-off, not a reclassification of the OV-GM family). Added TC04 Find (was 4 TCs, now 5). Fixed test code `AUTOTEST_AREA` (confirmed free via fresh oracledb query on OV_AREA, before AND after the live run: 0/0). Removed the screen-local `Area Should Exist/Not Exist In DB` wrapper keywords - verification now delegates entirely to T2's `Verify Object Insert Exists/Form Record/Found/Removed/Does Not Exist` (confirmed via grep: zero inline DB-verify calls remain in area_iud.robot). Explicit grid-filter wiring (`Find/Clear Area Row By Filter`) added into Update/Find/Verify-Found/Delete. Properties-file-driven insert/update via T2's `Insert/Update Object From Properties` (testdata/area_{insert,update,form_verify,grid_verify}.properties). Real field labels confirmed live: screen-prefixed "Area Code"/"Area Name" (not generic "Code"/"Name") - every T2 call passes `code_label=Area Code`. UPDATED 2026-08-26 (rebase of stale PR #522 onto this 5-TC structure): the Production Unit navigator fill in `Open Area Screen With Navigator Values Populated` now delegates to a new shared T2 keyword `Apply Navigator From Properties` (resources/manage_object.resource) driven by `testdata/area_navigator.properties`, instead of the inline `Select EC Dropdown Option`+`Apply Navigator` this 5-TC conversion originally wrote - any other OV-GM screen with a navigator can reuse the same shared keyword with its own properties file (single-dropdown/same-row cascades only; per-field nav-group screens are a documented known limitation, not yet solved). RF live 5/5 (re-confirmed on the new keyword), full-tree dryrun 846/846 (matches the post-#521 baseline exactly, zero collisions), robocop +3 issues vs the pre-existing 19-issue baseline for these 3 files (1 LEN01 on the new keyword's name + 2 DEPR05 `Set Variable`, same categories as the adjacent sibling keyword `Apply OV-GM Navigator First Available` - parity, no new issue type), self-clean confirmed via fresh oracledb connection (0 residual AUTOTEST_AREA rows in OV_AREA), 2 existing OV-GM canary screens with their OWN bespoke navigator-fill logic re-run live UNCHANGED to prove zero regression from the shared-file addition: Well 4/4, Test Separator 4/4. No other 5-TC structure/login/test-code/properties-driven-insert-update/grid-filter wiring touched. Playwright driver (`ec_iud_area.py`) still left untouched. | see docs/ov-non-bank-targets.md |
| External Location (OV-GM, CO.0227) | OK Done 2026-08-01 (base IUD, verify_screen PASS) - navigator-fill converted 2026-08-26 (PR #524, depends-on) to the shared T2 "Apply Navigator From Properties" w/ an intentionally-empty properties file (zero-mandatory-nav edge case for the keyword built for Area) - behaviour unchanged (bare GO). MODIFIED 2026-08-26 (this PR): owner standing rule - any EC screen with a navigator section matching Area's layout follows Area's FULL pattern, not just the navigator-fill piece. Converted the RF suite's STRUCTURE to Area's 5-TC/per-TC-login/pure-screen-verify shape while KEEPING External Location's genuine GO-only/no-mandatory-nav-scope navigator behaviour exactly as-is (this is a structural conversion, NOT a claim the screen now has a mandatory nav scope). Added TC04 Find (was 4 TCs, now 5). Fixed test code `AUTOTEST_EXTERNAL_LOCATION` (confirmed free via fresh oracledb query on OV_EXTERNAL_LOCATION, before AND after the live run: 0/0). Removed the screen-local `External Location Should/Should Not Exist In DB` wrapper keywords and their inline calls - verification now delegates entirely to T2's `Verify Object Insert Exists/Form Record/Found/Removed/Does Not Exist` (confirmed via grep: zero inline DB-verify calls remain in external_location_iud.robot). Explicit grid-filter wiring (`Find/Clear External Location Row By Filter`) added into Update/Find/Verify-Found/Delete (12 explicit + 3 T2-internal = 15 `Find Object Row By Filter` hits in output.xml). Properties-file-driven insert/update via T2's `Insert/Update Object From Properties` (testdata/external_location_{insert,update,form_verify,grid_verify}.properties). Real field labels confirmed (ec-ui-knowledge/screens/external_location.md): screen-prefixed "External Location Code"/"External Location Name" - every T2 call passes `code_label=External Location Code`. RF live 5/5, full-tree dryrun 847/847 (zero collisions), robocop 7 issues (VAR02 x2 + DOC02 x5) - exact parity with Area's own reference-pattern files (same command run against area_page.resource/area_iud.robot: also 7 issues, same categories), DB self-clean confirmed via fresh oracledb connection (0/0 before+after). No shared T1/T2 file changes this round (PR #524's `Apply Navigator From Properties` addition already covers this screen's navigator). | see docs/ov-non-bank-targets.md, docs/bank-pattern-conversion-checklist.md |
| Contract Capacity (OV-GM, CO.2044) | OK Done 2026-08-01 (base IUD, verify_screen PASS) - MODIFIED 2026-08-26: owner standing rule - any EC screen with a navigator section matching Area's layout follows Area's FULL pattern. Converted from 4 TCs (single suite-level login, hardcoded field-id inserts, timestamped `AUTOTEST_CC<timestamp>` code) to 5 TCs (added TC04 Find) with per-TC Login/Logout, fixed test code `AUTOTEST_CONTRACT_CAPACITY` (confirmed free via fresh oracledb query on OV_CONTRACT_CAPACITY, 0/0 before+after), navigator fill delegated to the shared T2 `Apply Navigator From Properties` driven by `testdata/contract_capacity_navigator.properties` (Business Unit=TS5 BU, the same value the pre-existing driver already proved live), properties-file-driven insert/update via T2's `Insert/Update Object From Properties` (`testdata/contract_capacity_{insert,update,form_verify,grid_verify}.properties`), explicit `Find/Clear Contract Capacity Row By Filter` grid-filter wiring into Update/Find/Verify-Found/Delete (15 `Find Object Row By Filter` hits in output.xml), and PURE SCREEN verification only (zero inline DB-verify calls confirmed via grep). Contract Capacity's genuine mandatory Business Unit navigator gesture is UNCHANGED - structural conversion only. RF live 5/5, full-tree dryrun 850/850 (zero collisions), robocop 7 issues (VAR02 x1 + DOC02 x5 + VAR02-doc) - exact parity with Area's own reference-pattern files (same command against area_page.resource/area_iud.robot: also 7 issues). DB self-clean confirmed via fresh oracledb connection (0 residual AUTOTEST_CONTRACT_CAPACITY rows in OV_CONTRACT_CAPACITY). No shared T1/T2 file changes this round. Playwright driver (`py/contract_capacity_iud.py`) left UNTOUCHED - RF structural conversion only | see docs/ov-non-bank-targets.md, docs/bank-pattern-conversion-checklist.md |
| Collection Point (OV-GM, CO.0205) | OK Done 2026-08-01 (base IUD, verify_screen PASS: RF 4/4 + Playwright 8/8, DB-verified vs OV_COLLECTION_POINT). MODIFIED 2026-08-26: FULL Area-pattern structural conversion (owner standing rule: any navigator screen matching Area's layout follows Area's FULL pattern). Genuine 3-level Production Unit -> Area -> Operator Route SAME-ROW cascade (C:1/C:2/C:3, C:4 absent - confirmed live) now delegates to the shared T2 `Apply Navigator From Properties` driven by `testdata/collection_point_navigator.properties` (Op Production Unit=P3 Production Unit, Op Area=P3 Area, Op Operator Route=Oper Route 1). Shared keyword's flat 0.7s sleep confirmed live SUFFICIENT at BOTH cascade transitions - no shared-file change needed, no per-screen extra Sleep added. Added TC04 Find (was 4 TCs, now 5). Fixed test code `AUTOTEST_COLLECTION_POINT` (confirmed free via fresh oracledb query on OV_COLLECTION_POINT, before AND after the live run: 0/0). Removed the screen-local `Collection Point Should/Should Not Exist In DB` wrapper keywords - verification now delegates entirely to T2's `Verify Object Insert Exists/Form Record/Found/Removed/Does Not Exist` (confirmed via grep: zero inline DB-verify calls remain in collection_point_iud.robot). Explicit grid-filter wiring (`Find/Clear Collection Point Row By Filter`) added into Update/Find/Verify-Found/Delete (11 T3-wrapper / 15 underlying T2 `Find Object Row By Filter` hits in output.xml). Properties-file-driven insert/update/verify via T2 (testdata/collection_point_{insert,update,form_verify,grid_verify,navigator}.properties). Real field labels confirmed live via the pre-existing driver: screen-prefixed "Collection Point Code"/"Collection Point Name" - every T2 call passes `code_label=Collection Point Code`. RF live 5/5, full-tree dryrun 850/850 (zero collisions), robocop 7 issues (VAR02 x2 + DOC02 x5) - exact parity with Area's/Facility Class 1's own reference-pattern files. DB self-clean confirmed via fresh oracledb connection (0/0 before+after). No shared T1/T2 file changes this round. | see docs/ov-non-bank-targets.md |
| Action Trigger (custom-URL OV, CO.0193) | OK Done 2026-08-01 - RF 4/4 pass + Playwright 7/7 via verify_screen.py (OVERALL PASS), DB-verified vs OV_CONTROL_POINT (Name), self-clean; custom-URL OV (no navigator/GO; toolbar Refresh); label-driven | see docs/ov-non-bank-targets.md |
| Operator Route (OV-GM, CO.0244) | OK Done 2026-08-01 (base IUD, verify_screen PASS) - MODIFIED 2026-08-26: owner standing rule - any EC screen with a navigator section matching Area's layout follows Area's FULL pattern, not just the navigator-fill piece. Converted the RF suite's STRUCTURE to Area's 5-TC/per-TC-login/pure-screen-verify shape while KEEPING Operator Route's genuine mandatory 2-level Production Unit -> Area same-row cascade + GO exactly as-is (PROVEN explicit values P3 Production Unit / P3 Area carried over unchanged from the pre-conversion driver). Added TC04 Find (was 4 TCs, now 5). Fixed test code `AUTOTEST_OR` (confirmed free via fresh oracledb query on OV_OPERATOR_ROUTE, before AND after the live run: 0/0). Removed the screen-local `Operator Route Should/Should Not Exist In DB` wrapper keywords and their inline calls - verification now delegates entirely to T2's `Verify Object Insert Exists/Form Record/Found/Removed/Does Not Exist` (confirmed via grep: zero inline DB-verify calls remain in operator_route_iud.robot). Navigator fill delegates to the shared T2 `Apply Navigator From Properties` (resources/manage_object.resource) driven by `testdata/operator_route_navigator.properties`. Explicit grid-filter wiring (`Find/Clear Operator Route Row By Filter`) added into Update/Find/Verify-Found/Delete (15 `Find Object Row By Filter` hits in output.xml). Properties-file-driven insert/update via T2's `Insert/Update Object From Properties` (testdata/operator_route_{insert,update,form_verify,grid_verify}.properties). Real field labels confirmed (ec-ui-knowledge/screens/operator_route.md): screen-prefixed "Operator Route Code"/"Operator Route Name" - every T2 call passes `code_label=Operator Route Code`. RF live 5/5, full-tree dryrun 850/850 (zero collisions), robocop parity vs Area's own reference-pattern files (5x DOC02 per screen), DB self-clean confirmed via fresh oracledb connection (0/0 before+after). No shared T1/T2 file changes this round. Playwright driver (`py/operator_route_iud.py`) left untouched. | see docs/ov-non-bank-targets.md, docs/bank-pattern-conversion-checklist.md |
| Chemical Stream Hookup (OV-GM, CO.0260) | OK Done 2026-08-01 (base IUD, verify_screen PASS) - MODIFIED 2026-08-26: owner standing rule - any EC screen with a navigator section matching Area's layout follows Area's FULL pattern, not just the navigator-fill piece. RF suite STRUCTURE converted to the Area-pattern 5-TC/per-TC-login/pure-screen-verify shape while KEEPING both the genuine mandatory 3-level cascade navigator (Production Unit -> Area -> Facility Class 1 + GO) AND this screen's standalone `mandatory_field_gate.resource` pre-flight check exactly as-is (owner instruction). Added TC04 Find (was 4 TCs, now 5). Fixed test code `AUTOTEST_CSH` (confirmed free via fresh oracledb query on OV_CHEM_STRM_HOOKUP, 0 residual before AND after the live run). Removed the screen-local `Chemical Stream Hookup Should/Should Not Exist In DB` wrapper keywords - verification now delegates entirely to T2's `Verify Object Insert Exists/Form Record/Found/Removed/Does Not Exist` (confirmed via grep: zero inline DB-verify calls remain in chemical_stream_hookup_iud.robot). Explicit grid-filter wiring (`Find/Clear Chemical Stream Hookup Row By Filter`) added into Update/Find/Verify-Found/Delete (22 `Find Object Row By Filter` hits in output.xml). Properties-file-driven insert/update via a T3-local `Fill Object Form Fields And Save With Gate` helper (inlines the mandatory-field gate call before Save, since T2's own Insert/Update Object From Properties has no gate step) - `testdata/chemical_stream_hookup_{insert,update,form_verify,grid_verify}.properties`. The cascade navigator now delegates to the shared T2 `Apply Navigator From Properties` (resources/manage_object.resource) driven by `testdata/chemical_stream_hookup_navigator.properties`, replacing the prior `Apply OV-GM Navigator First Available` inline call - values (`AS1 EC Exploration Norway` / `AS1_Area` / `AS1_Facility_01`) confirmed LIVE via a temporary probe reading back each column's actual first-available resolution, not guessed or copied from a sibling screen. RF live 5/5, full-tree dryrun 850/850 (zero collisions), robocop 7 issues (VAR02 x2 + DOC02 x5) - exact parity with Area's own reference-pattern files, DB self-clean confirmed via fresh oracledb connection (0 residual AUTOTEST% rows in OV_CHEM_STRM_HOOKUP). No shared T1/T2 file changes this round. Playwright driver (`py/chemical_stream_hookup_iud.py`) left untouched (RF `.robot` structural conversion only). | see docs/ov-non-bank-targets.md |
| Price Rate (OV-GM, CO.3024) | OK Done 2026-08-02 (base IUD, verify_screen PASS) - CONVERTED 2026-08-26 to the Area-pattern RF suite STRUCTURE: 5 TCs (added TC04 Find), per-TC Login/Logout, fixed test code `AUTOTEST_PRICE_RATE` (confirmed free via fresh oracledb query on OV_PRICE_RATE, before AND after the live run: 0/0). Removed the screen-local `Price Rate Should Exist/Not Exist In DB` wrapper keywords - verification now delegates entirely to T2's `Verify Object Insert Exists/Form Record/Found/Removed/Does Not Exist` (confirmed via grep: zero inline DB-verify calls remain in price_rate_iud.robot). Explicit grid-filter wiring (`Find/Clear Price Rate Row By Filter`) added into Update/Find/Verify-Found/Delete. Properties-file-driven insert/update via T2's `Insert/Update Object From Properties` (testdata/price_rate_{insert,update,form_verify,grid_verify}.properties). The mandatory Business Unit navigator fill now uses the shared T2 `Apply Navigator From Properties` driven by `testdata/price_rate_navigator.properties`, same "SS2 BU" value the prior OLD-pattern bespoke inline fill already proved live. RF live 5/5, full-tree dryrun 850/850 (0 collisions), robocop 7 issues on the 2 changed files (parity with Area's own 7-issue baseline, all DOC02/VAR02), self-clean confirmed via fresh oracledb connection (0 residual AUTOTEST rows in OV_PRICE_RATE), filter keyword fired 14x (output.xml grep). Old Playwright driver (`py/price_rate_iud.py`) left untouched this round (RF-only conversion). | see docs/ov-non-bank-targets.md |
| Contract Inventory (OV-GM, CO.2054) | OK Done 2026-08-02 (base IUD, verify_screen PASS) - CONVERTED 2026-08-26 to the FULL Area-pattern RF suite STRUCTURE: 5 TCs (added TC04 Find), per-TC Login/Logout, fixed test code `AUTOTEST_CONTRACT_INVENTORY` (confirmed free via fresh oracledb query on OV_CONTRACT_INVENTORY, 0 residual before AND after the live run). Removed the screen-local `Contract Inventory Should/Should Not Exist In DB` wrapper keywords - verification now delegates entirely to T2's `Verify Object Insert Exists/Form Record/Found/Removed/Does Not Exist` (confirmed via grep: zero inline DB-verify calls remain in contract_inventory_iud.robot). Explicit grid-filter wiring (`Find/Clear Contract Inventory Row By Filter`) added into Update/Find/Verify-Found/Delete (26 `Find Object Row By Filter` hits in output.xml). Properties-file-driven insert/update via T2's `Insert/Update Object From Properties` (testdata/contract_inventory_{insert,update,form_verify,grid_verify}.properties). Navigator fill now uses the shared T2 `Apply Navigator From Properties` driven by `testdata/contract_inventory_navigator.properties` - ZERO shared-file changes needed (single-row, increasing-column shape already supported). **Live recon correction**: the pre-existing "Business Unit -> Contract Area cascade" note reads as 2 mandatory levels, but the live mandatory-yellow DOM check found ONLY Business Unit (C:1) is genuinely mandatory+empty; Contract Area (C:2) stays optional even after C:1 is filled - kept both filled anyway for parity with the already-proven prior scope. RF live 5/5, full-tree dryrun 878/878 (zero collisions), robocop 7 issues (2 VAR02 + 5 DOC02) - exact parity with Facility Class 1's own baseline, DB self-clean confirmed via fresh independent oracledb connection (0 residual AUTOTEST% rows). No shared T1/T2 file changes this round. Playwright driver (`py/contract_inventory_iud.py`) left untouched (RF `.robot`/`.resource` structural conversion only). | see docs/ov-non-bank-targets.md, docs/bank-pattern-conversion-checklist.md |
| Contract (OV-GM, CO.2016) | OK Done 2026-08-26 - FULL Area-pattern conversion: RF 5/5 pass live (TC01-05 clean-state/insert/update/find/delete), DB self-clean confirmed via fresh oracledb connection (0 residual `AUTOTEST%` rows in OV_CONTRACT before and after), fixed test code `AUTOTEST_CONTRACT`. Navigator (Business Unit=TS5 BU, single dropdown `nav:form:G:0:R:1:C:1:dd`) now filled via the shared T2 `Apply Navigator From Properties` (resources/manage_object.resource) driven by `testdata/contract_navigator.properties`, reusing the exact BU value already proven live in the screen's own prior driver - not re-derived. Per-TC login/logout with dedicated `CONTRACT_EC_USER`/`CONTRACT_EC_PASS`; explicit grid-filter wiring (`Find/Clear Contract Row By Filter` -> shared T2, 5 `Find Object Row By Filter` hits confirmed via output.xml grep); zero inline DB-verify calls left in the .robot/.resource files (removed `Contract Should Exist/Not Exist In DB` - DB proof is the mandatory live-run self-clean check). 5 properties files: `testdata/contract_{insert,update,form_verify,grid_verify,navigator}.properties`. Full `tests/` dryrun 850/850 pass; robocop 7 issues (2 VAR02 + 5 DOC02), exact parity with Area's own baseline (not a regression). No shared T1/T2 file edits. Superseded prior inline-navigator/suite-login/4-TC build (OV-GM, RF 4/4 pass + Playwright 8/8 via verify_screen.py, 2026-08-02) | see docs/ov-non-bank-targets.md |
| Property (OV-GM, SP.0059) | OK Done 2026-08-02, converted to Area-parity STRUCTURE 2026-08-26 - 5 TCs incl. TC04 Find, per-TC Login/Logout, fixed `AUTOTEST_PROPERTY` (confirmed free in `OV_PROPERTY` before/after the live run via fresh oracledb connection), properties-file-driven insert/update/verify, zero inline DB-verify calls. Live recon (`Workplaces/property-area-pattern/recon_property_nav.py`, read-only) confirmed the 2-group navigator DOM shape (G:0 Date already defaulted, G:1 Business Unit the ONLY genuinely mandatory+empty group at C:0) fits Area's single-group-needs-fill pattern, same as Tract. Shared T2 `Apply Navigator From Properties` extended additively with optional `${group}=0`/`${start_col}=1` args (all existing callers unaffected, confirmed via a live Price Object regression canary 5/5 PASS) so Property can call `group=1 start_col=0`. Live 5/5, full-tree dryrun 878/878, robocop parity with Area (7 issues, same VAR02+DOC02 kind/count), DB self-clean 0 residual `AUTOTEST%` rows via a fresh independent connection, filter keyword fired 15x via output.xml grep | see docs/ov-non-bank-targets.md |
| Price Index (OV-GM, CO.3009) | OK Done 2026-08-02 - RF 4/4 pass + Playwright 8/8 via verify_screen.py (OVERALL PASS), DB-verified vs OV_PRICE_INDEX (Name), self-clean; OV-GM navigator-gated (Business Unit, same nav group as Date); label-driven; parent_dd Business Unit must equal nav scope; Start Date must be >= 2003-01-01 (referenced Business Unit's own effective date, same root cause as Property) - see JOURNAL | see docs/ov-non-bank-targets.md |
| Message Group (OV-GM, CO.0236) | OK Done 2026-08-02 - RF 4/4 pass + Playwright 8/8 via verify_screen.py (OVERALL PASS), DB-verified vs OV_MESSAGE_GROUP (Name + FUNCTIONAL_AREA_CODE='ADM' explicitly re-checked), self-clean; OV-GM navigator-gated (Functional Area, first-available); label-driven; parent_dd Functional Area must equal nav scope; Start Date must be >= 2003-01-01 (referenced Functional Area's own effective date, same root cause as Property/Price Index/Royalty Contract) - see JOURNAL | see docs/ov-non-bank-targets.md |
| Division Order (OV-GM, RC.0058) | OK Done 2026-08-02 - RF 4/4 pass + Playwright 8/8 via verify_screen.py (OVERALL PASS), DB-verified vs OV_DIVISION_ORDER (Name), self-clean; OV-GM navigator-gated (Business Unit, own nav group separate from Date); label-driven; End Date functionally mandatory despite not flagged yellow (same as Royalty Contract/Contract); previously misclassified as TV, actually normal OV-GM sharing the CONTRACT base table with Royalty Contract - see JOURNAL | see docs/ov-non-bank-targets.md |
| Constant Standard (TV-style, CO.0102) | OK Done 2026-08-02 - RF 4/4 pass + Playwright 7/7 via verify_screen.py (OVERALL PASS), DB-verified vs OV_CONSTANT_STANDARD (Name), self-clean; bespoke driver (not gen_ovgm.py); root cause of 2 prior park attempts was a case-sensitivity bug (visible ALL-CAPS menu text is CSS, real DOM text is title-case) + wrong-<li>-scope; Delete = End Date=Start Date inline (class IS date-effective despite TV-looking grid) - see JOURNAL | see docs/ov-non-bank-targets.md |
| Royalty Contract (OV-GM, RC.0059) | OK Done 2026-08-15 - RF 3/3 pass + Playwright 7/7 via verify_screen.py (OVERALL PASS), DB-verified vs OV_ROYALTY_CONTRACT (Name); OV-GM navigator-gated (Business Unit, own nav group G:1 separate from Date G:0); label-driven. **Insert + Update only** - Delete permanently out of scope (owner-confirmed, closes Issue #336): Contract Template "Royalty Fixed Percentage Canada" auto-provisions CNTR_PG_SETUP child rows with no UI path to remove them, genuine EC product limitation, not a defect (see PR #331 root cause). **Self-clean impossible by design** - same accepted precedent as Production Day Table; 5 AUTOTEST_RC_* rows permanently residual as of this PR - see JOURNAL | see docs/ov-non-bank-targets.md |
| Financial Item Definition (OV, FI.0001) | OK Done (engine-only bundle) 2026-08-16 - Playwright 3/3 (Insert/Update/Delete) via engine.py, DB-verified vs OV_FINANCIAL_ITEM, self-clean, full I-U-D; custom-URL, no navigator; label-driven. Built 2026-08-14 as Universal Screen Engine Phase 4 Pilot 1 (first genuinely-new screen the engine built cold - 3 real engine gaps found+fixed, see design doc section 23) but never packaged into a screens/ bundle + registry/scorecard rows until this pass - proven working the whole time, just undocumented. **RF T3/suite deliberately DEFERRED** (owner decision 2026-08-16, see CHECKLIST.md items 8-9) - will revisit once RF can call the engine directly | see docs/universal_screen_engine_design.md section 23 |
| Financial Item Template (TV, FI.0002) | OK Done (engine-only bundle) 2026-08-16 - Playwright 3/3 (Insert/Update/Delete, physical) via engine.py, DB-verified vs FINANCIAL_ITEM_TEMPLATE, self-clean, full I-U-D; no navigator (filters only, all optional); label-driven. Built 2026-08-14 as Universal Screen Engine Phase 4 Pilot 2 (first-ever TV generator, 3 real gaps found+fixed, see design doc section 23) but never packaged until this pass; new-row resolution needed BOTH Code+Name cells empty (a generic blank-cell match hits an existing row's blank optional column first). **RF T3/suite deliberately DEFERRED** (owner decision 2026-08-16, see CHECKLIST.md items 8-9) - will revisit once RF can call the engine directly | see docs/universal_screen_engine_design.md section 23 |
| Project Data Mapping Setup (OV, SP.0039) | OK Done (engine-only bundle) 2026-08-16 - Playwright 3/3 (Insert/Update/Delete) via engine.py, DB-verified vs OV_COST_MAPPING, self-clean, full I-U-D; NONSTANDARD navigator (StandardNavigator:form:..., real GO=buttongo:form:B); label-driven. Built 2026-08-14 as Universal Screen Engine Phase 4 Pilot 3 - by far the deepest pilot (genuine multi-level cross-screen master-data dependency chain: Target Property/Project/Reference each built on a separate screen; 3 real engine bugs found+fixed - nav/GO id scheme, label-shadowing, popup-vs-dropdown misclassification - see design doc's "Pilot 3" section). Original session proved INSERT+DELETE only; this pass closes UPDATE (with the Reference-field-blank-on-select fix applied explicitly) and packages the bundle. Date-effectivity gotcha found+fixed this pass: popup-backed fields only offer options valid on/before the row's own Start Date - resolved via real DB queries, not live guessing. **RF T3/suite deliberately DEFERRED** (same reasoning as Financial Item Definition/Template) - will revisit once RF can call the engine directly | see docs/universal_screen_engine_design.md "Pilot 3" section |
| State (OV, Basic Objects) | OK Done 2026-08-22 - RF 5/5 pass (rebuilt), DB-verified vs OV_STATE (State Code), self-clean. Converted from the older hardcoded-field-id/generated-code suite to Bank/Object List's label-driven, properties-file-driven, T2-consolidated pattern (owner: "reflect the current EC screen exactly the same way as the OV_BANK IUD related task"); 1st of 6 screens in this batch (Region/Functional Area/Business Unit/Production Unit/Company next). **Found + fixed a real T2 generalization gap**: `Find Object By Code`/`Insert Object From Properties`/`Update Object From Properties` (+ their Verify wrappers) hardcoded the literal label "Code" - broke on State's screen-prefixed "State Code"/"State Name" labels. Fixed via a new optional `${code_label}=Code` parameter on all 7 affected T2 keywords (backward-compatible; Bank/Object List regression 5/5+5/5 both still pass) | see PR (feature/state-bank-pattern); remaining 5 screens in this batch not yet started |
| Region (OV, Basic Objects) | OK Done 2026-08-22 - RF 5/5 pass first attempt (rebuilt), DB-verified vs OV_REGION (Region Code), self-clean. 2nd of 6 screens in this batch; same conversion as State, but this time field labels ("Region Code"/"Region Name") were confirmed via a live RF recon script (tmp/recon_region_labels.robot) BEFORE building, applying the lesson from State's live-failure discovery - no wasted live run this time. Reuses the `${code_label}` T2 parameter added on State's PR; no further T2 changes needed | see PR (feature/region-bank-pattern, stacked on feature/state-bank-pattern); remaining 4 screens (Functional Area/Business Unit/Production Unit/Company) not yet started |
| Country (OV, Basic Objects) | OK Done 2026-08-23 - RF 5/5 pass first attempt (rebuilt), DB-verified vs OV_COUNTRY (CODE), self-clean (0 residual `AUTOTEST_COUNTRY` rows via fresh oracledb connection, confirmed both pre-run free and post-run clean). Batch-2 plain-OV Bank-pattern conversion (Country/County/Regulatory Permits/Currency/VAT Code, 5 parallel isolated clones); converted from the older hardcoded-field-id, no-properties-file pattern to Bank/State's label-driven, properties-file-driven, T2-consolidated pattern, with the grid Find/Clear Row By Filter wiring included from the start (not deferred). Field labels are "Country Code"/"Country Name" (screen-prefixed, same as State/Region) - confirmed via a live RF field-label recon script BEFORE building (dumped 14 objectForm/10 updateAttributes ECCell labels + MandatoryCellStyle scan): only Country Code/Country Name/Start Date are mandatory; Master System Code/Name, Local Name, End Date, Dialing Code, Comments, Description, Nationality name all confirmed optional and omitted. Reuses the existing `${code_label}` T2 parameter unchanged; no shared T1/T2 file edits. Full `tests/` dryrun 730/730 pass; robocop 9 issues (4 VAR02 + 5 DOC02), identical in kind/count to State's own baseline. Grid filter wiring verified via output.xml grep: `Find Country Row By Filter` fired exactly 5 times (Update/Find/Verify-Insert-Exists/Verify-Found/Delete) | see PR (feature/country-bank-pattern, independent clone under Workplaces/country) |
| Functional Area (OV, Basic Objects) | OK Done 2026-08-22 - RF 5/5 pass first attempt (rebuilt), DB-verified vs OV_FUNCTIONAL_AREA (Area Code), self-clean. 3rd of 6 screens in this batch; field labels are "Area Code"/"Area Name" (NOT "Functional Area Code" - screen name != field prefix) and "Start date" is lowercase-'d' - confirmed via live RF recon (tmp/recon_fa_labels.robot) BEFORE building. Reuses the `${code_label}` T2 parameter; no further T2 changes needed; no live failure | see PR (feature/functional-area-bank-pattern, stacked on feature/region-bank-pattern); remaining 3 screens (Business Unit/Production Unit/Company) not yet started |
| Business Unit (OV, Basic Objects) | OK Done 2026-08-22 - RF 5/5 pass first attempt (rebuilt), DB-verified vs OV_BUSINESS_UNIT (Business Unit Code), self-clean. 4th of 6 screens in this batch; field labels are "Business Unit Code"/"Business Unit Name" (full screen-name prefix, standard Start Date capitalization) - confirmed via live RF recon (tmp/recon_bu_labels.robot) BEFORE building. Reuses the `${code_label}` T2 parameter; no further T2 changes needed; no live failure | see PR (feature/business-unit-bank-pattern, stacked on feature/functional-area-bank-pattern); remaining 2 screens (Production Unit/Company) not yet started |
| Production Unit (OV, Basic Objects) | OK Done 2026-08-22 - RF 5/5 pass first attempt (rebuilt), DB-verified vs OV_PRODUCTIONUNIT (Production Unit Code), self-clean. 5th of 6 screens in this batch; field labels are "Production Unit Code"/"Production Unit Name" (full screen-name prefix) - confirmed via live RF recon (tmp/recon_pu_labels.robot) BEFORE building; DB view name has no underscore (OV_PRODUCTIONUNIT, unlike the screen label) - checked, not assumed; optional "System of Measurement" dropdown confirmed non-mandatory (tmp/recon_pu_mandatory.robot) and deliberately omitted. Reuses the `${code_label}` T2 parameter; no further T2 changes needed; no live failure | see PR (feature/production-unit-bank-pattern, stacked on feature/business-unit-bank-pattern); remaining 1 screen (Company) not yet started |
| Company (OV, Commercial Objects) | OK Done 2026-08-22 - RF 5/5 pass on retry (rebuilt), DB-verified vs OV_COMPANY (Company Code), self-clean. 6th/LAST of 6 screens in this batch; field labels are "Company Code"/"Company Name" (full screen-name prefix); 30+ optional fields confirmed non-mandatory and omitted - confirmed via live RF recon (tmp/recon_company_labels.robot, tmp/recon_company_mandatory.robot) BEFORE building. **One non-reproducible TC05 flake first run**: grid still showed the deleted row right after Save; DB ground truth (`OV_COMPANY` = 0 rows) proved the delete had actually succeeded; re-run 5/5 clean. Root-caused as a likely grid-refresh timing quirk specific to Company's much larger PAGINATED grid (8 pages, hundreds of rows) vs the near-empty grids on the other 5 screens - not chased further (non-reproducible, underlying mechanism DB-confirmed correct). Reuses the `${code_label}` T2 parameter; no further T2 changes needed | see PR (feature/company-bank-pattern, stacked on feature/production-unit-bank-pattern); **batch of 6 screens COMPLETE** |
| Revenue Order (OV, Financial Objects) | OK Done 2026-08-22 - RF 5/5 pass (rebuilt), DB-verified vs OV_FIN_REVENUE_ORDER (Code), self-clean (0 residual `AUTOTEST_REVENUE_ORDER` rows via fresh connection; real production row `RO_01` confirmed untouched). Converted from the older hardcoded-field-id/generated-code/per-suite-login-teardown suite to Bank/State's label-driven, properties-file-driven, T2-consolidated pattern with per-TC Login/Logout, matching batch1_shared_findings.md ground rules. Custom-URL OV (NO navigator/GO) - confirmed live via read-only DOM scan: grid `nav:form:T_data` count=1, `manage_object_nav_` count=0, GO button count=0, so T2's `Save And Refresh List` correctly falls back to toolbar Refresh. Field labels are the generic "Code"/"Name" (NOT screen-prefixed like State) - confirmed live via the objectForm field-dump scan AND a read-only updateAttributes scan on the real existing row RO_01 (no edits/Save); no mandatory fields beyond Code/Name/Start Date, matching the registry's prior claim | see PR (feature/revenue-order-bank-pattern); sibling batch screens Cost Centre/WBS in separate clones |
| WBS (OV custom-URL, Financial Objects) | OK Done 2026-08-22 - RF 5/5 pass (rebuilt), DB-verified vs OV_FIN_WBS (AUTOTEST_WBS: 0 rows before + after, fresh oracledb connection), self-clean. Converted from the older hardcoded-field-id/generated-code suite to Bank/State's label-driven, properties-file-driven, T2-consolidated pattern (pilot batch 1 of 3 alongside Cost Centre/Revenue Order, run in parallel isolated clones). Live recon confirmed the registry's claims exactly: plain (not screen-prefixed) "Code"/"Name" labels; only Code/Name/Start Date mandatory-and-empty (yellow `rgb(252, 249, 192)`) on the New Object form - End Date/Description/WBS/External Ref/Split Share/Comments all optional; grid `nav:form:T_data` (count 1) with 0 visible GO buttons, confirming custom-URL OV (T2's `Save And Refresh List` GO/Refresh auto-detect fallback used as-is, no shared-file change needed). Full `tests/` dryrun 719/719 pass; fixed test code `AUTOTEST_WBS` confirmed free pre-run and clean post-run | see docs/ec_screen_registry.md; no shared-file edits |
| Report Context (OV custom-URL, Reporting > Excel Report Templates) | OK Done 2026-08-24 - BRAND-NEW build, no prior automation existed. RF 5/5 on retry (first attempt 4/5: TC02 failed on a `nav:form:T:sfilter0_ft_filter` visibility timeout inside the shared T2 grid-filter-toggle mechanism - identical code re-run clean 5/5 with zero changes, confirming a one-off environment/timing flake, not a defect; no shared T1/T2 file touched either way), DB-verified vs OV_REPT_CONTEXT (AUTOTEST_REPORT_CONTEXT: 0 rows before + after, fresh oracledb connection). CLASS_NAME=REPT_CONTEXT resolved via a direct class_cnfg query BY CLASS_NAME rather than the stock resolve/scan scripts' LABEL lookup - class_property_cnfg LABEL for this class is "Reporting context" (lowercase 'context', generic), NOT "Report Context" (the actual menu/BUSINESS_FUNCTION.NAME), so the stock scripts' LABEL-keyed lookup silently returns no match/CLASS_TYPE=None on this exact screen; patched a local copy (tmp/scan_rept_context.py) to hardcode the DB-confirmed CLASS_TYPE=OBJECT instead of trusting the mismatched LABEL lookup. Live scan confirmed NO navigator at all (fields=[], go=[] - not even a date-only nav/GO like Report Area) and grid `nav:form:T_data` renders directly on open - matches the WBS/Calendar/Account/Cost Centre/Revenue Order custom-URL family, NOT Report Area's manage-object+GO shape, despite both living under top-level Reporting; confirmed live before assuming Bank's usual Apply-Navigator step applied. Plain (not screen-prefixed) "Code"/"Name" labels; only Code/Name/Start Date mandatory-and-empty on the New Object form - End Date/Description/Comments all optional and omitted. Full `tests/` dryrun 790/790 pass; robocop 9 issues (5 DOC02 + 1 VAR02 unused-var pattern), identical in kind/count to WBS's own baseline; filter keyword (`Find Report Context Row By Filter`) confirmed fired 15x via output.xml grep. No T2/T1 changes needed; new 3-level menu subfolder (`pageobjects/tests/Reporting/Excel_Report_Templates/`) created | see PR (feature/report-context-iud, isolated clone Workplaces/report_context); adds new registry + scorecard rows |
| Payment Scheme (OV, Financial Objects) | OK Done 2026-08-22 - RF 5/5 pass first attempt (rebuilt), DB-verified vs OV_PAYMENT_SCHEME (Code), self-clean (0 residual rows before and after). Round 2 batch (Account/Exchange Rate Source/Payment Scheme, independent branches off master). Converted from the older hardcoded-field-id pattern to Bank/State's label-driven, properties-file-driven, T2-consolidated pattern. Field labels are the generic "Code"/"Name" (NOT screen-prefixed like State) - confirmed live via a throwaway RF recon script (tmp/recon_payment_scheme.robot) dumping objectForm labels + background-color/class of each field: only Code/Name/Start Date are mandatory (yellow `rgb(252, 249, 192)`); End Date/Comments/Description are optional and omitted. Manage-object OV confirmed (grid `manage_object_nav_nav:form:T_data` count=1, GO button count=1) - matches the registry's prior stub claim exactly, no gotcha found. CODE column confirmed 32 chars (`AUTOTEST_PAYMENT_SCHEME` = 23 chars, no shortening needed) via `all_tab_columns` query. No T2/T1 changes needed | see PR (feature/payment-scheme-bank-pattern, independent off master) |
| Exchange Rate Source (OV, Financial Objects) | OK Done 2026-08-22 - RF 5/5 pass first attempt (rebuilt), DB-verified vs OV_FOREX_SOURCE (Code), self-clean. Independent Round-2 screen (branched off master separately from Account/Payment Scheme, not stacked). Field labels are the GENERIC "Code"/"Name" (NOT screen-prefixed like State's) - confirmed via live RF recon (throwaway `tests/_recon_ers.robot`, deleted before commit) that dumped the objectForm/updateAttributes outerHTML directly: only Code/Name/Start Date carry `MandatoryCellStyle` (Description/Comments/URL confirmed optional and omitted); Code field has no maxLength restriction (confirmed live via the DOM `maxLength` property = -1), so the full `AUTOTEST_EXCHANGE_RATE_SOURCE` code was used as-is, no shortening needed. `${code_label}` T2 parameter reused unchanged (defaults to "Code"); no T2/T1 changes needed. Two new LEN32 robocop findings (variable names too long, 43/40 chars) fixed by dropping the redundant `_PROPERTIES` suffix from the two verify-file variable names before commit - net 9 robocop issues (4 VAR02 + 5 DOC02), identical in kind/count to State's own baseline, zero unresolved new issues | PR TBD (feature/exchange-rate-source-bank-pattern, off master, independent of Account/Payment Scheme) |
| Account (OV, Financial Objects, custom-URL golden exemplar) | OK Done 2026-08-22 - RF 5/5 pass on retry, DB-verified vs OV_FIN_ACCOUNT (Code), self-clean. Round 2 batch (independent branch off master, alongside Exchange Rate Source/Payment Scheme); converted from the older hardcoded-field-id/generated-code suite to Bank/State's label-driven, properties-file-driven, T2-consolidated pattern. Field labels are the generic "Code"/"Name" (not screen-prefixed); confirmed live custom-URL OV (no GO, grid `nav:form:T_data`). 2 confirmed-mandatory extras (MandatoryCellStyle CSS class, live-checked on both the New Object form and an existing real row): dropdown **Cost Object Type** (live options Cost Center/Revenue Order/WBS Element/None - `None` picked as a real catalogued option with no dependent master-data link) and text **GL Account** (999999). **First live attempt failed TC03-05** (row-select timeout): OV_FIN_ACCOUNT has 110+ rows on a 20-rows-per-page grid, and AUTOTEST_ACCOUNT (starts with a letter) sorts alphabetically after every existing numeric GL code, landing off page 1 - T2's Select Object Row/T1's Row Should Exist only check the currently-rendered page. **Reviewer note (this merge): the row-locate mechanism described below was superseded the same day** - a follow-up commit on this branch (after PR #422 was opened) replaced the page-walking fix with `resources/grid_menu.resource`'s Code-column filter (`Filter Grid Text Column By Value`/`Clear Grid Text Column Filter`, already proven live on Business Function), per an owner-directed improvement; re-verified 724/724 dryrun + 5/5 live + DB self-clean (0/110) after the change. First-attempt fix (superseded, kept here for history): 2 new T3-LOCAL keywords in account_page.resource (`Go To Account Row Page`/`Account Row Absent Across All Pages`, same page-walk pattern already used by Language's `Find Language Row`) - no shared T1/T2 file touched either way; re-run 5/5 clean. Residual row from the failed attempt cleaned up (End Date=Start Date) and DB re-confirmed 0/110 before the successful re-run | see PR (feature/account-bank-pattern) |
| VAT Code (OV, Financial Objects, manage-object) | OK Done 2026-08-23 - RF 5/5 pass (1 fix mid-build), DB-verified vs OV_VAT_CODE (Code), self-clean (0 residual `AUTOTEST_VAT` rows, fresh oracledb connection, before and after). Batch-2 conversion (Country/County/Regulatory Permits/Currency/VAT Code, 5 parallel isolated clones) from the older hardcoded-field-id pattern to Bank/Account's label-driven, properties-file-driven, T2-consolidated pattern, filter wiring included from the start. Live recon (tmp/recon_vat_code_*.py, deleted before commit) CONFIRMED the registry's prior "(dds: Country, VAT Type; dates at R1/R2!)" note: TWO mandatory reference dropdowns - **Country** (17 real options: Any/Argentina/Australia/Bolivia/Canada/China/Denmark/Germany/Great Britain/Hong Kong/Italy/MHM13_Norway/Norway/Spain/Sweden/The Netherlands/USA) and **VAT Type** (2 real options: Income/Cost) - plus mandatory text **VAT Code** (distinct from the object's own Code) and **Rate (Decimal)**, all confirmed via a raw outerHTML dump ({mandatory:true} MandatoryCellStyle sits on the dropdown's wrapping `<span class="...dd">`, not on the dd_input/dd_hinput `<input>` children - a per-input class check alone misses it). objectForm (Insert) has Start Date at R1 and End Date at R2 (both present in Insert, unlike Bank/Account); no issue since every field is label-resolved, not row-position-resolved. **One live TC02 failure, fixed same session**: the insert properties file initially used `__FIRST__` for Country/VAT Type, but TC02's Verify VAT Code Record Exists round-trips the live screen back against that SAME file (T2's Verify Object Insert Exists) - `__FIRST__` never resolves to the literal selected text, so it always fails ("Field 'Country' shows 'Any' in updateAttributes, expected '__FIRST__'"). Fixed by using the literal confirmed first-option text ("Any"/"Income") in the insert file instead, matching Account's own precedent (`None` for Cost Object Type) of a literal real catalogued value, not a placeholder. Full `tests/` dryrun 730/730 pass; robocop 9 issues (4 VAR02 + 5 DOC02), identical in kind/count to the Bank/Account/State baseline; filter keyword (`Find VAT Code Row By Filter`) confirmed fired 7x via output.xml grep. No T2/T1 changes needed | see PR #431 (feature/vat-code-bank-pattern) |
| Regulatory Permits (OV custom-URL, Basic Objects) | OK Done 2026-08-23 - RF 5/5 pass on retry, DB-verified vs OV_REGULATORY_PERMITS (Permit Code), self-clean (0 rows before + after, fresh oracledb connection; view was 0 rows total on this sandbox before this suite's own TC02 created the first-ever row). Batch-2 conversion (parallel isolated clone alongside Country/County/Currency/VAT Code) from the older hardcoded-field-id/generated-code suite to Bank/Account's label-driven, properties-file-driven, T2-consolidated pattern. Field labels are SCREEN-PREFIXED "Permit Code"/"Permit Name" (NOT generic "Code"/"Name") - confirmed live via the objectForm field dump. Custom-URL OV confirmed live: grid `nav:form:T_data` (NOT the `manage_object_nav_` prefix) but WITH a GO button (`button:form:B` count=1) - a shape distinct from both Bank (manage_object_nav_ grid + GO) and Account/Cost Centre (nav:form:T_data grid, no GO); `Save And Refresh List`'s existing GO/Refresh auto-detect handled it correctly with zero shared-file changes. One confirmed-mandatory extra: dropdown **Regulatory Agency** (MandatoryCellStyle CSS class, live-checked) - a mandatory FORM FIELD on the Insert form, NOT a navigator/scope dropdown, so no scope-mismatch (live options Texas RRC/California DOG/MMS confirmed via the dropdown panel's data-item-label attributes; "Texas RRC" used, matching the pre-existing old-pattern file's own value). **First live attempt failed TC04** (grid-verify mismatch): assumed the grid displays Start Date (Bank/Account's convention) but the real rendered row was `AUTOTEST_REGULATORY_PERMITS / AUTOTEST Regulatory Permits UPDATED / Texas RRC` - grid columns are actually Permit Code/Permit Name/Regulatory Agency; fixed testdata/regulatory_permits_grid_verify.properties on this real evidence, re-run 5/5 clean. 2 LEN32 robocop findings (variable names 41/40 chars) fixed by dropping the redundant `_PROPERTIES` suffix (same fix as Exchange Rate Source) - net 9 robocop issues (4 VAR02 + 5 DOC02), identical in kind/count to the established baseline. Full `tests/` dryrun 730/730 pass; filter-wiring included from the start (`Find/Clear Regulatory Permits Row By Filter`, delegating to shared T2), confirmed fired 7 times via output.xml | see PR (feature/regulatory-permits-bank-pattern, off master, independent of Country/County/Currency/VAT Code) |
| Field Group (OV, Commercial Objects, manage-object) | OK Done 2026-08-23 - RF 5/5 pass first attempt, DB-verified vs OV_FIELD_GROUP (Code), self-clean (0 residual `AUTOTEST_FIELD_GROUP` rows via fresh oracledb connection, confirmed both pre-run free and post-run clean). Batch-3 5-screen conversion (Customer/Field Group/Licence/MMS Lease/Operator Lease, 5 parallel isolated worker clones, see `tmp/batch3_shared_findings.md`) from the older hardcoded-field-id pattern to Bank/Country's label-driven, properties-file-driven, T2-consolidated pattern, with the grid Find/Clear Row By Filter wiring included from the start. Plain manage-object OV confirmed live (matches registry) - no mandatory nav scope. Field labels confirmed live as the generic "Code"/"Name" (NOT screen-prefixed): objectForm has Code/Name/Start Date (all mandatory, `MandatoryCellStyle`), plus End Date/Description/Comments/Field Group Type (dropdown)/Reporting Field Group Indicator (checkbox), all confirmed optional via a live objectForm outerHTML dump; updateAttributes mirrors the same shape (Code read-only `MandatoryCellStyleWhite`). Description included in insert/update/verify for business-realistic test data (matches Bank's own convention of including a couple of optional descriptive fields); dropdown/checkbox deliberately left untested (optional, no business need for this IUD flow). Grid confirmed live to show Code/Name/Start Date/End Date (4 columns, C0-C3). objectdates End Date id (`tab:tabPanel:objectdates:form:G:0:R:0:C:3:da_input`) live-confirmed to match the old pre-conversion file's own value exactly. Full `tests/` dryrun 735/735 pass; robocop 9 issues (4 VAR02 + 5 DOC02), identical in kind/count to the established baseline; filter keyword (`Find Field Group Row By Filter`) confirmed fired 5x via output.xml grep. No T2/T1 changes needed | see PR (feature/field-group-bank-pattern, independent isolated clone under Workplaces/field_group) |
| Field (OV-GM, Commercial Objects, groupmodel) | OK Done 2026-08-26 - RF 5/5 pass, DB-verified vs OV_FIELD (Field Code), self-clean (0 residual `AUTOTEST_FIELD` rows via fresh oracledb connection, confirmed both pre-run free and post-run clean). Follow-up to PR #525 (which converted ONLY the navigator-fill delegation, to the shared T2 `Apply Navigator From Properties`) - this brings the rest of the suite's STRUCTURE up to Area's full pattern per owner standing rule 2026-08-26: any EC screen with a navigator matching Area's layout must follow Area's FULL pattern, not just the navigator-fill piece. Genuine OV-GM screen (mandatory Area dropdown + GO) - kept exactly as-is, this conversion is structural only. Field labels confirmed live as SCREEN-PREFIXED "Field Code"/"Field Name" (NOT generic "Code"/"Name", matching Area's own convention) via a fresh objectForm ECCell + mandatory-class dump: only Field Code/Field Name/Start Date carry `MandatoryCellStyle` (Sort Order/Description/Master System Code+Name/Commercial Entity/Parent field/Operator/Country/State/Comments/Full field name/Non equity Indicator all confirmed optional). Grid headers confirmed live: Field Code/Field Name/Start Date/End Date. Insert sets **Geo Area** = navigator Area value (`Offshore area`) - the groupmodel visibility link (no 'Op Area' dd), NOT statically mandatory but kept as a genuine business-rule requirement (from the screen's own pre-existing driver/registry note) for the new row to be visible under this OV-GM navigator scope; deliberately excluded from the round-trip form-label list (same re-render caveat as Area's Op Production Unit). Added: 5 TCs (Verify Clean State/Insert/Update/Find/Delete) with per-TC `Login To EC Application`/`Logout From EC Application` on ONE browser opened once in Suite Setup (`Open EC Application`); dedicated `FIELD_EC_USER`/`FIELD_EC_PASS` credential pair (additive-only in `resources/credentials.py`); fixed test code `AUTOTEST_FIELD` (confirmed free in `OV_FIELD` via a fresh oracledb connection before use); 4 new properties files (`testdata/field_{insert,update,form_verify,grid_verify}.properties`) driving the shared T2 `Insert/Update Object From Properties`/`Verify Object *`; explicit `Find/Clear Field Row By Filter` grid-filter wiring into Update/Find/Verify-Found/Delete (29 filter-keyword hits confirmed in live output.xml); PURE SCREEN verification only - zero inline DB-verify calls left in `field_iud.robot` (confirmed via grep; the DB check now lives only inside the shared T2 `Verify Object Removed`). Full `tests/` dryrun 847/847 pass; robocop 7 issues (2 VAR02 + 5 DOC02), identical in kind/count to Area's own baseline. No shared T1/T2 file changes (`resources/manage_object.resource`/`common.resource` untouched - the navigator shape already fit the existing `Apply Navigator From Properties` keyword from PR #525/#523) | see PR (feature/field-full-bank-pattern, stacked on feature/field-navigator-from-properties / PR #525, independent isolated worktree under Workplaces/field_bank_pattern) |
| Customer (OV manage-object, Commercial Objects) | OK Done 2026-08-23 - RF 5/5 pass first attempt, DB-verified vs OV_CUSTOMER (fresh oracledb connection: AUTOTEST_CUST count=0 both before and after the run), self-clean confirmed. Batch-3 5-screen conversion (Customer/Field Group/Licence/MMS Lease/Operator Lease, 5 parallel isolated clones under Workplaces/, see tmp/batch3_shared_findings.md) from the older hardcoded-field-id pattern to Bank/Account's label-driven, properties-file-driven, T2-consolidated pattern, filter wiring included from the start. Live recon (throwaway tmp/recon_customer*.robot, deleted before commit) confirmed nav-free (0 mandatory nav dropdowns) and CONFIRMED the old page object's own prior recon exactly: mandatory fields on BOTH objectForm and updateAttributes are Code, Name, Start Date, **ERP Customer Code** (text), **Official Name** (text), **Customer Group** (dropdown) - all MandatoryCellStyle. Customer Group's real first option is the literal `Non Group` - used verbatim in the insert properties file (not `__FIRST__`), per the VAT Code round-trip-verify gotcha (TC02's Verify Object Insert Exists compares the live screen back against the SAME properties file the insert used). Grid columns confirmed Code/Name/Start Date/End Date (Bank convention). Fixed test code `AUTOTEST_CUST`; no scope mismatch, no T2/T1 shared-file changes needed. Full `tests/` dryrun 735/735 pass; robocop 7 issues (2 VAR02 + 5 DOC02), fewer than the established 9-issue baseline; filter keyword (`Find Customer Row By Filter`) confirmed fired 5x via output.xml grep (Update/Find/Verify-Insert-Exists/Verify-Found/Delete) | see PR (feature/customer-bank-pattern, independent clone under Workplaces/customer) |
| Operator Lease (OV, Commercial Objects, manage-object) | OK Done 2026-08-23 - RF 5/5 pass first attempt (rebuilt), DB-verified vs OV_OPERATOR_LEASE (Operator Lease Code), self-clean (0 residual `AUTOTEST_OPERATOR_LEASE` rows via fresh oracledb connection, confirmed both pre-run free and post-run clean). Batch-3 conversion (Customer/Field Group/Licence/MMS Lease/Operator Lease, 5 parallel isolated clones) from the older hardcoded-field-id, no-properties-file pattern to Bank/State/Country's label-driven, properties-file-driven, T2-consolidated pattern, with the grid Find/Clear Row By Filter wiring included from the start. Manage-object OV confirmed nav-free live (grid `manage_object_nav_nav:form:T_data` count=1; the top "Date" field + GO button is the same universal as-at-date filter Bank also has - default value pre-filled, auto-queries on page load, NOT a blocking mandatory nav requirement - verified by a direct side-by-side screenshot compare against the live Bank screen, which shows the identical Date/GO bar). Field labels are "Operator Lease Code"/"Operator Lease Name" (screen-prefixed, same convention as Country/State) - confirmed via a live RF field-label recon dumping objectForm (7 ECCell labels: Code/Name/Description/Start Date/End Date; mandatory: Code/Name/Start Date only) and updateAttributes (3 ECCell labels: Code/Name/Description; mandatory: Code/Name only, Description optional). The grid was genuinely empty (0 rows in OV_OPERATOR_LEASE on this sandbox, confirmed via DB query) so updateAttributes labels were confirmed via a live throwaway Insert+Delete round-trip (RECON_OL_TMP, self-cleaned, DB re-checked empty after) rather than selecting an existing row. Grid columns confirmed live: Operator Lease Code/Operator Lease Name/Start Date/End Date. Full `tests/` dryrun 735/735 pass; robocop 9 issues (4 VAR02 + 5 DOC02), identical in kind/count to the established baseline; filter keyword (`Find Operator Lease Row By Filter`) confirmed fired 5 times via output.xml grep. Reuses the existing `${code_label}` T2 parameter unchanged; no shared T1/T2 file edits | see PR #436 (feature/operator-lease-bank-pattern, independent clone under Workplaces/operator_lease) |
| State Lease (OV, Commercial Objects, manage-object) | OK Done 2026-08-23 - RF 5/5 pass first attempt, DB-verified vs OV_STATE_LEASE (fresh oracledb connection: `AUTOTEST_STL` count=0 both before and after the run; the view was genuinely 0 rows total on this sandbox), self-clean confirmed. Batch-4 5-screen conversion (State Lease/Vendor/Cost Object Mapping/DOA Credit Limit/Product Description, 5 parallel isolated clones under Workplaces/, see `tmp/batch4_shared_findings.md`) from the older hardcoded-field-id, no-properties-file pattern to Bank/Customer's label-driven, properties-file-driven, T2-consolidated pattern, with grid Find/Clear Row By Filter wiring included from the start. Manage-object OV confirmed nav-free live (grid `manage_object_nav_nav:form:T_data`; the universal as-at-date + GO bar is present but not a blocking mandatory nav requirement, same as every other Bank-pattern screen). Field labels are screen-prefixed "State Lease Code"/"State Lease Name" (NOT generic "Code"/"Name") - confirmed via a live RF field-label recon dumping objectForm (7 ECCell matches: State Lease Code/State Lease Name/Description/Start Date row/End Date row; mandatory: State Lease Code, State Lease Name, Start Date only - Description and End Date confirmed optional via `MandatoryCellStyle` class checks). Notably, unlike Bank/Customer, this screen's objectForm (Insert) DOES show an End Date field alongside Start Date - left unset since it's optional and Delete still uses the standard objectdates End Date=Start Date mechanism. The grid was genuinely empty (0 rows in OV_STATE_LEASE, confirmed via direct DB query) so updateAttributes labels were confirmed via a live throwaway Insert+Delete round-trip (`RECON_STL`, self-cleaned, DB re-checked absent after each attempt - one residual row was left by an earlier interrupted recon run and cleaned up via a dedicated cleanup script before the final round-trip): updateAttributes carries only State Lease Code/State Lease Name/Description (3 labels, no Start/End Date - matches Bank/Customer's convention that dates live only in objectForm/objectdates). Grid columns confirmed live: Code/Name/Start Date/End Date (4 columns, Bank convention). objectdates End Date id (`tab:tabPanel:objectdates:form:G:0:R:0:C:3:da_input`) live-confirmed via the round-trip delete. Fixed test code `AUTOTEST_STL` confirmed free in OV_STATE_LEASE. Full `tests/` dryrun 740/740 pass; robocop 7 issues (2 VAR02 + 5 DOC02), fewer than the established 9-issue baseline; filter keyword (`Find State Lease Row By Filter`) confirmed fired 5 times via output.xml grep. No scope mismatch, no T2/T1 shared-file changes needed | see PR (feature/state-lease-bank-pattern, independent isolated clone under Workplaces/state_lease) |
| MMS Lease (OV, Commercial Objects, manage-object) | OK Done 2026-08-23 - RF 5/5 pass first attempt (rebuilt), DB-verified vs OV_MMS_LEASE (MMS Lease Code), self-clean (0 residual `AUTOTEST_MMS_LEASE` rows, fresh oracledb connection, confirmed both pre-build free and post-run clean). Batch-3 conversion (Customer/Field Group/Licence/MMS Lease/Operator Lease, 5 parallel isolated clones, see `tmp/batch3_shared_findings.md`) from the older hardcoded-field-id pattern (no properties file, generated-per-run code) to Bank/Country's label-driven, properties-file-driven, T2-consolidated pattern, with the grid Find/Clear Row By Filter wiring included from the start. No mandatory nav dropdown/date before the grid loads - confirmed live via a real recon script (`manage_object_nav` GO button count = 0), matching the registry's documented plain manage-object navigator (grid id `manage_object_nav_nav:form:T_data`, reused as `${OV_MANAGE_OBJECT_TABLE}`). Field labels are SCREEN-PREFIXED "MMS Lease Code"/"MMS Lease Name" - confirmed live via a field-label recon dumping objectForm (6 labels: MMS Lease Code, MMS Lease Name, Description, Start Date, End Date, Operator; 3 mandatory: Code/Name/Start Date) and updateAttributes (4 labels: MMS Lease Code, MMS Lease Name, Description, Operator; 2 mandatory: Code/Name) - Description and Operator confirmed optional and deliberately omitted (IUD-fill-only-needed-fields convention). Recon included a real insert+select+delete+DB-verify cycle on a throwaway record (before the real suite was built) to directly observe updateAttributes/objectdates live, rather than assuming Country's shape carried over. Grid columns confirmed MMS Lease Code/MMS Lease Name/Start Date/End Date (Code/Name/Start Date used in grid_verify, matching Country's convention, not Regulatory Permits' custom set). Full `tests/` dryrun 735/735 pass; robocop 9 issues (4 VAR02 + 5 DOC02), identical in kind/count to the established baseline; filter keyword (`Find MMS Lease Row By Filter`/`Clear MMS Lease Row Filter`) confirmed fired 5x each via output.xml grep. No T2/T1 changes needed | see PR (feature/mms-lease-bank-pattern, independent clone under Workplaces/mms_lease) |
| Cost Object Mapping (OV, Financial Objects, manage-object) | OK Done 2026-08-23 - RF 5/5 pass (1 fix mid-build), DB-verified vs OV_FIN_COST_OBJECT (Code), self-clean (0 residual `AUTOTEST_CMAP` rows via fresh oracledb connection, confirmed both pre-run free and post-run clean; total row count unchanged at 90 before/after). Batch-4 5-screen conversion (State Lease/Vendor/Cost Object Mapping/DOA Credit Limit/Product Description, 5 parallel isolated clones, see `tmp/batch4_shared_findings.md`) from the older hardcoded-field-id pattern to Bank/Customer's label-driven, properties-file-driven, T2-consolidated pattern, with the grid Find/Clear Row By Filter wiring included from the start. NOT a scope mismatch despite the name: confirmed live to be a genuine manage-object OV with Code/Name/objectForm (generic "Code"/"Name" labels, not screen-prefixed), not a pure linking grid. Confirmed generic "Code"/"Name" mandatory plus FOUR mandatory reference dropdowns (raw outerHTML dump, same MandatoryCellStyle-on-wrapping-`<span>` technique as VAT Code/Customer): **Object Type** (Cost Center/Revenue Order/WBS Element), **Cost Object** (a CASCADE dropdown - empty with the banner "Dependent field 'Start Date' is empty" until BOTH Start Date AND Object Type are already set; a live insert+delete round-trip on a throwaway RECON_CMAP_TEST code proved the fill order Code->Name->Start Date->Object Type->Cost Object->Company->Distribution Object Type actually Saves), **Company** (picked the clean literal `Acme Chemicals`, avoiding options with leading/internal double-spaces like `"  Company  BP"`), **Distribution Object Type** (Country/Delivery Point/Field/Process Train/Well). All 4 use real literal option text, not `__FIRST__` (VAT Code round-trip-verify gotcha). Grid columns confirmed Code/Name/Start Date (Bank convention). Full `tests/` dryrun 740/740 pass; robocop 9 issues (4 VAR02 + 5 DOC02), identical in kind/count to the established baseline; filter keyword (`Find Cost Object Mapping Row By Filter`) confirmed fired 5x via output.xml grep. No T2/T1 changes needed | see PR (feature/cost-object-mapping-bank-pattern, independent clone under Workplaces/cost_object_mapping) |
| Licence (OV, Commercial Objects, manage-object) | OK Done 2026-08-23 - RF 5/5 pass first attempt (rebuilt), DB-verified vs OV_LICENCE (Licence Code), self-clean (0 residual `AUTOTEST_LICENCE` rows via fresh oracledb connection, confirmed both pre-run free and post-run clean). Batch-3 plain-OV Bank-pattern conversion (Customer/Field Group/Licence/MMS Lease/Operator Lease, 5 parallel isolated clones, `tmp/batch3_shared_findings.md`), converted from the older hardcoded-field-id/no-properties-file pattern to Bank/Country's label-driven, properties-file-driven, T2-consolidated pattern, with the grid Find/Clear Row By Filter wiring included from the start. Field labels are SCREEN-PREFIXED "Licence Code"/"Licence Name" (NOT generic "Code"/"Name") - confirmed live via a throwaway RF field-label recon script (tmp/recon_licence.robot, deleted before commit) dumping every ECCell label in objectForm (8 labels: Licence Code, Licence Name, Sort Order, Description, Master System Code, Master System Name, Start Date, End Date) and updateAttributes (6 labels, same minus Start/End Date) plus a MandatoryCellStyle scan: only Licence Code/Licence Name/Start Date are mandatory; Sort Order/Description/Master System Code/Master System Name confirmed optional and omitted. Manage-object OV confirmed live (grid `manage_object_nav_nav:form:T_data`, GO button count=0 - nav-free, matching the registry's prior "manage-object" claim). Grid columns confirmed live as Licence Code/Licence Name/Start Date/End Date via header text + an existing real row's raw cell text (`EEA_OFF-B / EC Norway offshore / 2003-01-01 / <blank>`). OV_LICENCE view + columns confirmed via `ALL_VIEWS`/`ALL_TAB_COLUMNS` query (SORT_ORDER/DESCRIPTION/MASTER_SYS_CODE/MASTER_SYS_NAME/OBJECT_START_DATE/OBJECT_END_DATE), 12 pre-existing rows, `AUTOTEST_LICENCE` confirmed free before build. Reuses the existing `${code_label}` T2 parameter unchanged; no shared T1/T2 file edits. Full `tests/` dryrun 735/735 pass; robocop 12 issues (4 VAR02 + 5 DOC02 on the new suite + 3 pre-existing credentials.py COM04/DOC03/MISC06 findings unrelated to this change), identical in kind/count to the established baseline. Grid filter wiring verified via output.xml grep: `Find Licence Row By Filter` fired exactly 5 times (Update/Find/Verify-Insert-Exists/Verify-Found/Delete) | see PR (feature/licence-bank-pattern, independent clone under Workplaces/licence) |
| Vendor (OV, Commercial Objects, manage-object) | OK Done 2026-08-23 - RF 5/5 pass first attempt (rebuilt), DB-verified vs OV_VENDOR (fresh oracledb connection: AUTOTEST_VEND count=0 both before and after the run), self-clean confirmed. Batch-4 conversion (State Lease/Vendor/Cost Object Mapping/DOA Credit Limit/Product Description, 5 parallel isolated clones under Workplaces/, see `tmp/batch4_shared_findings.md`) from the older hardcoded-field-id pattern to Bank/Customer's label-driven, properties-file-driven, T2-consolidated pattern, filter wiring included from the start. Live recon (throwaway tmp/recon_vendor*.robot, deleted before commit) confirmed nav-free (grid `manage_object_nav_nav:form:T_data`, 20 pre-existing rows; the top Date+GO bar is the same universal as-at-date filter Bank/Customer/Operator Lease also have, NOT a mandatory nav requirement) and confirmed the old page object's own prior recon exactly: mandatory fields on BOTH objectForm and updateAttributes are Code, Name, Start Date, **ERP Vendor Code** (text), **Official Name** (text), **Vendor Group** (dropdown) - all `MandatoryCellStyle`/`MandatoryCellStyleWhite`. Vendor Group's real first option is the literal `Contract Owner Vendor` - used verbatim in the insert properties file (not `__FIRST__`), per the VAT Code round-trip-verify gotcha. Grid columns confirmed live Code/Name/Start Date/End Date (Bank/Customer convention) via an existing row's raw cell text (`BUTANE_SELLER / BUTANE Seller Ltd / 2003-01-01 / <blank>`). Fixed test code `AUTOTEST_VEND`; no scope mismatch, no T2/T1 shared-file changes needed. Full `tests/` dryrun 740/740 pass; robocop 7 issues (2 VAR02 + 5 DOC02), fewer than the established 9-issue baseline; filter keyword (`Find Vendor Row By Filter`) confirmed fired 5x via output.xml grep (Update/Find/Verify-Insert-Exists/Verify-Found/Delete) | see PR (feature/vendor-bank-pattern, independent clone under Workplaces/vendor) |
| DOA Credit Limit (OV, Financial Objects, manage-object) | OK Done 2026-08-23 - RF 5/5 pass (2 fixes mid-build), DB-verified vs OV_DOA_CREDIT_LIMIT (DOA Credit Limit Code), self-clean (0 residual `AUTOTEST_DOA`/`RECON_DOA_SAVE` rows via fresh oracledb connection, confirmed pre-run free and post-run clean; original 3 production rows untouched). Batch-4 plain-OV Bank-pattern conversion (State Lease/Vendor/Cost Object Mapping/DOA Credit Limit/Product Description, 5 parallel isolated clones, `tmp/batch4_shared_findings.md`), converted from the older hardcoded-field-id pattern to Bank/VAT Code's label-driven, properties-file-driven, T2-consolidated pattern, filter wiring included from the start. Field labels are SCREEN-PREFIXED "DOA Credit Limit Code"/"DOA Credit Limit Name" - confirmed live via a throwaway RF recon script (tmp/recon_doa.robot, deleted before commit). Mandatory extras confirmed via a raw outerHTML dump: dropdown **DOA Type** (literal "Amount Based", first of 2 real options) and **Role Name** (literal "ANALYTICS.REPORTADMIN", first of 33 real options) + numeric text **Credit Limit**. **Two real live-only findings, fixed mid-build (2 retries, both root-caused via screenshot/DB evidence, not guessed)**: (1) **Currency** is statically `{mandatory:false}` but is a CONDITIONAL-mandatory EC business rule when DOA Type=Amount Based - first live Save attempt failed with a real inline validation banner "Amount Based DOA Requires a currency" (tmp/recon_doa3_save.robot); fixed by adding Currency=USD to the insert data, confirmed live to persist correctly (OV_DOA_CREDIT_LIMIT.CURRENCY_CODE='USD'). (2) **Role Name** re-renders showing its Description ("Report Administrator") after any updateAttributes reload, not the raw code used to select it ("ANALYTICS.REPORTADMIN") - live-DOM round-trip comparison against the raw code always failed; excluded from the screen-level form-label check (DB ground-truth via OV_DOA_CREDIT_LIMIT.ROLE_ID already independently proves it, matching Bank's own precedent of picking one canonical representation per field when DB/screen genuinely differ). Grid confirmed live to show 4 columns (DOA Credit Limit Code/Name/Start Date/End Date). Full `tests/` dryrun 740/740 pass; robocop 6 issues (1 VAR02 + 5 DOC02), fewer than the established 9-issue baseline. Grid filter wiring verified via output.xml grep: `Find DOA Credit Limit Row By Filter` fired exactly 7 times. No shared T1/T2 file edits | see PR (feature/doa-credit-limit-bank-pattern, independent isolated clone under Workplaces/doa_credit_limit) |
| Sales Order (OV, Financial Objects, manage-object) | OK Done 2026-08-23 - RF 5/5 pass first attempt, DB-verified vs OV_PRODUCT_SALES_ORDER (Product Sales Order Code), self-clean (0 residual `AUTOTEST_SO` rows via fresh oracledb connection, confirmed pre-run free and post-run clean). Batch-5 plain-OV Bank-pattern conversion (see `tmp/batch5_shared_findings.md`), converted from the older hardcoded-field-id pattern to Bank/Cost Object Mapping's label-driven, properties-file-driven, T2-consolidated pattern, filter wiring included from the start. Live recon (throwaway tmp/recon_so*.py, deleted before commit) confirmed nav-free (NAV_DD_COUNT=0, only the universal Date+GO as-at-date bar) and resolved the shared-findings naming concern (possible document-header-plus-lines shape) - this is a genuine Code/Name manage-object OV, same outcome as Cost Object Mapping in Batch 4. Field label is SCREEN-PREFIXED "Product Sales Order Code" (not generic "Code"). Mandatory extras confirmed via a raw outerHTML/class dump: two reference dropdowns **Company** and **Field**, NEITHER a cascade (both populate immediately, no "Dependent field" banner) - real literal option text used (not `__FIRST__`, per the VAT Code gotcha): Company=`Acme Chemicals` (same clean option already proven on Cost Object Mapping), Field=`Apollo`. objectForm also carries End Date/Description/Comments/Restrict Use (checkbox), all optional and omitted. updateAttributes carries Code (read-only guard)/Name/Description/Comments/Restrict Use/Company/Field - no Start/End Date (same convention as Bank/Cost Object Mapping). Grid confirmed live to show 4 columns (Product Sales Order Code/Name/Start Date/End Date, 20+ pre-existing rows e.g. BU_0001). objectdates End Date id (`tab:tabPanel:objectdates:form:G:0:R:0:C:3:da_input`) live-confirmed. Full `tests/` dryrun 745/745 pass; robocop 7 issues (2 VAR02 + 5 DOC02) on the changed files, at or below the established 9-issue baseline. Grid filter wiring verified via output.xml grep: `Find Sales Order Row By Filter` fired exactly 5 times. No shared T1/T2 file edits | see PR (feature/sales-order-bank-pattern, independent isolated clone under Workplaces/sales_order) |
| Account Mapping (OV, Financial Objects, manage-object, custom-URL) | OK Done 2026-08-23 - RF 5/5 pass (1 retry mid-build), DB-verified vs OV_FIN_ACCOUNT_MAPPING (Code), self-clean (0 residual `AUTOTEST_AM` rows via fresh oracledb connection, confirmed pre-run free and post-run clean; total row count unchanged at 75 before/after). Batch-6 plain-OV Bank-pattern conversion (FINAL screen of the original 23-screen candidate pool, `tmp/batch6_shared_findings.md`), converted from the older hardcoded-field-id/no-properties-file pattern (a previously-parked screen unparked 2026-06-12 via its own Playwright IUD bundle) to Bank/Cost Object Mapping's label-driven, properties-file-driven, T2-consolidated pattern, filter wiring included from the start. NOT a scope mismatch despite the "Mapping" name (batch-6 shared-findings' explicit worry): confirmed live to be a genuine Code/Name manage-object OV with an objectForm-New-Object flow, same outcome as Cost Object Mapping in Batch 4. Confirmed custom-URL/no-navigator (GO-button locator `button:form:B` count=0) with its own grid id `manageObject:form:T_data` - deliberately NOT reusing the shared T2 `${OV_MANAGE_OBJECT_TABLE}` constant, which resolves to the DIFFERENT navigator-based `manage_object_nav_nav:form:T_data` id. Reused the EXACT reference combination this screen's own PRIOR Playwright IUD bundle already proved live-PASS on 2026-06-12 (`screens/.../Account_Mapping/account_mapping_sow.md`) - a fresh DB check 2026-08-23 confirmed `JOU_ENT_ALL_ALL_ALL_ACCRUAL_CREDIT` is still free (no existing OV_FIN_ACCOUNT_MAPPING row uses it). EIGHT mandatory reference dropdowns confirmed live via a raw outerHTML/class dump (mandatory class sits on the input/dd-span itself, one level deeper than the usual wrapping-tableCell technique used on VAT Code/Customer/Cost Object Mapping): Line Item Type, Financial Code, Company Category, Status, Debit / Credit, Debit PK, Credit PK, Financial Account; Account Category is statically non-mandatory but functionally required as a cascade dependency for Financial Account (listed before it in the insert properties file, same cascade pattern as Cost Object Mapping's Object Type->Cost Object). Grid confirmed live to be a 75-row custom grid with 13 columns (Code, Name, Product, Line Item Type, Financial Code, Company Category, Company, Status, Debit / Credit, Debit PK, Credit PK, Account Category, Financial Account) and NO Start Date column (unlike Bank's/Cost Object Mapping's simpler 3-column grid, same documented variant as Regulatory Permits) - grid_verify only checks Code/Name. **1 live retry, root-caused via the exact live failure text**: Line Item Type re-renders as the short internal code `ALL` after any updateAttributes reload instead of the literal `All Line Item Types` picked at Insert - same documented re-render gotcha as DOA Credit Limit's Role Name (Batch 4); excluded from the live-DOM round-trip form-label list, DB ground-truth (Code Should Be Present In View) still covers it. Fixed test code `AUTOTEST_AM`. Full `tests/` dryrun 750/750 pass; robocop 7 issues (2 VAR02 + 5 DOC02) on the changed files, at or below the established 9-issue baseline. Grid filter wiring verified via output.xml grep: `Find Account Mapping Row By Filter` fired exactly 5 times. No shared T1/T2 file edits | see PR (feature/account-mapping-bank-pattern, independent isolated clone under Workplaces/account_mapping) |
| Stream Item Category Split Key (OV, custom-URL, Revenue Split Keys) | OK Done 2026-08-25 - RF 5/5 pass first attempt, DB-verified vs OV_SPLIT_KEY (Split Key Code, SPLIT_TYPE=STREAM_ITEM_CATEGORY), self-clean (0 residual `AUTOTEST_SPLITKEY_STREAMCAT` rows via fresh oracledb connection, confirmed pre-run free and post-run clean; STREAM_ITEM_CATEGORY row count unchanged at 3 before/after). Bank-pattern new-screen build (`ec-bank-pattern-new-screen` skill), isolated worktree `/c/tmp/wt-sicsk` off origin/master (kept separate from 5 sibling Split Key screens being built in parallel in their own worktrees). **This screen is 1 of 6 siblings sharing the SAME base view `OV_SPLIT_KEY`** (Product/Company/Field/Stream Item Category/Other/Stream Item split keys), distinguished only by `SPLIT_TYPE` - confirmed live via a `GROUP BY SPLIT_TYPE` query (8 distinct types, 109 total rows pre-build). Used a distinctly-scoped test code `AUTOTEST_SPLITKEY_STREAMCAT` (not a generic `AUTOTEST_SPLIT_KEY`) specifically to avoid colliding with the other 5 screens' own fixed test codes on this shared view. Confirmed distinct from "Split Item Other" (CD.0017, different URL controller/view, already built) and plain "Stream Item Category" (different, already-built, unrelated screen). Custom-URL OV confirmed live (grid `nav:form:T_data`, no navigator field to fill, no GO needed - matches Report Context/Product Price Object). Field labels screen-prefixed "Split Key Code"/"Split Key Name"; only Split Key Code/Split Key Name/Start Date mandatory (confirmed via live scan_ec_screen.py); no dropdowns needed for mandatory fill (Value Method/Rounding Rule both confirmed optional, left unset). Full `tests/` dryrun 815/815 pass; robocop 9 issues (4 VAR02 + 5 DOC02), matching the established custom-URL-OV baseline (Report Context) exactly after renaming long properties-file variable names to an abbreviated `SICSK_` prefix to clear LEN32 (same convention as Target Mapping Configuration's `TMC_` prefix). Grid filter wiring verified via output.xml grep: `Find Object Row By Filter` fired exactly 15 times. No shared T1/T2 file edits | see PR (feature/stream-item-category-split-key-bank-pattern, isolated worktree /c/tmp/wt-sicsk) |
| Field Split Key (OV, Configuration > Assets > Revenue Split Keys, manage-object-shaped custom-URL) | OK Done 2026-08-25 - BRAND-NEW build, no prior automation existed. RF 5/5 pass first attempt, DB-verified vs OV_SPLIT_KEY (Split Key Code), self-clean (0 residual `AUTOTEST_SPLITKEY_FIELD` rows via fresh oracledb connection, confirmed pre-run free and post-run clean). One of 6 sibling "* Split Key" screens (Product/Company/Field/Stream Item Category/Other/Stream Item, BF_CODEs CD.0036/CD.0044/CD.0095/CD.0042/CD.0046/CD.0156) sharing the SAME base view OV_SPLIT_KEY, differentiated only by SPLIT_TYPE - a prior blanket exclusion of any `split_key`-URL screen was corrected 2026-08-25 (see docs/ov-non-bank-targets.md dated note) after live recon proved all 6 are plain Bank-shaped master-data screens, not percentage-allocation grids. Live URL confirmed: `manage_object_split_key/CLASS_NAME/SPLIT_KEY/SPLIT_TYPE/FIELD`. Uses a uniquely-scoped fixed test code (`AUTOTEST_SPLITKEY_FIELD`, NOT a generic `AUTOTEST_SPLIT_KEY`) specifically to avoid cross-screen collision with the 5 sibling builds running in parallel against the same shared view. Grid id is SCREEN-SPECIFIC `nav:form:T_data` (custom-URL shape), NOT the shared `${OV_MANAGE_OBJECT_TABLE}` constant, even though the screen DOES have a Date+GO navigator like Bank - the real GO button id is also screen-specific (`navButton:form:B`, not `button:form:B`), so T2's `Save And Refresh List` correctly falls through to its Refresh-toolbar branch after Save/Update - confirmed functionally correct (grid reloads with fresh data) via the live 5/5 run, not just assumed. Live pristine-form scan confirmed only 3 mandatory fields (yellow `MandatoryCellStyle`): Split Key Code, Split Key Name, Start Date; Description/Comments/Value Method (dropdown)/Shares add to 100% (checkbox)/Rounding Rule (dropdown)/Number of Rounding Decimals/Number Display Format (dropdown) all confirmed optional and left untouched (no dropdown round-trip risk taken on). objectdates End Date id (`tab:tabPanel:objectdates:form:G:0:R:0:C:3:da_input`) live-confirmed by selecting an existing production row (read-only). Treeview path confirmed via `TV_CTRL_CONFIGURATION_STORAGE` + live nav: Configuration > Assets > Revenue Split Keys > Field Split Key. Full `tests/` dryrun 825/825 pass (post-merge of concurrent sibling batches); robocop 7 issues (1 VAR02 + 6 DOC02) on the changed files, at or below the established 9-issue baseline. Grid filter wiring verified via output.xml grep: `Find Object Row By Filter`/`Find Field Split Key Row By Filter` fired 22 times. No shared T1/T2 file edits (only an additive `FIELD_SPLIT_KEY_EC_USER`/`_EC_PASS` pair appended to `resources/credentials.py`, same convention as every other screen) | see PR (feature/field-split-key-bank-pattern) |
| Tank (OV-GM, Configuration > Assets > Tank and Storage Objects) | OK Done 2026-08-26 - BRAND-NEW build via `ec-area-pattern-new-screen` skill, no prior automation existed (a fresh repo grep confirmed nothing for plain Tank/OV_TANK before this - Chemical_Tank/Chemical_Transport_Tank/Storage/Storage_Flow/daily_tank_status_vcf are unrelated siblings). RF 5/5 pass first attempt, DB-verified vs OV_TANK (Code), self-clean (0 residual `AUTOTEST%` rows via a fresh independent oracledb connection run AFTER the live suite). Navigator shape confirmed live via a real DOM scan (not extrapolated from a sibling): single row, increasing column `nav:form:G:0:R:1:C:0..3` - C:0 is a Date field with a working default (2026-08-25 on the day scanned) that is deliberately left untouched (GO succeeds with just the C:1..C:3 cascade filled), C:1/C:2/C:3 are a genuine Production Unit -> Area -> Facility Class 1 cascade, the SAME shape and SAME "P1 Production Unit"/"P1 Area"/"P1 Facility 1" values Well already uses (confirmed selectable + grid-loading live), filled via the shared T2 `Apply Navigator From Properties` (not first-available, per the skill's own instruction to use explicit confirmed values). Live pristine-form scan confirmed 5 mandatory fields (yellow `rgb(252,249,192)` background): Tank Code, Tank Name, Start Date, Tank Meter Freq. (dropdown, `__FIRST__`), Use in BF (dropdown, `__FIRST__`). A self-cleaning probe insert/update/delete (`AUTOTEST_TANK_RECON`, never the real `AUTOTEST_TANK` code) confirmed Op Production Unit/Op Area/Op Facility Class 1 exist on objectForm, are NOT mandatory, and are NOT auto-populated from the navigator scope - filled explicitly to the nav's own P1 values so the new row stays visible under this OV-GM scope (same requirement as Area's own Op Production Unit). Treeview path confirmed via a live expand of Configuration > Assets > "Tank and Storage Objects" (Tank listed as a direct sibling of Storage/Manage Tank/Maintain Tanks there, not guessed from Storage's location alone). Grid columns confirmed live: Tank Code/Tank Name/Start Date/End Date (Area/Bank convention). objectdates Delete End Date id (`tab:tabPanel:objectdates:form:G:0:R:0:C:3:da_input`) live-confirmed via the same probe round-trip. Fixed test code `AUTOTEST_TANK` confirmed free in OV_TANK before the build. Full `tests/` dryrun 854/854 pass; robocop 7 issues on the changed files, exact parity with area_page.resource/area_iud.robot's own 7-issue baseline (same VAR02+DOC02 kind/count). Grid filter wiring verified via output.xml grep: `Find Object Row By Filter` fired 15 times. Zero inline DB-verify calls in the .robot file (grep for `Should Exist In DB`/`Field Should Equal In View`/`Code Should Be Present In View`/`Code Should Be Absent In View` returns no matches - pure-screen-verify convention). No shared T1/T2 file edits (only an additive `TANK_EC_USER`/`TANK_EC_PASS` pair appended to `resources/credentials.py`) | see PR (feature/tank-area-pattern, isolated worktree agent-a61fb8530073b73de) |
