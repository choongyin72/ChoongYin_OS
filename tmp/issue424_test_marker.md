# Issue #424 verification marker

Throwaway file used to prove `safe_commit.py`'s growth-detection actually fires on a
second push to the same branch (the file list should show this file as NEW since the
previous push). Deleted before this branch's real PR is opened.
