# CHECKLIST - Item 2: Service (CO.2103) OV-GM IUD bundle

Ticks are executed-command output. The gate ticks come from `verify_screen.py` -> `VERIFY-REPORT.md`
(**OVERALL: PASS**, exit 0); nothing here is typed from confidence.

## Recon before any code (read-only)
- [x] step-0 `scripts/check_known_issue.py "Service" SERVICE` -> exit 2; the one relevant hit was the target
      row (BF CO.2103, `OV_SERVICE`), the other 13 were incidental matches on the word "service".
- [x] family PROVEN live: OV-GM, grid `manageObject:form:T_data`, stable 3/3 scanner runs.
- [x] navigator: Date | Business Unit (only mandatory) | Contract Area | Contract.
- [x] `OV_SERVICE` has NO business-unit column - scope comes from `CONTRACT_ID` (58 columns read).
- [x] existing-row shape mirrored rather than guessed (`TS3_SHIPPER_A_P2P`).

## Traps caught BEFORE the live run (each would have cost a failed run)
- [x] `apply_ovgm_navigator` default `levels=4` RAISES here - C:3 exists with zero options -> `nav_levels: 1`.
- [x] first-available Business Unit ('EC LNG Norway') owns NO contracts, so a row could never list under it
      -> explicit `nav_value: "TS3 BU1"` (the BU that owns all 43 existing rows' contracts).
- [x] labels vs codes resolved from the DB: `TS3 BU1`=TS3_BU1, `TS3 GTA Shipper A`=TS3_GTA_SHP_A,
      `TS3 Transport System`=TS3_SYSTEM.

## The two failures during the build, and what they actually were
- [x] FAILURE 1 - the explicit Contract/Transport System values were IGNORED; the row saved
      `TRANS_INV_BLEND` / `TS5_TS`. Root cause found by reading the engine source, not guessing:
      `select_dropdown` SILENTLY falls back to the first option when the requested label is absent
      (`opt = None  # requested value absent -> retry accepting ANY (first) option`). Deliberate, for
      cascade children - but it substitutes a wrong value with no error.
- [x] FAILURE 2 - why the labels were absent: read-only panel probe showed 49 Contract options with NO TS3
      entries and only `TS5 Transport System`, because at start date 2003-01-01 the TS3 objects do not exist.
      DB: TS3_GTA_SHP_A effective **2011-01-01 -> 2031-01-01**, TS3_SYSTEM from 2011-01-01, BU TS3_BU1 from
      2011-01-01, and every existing SERVICE row starts 2011-01-01. Fixed with `start_date: "2011-01-01"`.
      **Neither failure was in the engine - both were my inputs.**

## Generator capabilities added (opt-in, both layers)
- [x] `nav_value` + `nav_levels` - explicit navigator value via Area's proven pattern
      (`Select EC Dropdown Option` -> `Apply Navigator`), Playwright and RF.
- [x] `start_date` override (default unchanged).
- [x] `extra_dropdowns` accepts `["Label","Value"]` pairs as well as plain labels.
- [x] REGRESSION: an old-style config still emits `pu = ec.apply_ovgm_navigator(page)`,
      `START_DATE = "2000-01-01"`, `Apply OV-GM Navigator First Available`, `${START_DATE} 2000-01-01`.
      Only addition is an unused `${NAV_DD}` T3 variable (benign; stated, not hidden).
- [x] robocop DEPR05 (`Set Variable` -> `VAR`) fixed in the TEMPLATE, not the output.

## Gates - auto-generated by verify_screen.py (OVERALL PASS)
- [x] 10 robocop exit 0 - [x] 11 dryrun 4/4 - [x] 12 LIVE RF 4/4 pass 0 fail - [x] PW driver 8/8
- [x] 13 DB ground-truth vs `OV_SERVICE` - [x] 14 full I-U-D - [x] 15 self-clean - [x] 16 hygiene exit 0

## Deliverables
- [x] 1 `service_sow.md` - [x] 2 `README.md` - [x] 3 `JOURNAL.md` - [x] 4 `py/service_iud.py`
- [x] 5 `investigation/` - [x] 6 `evidence/` (6 png) - [x] 7 `CHECKLIST.md`
- [x] 8 T3 `service_page.resource` - [x] 9 suite `service_iud.robot`
- [x] 17 registry row - [x] 18 scorecard row - [x] 20 KB map `ec-ui-knowledge/screens/service.md`
- [x] 0b check-existing grep now matches IDENTIFIERS (`service_iud`/`service_page`/`service_sow`), not the
      bare word "service" which had produced 8 false hits; Report Group regression still ticks with 0.
- [ ] 19 PR - cannot be ticked before the PR exists (#235). This PR is that item.

## No-loss / append-only / sandbox
- [x] `git status` shows changes ONLY in the Service bundle + the 3 doc rows + the 2 tmp tools - nothing else.
- [x] R23: registry, scorecard and manifest each `1 insertion, 0 deletions` - pure appends.
- [x] Read the appended row back and CAUGHT a wrong claim ("Op PU first-available" on a screen with
      `has_op_pu: false`); reverted the rows, fixed the descriptor, re-appended. The 15 existing OV-GM rows
      that DO have Op PU are unchanged.
- [x] Node's shipped `README.md` was overwritten by my regression run (I pointed it at `screen_folder: Node`);
      caught via `git status` and restored - verified intact.
- [x] sandbox: `0` AUTOTEST rows in `OV_SERVICE`; the one row from the failed run closed via
      End Date = Start Date with the full row logged first.
- [x] hygiene across the manifest -> `RESULT: PASS`.
