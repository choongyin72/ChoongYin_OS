import os, oracledb
dsn = os.environ.get("EC_DB_DSN", "localhost:1521/ORCL")
user = os.environ.get("EC_DB_USER", "ECKERNEL_EC")
pw = os.environ.get("EC_DB_PASS", "energy")
conn = oracledb.connect(user=user, password=pw, dsn=dsn, tcp_connect_timeout=15)
cur = conn.cursor()
cur.execute("SELECT COUNT(*) FROM OV_BANK WHERE CODE = 'AUTOTEST_CANARY_BANK'")
print("AUTOTEST_CANARY_BANK in OV_BANK (should be 0):", cur.fetchone()[0])
cur.execute("SELECT COUNT(*) FROM T_BASIS_LANGUAGE WHERE LANGUAGE = 'ZZ'")
print("ZZ in T_BASIS_LANGUAGE (should be 0):", cur.fetchone()[0])
cur.close(); conn.close()
