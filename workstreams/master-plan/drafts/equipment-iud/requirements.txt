# EC Equipment IUD Test — Python dependencies
# Install:  py -m pip install -r requirements.txt
# Verified versions as of 2026-06-06 (Python 3.14, win32)

# --- Playwright implementation (ec_iud_equipment.py) ---
playwright==1.60.0
# After install:  playwright install chromium

# --- Robot Framework implementation (ec_iud_equipment.robot) ---
robotframework==7.4.2
robotframework-browser==19.15.1
# After install:  py -m Browser.entry init   (downloads Node + Chromium)

# --- DB verification (investigation/db_*.py) ---
oracledb==4.0.1
# Thin mode — no Oracle Instant Client required
