# Cargo Planning Forecast - IUD bundle

**Screen:** EC Transport > Cargo Planning > Forecast > Cargo Planning Forecast (BF CP.0030).
Custom forecast-manager layout: per-field nav groups (PU/Area/FC1/**Storage**, SPECIFIC P1 values),
grid `fcst:form:T_data`, mandatory **End Date** at insert (row spans the nav date), Storage Name =
nav Storage. The `new_fcst` panel + COPY buttons are the copy-existing dialog (untouched). View
`OV_FCST_MNGR_FCST_LIST` (empirically resolved; row also in `OV_FORECAST_TRAN_CP`). DELETE =
End Date = Start Date (proven true delete).

- **Driver:** `py/cargo_planning_forecast_iud.py` (thin, shared engine; screen-local per-field-group
  navigator + dual-view resolution/self-clean).
- **RF:** T3 `pageobjects/EC_Transport/Cargo_Planning/Forecast/cargo_planning_forecast_page.resource`
  + suite `tests/EC_Transport/Cargo_Planning/Forecast/cargo_planning_forecast_iud.robot` (4 TCs,
  in-suite DB asserts).
- **Verify:** `VERIFY-REPORT.md` (auto-generated by `scripts/verify_screen.py`) - OVERALL PASS.
