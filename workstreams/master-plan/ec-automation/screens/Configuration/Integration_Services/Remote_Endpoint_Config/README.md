# Remote Endpoint Configuration - EC Object IUD bundle

**Screen:** Configuration > Integration Services > Remote Endpoint Configuration (BF CO.1082).
TV-style inline-editable grid, no navigator, physical delete (`TIME_SCOPE_CODE=INVARIANT`). Code
must be lowercase alphanumeric + hyphens only (a DNS-slug format) - see `remote_endpoint_config_sow.md`
+ `JOURNAL.md` for the full quirk list. Driver `py/remote_endpoint_config_iud.py`; T3/suite under
`Configuration/Integration_Services`.
