# Lifting Account - IUD bundle

**Screen:** Configuration > Assets > Transport_Objects > Lifting Account (BF CO.2004). OV-GM
**4-level nav variant** (grid `manageObject:form:T_data`): 3-column cascade + mandatory Storage dd
on a second navigator row, filled with SPECIFIC owner-provided P1 values (first-available AS1 path
has an empty Storage level - the original park reason, now root-caused). View `OV_LIFTING_ACCOUNT`.
Date-effective; DELETE = End Date = Start Date.

- **Driver:** `py/lifting_account_iud.py` (thin, shared engine; screen-local
  `apply_lifting_account_navigator` fills the 4 levels with specific values then GO).
- **RF:** T3 `pageobjects/Configuration/Assets/Transport_Objects/lifting_account_page.resource`
  (screen-local `Apply Lifting Account Navigator`) + suite
  `tests/Configuration/Assets/Transport_Objects/lifting_account_iud.robot` (4 TCs, in-suite DB asserts).
- **Nav scope:** P1 Production Unit -> P1 Area -> P1 Facility 1 -> Storage P1_CRUDE_STOR.
  Insert: Company Name first-available + Storage Name = P1_CRUDE_STOR (parent-matching).
- **Verify:** `VERIFY-REPORT.md` (auto-generated by `scripts/verify_screen.py`) - OVERALL PASS.
