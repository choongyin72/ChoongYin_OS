# Lifting Account (CO.2004) — IUD bundle

**Screen:** Configuration > Assets > Transport_Objects > Lifting Account (BF CO.2004). OV-GM
(groupmodel manage-object), navigator-gated (grid `manageObject:form:T_data` stays empty until the
mandatory navigator cascade is filled and GO is clicked). View `OV_LIFTING_ACCOUNT`. Date-effective;
DELETE = End Date = Start Date.

**Current pattern (since PR #562, 2026-08-27):** Area-pattern RF suite — 5-TC, per-TC login/logout,
properties-file-driven insert/update, explicit grid-filter wiring, pure-screen verification. See
`lifting_account_sow.md` and `JOURNAL.md` for the real conversion history, including the shared-
keyword regression caught by the reviewer's live-canary gate before merge.

This backfill task (2026-08-28, `docs/lean-deliverable-backfill-workorder.md` Batch 5) refreshed
this pre-existing bundle's documentation/evidence to match the current, already-merged automation.
It did NOT rebuild or re-verify the automation from scratch.

## Files
- Playwright driver (untouched by PR #562 and by this backfill): `py/lifting_account_iud.py`
- RF page object (T3): `pageobjects/Configuration/Assets/Transport_Objects/lifting_account_page.resource`
- RF suite: `tests/Configuration/Assets/Transport_Objects/lifting_account_iud.robot`
- Test data (properties-file-driven since PR #562):
  `testdata/lifting_account_{navigator,insert,update,form_verify,grid_verify}.properties`
- Shared keyword (read-only — NOT modified by this backfill task): `resources/manage_object.resource`
  (`Apply Navigator From Properties`, extended in PR #562 with `${row2_from}`/`${row2}`/
  `${row2_start_col}`)
- KB selector map: `ec-ui-knowledge/screens/lifting_account.md`
- `VERIFY-REPORT.md` — auto-generated by `scripts/verify_screen.py` for the ORIGINAL 2026-07-30
  hand-built 4-TC build; predates the Area-pattern conversion and is kept as historical record. The
  current suite's own re-confirmation evidence (5-TC, post-PR #562) is in `evidence/` and cited in
  `CHECKLIST.md`.

## Run commands (from `workstreams/master-plan/ec-automation/`)

Dryrun (syntax/collision check only):
```
robot --dryrun --outputdir Workplaces/lifting-account-backfill/dryrun tests/Configuration/Assets/Transport_Objects/lifting_account_iud.robot
```

Live headless run (the 5-TC suite):
```
EC_HEADLESS=true robot --outputdir Workplaces/lifting-account-backfill/live tests/Configuration/Assets/Transport_Objects/lifting_account_iud.robot
```

Live headed run (visual confirmation):
```
EC_HEADLESS=false robot --outputdir Workplaces/lifting-account-backfill/live-headed tests/Configuration/Assets/Transport_Objects/lifting_account_iud.robot
```

robocop:
```
robocop check pageobjects/Configuration/Assets/Transport_Objects/lifting_account_page.resource tests/Configuration/Assets/Transport_Objects/lifting_account_iud.robot
```

## DB self-clean check (fresh connection, run AFTER the suite)
```sql
SELECT COUNT(*) FROM OV_LIFTING_ACCOUNT WHERE CODE LIKE 'AUTOTEST%';
-- expected: 0 (TC05 deletes the fixed test code AUTOTEST_LA_001 every run)
```

## Evidence
See `evidence/` — the original build's screenshots (`la_0*.png` + `results.json`, 2026-07-30) plus
this backfill task's own one-time live confirmation run of the current 5-TC suite (`output.xml`,
`log.html`, `report.html`, `results-summary.md`, 2026-08-28).
