# VERIFY-REPORT - Project Data Mapping Setup

_Hand-assembled, NOT auto-generated by `scripts/verify_screen.py`. That tool requires `--t3`/
`--suite` (both mandatory args) - it cannot run without RF files, and none exist for this screen
(owner decision, same reasoning as Financial Item Definition/Template's PR #379, see
`CHECKLIST.md` items 8-9). Every tick below is backed by a real, just-executed command cited
inline - none are hand-typed guesses._

**OVERALL: PARTIAL PASS (Playwright + hygiene side only; RF side deferred, not a failure)**

- [x] **PW** Playwright driver 3/3 - `EC_HEADED=0 py -X utf8
      workstreams/master-plan/ec-automation/py/project_data_mapping_setup_iud.py`, exit=0, printed
      `Overall: ALL PASS` (insert/update/delete all PASS). Independently re-confirmed via
      `playwright/ec_iud_project_data_mapping_setup.py` (the delegator), same result.
- [x] **13** DB ground-truth - direct query after each run: `SELECT CODE FROM OV_COST_MAPPING
      WHERE CODE = 'AUTOTEST_PDMS_007'` returned 0 rows both times (confirms the delete step
      really removed it, not just the driver's own optimistic print).
- [x] **14** FULL I-U-D scope - Insert + Update + Delete all executed and PASSed in the same runs
      above, including UPDATE (which the original Phase 4 Pilot 3 session left unproven) with the
      Reference-field-blank-on-select fix explicitly applied and verified.
- [x] **15** Self-clean confirmed - 0 residual for `AUTOTEST_PDMS_007`, same DB query as #13,
      confirmed after both the direct driver run and the delegator re-run.
- [x] **16** Hygiene PASS - `py scripts/check_bundle_hygiene.py`, exit=0: "RESULT: PASS - no
      hardcoded creds (R16), pure ASCII (R20), no CHECKLIST/VERIFY-REPORT contradictions, doc rows
      match declared families" (2 pre-existing warnings in an unrelated Contract Area bundle, not
      this one).
- [ ] **10** robocop clean - N/A, no RF files exist (deferred, see note below).
- [ ] **11** `robot --dryrun` - N/A, no RF suite exists (deferred).
- [ ] **12** LIVE RF suite - N/A, no RF suite exists (deferred).

## Note on the RF gap
Same reasoning as `CHECKLIST.md` and Financial Item Definition/Template's PR #379: this screen was
built via the Universal Screen Engine (`engine.py`), not the classic T2/T3 pattern. RF T3/suite is
deliberately deferred rather than built now or faked as passing - will revisit once RF can call
the engine directly instead of duplicating a hand-written T3.
