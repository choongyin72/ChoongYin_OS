# Node - EC Object IUD bundle

**Screen:** Configuration > Assets > Calculation Objects > Node (BF CD.0006)
**Type:** OV-GM (manage-object, groupmodel; grid `manageObject:form:T_data`) - navigator-GATED, date-effective.

First OV-GM screen delivered on top of the item-1 gated-navigator capability (PR #244:
`apply_ovgm_navigator` / `Apply OV-GM Navigator First Available`).

## Contents
- `node_sow.md` - the filled spec (identity, gated navigator, scanned New-Object form, known risks, IUD plan).
- `VERIFY-REPORT.md` - auto-generated by `scripts/verify_screen.py` (ticks from REAL gate exit codes / counts).
- Driver: `../../../../../py/node_iud.py` (Playwright, shared engine `ec_object_iud.py`).
- RF T3: `../../../../../pageobjects/Configuration/Assets/Calculation_Objects/node_page.resource`.
- RF suite: `../../../../../tests/Configuration/Assets/Calculation_Objects/node_iud.robot`.

## Run
```
# full gate (robocop + hygiene + dryrun + live RF + live driver)
py scripts/verify_screen.py --name "Node" \
  --t3 workstreams/master-plan/ec-automation/pageobjects/Configuration/Assets/Calculation_Objects/node_page.resource \
  --suite workstreams/master-plan/ec-automation/tests/Configuration/Assets/Calculation_Objects/node_iud.robot \
  --driver workstreams/master-plan/ec-automation/py/node_iud.py \
  --out workstreams/master-plan/ec-automation/screens/Configuration/Assets/Calculation_Objects/Node/VERIFY-REPORT.md

# just the driver, headed
EC_HEADED=1 py -X utf8 workstreams/master-plan/ec-automation/py/node_iud.py
```

## Key finding (ground truth)
The insert **Op Production Unit** panel is date-filtered and does NOT contain the navigator first-available
PU - the row still lists after GO with first-available Op PU. So Op PU is set `__FIRST__`, not forced to the
nav PU. Probe: `tmp/node/probe_op_pu.py`. Never assume the nav value is a valid parent-dd option.
