# Well Bore — IUD bundle

**Screen:** Configuration > Assets > Well_and_Reservoir_Objects > Well Bore (BF CO.0054). OV-GM
with genuinely PER-FIELD nav groups `nav:form:G:1..G:4:R:1:C:0` (Production Unit / Area / Facility
Class 1 / Well & Well Hookup) needing SPECIFIC values (G:4 = a real well, `P1 W008 OP`; the
first-available option is a graph object with no bores). G:5 ('Well') is an unusable filter —
scan-flagged mandatory but 0 options under every scope tried, deliberately skipped. Mandatory
**Well popup** with list grid `Objects:form:T_data` (screen-local picker; picks the nav-scope well
by value). View `OV_WELL_BORE` (versioned; 158 rows). Date-effective; DELETE = End Date = Start Date.

Converted to the full Area-pattern RF structure in PR #564 (merged 2026-08-27) via a bespoke
screen-local T3 navigator keyword (`resources/manage_object.resource` NOT touched — see
`well_bore_sow.md`/`JOURNAL.md` for the full story). This bundle is a 2026-08-28 documentation/
evidence backfill (per `docs/lean-deliverable-backfill-workorder.md`, Batch 5) — the RF automation
itself was NOT rebuilt or modified for this backfill.

## Files
- `well_bore_sow.md` — classification, navigator shape, mandatory fields, test data, dev story.
- `JOURNAL.md` — full build history (2026-07-31 original build + 2026-08-27 Area-pattern
  conversion + 2026-08-28 documentation backfill), including the genuine mid-task registry-row
  collision incident under "Blockers -> resolution."
- `CHECKLIST.md` — the 21-gate `docs/IUD-DELIVERABLE-CHECKLIST.md`, ticked with real evidence.
- `evidence/` — `wb_0[1-5]_*.png` etc. (2026-07-31 original Playwright run) and
  `backfill_2026-08-28/` (this backfill's fresh live RF run + robocop + hygiene + DB self-clean).
- `investigation/` — pre-existing read-only recon scripts (`recon.py`, `recon_wb_popup*.py`,
  `recon_wellbore_p1b.py`) — UNCHANGED, waived from further build per Section H of the checklist
  (Playwright driver/investigation stays waived; the Universal Screen Engine is the go-forward
  replacement).
- `VERIFY-REPORT.md` — auto-generated by `scripts/verify_screen.py` from the 2026-07-31 build
  (OVERALL PASS at that time; predates the Area-pattern conversion and this backfill).

## Driver / RF files (unchanged by this backfill)
- Driver: `py/well_bore_iud.py` (thin, shared engine + screen-local `apply_well_bore_navigator`
  and `pick_well_popup`).
- T3 page object: `pageobjects/Configuration/Assets/Well_and_Reservoir_Objects/well_bore_page.
  resource` (bespoke `Apply Well Bore Navigator From Properties` + `Pick Well Popup` keywords).
- Suite: `tests/Configuration/Assets/Well_and_Reservoir_Objects/well_bore_iud.robot` (5 TCs).
- Test data: `testdata/well_bore_{navigator,insert,update,form_verify,grid_verify}.properties`.

## Exact commands

Dryrun (from `workstreams/master-plan/ec-automation/`):
```
py -m robot --dryrun tests/Configuration/Assets/Well_and_Reservoir_Objects/well_bore_iud.robot
```

Live headless run:
```
EC_HEADLESS=true py -m robot tests/Configuration/Assets/Well_and_Reservoir_Objects/well_bore_iud.robot
```

Live headed run (visual confirmation):
```
EC_HEADLESS=false py -m robot tests/Configuration/Assets/Well_and_Reservoir_Objects/well_bore_iud.robot
```

Robocop (parity check against the 7-issue baseline):
```
py -m robocop check tests/Configuration/Assets/Well_and_Reservoir_Objects/well_bore_iud.robot pageobjects/Configuration/Assets/Well_and_Reservoir_Objects/well_bore_page.resource
```

DB self-clean check (fresh independent connection — `localhost:1521/ORCL`, `ECKERNEL_EC`):
```sql
SELECT COUNT(*) FROM OV_WELL_BORE WHERE UPPER(CODE) LIKE 'AUTOTEST%';  -- expect 0 after a run
SELECT COUNT(*) FROM OV_WELL_BORE;                                      -- expect 158 (pre-existing rows intact)
```

Repo-root hygiene gate:
```
py scripts/check_bundle_hygiene.py
```
