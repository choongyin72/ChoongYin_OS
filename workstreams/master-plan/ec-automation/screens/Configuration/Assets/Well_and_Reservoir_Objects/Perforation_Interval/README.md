# Perforation Interval - IUD bundle

**Screen:** Configuration > Assets > Well_and_Reservoir_Objects > Perforation Interval (BF CO.0153).
OV-GM with 7 per-field nav groups; uses the full P1 well chain (G:4 well -> G:6 well bore ->
G:7 well bore interval); G:5 has zero options anywhere and is skipped. Mandatory **Well Bore Interval
popup of the INNER-GO type** (inherits the outer scope, but the list only appears after the popup's
own GO) + mandatory **Reservoir Block Formation** dropdown. View `OV_PERF_INTERVAL` (225 rows).
Date-effective; DELETE = End Date = Start Date.

- **Driver:** `py/perf_interval_iud.py` (thin, shared engine + screen-local `apply_perf_navigator`
  and `pick_wbi_popup` incl. the inner GO).
- **RF:** T3 `pageobjects/Configuration/Assets/Well_and_Reservoir_Objects/perf_interval_page.resource`
  (`Select Nav Group Value`, `Open Well Bore Interval Popup List`, `Pick Well Bore Interval Popup`)
  + suite `tests/Configuration/Assets/Well_and_Reservoir_Objects/perf_interval_iud.robot` (4 TCs).
- **Verify:** `VERIFY-REPORT.md` (auto-generated by `scripts/verify_screen.py`) - OVERALL PASS.
