# Chemical Stream - IUD bundle

**Screen:** Configuration > Assets > Chemical_Objects > Chemical Stream (BF CO.0258). OV-GM (grid
`manageObject:form:T_data`) with a mandatory **From Connection popup** (stream_node_ref_popup:
inner Object Type CHEM_TANK + inner GO + grid `manage_object_nav_nav:form:T_data` - screen-local
pickers in driver and T3; the generic PopupList-based helpers do not fit). Navigator = SPECIFIC P1
values (popup source empty under first-available AS1). View `OV_CHEM_STREAM`. Date-effective;
DELETE = End Date = Start Date.

- **Driver:** `py/chemical_stream_iud.py` (thin, shared engine + screen-local
  `pick_from_connection_popup` and `apply_chem_stream_navigator`).
- **RF:** T3 `pageobjects/Configuration/Assets/Chemical_Objects/chemical_stream_page.resource`
  (screen-local `Pick From Connection Popup` + `Apply Chemical Stream Navigator`) + suite
  `tests/Configuration/Assets/Chemical_Objects/chemical_stream_iud.robot` (4 TCs, in-suite DB asserts).
- **Insert extras:** Chemical Stream Type first-available; From Connection = first CHEM_TANK under
  the P1 scope. Start Date 2020-01-01 (form quirk: Start Date precedes Code).
- **Verify:** `VERIFY-REPORT.md` (auto-generated by `scripts/verify_screen.py`) - OVERALL PASS.
