# Document Template (CD.0013) - OV IUD bundle

_Refreshed 2026-08-28 (deliverable backfill, `docs/lean-deliverable-backfill-workorder.md`
Batch 12, the FINAL batch) to reflect the 2026-08-24 Bank-pattern conversion (PR #484)._

Manage-Object (OV) screen: **Configuration > Assets > Revenue_Document_Objects > Document
Template**. Full Insert / Update / Delete (End Date = Start Date), DB-verified against
`OV_DOC_TEMPLATE`, self-cleaning. Originally built label-driven on the shared engine + T2
(2026-07-26); upgraded to the full **Bank pattern** (properties-file-driven insert/update/verify
+ explicit grid-filter wiring) by PR #484 (2026-08-24, Phase 3 of the Bank-pattern conversion
project).

## Artifacts
- **SOW:** `document_template_sow.md`
- **Playwright driver:** `../../../py/document_template_iud.py` (thin; shared engine + DbVerify -
  unchanged by the Bank-pattern conversion; permanently waived from further rebuilds per Section H
  of `docs/IUD-DELIVERABLE-CHECKLIST.md` - the Universal Screen Engine replaces this role)
- **RF T3:** `../../../pageobjects/Configuration/Assets/Revenue_Document_Objects/document_template_page.resource`
- **RF suite:** `../../../tests/Configuration/Assets/Revenue_Document_Objects/document_template_iud.robot`
  (5 TCs: Verify Clean State / Insert / Update / Find / Delete)
- **Test data:** `../../../testdata/document_template_{insert,update,form_verify,grid_verify}.properties`
- **investigation/** `recon.py` (pre-existing, 2026-07-26 - not re-run by this backfill)
- **evidence/** original `document_template_0[1-5]_*.png` + `rf_report.html` (2026-07-26,
  preserved); `backfill_2026-08-28/` (this task's own re-run evidence)
- **KB selector map:** `../../../../../ec-ui-knowledge/screens/document_template.md`
- **VERIFY-REPORT.md** - auto-generated by `scripts/verify_screen.py` at the original 2026-07-26
  build (OVERALL PASS); PR #484's own re-verification numbers are cited in `JOURNAL.md` and the
  registry row, not re-generated by this backfill (see "Do NOT re-run the full original build"
  in the work order).

## Exact commands (run from `workstreams/master-plan/ec-automation/`)
```bash
# Dryrun
py -m robot --dryrun tests/Configuration/Assets/Revenue_Document_Objects/document_template_iud.robot

# Live headless run
EC_HEADLESS=true py -m robot tests/Configuration/Assets/Revenue_Document_Objects/document_template_iud.robot

# Robocop
py -m robocop check pageobjects/Configuration/Assets/Revenue_Document_Objects/document_template_page.resource \
    tests/Configuration/Assets/Revenue_Document_Objects/document_template_iud.robot

# Hygiene (run from repo root, or set REPO_ROOT to the repo root)
REPO_ROOT=<repo root> py scripts/check_bundle_hygiene.py

# DB self-clean (fresh connection; same resolution as libraries/DbVerify.py:
# EC_DB_USER=ECKERNEL_EC / EC_DB_PASS=energy / EC_DB_DSN=localhost:1521/ORCL)
# SELECT CODE FROM OV_DOC_TEMPLATE WHERE CODE = 'AUTOTEST_DOCUMENT_TEMPLATE';   -- expect 0 rows
```

## Verified (real runs, not hand-ticked)
- **Original 2026-07-26 build:** robocop 0, hygiene 0, dryrun 4/4, LIVE RF 4/4, Playwright 7/7,
  self-clean 0 residual.
- **PR #484 conversion (2026-08-24) + 2026-08-25 alignment fix:** live RF 5/5, full-tree dryrun
  810/810, filter fired 18x, fresh-connection DB self-clean 0 residual (per the registry row).
- **This backfill's own re-run (2026-08-28):** dryrun 5/5, live 5/5, robocop 9 issues (baseline
  noise - Bank's own suite returns 13 of the same two classes), hygiene PASS, filter fired 15x, DB
  self-clean 0 residual. See `evidence/backfill_2026-08-28/results_summary.md`.
