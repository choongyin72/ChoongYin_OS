# Vendor screen (OV_VENDOR) - Verify test data: the expected CURRENT state of the test
# vendor after TC02 Insert + TC03 Update, used by TC04 Find/Verify to round-trip check the
# grid + form actually SHOW what was saved. Kept as its OWN file (not reused from
# vendor_insert.properties/vendor_update.properties) because it represents a MERGED state:
# Name and Description come from the update, every other field is unchanged since insert.
# Start Date is included for completeness but NOT checked by ${VENDOR_FORM_LABELS} (it lives
# only in objectdates, not updateAttributes - same convention as Bank/Customer).
Code=AUTOTEST_VEND
Name=AUTOTEST Vendor UPDATED
Start Date=2000-01-01
Description=AUTOTEST Vendor Description UPDATED
ERP Vendor Code=ERP999
Official Name=AUTOTEST Official Name
Vendor Group=Contract Owner Vendor
