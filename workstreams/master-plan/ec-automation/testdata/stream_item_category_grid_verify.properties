# Stream Item Category screen (OV_STREAM_ITEM_CATEGORY) - Grid-level verify test data: the
# table grid only displays a subset of the record - Code, Name, Start Date, End Date - fewer
# columns than the form. Kept as its OWN file, separate from stream_item_category_form_verify
# .properties (the full form-record field set), matching Bank/Berth's convention. End Date is
# excluded here - it stays blank until deleted.
Code=AUTOTEST_SIC
Name=AUTOTEST Stream Item Category UPDATED
Start Date=2000-01-01
