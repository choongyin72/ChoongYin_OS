# Canal screen (OV_CANAL) - Update test data. Column = value, one per line, read at Update time.
# Key is the ACTUAL on-screen field label ("Canal Name", screen-prefixed - see canal_insert.properties).
# Only fields that actually exist in the updateAttributes form may be listed here - Canal Code is
# read-only there and Start Date/End Date live only in objectdates, not updateAttributes (matches
# Bank's own confirmed shape). Field set matches the already-proven Playwright driver's UPDATE_FIELDS
# (py/canal_iud.py: Canal Name only) - not an independent guess.
Canal Name=Kiel Canal UPDATED
