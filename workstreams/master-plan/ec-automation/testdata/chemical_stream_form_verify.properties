# Chemical Stream screen (OV_CHEM_STREAM) - Verify test data (TC04 Find/Verify round-trip
# check): the expected CURRENT state of the test object after TC02 Insert + TC03 Update. Kept as
# its OWN file (not reused from chemical_stream_insert.properties/chemical_stream_update.properties)
# because it represents a MERGED state: Chemical Stream Name comes from the update, Chemical
# Stream Code is unchanged since insert. Chemical Stream Type/From Connection are deliberately
# excluded (same re-render/read-only caveat as Area's Op Production Unit / Facility Class 1's Op
# Production Unit-Op Area - a dropdown/popup pin's display text can re-render differently after
# reload, so only the two plain-text labels are round-tripped).
Chemical Stream Code=AUTOTEST_CHS
Chemical Stream Name=Automation Test Chemical Stream UPDATED
