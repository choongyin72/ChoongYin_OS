# Stream - All screen (OV_STREAM) - Grid-level verify test data: the table grid only displays a
# subset of the record - Stream Code, Stream Name, Start Date - same convention as
# bank_grid_verify.properties/berth_grid_verify.properties. Name here reflects the POST-UPDATE
# state (used by TC04 Find/Verify, after TC03 Update runs). Keys here are dict lookups only
# (matched as a raw-row-text substring, not live DOM labels).
Stream Code=AUTOTEST_STREAM
Stream Name=AUTOTEST Stream UPDATED
Start Date=2000-01-01
