# Cost Object Mapping screen (OV_FIN_COST_OBJECT) - Grid-level verify test data: the table grid
# (summary data section) only displays Code, Name, Start Date (confirmed live 2026-08-23 - same
# 3 columns as Bank/Account/Country; End Date stays blank until deleted so it is excluded here).
# Kept as its OWN file, separate from cost_object_mapping_form_verify.properties (the full
# form-record field set).
Code=AUTOTEST_CMAP
Name=AUTOTEST Cost Object Mapping UPDATED
Start Date=2003-01-01
