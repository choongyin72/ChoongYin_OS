# Stream - All screen (OV_STREAM) - Update test data. Column = value, one per line, read at
# Update time. Only Stream Name is listed - Stream Code is read-only in updateAttributes and
# Start Date lives only in objectdates, not updateAttributes (same pattern as Bank/Berth/State).
# Stream Type/Alloc Period stay unchanged from Insert (already saved, not re-filled on Update).
Stream Name=AUTOTEST Stream UPDATED
