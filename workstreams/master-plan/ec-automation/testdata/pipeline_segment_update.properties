# Pipeline Segment screen (OV_PIPELINE_SEGMENT) - Update test data. Column = value, one per line,
# read at Update time. Only Pipeline Segment Name is listed - live DOM scan (2026-08-26,
# tmp/_recon_pipeline_segment3.robot, deleted before commit) found updateAttributes:form:G:0:R:1
# (Pipeline Segment Name) editable, matching every other converted screen's TC03 (only Name is
# re-edited, not Code, even where Code happens to be editable in updateAttributes - Pipeline
# Segment's Code field IS an editable <input> here, unlike Meter's read-only one, but this
# conversion deliberately keeps the same "only Name changes" scope as every other screen, per the
# owner's structural-only conversion rule).
Pipeline Segment Name=Automation Test Pipeline Segment UPDATED
