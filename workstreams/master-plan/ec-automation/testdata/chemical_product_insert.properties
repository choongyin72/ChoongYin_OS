# Chemical Product screen (OV_CHEM_PRODUCT) - Insert test data. Column = value, one per line,
# read at Insert time IN ORDER (PropertiesReader.read_properties preserves file order, and
# Insert Object From Properties fills fields in that same order).
# Mandatory fields confirmed live via scan_ec_screen.py (2026-08-24, tab:tabPanel:objectForm
# fields): Chemical Product Code, Chemical Product Name, Start Date, Meas. Units (dropdown,
# mand=True on the objectForm row - NOT a cascade, confirmed by a raw dropdown-options dump
# with no "Dependent field" banner). Product Type / Vendor Company / Vendor Comment /
# Est. Days For Delivery / Description are all mand=False and deliberately NOT filled here
# (IUD fills only needed fields).
# Meas. Units literal option = "Liter" (a clean option from the live dropdown dump: Liter,
# M3, bbls, %, US Gallons, cm, inch, voltage - not __FIRST__, matching the VAT Code/Sales
# Order precedent that __FIRST__ never resolves to literal text for the round-trip compare).
Chemical Product Code=AUTOTEST_CHEMPROD
Chemical Product Name=AUTOTEST Chemical Product
Start Date=2000-01-01
Meas. Units=Liter
