# Property screen (OV_PROPERTY) - Update test data. Column = value, one per line, read at
# Update time. Only Property Name is listed - Property Code is read-only in updateAttributes
# (same convention as every other Area-pattern-converted screen).
Property Name=Automation Test Property UPDATED
