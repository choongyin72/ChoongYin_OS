# Reservoir Block screen (OV_RESV_BLOCK) - Update test data. Column = value, one per line, read
# at Update time. Only Reservoir Block Name may be listed here - Reservoir Block Code is
# read-only in updateAttributes and Start Date lives only in objectdates, not
# updateAttributes (same pattern as Bank/Berth), matching the already-proven Playwright driver
# py/reservoir_block_iud.py (UPDATE_FIELDS: Reservoir Block Name only).
Reservoir Block Name=AUTOTEST Reservoir Block UPDATED
