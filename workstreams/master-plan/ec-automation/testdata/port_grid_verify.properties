# Port screen (OV_PORT) - Grid-level verify test data: the table grid only displays a
# subset of the record - Port Code, Port Name, Start Date - same convention as
# bank_grid_verify.properties/berth_grid_verify.properties. Name here reflects the POST-UPDATE
# state (used by TC04 Find/Verify, after TC03 Update runs). Keys here are dict lookups only
# (matched against raw row text, not live DOM labels), but named to match the real grid columns
# for clarity.
Port Code=AUTOTEST_PORT
Port Name=AUTOTEST Port UPDATED
Start Date=2000-01-01
