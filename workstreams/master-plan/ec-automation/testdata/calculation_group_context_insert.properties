# Calculation Group Context screen (OV_CALC_GRP_CONTEXT) - Insert test data.
# Column = value, one per line, read at Insert time by T2 Insert Object From Properties.
# Fixed test code AUTOTEST_CGC_BANK (batch 7 convention) - confirmed absent from
# OV_CALC_GRP_CONTEXT before this was wired in (fresh oracledb check, 2026-08-23).
# Calculation Group Object Class / Calculation Group List Class are optional dropdowns
# (screen SOW: "no mandatory dropdowns") but the proven Python driver
# (py/calculation_group_context_iud.py INSERT_FIELDS) fills them __FIRST__ defensively on
# every insert - matched here via Fill OV Field By Label Any Kind's dropdown auto-detection
# (dd_input -> Fill OV Dropdown By Label, which already supports the __FIRST__ literal).
# NOT included in the round-trip verify label list (@{CALCULATION_GROUP_CONTEXT_FORM_LABELS}
# only checks Code/Name), so the batch's __FIRST__-fails-round-trip gotcha does not apply here.
Calculation Group Context Code=AUTOTEST_CGC_BANK
Calculation Group Context Name=AUTOTEST Calculation Group Context Bank
Start Date=2000-01-01
Calculation Group Object Class=__FIRST__
Calculation Group List Class=__FIRST__
