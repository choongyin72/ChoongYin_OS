# Payment Term screen (OV_PAYMENT_TERM) - Verify test data: the expected CURRENT state of the test
# payment term after TC02 Insert + TC03 Update, used by TC04 Find/Verify to round-trip check the
# form actually SHOWS what was saved - a live-DOM check, independent of (in addition to) the DB
# assertions run separately. Represents a MERGED state: Name comes from the update, Payment Term
# Code/Date Calculation Method/Number Of Days are unchanged since insert (mirrors
# bank_form_verify.properties' own merged-state convention). Start Date is deliberately excluded -
# it lives only in objectForm/objectdates, not updateAttributes (same exclusion Bank/Disposition
# Type document for their own Start Date).
Payment Term Code=AUTOTEST_PAYMENT_TERM
Name=AUTOTEST Payment Term UPDATED
Date Calculation Method=Fixed number of Days
Number Of Days=30
