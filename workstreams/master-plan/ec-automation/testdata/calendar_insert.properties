# Calendar screen (OV_CALENDAR) - Insert test data. Column = value, one per line, read at
# Insert time. Only Calendar Code / Calendar Name / Start Date are mandatory (live
# MandatoryCellStyle scan 2026-08-23: {mandatory:true} on R:0 Code, R:1 Name, R:2 Start
# Date only) - the 7 weekday indicator checkboxes (Business day - Monday..Sunday) and
# Description/Comments/End Date are optional, left at default (matching the prior
# hand-written driver's own scope, no expansion).
Calendar Code=AUTOTEST_CALENDAR
Calendar Name=AUTOTEST Calendar
Start Date=2000-01-01
