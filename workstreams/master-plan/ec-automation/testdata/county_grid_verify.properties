# County screen (OV_COUNTY) - Grid-level verify test data: the grid (list section) only displays
# County Code, County Name, Start Date, End Date (confirmed live 2026-08-23, county_recon.robot
# screenshot) - far fewer columns than the form. Kept as its OWN file, separate from
# county_form_verify.properties (the full form-record field set). End Date is excluded here - it
# stays blank until deleted (matches Bank's own grid_verify convention).
County Code=AUTOTEST_COUNTY
County Name=AUTOTEST County UPDATED
Start Date=2000-01-01
