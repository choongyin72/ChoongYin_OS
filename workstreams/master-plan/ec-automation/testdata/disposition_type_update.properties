# Disposition Type screen (OV_DISPOSITION_TYPE) - Update test data. Column = value, one per line,
# read at Update time. Only fields that actually exist in the updateAttributes form may be listed
# here - Disposition Code is read-only there and Start Date/End Date live only in objectdates, not
# updateAttributes (per ec-ui-knowledge/screens/disposition_type.md Update tab layout). Field set
# matches the already-proven Python driver's own UPDATE_FIELDS (py/disposition_type_iud.py:
# Disposition Name, Description) - not an independent guess.
Disposition Name=AUTOTEST Disposition Type UPDATED
Description=AUTOTEST desc UPDATED
