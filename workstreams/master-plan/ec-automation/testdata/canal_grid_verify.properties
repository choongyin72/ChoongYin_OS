# Canal screen (OV_CANAL) - Grid-level verify test data: the table grid (summary data section)
# only displays a subset of the record - Code, Name, Start Date, End Date - far fewer columns than
# the form record. Keys match the screen's own field labels (see canal_insert.properties). Kept as
# its OWN file, separate from canal_form_verify.properties (the full form-record field set),
# matching bank_grid_verify.properties. End Date is excluded here - it stays blank until deleted.
Canal Code=CANAL_KIEL
Canal Name=Kiel Canal UPDATED
Start Date=2000-01-01
