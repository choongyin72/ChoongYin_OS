# Vendor screen (OV_VENDOR) - Grid-level verify test data: the table grid (summary data
# section) only displays Code, Name, Start Date, End Date (confirmed live 2026-08-23 - same 4
# columns as Bank/Account/Country/Customer). Kept as its OWN file, separate from
# vendor_form_verify.properties (the full form-record field set). End Date is excluded here -
# it stays blank until deleted.
Code=AUTOTEST_VEND
Name=AUTOTEST Vendor UPDATED
Start Date=2000-01-01
