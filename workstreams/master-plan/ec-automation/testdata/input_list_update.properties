# Input List screen (OV_STREAM_ITEM_COLLECTION) - Update test data. Column = value, one per line,
# read at Update time. Only Name may be listed here - matching the already-proven Playwright
# driver's own UPDATE_FIELDS (py/input_list_iud.py: Name only) - Code is read-only in
# updateAttributes and Start Date/List Category are Insert-only fields there.
Name=AUTOTEST Input List UPDATED
