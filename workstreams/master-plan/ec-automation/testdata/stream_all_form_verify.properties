# Stream - All screen (OV_STREAM) - Verify test data: the expected CURRENT state of the test
# stream after TC02 Insert + TC03 Update, used by TC04 Find/Verify to round-trip check the
# updateAttributes form actually SHOWS what was saved. Stream Code/Stream Name/Stream Type/
# Alloc Period are all present+mandatory in updateAttributes (confirmed live via a read-only
# scan of an existing production row) - Start Date is Insert-only, not listed here (same as
# Bank/Berth).
Stream Code=AUTOTEST_STREAM
Stream Name=AUTOTEST Stream UPDATED
Stream Type=Measured
Alloc Period=Daily
