# Stream Item Split Key screen (OV_SPLIT_KEY, SPLIT_TYPE=STREAM_ITEM_SPLIT) - Insert test data.
# Column = value, one per line, read at Insert time. Live recon (2026-08-25) confirmed
# "Split Key Code"/"Split Key Name" are the New-Object form labels (GENERIC "Split Key" prefix,
# same as the class's own base labels - not screen-prefixed with "Stream Item"). Only Split Key
# Code/Split Key Name/Start Date are mandatory (yellow) on this screen - Description/Comments/
# Value Method/Shares add to 100%/Rounding Rule/Number of Rounding Decimals/Number Display
# Format are all optional, so no dropdown fill is needed here.
# Test code is scoped distinctly (AUTOTEST_SPLITKEY_STREAMITEM, not a generic AUTOTEST_SPLIT_KEY)
# to avoid collision with the 5 sibling Split Key screens sharing OV_SPLIT_KEY in this same round.
Split Key Code=AUTOTEST_SPLITKEY_STREAMITEM
Split Key Name=AUTOTEST Stream Item Split Key
Start Date=2000-01-01
