# Region screen (OV_REGION) - Grid-level verify test data: the table grid only displays a
# subset of the record - Region Code, Region Name, Start Date - same convention as
# state_grid_verify.properties/bank_grid_verify.properties. Name here reflects the
# POST-UPDATE state (used by TC04 Find/Verify, after TC03 Update runs).
Region Code=AUTOTEST_REGION
Region Name=AUTOTEST Region UPDATED
Start Date=2000-01-01
