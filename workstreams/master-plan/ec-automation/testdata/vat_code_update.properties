# VAT Code screen (OV_VAT_CODE) - Update test data. Column = value, one per line, read at
# Update time. Only Name is listed here - Code is read-only in updateAttributes, Start Date/End
# Date live only in objectForm/objectdates (not updateAttributes), and VAT Code/Country/VAT
# Type/Rate (Decimal) are left unchanged from Insert (same pattern as Bank/Account: only the
# field(s) actually being changed are listed).
Name=AUTOTEST VAT Code UPDATED
