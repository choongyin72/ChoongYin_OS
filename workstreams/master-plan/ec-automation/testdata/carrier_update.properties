# Carrier screen (OV_CARRIER) - Update test data. Column = value, one per line, read at Update
# time. Only Carrier Name may be listed here - Carrier Code is read-only in updateAttributes and
# Start Date lives only in objectdates, not updateAttributes (same pattern as Bank/Berth/Port),
# matching the prior SOW recon (updateAttributes: Code R:0 read-only, Name R:1 edited).
Carrier Name=AUTOTEST Carrier UPDATED
