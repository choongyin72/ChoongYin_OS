# Calculation Group Context screen (OV_CALC_GRP_CONTEXT) - Grid-level verify test data: the
# table grid (summary data section) only displays a subset of the record - Code, Name, Start
# Date - far fewer columns than the form. Kept as its OWN file, separate from
# calculation_group_context_form_verify.properties, matching Bank's own grid/form split.
# End Date excluded - it stays blank until deleted.
Calculation Group Context Code=AUTOTEST_CGC_BANK
Calculation Group Context Name=AUTOTEST Calculation Group Context Bank UPDATED
Start Date=2000-01-01
