# Bank Account screen (OV_BANK_ACCOUNT) - Insert test data. Column = value, one per line, read
# at Insert time via PropertiesReader.read_properties. Confirmed LIVE via a fresh field-inventory
# scan of objectForm (2026-08-23, tmp/recon_bankaccount_labels.robot, 30 ECCell labels dumped):
# Code label is SCREEN-PREFIXED "Bank Account Code" (unlike Bank's own generic "Code") - Name is
# generic. Mandatory: Bank Account Code, Name, Start Date (static MandatoryCellStyle), Bank dd and
# Currency dd (static MandatoryCellStyle). Sort Code and Customer dd are NOT static-mandatory on
# this live pass, but are kept per the already-proven Playwright driver
# (py's screens/.../Bank_Account/playwright/ec_iud_bank_account.py CFG, live-PASS 2026-06-11,
# ins_extra=Sort Code + ins_dd_first=[Customer, Bank, Currency]) - trusting the proven driver's
# field set over the static scan, per the Process Train Batch-9 lesson (a conditional-mandatory
# business rule can be invisible to static CSS, only surfacing as a Save-time banner). A live
# Vendor dd also exists at R22 (not in the proven driver, not static-mandatory) - deliberately
# OMITTED (IUD fill only needed fields). Bank/Customer/Currency use __FIRST__ (Storage Flow
# Batch-10 precedent: a resolved reference dropdown value can re-render as different display text
# after reload, so the round-trip form-label compare deliberately excludes these 3 labels -
# DB ground truth is Code presence in OV_BANK_ACCOUNT, not per-dropdown value).
Bank Account Code=AUTOTEST_BACC
Name=AUTOTEST Bank Account
Start Date=2003-01-01
Sort Code=000000
Bank=__FIRST__
Customer=__FIRST__
Currency=__FIRST__
