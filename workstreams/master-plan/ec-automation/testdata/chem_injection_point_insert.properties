# Chemical Injection Point screen (OV_CHEM_INJ_POINT) - Insert test data. Column = value, one
# per line, read at Insert time. Chem Inj Point Code/Chem Inj Point Name are SCREEN-PREFIXED
# labels (confirmed live 2026-08-26 objectForm/proven driver py/chem_injection_point_iud.py,
# verify_screen PASS 2026-07-30).
# Op Production Unit=__FIRST__ (NOT a hardcoded label) - a live 2026-08-26 diagnostic
# (tmp/recon_cip_pu_scroll_diagnosis.py) found this specific objectForm autocomplete renders
# only a 5-row MRU/default subset (EC-UT-GENERIC/FRMW PU/P1/P3/automation_ui_test) before the
# full ~25-row reference list loads, so a hardcoded exact-label match
# ("AS1 EC Exploration Norway") is flaky - the requested label can be absent from whichever
# subset happens to render within the click-wait window. The already-proven, currently-passing
# driver (py/chem_injection_point_iud.py, verify_screen PASS 2026-07-30) never hit this because
# it uses "__FIRST__" (accept whichever option is visible, no exact-label wait) for this exact
# field - and the resulting row still shows under the navigator scope regardless of which
# specific Op Production Unit value gets picked, so this field is NOT actually required to
# match the nav scope for grid visibility on THIS screen (unlike the Area/Facility Class 1
# rationale, which does not hold here - confirmed by the proven driver's own passing behavior,
# not assumed). Kept as "__FIRST__" to match that exact, already-tested mechanism.
Chem Inj Point Code=AUTOTEST_CIP
Chem Inj Point Name=Automation Test Chem Inj Point
Start Date=2000-01-01
Op Production Unit=__FIRST__
