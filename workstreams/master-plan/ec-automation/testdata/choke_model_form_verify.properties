# Choke Model screen (OV_CHOKE_MODEL) - Verify test data: the expected CURRENT state of the test
# choke model after TC02 Insert + TC03 Update, used by TC04 Find/Verify to round-trip check the
# form actually SHOWS what was saved - a live-DOM check, independent of (in addition to) the DB
# assertions run separately. Represents a MERGED state: Name/Description come from the update,
# Choke Model Code is unchanged since insert. Start Date is deliberately excluded - it lives only
# in objectForm/objectdates, not updateAttributes (same exclusion Bank/Disposition Type/Payment
# Term document for their own Start Date).
Choke Model Code=AUTOTEST_CHOKE_MODEL
Choke Model Name=AUTOTEST Choke Model UPDATED
Description=AUTOTEST desc UPDATED
