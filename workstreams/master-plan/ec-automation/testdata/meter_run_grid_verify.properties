# Meter Run screen (OV_METER_RUN) - Grid-level verify test data: the table grid only displays a
# subset of the record - Meter Run Code, Meter Run Name, Start Date - same convention as
# berth_grid_verify.properties/bank_grid_verify.properties. Name here reflects the POST-UPDATE
# state (used by TC04 Find/Verify, after TC03 Update runs). Keys here are dict lookups only
# (matched against raw row text, not live DOM labels), but named to match the real grid columns
# for clarity.
Meter Run Code=AUTOTEST_METER_RUN
Meter Run Name=AUTOTEST Meter Run UPDATED
Start Date=2000-01-01
