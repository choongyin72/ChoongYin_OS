# State Lease screen (OV_STATE_LEASE) - Verify test data: the expected CURRENT state of the test
# state lease after TC02 Insert + TC03 Update, used by TC04 Find/Verify to round-trip check the
# grid + form actually SHOW what was saved. Kept as its OWN file (not reused from
# state_lease_insert.properties/state_lease_update.properties) because it represents a MERGED
# state: State Lease Name and Description come from the update, Code is unchanged since insert.
# Start Date is included for completeness but NOT checked by ${STATE_LEASE_FORM_LABELS} (it
# lives only in objectForm/objectdates, not updateAttributes - same convention as Bank/Customer).
State Lease Code=AUTOTEST_STL
State Lease Name=AUTOTEST State Lease UPDATED
Start Date=2000-01-01
Description=AUTOTEST State Lease Description UPDATED
