# Meter screen (OV_METER) - Verify test data (TC04 Find/Verify round-trip check): the expected
# CURRENT state of the test meter after TC02 Insert + TC03 Update. Kept as its OWN file (not
# reused from meter_insert.properties/meter_update.properties) because it represents a MERGED
# state: Meter Name comes from the update, Meter Code is unchanged since insert. Start Date/
# Meter Type/Delivery Point Name are deliberately excluded from this round-trip compare - Start
# Date lives only in objectdates (not updateAttributes), and Meter Type (dropdown)/Delivery Point
# Name (popup pin) can re-render different display text after reload, same caveat as every other
# converted screen's dropdown/popup fields (Area's Op Production Unit, Chemical Stream's
# Chemical Stream Type/From Connection).
Meter Code=AUTOTEST_METER
Meter Name=Automation Test Meter UPDATED
