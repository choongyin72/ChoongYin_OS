# Currency screen (OV_CURRENCY) - Update test data. Column = value, one per line, read at
# Update time. Only Name may be listed here - Code is read-only in updateAttributes and
# Start Date lives only in objectdates, not updateAttributes (same pattern as Bank/Cost
# Centre). Confirmed live 2026-08-23: updateAttributes label dump = Code, Name, Description,
# Comments, Active, "1/100 Units" (no Start/End Date).
Name=AUTOTEST Currency UPDATED
