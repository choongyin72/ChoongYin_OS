# Stream Item Category screen (OV_STREAM_ITEM_CATEGORY) - Insert test data.
# Column = value, one per line, read at Insert time. Generic "Code"/"Name" labels
# (NOT screen-prefixed - confirmed via direct grep, per batch10 shared findings).
# Mandatory field set (Code/Name/Start Date only) confirmed against the already-proven
# Playwright driver py/stream_item_category_iud.py (INSERT_FIELDS) - no optional dropdowns.
# Start Date lives in objectForm here (confirmed via the existing page object's
# "Fill OV Date By Label objectForm Start Date" call), NOT a separate objectdates form -
# T2's Fill OV Field By Label Any Kind auto-detects the da_input suffix and fills it as a
# date, so it can be listed directly in this properties file like Code/Name.
Code=AUTOTEST_SIC
Name=AUTOTEST Stream Item Category
Start Date=2000-01-01
