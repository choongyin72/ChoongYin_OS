# Report Area screen (OV_REPORT_AREA) - Insert test data. Column = value, one per line, read
# at Insert time. Field labels are "Report Area Code"/"Report Area Name" (screen-prefixed) -
# confirmed via the existing page object pageobjects/Reporting/report_area_page.resource and
# the already-proven Playwright driver py/report_area_iud.py (INSERT_FIELDS). Date label is
# "Start date" (lowercase "date") - confirmed via the pre-existing page object's recon comment
# ("Field labels (from recon): ... Date=\"Start date\"") and reproduced live: "Start Date"
# (capital D) fails with a 30s locator timeout since the label lookup is exact-match/
# case-sensitive (Fill OV Field By Label Any Kind, resources/manage_object.resource).
# Simplest OV: no Description, no dropdowns - only Report Area Code/Report Area Name/Start
# date are mandatory (IUD fills only needed fields).
Report Area Code=AUTOTEST_RPTA
Report Area Name=AUTOTEST Report Area
Start date=2000-01-01
