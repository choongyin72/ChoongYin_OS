# Storage Flow screen (OV_STORAGE_FLOW) - Update test data. Column = value, one per line,
# read at Update time. Only Storage Flow Name may be listed here - Storage Flow Code is
# read-only in updateAttributes and Start Date lives only in objectdates, not
# updateAttributes (same pattern as Bank/Berth/State), matching the already-proven
# Playwright driver py/storage_flow_iud.py (UPDATE_FIELDS: Storage Flow Name only).
Storage Flow Name=AUTOTEST Storage Flow UPDATED
