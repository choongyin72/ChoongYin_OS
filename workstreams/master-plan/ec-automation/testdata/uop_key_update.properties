# UOP Key screen (OV_FIN_UOP_DEPR_KEY) - Update test data. Column = value, one per line, read
# at Update time. Only Name is listed - matches the already-proven 2026-07-26 driver's own
# update field set (Name only); Code is read-only in updateAttributes and Start Date/End Date
# live only in objectdates, not updateAttributes (same convention as Bank).
Name=Autotest UOP Key UPDATED
