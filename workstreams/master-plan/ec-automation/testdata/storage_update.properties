# Storage screen (OV_STORAGE) - Update test data. Column = value, one per line, read at Update
# time. Only Storage Name is listed - Storage Code is read-only in updateAttributes (same
# convention as Area/Facility Class 1).
Storage Name=Automation Test Storage UPDATED
