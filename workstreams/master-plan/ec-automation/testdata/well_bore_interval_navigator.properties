# Well Bore Interval (CO.0057) OV-GM navigator - PER-FIELD groups nav:form:G:1..G:6:R:1:C:0
# (NOT a same-row cascade, so this file is NOT read by the shared T2 "Apply Navigator From
# Properties" - resources/manage_object.resource explicitly documents that keyword as covering
# ONLY the single-row/increasing-column cascade shape). Read instead by this screen's OWN
# bespoke T3 keyword "Apply Well Bore Interval Navigator" in well_bore_interval_page.resource
# (modeled on well_page.resource's pre-2026-08-26 "Apply Well Navigator" keyword), which loops
# key=value in file order, resolves each key to its known group id, then clicks GO once.
# G:5 ("Well") is deliberately OMITTED - live-confirmed (2026-08-27 recon,
# Workplaces/well-bore-interval-area-pattern/recon_wbi_nav_live.py) present in the DOM but
# offers ZERO usable options under this scope; the existing proven driver
# (py/well_bore_interval_iud.py NAV tuple) skips it and GO still succeeds.
# Values reused VERBATIM from py/well_bore_interval_iud.py's NAV tuple (confirmed live).
Production Unit=P1 Production Unit
Area=P1 Area
Facility Class 1=P1 Facility 1
Well & Well Hookup=P1 W008 OP
Well Bore=P1 W008 WB001
