# Price Rate screen (OV_PRICE_RATE) - Verify test data (TC04 Find/Verify round-trip check): the
# expected CURRENT state of the test record after TC02 Insert + TC03 Update. Kept as its OWN
# file (not reused from price_rate_insert.properties/price_rate_update.properties) because it
# represents a MERGED state: Price Rate Name comes from the update, Price Rate Code is unchanged
# since insert (same convention as area_form_verify.properties).
Price Rate Code=AUTOTEST_PRICE_RATE
Price Rate Name=Automation Test Price Rate UPDATED
