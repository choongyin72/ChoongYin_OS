# Price Rate screen (OV_PRICE_RATE) - Update test data. Column = value, one per line, read at
# Update time. Only Price Rate Name is listed - Price Rate Code is read-only in
# updateAttributes (same convention as every other converted OV-GM screen: Code is a create-once
# key).
Price Rate Name=Automation Test Price Rate UPDATED
