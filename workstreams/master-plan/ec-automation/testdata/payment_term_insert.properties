# Payment Term screen (OV_PAYMENT_TERM) - Insert test data. Column = value, one per line, read at
# Insert time. Field set matches the already-proven Python driver's own field set
# (screens/.../Payment_Term/playwright/ec_iud_payment_term.py: Code, Name, Start Date, Date
# Calculation Method dropdown, Number Of Days) - not an independent guess. Labels confirmed live
# via the driver's own evidence screenshot (evidence/pt_04_insert_filled.png): "Payment Term Code"
# is screen-prefixed, but "Name" is the GENERIC label (not "Payment Term Name"). The Method
# dropdown's real on-screen label is "Date Calculation Method" (not "Method") and the mandatory
# numeric cell's real label is "Number Of Days" (not "Offset"/"Day Value") - the driver's own
# internal variable names (METHOD/OFFSET) are generic clone-naming, not the live label text.
# ERP Code/Description/Comments/Calculation are optional and left unset (fill-only-needed-fields).
Payment Term Code=AUTOTEST_PAYMENT_TERM
Name=AUTOTEST Payment Term
Start Date=2000-01-01
Date Calculation Method=Fixed number of Days
Number Of Days=30
