# Account screen (OV_FIN_ACCOUNT) - Insert test data. Column = value, one per line, read at
# Insert time. Field labels are the generic "Code"/"Name" (NOT screen-prefixed) - confirmed live
# 2026-08-22 via the New Object form field dump. Two extra fields are mandatory (MandatoryCellStyle
# CSS class confirmed live on both the New Object form AND an existing real row's updateAttributes):
# "Cost Object Type" (dropdown) and "GL Account" (text). Cost Object Type's live options were read
# from its ui-autocomplete panel's data-item-label attributes: Cost Center / Revenue Order /
# WBS Element / None - "None" was picked because it is a real catalogued option that does NOT
# implicitly reference another master-data object (unlike Cost Center/Revenue Order/WBS Element,
# which would need a matching real record of that type to be a valid link). GL Account is a plain
# text field (tooltip: "The ERP account number") - real production rows use numeric account codes
# (e.g. 1410010, 4000000); 999999 is used here as an obviously-test numeric value.
Code=AUTOTEST_ACCOUNT
Name=AUTOTEST Account
Start Date=2000-01-01
Cost Object Type=None
GL Account=999999
