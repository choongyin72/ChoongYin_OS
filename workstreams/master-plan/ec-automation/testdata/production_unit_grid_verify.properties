# Production Unit screen (OV_PRODUCTIONUNIT) - Grid-level verify test data: the table grid
# only displays a subset of the record - Code, Name, Start Date - same convention as the
# other rebuilt screens. Keys here are dict lookups only (matched against raw row text, not
# live DOM labels).
Production Unit Code=AUTOTEST_PU
Production Unit Name=AUTOTEST Production Unit UPDATED
Start Date=2000-01-01
