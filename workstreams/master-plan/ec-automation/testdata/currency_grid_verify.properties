# Currency screen (OV_CURRENCY) - Grid-level verify test data: the table grid only displays
# a subset of the record - Code, Name, Start Date - same convention as
# bank_grid_verify.properties/cost_centre_grid_verify.properties. Name here reflects the
# POST-UPDATE state (used by TC04 Find/Verify, after TC03 Update runs).
Code=AUTOTEST_CURRENCY
Name=AUTOTEST Currency UPDATED
Start Date=2000-01-01
