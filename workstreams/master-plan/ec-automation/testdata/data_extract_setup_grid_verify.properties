# Data Extract Setup screen (OV_SUMMARY_SETUP) - Grid-level verify test data: the table grid
# only displays a subset of the record - Data Extract Setup Code, Data Extract Setup Name,
# Start Date, End Date - same convention as bank_grid_verify.properties/
# data_extract_set_grid_verify.properties. Name here reflects the POST-UPDATE state (used by
# TC04 Find/Verify, after TC03 Update runs). Keys here are dict lookups only (matched against
# raw row text, not live DOM labels).
Data Extract Setup Code=AUTOTEST_DXS
Data Extract Setup Name=AUTOTEST Data Extract Setup UPDATED
Start Date=2000-01-01
