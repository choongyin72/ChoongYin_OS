# Process Train screen (OV_PROCESS_TRAIN) - Insert test data. Column = value, one per line,
# read at Insert time. Field labels are "Process Train Code"/"Process Train Name"
# (screen-prefixed) - confirmed via ec-ui-knowledge/screens/process_train.md and the
# already-proven Playwright driver py/process_train_iud.py.
# CORRECTION (2026-08-23, Batch 9 live repro): the KB doc's "no mandatory dropdowns" note
# (2026-07-26 scan) does NOT hold for actually persisting the record - a live RF attempt
# WITHOUT Production Facility Class 1 clicked Save successfully (button enabled, click
# succeeded) but the record never reached OV_PROCESS_TRAIN (0 rows) and left EC's own
# "unsaved changes" confirmation modal (#confirmationForm:confirmation_modal) open, stalling
# every subsequent click. Re-running the already-proven driver py/process_train_iud.py
# as-is (unmodified) - which DOES fill this dropdown with __FIRST__ - passed 7/7 cleanly.
# So Production Facility Class 1 is filled here too, matching the proven driver, even though
# the KB doc calls it optional; that doc will be corrected in the same PR (write-after).
Process Train Code=AUTOTEST_PT
Process Train Name=AUTOTEST Process Train
Start Date=2000-01-01
Production Facility Class 1=__FIRST__
