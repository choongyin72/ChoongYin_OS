# Product Group screen (OV_PRODUCT_GROUP) - Insert test data. Column = value, one per line,
# read at Insert time. Field labels are "Product Group Code"/"Product Group Name"
# (screen-prefixed) - confirmed live 2026-08-23 via the New Object form label dump, same
# convention as State/State Lease (NOT the generic "Code"/"Name" Bank/Object List use).
# Only mandatory fields filled (Start Date is the only CSS-mandatory field beyond Code/Name;
# Sort Order/Product Group Type/Comments are optional and left unset, matching the
# "fill only needed fields" convention for real IUD work).
Product Group Code=AUTOTEST_PRODUCT_GROUP
Product Group Name=AUTOTEST Product Group
Start Date=2000-01-01
