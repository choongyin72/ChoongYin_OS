# Calendar Collection screen (OV_CALENDAR_COLLECTION) - Insert test data. Column = value, one
# per line, read at Insert time. Field labels are GENERIC "Code"/"Name" (NOT screen-prefixed) -
# confirmed live 2026-08-23 via a New Object form field-label scan (8 ECCell labels: Code, Name,
# Start Date (mandatory), End Date, Description, Comments - no weekday-indicator checkboxes on
# this EC build, contrary to the pre-existing hardcoded-pattern page object's docstring; trusted
# the live DOM scan over that stale comment). Only Code/Name/Start Date are filled here
# (IUD-fill-only-needed-fields convention).
Code=AUTOTEST_CALENDAR_COLLECTION
Name=AUTOTEST Calendar Collection
Start Date=2000-01-01
