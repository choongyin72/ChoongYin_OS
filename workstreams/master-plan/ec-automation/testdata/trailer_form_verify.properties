# Trailer screen (OV_TRAILER) - Verify test data: the expected CURRENT state of the test
# trailer after TC02 Insert + TC03 Update, used by TC04 Find/Verify to round-trip check the
# updateAttributes form actually SHOWS what was saved. Only Trailer Code/Trailer Name are
# listed - Start Date is an Insert-only field, not present in updateAttributes (same as Bank/
# Berth). Trailer Type/UOM/Transport Company are dropdowns filled with __FIRST__ and are
# deliberately EXCLUDED from this round-trip list (Batch 10 gotcha: a dropdown can re-render
# its Description text instead of the code after reload, so it is not a stable live-DOM
# comparison target - excluded from round-trip, same convention as every other converted
# screen's dropdown fields).
Trailer Code=AUTOTEST_TRAILER
Trailer Name=AUTOTEST Trailer UPDATED
