# Pipeline Segment screen (OV_PIPELINE_SEGMENT) OV-GM navigator - single mandatory Business Unit
# dropdown at nav:form:G:0:R:1:C:1:dd, SAME single-dropdown shape as Area's own Production Unit
# navigator / Meter's own Business Unit navigator. Live DOM scan (2026-08-26) confirmed: G:0 only
# (no G:1/G:2), row 1 has 3 columns - C:0 Date (pre-filled default, not empty-mandatory), C:1
# Business Unit (MandatoryCellStyle, genuinely mandatory+empty), C:2 Pipeline (mandatory:false,
# optional filter). Only ONE line is listed here so the shared T2 "Apply Navigator From
# Properties" (resources/manage_object.resource) fills C:1 only, matching the confirmed-live
# requirement. "TS5 BU" is the SAME value the pre-existing pipeline_segment_iud.robot (${NAV_BU})
# already proved live (2026-06-12, Pipeline Segment's original 4/4 build).
Business Unit=TS5 BU
