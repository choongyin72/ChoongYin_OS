# Blend screen (OV_BLEND) - Grid-level verify test data: the table grid (summary data section)
# only displays a subset of the record - confirmed live 2026-08-23 via a fresh grid-header scan:
# Blend Code, Blend Name, Start Date, End Date. Kept as its OWN file, separate from
# blend_form_verify.properties (the full form-record field set). End Date is excluded here - it
# stays blank until deleted.
Blend Code=AUTOTEST_BLEND
Blend Name=Autotest Blend UPDATED
Start Date=2000-01-01
