# Choke Model screen (OV_CHOKE_MODEL) - Update test data. Column = value, one per line, read at
# Update time. Only fields that actually exist in updateAttributes may be listed here - Choke
# Model Code is read-only there, and Start Date/End Date live only in objectForm/objectdates, not
# updateAttributes (same exclusion Bank/Disposition Type/Payment Term document for their own
# Start Date). Matches the prior OLD-pattern build's own Update step (Name + Description), both
# confirmed present in updateAttributes via the prior build's own working field ids
# (choke_model_page.resource CHKM_UPD_NAME R1:C1, CHKM_UPD_DESC R3:C1).
Choke Model Name=AUTOTEST Choke Model UPDATED
Description=AUTOTEST desc UPDATED
