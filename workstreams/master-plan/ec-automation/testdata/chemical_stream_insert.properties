# Chemical Stream screen (OV_CHEM_STREAM) - Insert test data. Column = value, one per line, read
# at Insert time. Chemical Stream Code/Chemical Stream Name are SCREEN-PREFIXED labels (confirmed
# live 2026-07-30, pre-existing chemical_stream_page.resource's own "Fill OV Field By Label"
# calls). Start Date is listed FIRST because it precedes Code on this form (confirmed live) - the
# T3 insert keyword fills fields in this file's order. Chemical Stream Type=__FIRST__ picks
# whatever option is first-available (same as py/chemical_stream_iud.py's proven insert_fields).
# The mandatory "From Connection" popup field is DELIBERATELY NOT listed here - it needs the
# screen-local popup mechanism (Open From Connection Popup List / Pick From Connection Popup,
# preserved unchanged by this conversion), not the generic by-label popup fill.
Start Date=2020-01-01
Chemical Stream Code=AUTOTEST_CHS
Chemical Stream Name=Automation Test Chemical Stream
Chemical Stream Type=__FIRST__
