# Stream Item Split Key screen (OV_SPLIT_KEY, SPLIT_TYPE=STREAM_ITEM_SPLIT) - Grid-level verify
# test data: the table grid only displays a subset of the record - Split Key Code, Split Key
# Name, Start Date - same convention as bank_grid_verify.properties/berth_grid_verify.properties.
# Split Key Name here reflects the POST-UPDATE state (used by TC04 Find/Verify, after TC03
# Update runs). Keys here are dict lookups only (matched against raw row text, not live DOM
# labels), but named to match the real grid columns for clarity.
Split Key Code=AUTOTEST_SPLITKEY_STREAMITEM
Split Key Name=AUTOTEST Stream Item Split Key UPDATED
Start Date=2000-01-01
