# VAT Code screen (OV_VAT_CODE) - Verify test data: the expected CURRENT state of the test vat
# code after TC02 Insert + TC03 Update, used by TC04 Find/Verify to round-trip check the
# updateAttributes form actually SHOWS what was saved. Name reflects the POST-UPDATE value;
# VAT Code/Country/VAT Type/Rate (Decimal) are unchanged from Insert (TC03 never touches them).
# Country="Any"/VAT Type="Income" are the literal option text __FIRST__ resolved to at Insert
# time (confirmed live 2026-08-23 - see vat_code_insert.properties' own header). Start Date is
# objectForm/Insert-only, not present in updateAttributes (same as Bank/Account), so it is not
# listed here.
Code=AUTOTEST_VAT
VAT Code=AT9
Country=Any
VAT Type=Income
Name=AUTOTEST VAT Code UPDATED
Rate (Decimal)=0.1
