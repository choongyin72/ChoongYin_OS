# Property screen (OV_PROPERTY) - navigator test data for the shared T2 "Apply Navigator From
# Properties" keyword (resources/manage_object.resource). Confirmed LIVE 2026-08-26 via a
# fresh read-only recon (tmp/recon_property_nav.py, no Save/Insert/Delete):
#   - G:0 (fieldset "Date") already carries a non-empty default on load (today's date,
#     MandatoryCellStyleWhite = already-filled style) - nothing to fill here.
#   - G:1 (fieldset "Business Unit") is the ONLY genuinely mandatory+empty group
#     (MandatoryCellStyle = empty+mandatory style), single dropdown at C:0 (not C:1) -
#     confirmed live to be a valid option (16 total options dumped, "Royalty Canada" present).
# This is the SAME shape this session already proved fits Area's pattern (Tract): exactly ONE
# group genuinely mandatory+empty, regardless of group index. The shared keyword's ${group}/
# ${start_col} optional arguments (added this session) are called as ${group}=1 ${start_col}=0
# by property_page.resource to address this screen's own group/column position.
# Value "Royalty Canada" matches the prior hand-written driver (py/property_iud.py) AND the
# insert form's own "Business Unit Name" field (property_insert.properties) - kept IDENTICAL
# per the field-reuse rule (an inserted row is only grid-visible under this same nav scope).
Business Unit=Royalty Canada
