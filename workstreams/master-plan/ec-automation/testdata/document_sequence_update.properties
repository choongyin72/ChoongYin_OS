# Document Sequence screen (OV_DOC_SEQUENCE) - Update test data. Column = value, one per line,
# read at Update time. Only Name may be listed here - Code is read-only in updateAttributes and
# Start Date lives only in objectdates, not updateAttributes (same pattern as Bank/Report
# Context). Starting Point is deliberately NOT re-set on Update - matches the screen's own prior
# driver, which only ever filled it at Insert.
Name=AUTOTEST DOCUMENT SEQUENCE UPDATED
