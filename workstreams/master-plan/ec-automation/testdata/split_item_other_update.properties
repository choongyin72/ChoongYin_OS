# Split Item Other screen (OV_SPLIT_ITEM_OTHER) - Update test data. Column = value, one per
# line, read at Update time. Only Name may be listed here - Split Item Code is read-only in
# updateAttributes and Start Date lives only in objectdates, not updateAttributes (same
# pattern as Bank/Berth), matching the already-proven Playwright driver
# py/split_item_other_iud.py (UPDATE_FIELDS: Name only).
Name=AUTOTEST Split Item Other UPDATED
