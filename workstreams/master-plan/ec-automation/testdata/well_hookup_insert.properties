# Well Hookup screen (OV_WELL_HOOKUP) - Insert test data. Column = value, one per line, read at
# Insert time. Well Hookup Code/Well Hookup Name are SCREEN-PREFIXED labels (matches the already-
# proven py/well_hookup_iud.py + pageobjects/.../well_hookup_page.resource, both still passing
# 2026-07-30). Op Production Unit IS filled (__FIRST__) here - unlike Facility Class 1's Op
# Production Unit/Op Area (left blank, out of scope), Well Hookup's own already-proven driver
# treats this dropdown as a genuine mandatory field on objectForm and fills it every run; kept as
# an EXPLICIT insert field for that reason. It is deliberately EXCLUDED from
# well_hookup_page.resource's @WH_FORM_LABELS verify list (below) because "__FIRST__" is a fill
# directive, not a real comparable screen value.
Well Hookup Code=AUTOTEST_WH
Well Hookup Name=Automation Test Well Hookup
Start Date=2000-01-01
Op Production Unit=__FIRST__
