# Berth screen (OV_BERTH) - Update test data. Column = value, one per line, read at Update
# time. Only Berth Name may be listed here - Berth Code is read-only in updateAttributes and
# Start Date lives only in objectdates, not updateAttributes (same pattern as Bank/State),
# matching the already-proven Playwright driver py/berth_iud.py (UPDATE_FIELDS: Berth Name only).
Berth Name=AUTOTEST Berth UPDATED
