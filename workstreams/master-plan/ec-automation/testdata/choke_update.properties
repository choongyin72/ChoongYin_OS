# Choke screen (OV_CHOKE) - Update test data. Column = value, one per line, read at Update time.
# Only fields that actually exist in updateAttributes may be listed here - Choke Code is
# read-only there, and Start Date/End Date live only in objectForm/objectdates, not
# updateAttributes (same exclusion Bank/Choke Model/Disposition Type document for their own
# Start Date). Matches the prior OLD-pattern build's own Update step (Name + Comments), both
# confirmed present in updateAttributes via the prior build's own working field ids
# (choke_page.resource CHOKE_UPD_NAME R1:C1, CHOKE_UPD_COMMENTS R4:C1) and the already-proven
# Playwright driver's own UPDATE_FIELDS (py/choke_iud.py).
Choke Name=AUTOTEST Choke UPDATED
Comments=AUTOTEST cmt UPDATED
