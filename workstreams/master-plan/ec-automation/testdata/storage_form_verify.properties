# Storage screen (OV_STORAGE) - Verify test data (TC04 Find/Verify round-trip check): the
# expected CURRENT state of the test object after TC02 Insert + TC03 Update. Kept as its OWN file
# (not reused from storage_insert.properties/storage_update.properties) because it represents a
# MERGED state: Name comes from the update, Code is unchanged since insert. Storage Type/Product
# Name are deliberately excluded (same rationale as every other converted screen's __FIRST__
# dropdown fields - display text can re-render differently after reload, not a safe round-trip
# comparison target).
Storage Code=AUTOTEST_STG
Storage Name=Automation Test Storage UPDATED
