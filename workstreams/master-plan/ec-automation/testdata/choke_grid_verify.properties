# Choke screen (OV_CHOKE) - Grid-level verify test data: values must appear as substrings in
# the grid row's rendered text (Verify Object Row Fields From Properties checks Should Contain
# against the whole row text, not a header-column match) - mirrors Bank/Choke Model's own
# grid_verify.properties convention. Post-update state (Name updated).
Choke Code=AUTOTEST_CHOKE
Choke Name=AUTOTEST Choke UPDATED
Start Date=2000-01-01
