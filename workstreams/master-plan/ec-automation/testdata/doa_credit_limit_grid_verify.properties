# DOA Credit Limit screen (OV_DOA_CREDIT_LIMIT) - Grid-level verify test data: the table grid
# only displays Code/Name/Start Date/End Date (4 columns - confirmed live via tmp/recon_doa.robot,
# deleted before final commit: manage_object_nav_nav:form:T_data row has exactly 4 <td> cells).
# Keys here are generic display labels (Code/Name/Start Date), matching Bank/VAT Code's own
# grid_verify convention - Verify Object Row Fields From Properties does a plain "row text
# contains value" check, it does not resolve these keys against the form's own field labels.
# End Date is excluded - it stays blank until deleted.
Code=AUTOTEST_DOA
Name=AUTOTEST DOA Credit Limit UPDATED
Start Date=2003-01-01
