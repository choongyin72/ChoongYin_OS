# Input List screen (OV_STREAM_ITEM_COLLECTION) - Grid-level verify test data: the table grid
# only displays a subset of the record - Code, Name, Start Date - same convention as
# bank_grid_verify.properties/object_list_grid_verify.properties. Name here reflects the
# POST-UPDATE state (used by TC04 Find/Verify, after TC03 Update runs).
Code=AUTOTEST_INPUTLIST
Name=AUTOTEST Input List UPDATED
Start Date=2000-01-01
