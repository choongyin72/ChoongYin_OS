# Service screen (OV_SERVICE) - Update test data. Column = value, one per line, read at Update
# time. Only Service Name is listed - Service Code is read-only in updateAttributes (same
# convention as every other converted OV screen); the pre-existing proven driver only ever
# updated Service Name.
Service Name=Automation Test Service UPDATED
