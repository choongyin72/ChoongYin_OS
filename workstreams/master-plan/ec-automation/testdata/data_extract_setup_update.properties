# Data Extract Setup screen (OV_SUMMARY_SETUP) - Update test data. Column = value, one per line,
# read at Update time. Only Data Extract Setup Name may be listed here - Data Extract Setup Code
# is read-only in updateAttributes and Data Extract Type only lives in objectForm (Insert), not
# updateAttributes (confirmed via ec-ui-knowledge/screens/data_extract_setup.md: "Update
# (updateAttributes): Data Extract Setup Code (ro) - Data Extract Setup Name"), matching the
# already-proven Playwright driver py/data_extract_setup_iud.py (update Name only).
Data Extract Setup Name=AUTOTEST Data Extract Setup UPDATED
