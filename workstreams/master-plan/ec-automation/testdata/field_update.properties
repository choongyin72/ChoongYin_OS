# Field screen (OV_FIELD) - Update test data. Column = value, one per line, read at Update time.
# Only Field Name is listed - Field Code is read-only in updateAttributes (confirmed live
# 2026-08-26: same row as the Insert form) and Start Date lives only in objectdates, not
# updateAttributes (matches Area's own convention).
Field Name=Automation Test Field UPDATED
