# State Lease screen (OV_STATE_LEASE) - Grid-level verify test data: the table grid (summary
# data section) only displays Code, Name, Start Date, End Date (confirmed live 2026-08-23 - the
# SAME 4 columns as Bank/Account/Country/Customer). Kept as its OWN file, separate from
# state_lease_form_verify.properties (the full form-record field set). End Date is excluded here
# - it stays blank until deleted.
Code=AUTOTEST_STL
Name=AUTOTEST State Lease UPDATED
Start Date=2000-01-01
