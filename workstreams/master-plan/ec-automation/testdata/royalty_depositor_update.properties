# Royalty Depositor screen (OV_ROYALTY_DEPOSITOR) - Update test data. Column = value, one per
# line, read at Update time. Only fields that actually exist in updateAttributes may be listed
# here - Royalty Depositor Code is read-only there and Start Date/End Date live only in
# objectdates, not updateAttributes (same shape confirmed live on Bank/State). Field set matches
# the already-proven prior driver's own UPDATE field scope (Name only).
Royalty Depositor Name=Automation Test Royalty Depositor UPDATED
