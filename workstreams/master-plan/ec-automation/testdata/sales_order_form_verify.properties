# Sales Order screen (OV_PRODUCT_SALES_ORDER) - Verify test data: the expected CURRENT state
# of the test record after TC02 Insert + TC03 Update, used by TC04 Find/Verify to round-trip
# check the grid + form actually SHOW what was saved. Kept as its OWN file (not reused from
# sales_order_insert.properties/_update.properties) because it represents a MERGED state: Name
# comes from the update, every other field is unchanged since insert. Start Date is included
# for completeness but NOT checked by ${SALES_ORDER_FORM_LABELS} (it lives only in
# objectdates, not updateAttributes - same convention as Bank/Cost Object Mapping).
Product Sales Order Code=AUTOTEST_SO
Name=AUTOTEST Sales Order UPDATED
Start Date=2003-01-01
Company=Acme Chemicals
Field=Apollo
