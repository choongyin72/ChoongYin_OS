# Shift screen (OV_SHIFT) - Insert test data. Column = value, one per line, read at Insert time.
# Shift Code/Shift Name are SCREEN-PREFIXED labels (confirmed live via the prior driver,
# py/shift_iud.py insert_fields). Start Time (HH:MI) is the mandatory FREE-TEXT extra (value
# format '07:00' read from the existing P1 S001 row, owner technique: scan an existing row's
# populated values) - auto-detected as a plain text field by the shared "Fill OV Field By Label
# Any Kind" mechanism, no special-casing needed. Op Production Unit MUST equal the navigator's
# own PU value (P1 Production Unit) - parent-matching, from the existing row's own values, same
# as Area's own Op Production Unit entry.
Shift Code=AUTOTEST_SHIFT
Shift Name=Automation Test Shift
Start Date=2020-01-01
Start Time (HH:MI)=07:00
Op Production Unit=P1 Production Unit
