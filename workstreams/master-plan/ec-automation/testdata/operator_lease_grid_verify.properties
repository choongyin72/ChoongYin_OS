# Operator Lease screen (OV_OPERATOR_LEASE) - Grid-level verify test data: the table grid
# only displays a subset of the record - Operator Lease Code, Operator Lease Name, Start
# Date (confirmed live 2026-08-23 grid column header scan: Operator Lease Code, Operator
# Lease Name, Start Date, End Date) - same convention as bank_grid_verify.properties/
# country_grid_verify.properties. Name here reflects the POST-UPDATE state (used by TC04
# Find/Verify, after TC03 Update runs). Keys here are dict lookups only (matched against
# raw row text, not live DOM labels), but named to match the real grid columns for clarity.
Operator Lease Code=AUTOTEST_OPERATOR_LEASE
Operator Lease Name=AUTOTEST Operator Lease UPDATED
Start Date=2000-01-01
