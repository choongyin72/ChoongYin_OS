# Reservoir Block screen (OV_RESV_BLOCK) - Insert test data. Column = value, one per line, read
# at Insert time. Field labels are "Reservoir Block Code"/"Reservoir Block Name"
# (screen-prefixed) - confirmed via the already-proven Playwright driver
# py/reservoir_block_iud.py (INSERT_FIELDS) and the prior label-driven page object.
# Only Reservoir Block Code/Reservoir Block Name/Start Date are mandatory - optional dropdowns
# are deliberately NOT filled here (IUD fills only needed fields).
Reservoir Block Code=AUTOTEST_RESVB
Reservoir Block Name=AUTOTEST Reservoir Block
Start Date=2000-01-01
