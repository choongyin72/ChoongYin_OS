# Stream Item Category screen (OV_STREAM_ITEM_CATEGORY) - Verify test data: the expected
# CURRENT state of the test stream_item_category after TC02 Insert + TC03 Update, used by
# TC04 Find/Verify to round-trip check the grid + form actually SHOW what was saved - a
# live-DOM check, independent of the DB assertions already run in TC02/TC03. Kept as its OWN
# file (not reused from insert/update.properties) because it represents a MERGED state: Name
# comes from the update, Code/Start Date are unchanged since insert (matches Bank/Berth).
Code=AUTOTEST_SIC
Name=AUTOTEST Stream Item Category UPDATED
Start Date=2000-01-01
