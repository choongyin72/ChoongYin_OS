# Payment Scheme screen (OV_PAYMENT_SCHEME) - Update test data. Column = value, one per line,
# read at Update time. Only Name may be listed here - Code is read-only in updateAttributes and
# Start Date lives only in objectdates, not updateAttributes (same pattern as Bank/State).
Name=AUTOTEST Payment Scheme UPDATED
