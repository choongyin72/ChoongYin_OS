# Well Hookup (CO.0108) OV-GM navigator - genuine 3-level cascade (Production Unit -> Area ->
# Facility Class 1), same row increasing column: nav:form:G:0:R:1:C:1:dd (PU), C:2:dd (Area),
# C:3:dd (Facility Class 1). Fits the shared T2 "Apply Navigator From Properties"
# (resources/manage_object.resource) supported shape - confirmed live 2026-08-26
# (tmp/recon_well_hookup_navigator_cascade.py against this repo's local sandbox EC): C:1/C:2/C:3
# all present, C:4 absent, matching this screen's own registry entry ("3-level PU->Area->Facility
# Class 1 cascade").
#
# VALUES: the SAME ones the existing driver's "Apply OV-GM Navigator First Available"
# (py/ec_object_iud.py apply_ovgm_navigator / resources/manage_object.resource's sibling keyword)
# already selects successfully - first-available picks the alphabetically-first option in each
# panel, which live recon confirmed is "AS1 EC Exploration Norway" at C:1, "AS1_Area" at C:2 (only
# option once C:1 is that PU - same PU/Area pair Facility Class 1's own navigator resolves to,
# consistent with the shared hierarchy), and "AS1_Facility_01" at C:3 (only option once C:2 is
# AS1_Area). Captured here as EXPLICIT values, not re-invented - verify_screen PASS + RF 4/4 +
# Playwright 8/8 (2026-07-30) already proved this exact scope loads a non-empty, working screen.
Op Production Unit=AS1 EC Exploration Norway
Op Area=AS1_Area
Op Facility Class 1=AS1_Facility_01
