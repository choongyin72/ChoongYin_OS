# Sub Area OV-GM navigator - genuine 2-level cascade (Production Unit -> Area), same row
# increasing column: nav:form:G:0:R:1:C:1:dd (PU) then nav:form:G:0:R:1:C:2:dd (Area). Fits the
# shared T2 "Apply Navigator From Properties" (resources/manage_object.resource) supported shape -
# confirmed live 2026-08-26 (existing sub_area_page.resource/ec_iud_sub_area.py already used this
# exact 2-level id shape; re-confirmed here, not re-derived), matching this screen's registry entry
# ("cascading PU->Area + GO", docs/ec_screen_registry.md).
#
# VALUES: user-approved 2026-06-11 (per sub_area_sow.md/sub_area_iud.robot's pre-existing
# ${NAV_PU}/${NAV_AREA}) - "Production Unit" then "Offshore area" - carried forward unchanged.
Op Production Unit=Production Unit
Op Area=Offshore area
