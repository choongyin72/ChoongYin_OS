# Product Price Object screen (CD.0011) - Grid-level verify test data: the table grid only
# displays a subset of the record - Code/Name/Start Date, same convention as
# cost_centre_grid_verify.properties. Name here reflects the POST-UPDATE state (used by TC04
# Find/Verify, after TC03 Update runs). Keys here are dict lookups only (matched against raw
# row text, not live DOM labels) - if this screen's grid doesn't actually show these columns,
# Verify Object Found's grid-compare will fail loud and this file gets corrected against the
# live grid header, not guessed further.
Price Object Code=AUTOTEST_PPO
Price Object Name=AUTOTEST Product Price Object UPDATED
Start Date=2000-01-01
