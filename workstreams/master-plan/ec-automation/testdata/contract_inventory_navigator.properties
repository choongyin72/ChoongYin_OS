# Contract Inventory (CO.2054) OV-GM navigator - live recon 2026-08-26
# (tmp/recon_contract_inventory_mandatory_2026_08_26.py,
# tmp/recon_contract_inventory_cascade_2026_08_26.py): single group G:0, single row R:1,
# increasing column - nav:form:G:0:R:1:C:1:dd (Business Unit) then C:2:dd (Contract Area),
# C:3:dd (Contract) also present. ONLY C:1 (Business Unit) is genuinely mandatory-yellow+empty;
# C:2/C:3 stay white/optional even after C:1 is filled (confirmed: filling C:1 does NOT turn C:2
# yellow). Fits the shared T2 "Apply Navigator From Properties" (resources/manage_object.resource)
# same-row/increasing-column shape with ZERO shared-file changes.
#
# VALUES: same ones the existing driver/page-object already select successfully (live 4/4,
# verify_screen PASS 2026-08-02, registry: "Business Unit -> Contract Area cascade + GO") -
# captured here as EXPLICIT values, not re-invented. Contract Area is kept even though it is not
# strictly mandatory, to match the already-proven scope exactly.
Business Unit=TS5 BU
Contract Area=TS5 Contract Area
