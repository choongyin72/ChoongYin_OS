# Operator Route (CO.0244) OV-GM navigator - genuine 2-level cascade (Production Unit -> Area),
# same row increasing column: nav:form:G:0:R:1:C:1:dd (PU) then nav:form:G:0:R:1:C:2:dd (Area).
# Fits the shared T2 "Apply Navigator From Properties" (resources/manage_object.resource)
# supported shape (same shape already proven on Facility Class 1).
#
# VALUES: the same PROVEN explicit values already used by this screen's own pre-existing driver
# (py/operator_route_iud.py: ec.select_dropdown(... 'P3 Production Unit') /
# ec.select_dropdown(... 'P3 Area')) and pageobjects/.../operator_route_page.resource
# ("Open Operator Route Screen" keyword, pre-conversion) - captured here as EXPLICIT values, not
# re-derived. This screen's registry note explicitly warns first-available is NOT guaranteed to
# have data underneath it (scripts/find_populated_scope.py was used to find these), so these
# values are kept exactly as proven, never swapped for a first-available pick.
Op Production Unit=P3 Production Unit
Op Area=P3 Area
