# Report Area screen (OV_REPORT_AREA) - Grid-level verify test data: the table grid only
# displays a subset of the record - Report Area Code, Report Area Name, Start Date - same
# convention as bank_grid_verify.properties/berth_grid_verify.properties. Name here reflects
# the POST-UPDATE state (used by TC04 Find/Verify, after TC03 Update runs). Keys here are
# dict lookups only (matched against raw row text, not live DOM labels), but named to match
# the real grid columns for clarity.
Report Area Code=AUTOTEST_RPTA
Report Area Name=AUTOTEST Report Area UPDATED
Start Date=2000-01-01
