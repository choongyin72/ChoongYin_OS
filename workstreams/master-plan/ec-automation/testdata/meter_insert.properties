# Meter screen (OV_METER) - Insert test data. Column = value, one per line, read at Insert time
# and filled IN THIS FILE ORDER by the shared T2 "Insert Object From Properties"
# (resources/manage_object.resource) via "Fill OV Field By Label Any Kind" (auto-detects each
# field's kind - text/date/dropdown/popup - from its live DOM, so the popup here needs no
# special-casing in the T3 page object, unlike Chemical Stream's screen-local From Connection
# popup).
#
# ORDER IS DELIBERATE AND LOAD-BEARING (preserved unchanged from the pre-conversion
# meter_page.resource's own "Insert Meter Record" documentation, live-proven 2026-06-13): the
# Delivery Point popup's close callback resets the form's dirty/save state, so any Save
# immediately after it would be a silent no-op. Filling Start Date first, then the popup, THEN
# Code/Name/Type re-arms Save - this file's order (Start Date, Delivery Point Name, Meter Code,
# Meter Name, Meter Type) reproduces that exact sequence via the generic file-order fill.
#
# Field labels confirmed live (2026-08-26, tests/_recon_meter.robot/_recon_meter3.robot, deleted
# before commit): objectForm ECCell labels are "Meter Code"/"Meter Name"/"Start Date"/
# "Meter Type"/"Delivery Point Name" (screen-prefixed Code/Name, NOT the generic Bank/Object List
# labels) - all 5 confirmed MandatoryCellStyle (yellow) on the pristine insert form, matching
# exactly the pre-existing driver's documented mandatory set (no new/extrapolated fields added).
# "300005 PG Hoogerheide" is the SAME Delivery Point value the pre-existing meter_iud.robot
# (${DELIVERY_POINT}) already proved live and DB-verified (2026-06-13) under the ECP Norway BU
# navigator scope - reused, not reinvented; re-confirmed selectable live via a self-cleaning
# probe insert/delete (RECON_METER_PROBE, 2026-08-26).
Start Date=2020-01-01
Delivery Point Name=300005 PG Hoogerheide
Meter Code=AUTOTEST_METER
Meter Name=Automation Test Meter
Meter Type=Entry
