# Deferment Group screen (OV_DEFERMENT_GROUP) - Insert test data. Column = value, one per
# line, read at Insert time. Field labels are "Deferment Group Code"/"Deferment Group Name"
# (screen-prefixed) - confirmed via the existing page object comment (recon) and the
# already-proven Playwright driver py/deferment_group_iud.py (INSERT_FIELDS).
# Only Deferment Group Code/Deferment Group Name/Start Date are mandatory - all other
# dropdowns are optional and deliberately NOT filled here (IUD fills only needed fields).
Deferment Group Code=AUTOTEST_DEFERMENT_GROUP
Deferment Group Name=AUTOTEST Deferment Group
Start Date=2000-01-01
