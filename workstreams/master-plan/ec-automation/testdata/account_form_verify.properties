# Account screen (OV_FIN_ACCOUNT) - Verify test data: the expected CURRENT state of the test
# account after TC02 Insert + TC03 Update, used by TC04 Find/Verify to round-trip check the
# updateAttributes form actually SHOWS what was saved. Name reflects the POST-UPDATE value; Cost
# Object Type/GL Account are unchanged from Insert (TC03 never touches them). Start Date is
# Insert-only, not present in updateAttributes (same as Bank/State), so it is not listed here.
Code=AUTOTEST_ACCOUNT
Name=AUTOTEST Account UPDATED
Cost Object Type=None
GL Account=999999
