# HCB System screen (OV_BALANCE) - Update test data. Column = value, one per line, read at
# Update time. Only fields that actually exist in updateAttributes may be listed here - Code is
# read-only there and Start Date/End Date live only in objectdates, not updateAttributes (same
# shape as Bank). Field set matches the already-proven prior driver's own UPDATE field scope
# (Name only).
Name=Automation Test HCB System UPDATED
