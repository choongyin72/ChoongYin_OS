# DOA Credit Limit screen (OV_DOA_CREDIT_LIMIT) - Verify test data: the expected CURRENT state
# of the test doa credit limit after TC02 Insert + TC03 Update, used by TC04 Find/Verify to
# round-trip check the grid + form actually SHOW what was saved. Kept as its OWN file (not reused
# from doa_credit_limit_insert.properties/doa_credit_limit_update.properties) because it
# represents a MERGED state: Name comes from the update, every other field is unchanged since
# insert. Start Date is deliberately excluded - confirmed live (tmp/recon_doa2.robot) that it is
# objectForm/Insert-only, not present in updateAttributes, matching Bank/VAT Code's convention.
DOA Credit Limit Code=AUTOTEST_DOA
DOA Credit Limit Name=AUTOTEST DOA Credit Limit UPDATED
DOA Type=Amount Based
Credit Limit=5000
Currency=USD
Role Name=ANALYTICS.REPORTADMIN
