# Data Extract Set screen (OV_SUMMARY_SET) - Update test data. Column = value, one per line,
# read at Update time. Only Name may be listed here - Code is read-only in updateAttributes and
# Owner Class only lives in objectForm (Insert), not updateAttributes (confirmed via
# ec-ui-knowledge/screens/data_extract_set.md: "Update (updateAttributes): Code (ro) - Name"),
# matching the already-proven Playwright driver py/data_extract_set_iud.py (UPDATE_FIELDS: Name only).
Name=AUTOTEST Data Extract Set UPDATED
