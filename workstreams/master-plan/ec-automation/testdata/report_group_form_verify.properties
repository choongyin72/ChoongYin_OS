# Report Group screen (OV_REPORT_GROUP) - Verify test data: the expected CURRENT state of the
# test report group after TC02 Insert + TC03 Update, used by TC04 Find/Verify to round-trip
# check the grid + form actually SHOW what was saved - a live-DOM check, independent of (in
# addition to) the DB assertions run separately. Represents a MERGED state: Reporting Group Name
# comes from the update, Reporting Group Code/Start Date/Description are unchanged since insert
# (mirrors bank_form_verify.properties' own merged-state convention). Business Area is excluded -
# it is a dropdown resolved to __FIRST__ at Insert time and can re-render different display text
# after reload, so it is excluded from this round-trip compare (same documented exclusion rule
# as every other __FIRST__-filled dropdown on this project).
Reporting Group Code=AUTOTEST_REPORT_GROUP
Reporting Group Name=AUTOTEST Report Group UPDATED
Start Date=2000-01-01
Description=AUTOTEST Report Group description
