# Canal screen (OV_CANAL) - Verify test data: the expected CURRENT state of the test canal after
# TC02 Insert + TC03 Update, used by TC04 Find/Verify to round-trip check the grid + form actually
# SHOW what was saved - a live-DOM check, independent of (in addition to) the DB assertions already
# run in TC02/TC03. Keys are the ACTUAL on-screen field labels (see canal_insert.properties). Kept
# as its OWN file (not reused from canal_insert.properties or canal_update.properties) because it
# represents a MERGED state: Canal Name comes from the update, Canal Code and Start Date are
# unchanged since insert (same pattern as bank_form_verify.properties).
Canal Code=CANAL_KIEL
Canal Name=Kiel Canal UPDATED
Start Date=2000-01-01
