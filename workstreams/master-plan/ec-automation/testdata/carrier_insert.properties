# Carrier screen (OV_CARRIER) - Insert test data. Column = value, one per line, read at Insert
# time. Field labels are "Carrier Code"/"Carrier Name" (screen-prefixed) - confirmed via the
# prior SOW recon (carrier_sow.md section 2) and the proven Playwright driver
# (ec_iud_carrier.py), NOT the generic "Code"/"Name" Bank/Object List use. Carrier Code/
# Carrier Name/Start Date/Unit are mandatory - Carrier Group/Carrier Type/End Date and the
# rest (Capacity Volume/Mass, Dead Weight, Nationality, ...) are optional and deliberately
# NOT filled here (IUD fills only needed fields). Unit is a mandatory reference dropdown -
# filled __FIRST__ (throwaway record; value irrelevant, deleted again in TC05) - deliberately
# EXCLUDED from @{CARRIER_FORM_LABELS} in carrier_page.resource so the known
# __FIRST__-fails-round-trip-verify gotcha never applies (Batch 2 VAT Code precedent).
Carrier Code=AUTOTEST_CARRIER
Carrier Name=AUTOTEST Carrier
Start Date=2003-01-01
Unit=__FIRST__
