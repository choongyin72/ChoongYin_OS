# Chemical Stream screen (OV_CHEM_STREAM) - Update test data. Column = value, one per line, read
# at Update time. Only Chemical Stream Name is listed - Chemical Stream Code is read-only in
# updateAttributes, and Start Date/Chemical Stream Type/From Connection are not re-editable via
# this round-trip (matches Area's/Facility Class 1's own update-file convention).
Chemical Stream Name=Automation Test Chemical Stream UPDATED
