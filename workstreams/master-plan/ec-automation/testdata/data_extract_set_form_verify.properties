# Data Extract Set screen (OV_SUMMARY_SET) - Verify test data: the expected CURRENT state of
# the test data_extract_set after TC02 Insert + TC03 Update, used by TC04 Find/Verify to
# round-trip check the updateAttributes form actually SHOWS what was saved. Only Code/Name are
# listed - Owner Class is an Insert-only field (objectForm), not present in updateAttributes
# (same as Bank/Berth's Start Date exclusion pattern).
Code=AUTOTEST_DXT
Name=AUTOTEST Data Extract Set UPDATED
