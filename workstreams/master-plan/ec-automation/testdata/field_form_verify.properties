# Field screen (OV_FIELD) - Verify test data (TC04 Find/Verify round-trip check): the expected
# CURRENT state of the test field after TC02 Insert + TC03 Update. Kept as its OWN file (not
# reused from field_insert.properties/field_update.properties) because it represents a MERGED
# state: Field Name comes from the update, Field Code is unchanged since insert. Geo Area is
# deliberately excluded from the round-trip form-label list (see @{FIELD_FORM_LABELS} in
# field_page.resource) - it is filled via a resolved reference value which can re-render
# different display text after reload, same caveat as every other converted OV-GM screen's
# dropdown fields (Area's Op Production Unit, etc.).
Field Code=AUTOTEST_FIELD
Field Name=Automation Test Field UPDATED
