# Transport Zone OV-GM navigator - single-dropdown cascade (nav:form:G:0, C:1 only).
# Driven by resources/manage_object.resource's "Apply Navigator From Properties". Live recon
# (2026-08-26): G:0 has 3 columns - C:0 Date (mandatory:true but ALREADY defaulted/filled on
# load, no fill needed), C:1 Business Unit dropdown (mandatory:true, genuinely empty -
# MandatoryCellStyle - this is the ONLY field that needs a fill), C:2 a second dropdown
# (mandatory:false, optional filter - confirmed GO succeeds with C:2 left empty once C:1 is
# set). Value "TS5 BU" matches the insert form's "Transport System Name" = "TS5 Transport
# System" (recon-confirmed pairing, existing driver's proven value).
Business Unit=TS5 BU
