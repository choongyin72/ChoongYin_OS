# Functional Area screen (OV_FUNCTIONAL_AREA) - Grid-level verify test data: the table grid
# only displays a subset of the record - Code, Name, Start Date - same convention as
# state/region_grid_verify.properties. Keys here are dict lookups only (matched against raw
# row text, not live DOM labels - grid columns/case don't need to match objectForm exactly).
Area Code=AUTOTEST_FA
Area Name=AUTOTEST Functional Area UPDATED
Start Date=2000-01-01
