# Price Rate screen (OV_PRICE_RATE) - Insert test data. Column = value, one per line, read at
# Insert time. Price Rate Code/Price Rate Name are SCREEN-PREFIXED labels (confirmed live via
# the prior OLD-pattern price_rate_page.resource "Insert Price Rate Record" keyword and
# docs/ec_screen_registry.md line 332: mandatory Price Rate Code/Price Rate Name/Start Date +
# Frequency). Business Unit MUST equal the navigator's own value ("SS2 BU") or the inserted row
# is not visible under this OV-GM navigator scope. Frequency has no fixed proven value in this
# sandbox, so __FIRST__ (picks the first available dropdown option, same convention as every
# other converted OV-GM screen's mandatory-but-unconstrained dropdown) is used, matching the
# prior driver's own insert_fields entry for Frequency.
Price Rate Code=AUTOTEST_PRICE_RATE
Price Rate Name=Automation Test Price Rate
Start Date=2000-01-01
Frequency=__FIRST__
Business Unit=SS2 BU
