# Storage screen (OV_STORAGE) - Insert test data. Column = value, one per line, read at Insert
# time. Storage Code/Storage Name are SCREEN-PREFIXED labels (confirmed live 2026-08-26 objectForm
# label scan). Storage Type/Product Name are Storage's own genuine MANDATORY dropdowns (confirmed
# live objectForm label scan + the already-proven driver py/storage_iud.py, verify_screen PASS
# 2026-07-30, which fills them) - kept as __FIRST__ (a supported literal in the shared T2
# properties-fill mechanism, resources/manage_object.resource "Fill OV Field/Dropdown By Label")
# exactly as the proven driver already does, not re-derived to a specific option. Op Production
# Unit/Op Area/Op Facility Class 1 also exist on this objectForm (confirmed live) but the proven
# driver inserts successfully leaving them blank - trusting that proven behavior (owner standing
# rule: don't hunt for unstated requirements), NOT adding them here.
Storage Code=AUTOTEST_STG
Storage Name=Automation Test Storage
Start Date=2000-01-01
Storage Type=__FIRST__
Product Name=__FIRST__
