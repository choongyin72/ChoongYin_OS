# DOA Credit Limit screen (OV_DOA_CREDIT_LIMIT) - Insert test data. Column = value, one per
# line, read at Insert time. Field labels confirmed live 2026-08-23 via the New Object form's
# ECCell label dump (tmp/recon_doa.robot - deleted before final commit). This screen's own Code/
# Name fields are SCREEN-PREFIXED ("DOA Credit Limit Code"/"DOA Credit Limit Name"), not the
# generic "Code"/"Name" - confirmed live, not assumed from a sibling screen. Start Date uses
# 2003-01-01 (EC_TEST_START_DATE_REFDD convention, environment.py, matching VAT Code's own
# precedent) - this screen has TWO mandatory reference dropdowns (DOA Type/Role Name), and an
# earlier date can leave a version-filtered dropdown empty (registry: "EC Object Start Date =
# Version Filter"). Mandatory extras confirmed via the same live outerHTML dump ({mandatory:true}
# MandatoryCellStyle on each): "DOA Type" (reference dropdown, literal "Amount Based" - confirmed
# live to be the first of 2 real options: Amount Based/Quantity Based), "Credit Limit" (numeric
# text, ECNumberCell), and "Role Name" (reference dropdown, literal "ANALYTICS.REPORTADMIN" -
# confirmed live to be the first of 33 real catalogued options). Literal values are used for both
# dropdowns (NOT __FIRST__) deliberately - TC02's Verify DOA Credit Limit Record Exists compares
# the LIVE SCREEN back against this exact file (T2's Verify Object Insert Exists), so a __FIRST__
# placeholder would never match the resolved option text (VAT Code/Account precedent).
# Currency IS included despite its static CSS class being {mandatory:false} (UOM stays out) -
# a LIVE Save attempt without it failed with a real EC business-rule validation banner:
# "Amount Based DOA Requires a currency" (confirmed 2026-08-23 via tmp/recon_doa3_save.robot,
# deleted before final commit - a genuine conditional-mandatory rule tied to DOA Type=Amount
# Based, not visible via the static MandatoryCellStyle scan alone). Currency=USD confirmed live
# to be a real catalogued option (not the dropdown's first option, which is ARS - USD chosen as
# the business-realistic value matching this screen's own pre-existing production rows, all of
# which use USD).
DOA Credit Limit Code=AUTOTEST_DOA
DOA Credit Limit Name=AUTOTEST DOA Credit Limit
Start Date=2003-01-01
DOA Type=Amount Based
Credit Limit=5000
Currency=USD
Role Name=ANALYTICS.REPORTADMIN
