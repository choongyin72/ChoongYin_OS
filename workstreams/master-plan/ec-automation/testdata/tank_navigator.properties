# Tank (Configuration > Assets > Tank and Storage Objects) OV-GM navigator - 3-level dropdown
# cascade at C:1/C:2/C:3 (same row/increasing-column shape as Well - see well_page.resource
# "Apply Well Navigator"), preceded by an untouched Date column at C:0 with a working default
# (confirmed live 2026-08-26: default = current date, GO succeeds with the cascade alone -
# leaving Date untouched is NOT a structural blocker, "Apply Navigator From Properties" only
# ever fills C:1..C:N so this is a non-issue).
# Values confirmed live/selectable 2026-08-26 (same P1 taxonomy Well already uses):
Op Production Unit=P1 Production Unit
Op Area=P1 Area
Op Facility Class 1=P1 Facility 1
