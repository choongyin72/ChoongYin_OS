# Canal screen (OV_CANAL) - Insert test data. Column = value, one per line, read at Insert time.
# Keys are the ACTUAL on-screen field labels (live-confirmed: Canal's fields are labelled "Canal
# Code"/"Canal Name", screen-prefixed - not the generic "Code"/"Name" Bank/Object List use, same
# shape as State's own "State Code"). CANAL_KIEL confirmed free in OV_CANAL live (2026-08-23,
# fresh oracledb connection) - only real rows are SUEZ/PANAMA. Field set matches the already-proven
# Playwright driver's own INSERT_FIELDS (py/canal_iud.py: Canal Code, Canal Name, Start Date) - not
# an independent guess. Time Zone dropdown deliberately excluded (documented optional,
# docs/ec_screen_registry.md + canal_sow.md).
Canal Code=CANAL_KIEL
Canal Name=Kiel Canal
Start Date=2000-01-01
