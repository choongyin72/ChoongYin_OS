# Sales Order screen (OV_PRODUCT_SALES_ORDER) - Update test data. Column = value, one per
# line, read at Update time. Only fields that actually exist in updateAttributes may be listed
# here - Product Sales Order Code is read-only there, and Start Date/End Date live only in
# objectdates (confirmed live 2026-08-23, same convention as Bank/Cost Object Mapping). Name
# is the only field changed here, mirroring Bank's own single-field Update pair (this screen
# has no optional Description field paired with the update, unlike Cost Object Mapping).
Name=AUTOTEST Sales Order UPDATED
