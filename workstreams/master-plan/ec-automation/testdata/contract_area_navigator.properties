# Contract Area (Configuration > Assets > Contract Objects > Contract Area) OV-GM navigator -
# single-dropdown cascade (C:1 only), same shape as Area's own navigator.
# Driven by resources/manage_object.resource's "Apply Navigator From Properties" (added
# 2026-08-26). Value confirmed live (screenshot evidence,
# screens/Configuration/Assets/Contract_Objects/Contract_Area/evidence/ca_01_loaded.png, and the
# pre-existing suite's proven ${NAV_BU}/${PARENT_VALUE} = ECP Norway): the "Business Unit"
# navigator dropdown gates the grid; ECP Norway is a real, already-used Business Unit in this
# sandbox.
Business Unit=ECP Norway
