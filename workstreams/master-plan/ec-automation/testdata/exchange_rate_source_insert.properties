# Exchange Rate Source screen (OV_FOREX_SOURCE) - Insert test data. Column = value, one per
# line, read at Insert time. Field labels are the GENERIC "Code"/"Name" (NOT screen-prefixed
# like State's "State Code"/"State Name") - confirmed live 2026-08-22 via the New Object form
# outerHTML dump; only Code/Name/Start Date carry MandatoryCellStyle (Description/Comments/URL
# are optional, deliberately omitted). Code field has no maxLength restriction (confirmed live,
# maxLength=-1) so the standard AUTOTEST_EXCHANGE_RATE_SOURCE code is used as-is.
Code=AUTOTEST_EXCHANGE_RATE_SOURCE
Name=AUTOTEST Exchange Rate Source
Start Date=2000-01-01
