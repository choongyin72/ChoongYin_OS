# Region screen (OV_REGION) - Update test data. Column = value, one per line, read at Update
# time. Only Region Name may be listed here - Region Code is read-only in updateAttributes and
# Start Date lives only in objectdates, not updateAttributes (same pattern as State/Bank).
Region Name=AUTOTEST Region UPDATED
