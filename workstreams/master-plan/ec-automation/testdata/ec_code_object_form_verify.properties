# EC Code Object screen (OV_EC_CODE_OBJECT) - Verify test data: the expected CURRENT state of the
# test EC Code Object after TC02 Insert + TC03 Update, used by TC04 Find/Verify to round-trip check
# the grid + form actually SHOW what was saved - a live-DOM check, independent of (in addition to)
# the DB assertions run separately. Represents a MERGED state: Name comes from the update, Object
# Code/Start Date are unchanged since insert (mirrors bank_form_verify.properties' own merged-state
# convention). EC Code Type/EC Code are dropdowns resolved to __FIRST__ at Insert time and can
# re-render different display text after reload, so they are excluded from this round-trip compare
# (same documented exclusion rule as every other __FIRST__-filled dropdown on this project).
Object Code=AUTOTEST_EC_CODE_OBJECT
Name=AUTOTEST EC Code Object UPDATED
Start Date=2000-01-01
