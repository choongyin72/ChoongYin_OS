# Storage Flow screen (OV_STORAGE_FLOW) - Verify test data: the expected CURRENT state of
# the test storage_flow after TC02 Insert + TC03 Update, used by TC04 Find/Verify to
# round-trip check the updateAttributes form actually SHOWS what was saved. Only Storage
# Flow Code/Storage Flow Name are listed - Start Date is an Insert-only field, and Flow
# Direction/Storage are dropdowns filled with __FIRST__ (their live-rendered option label
# is not knowable ahead of time), same convention as Bank/Berth (they are excluded, not
# round-trip checked here).
Storage Flow Code=AUTOTEST_STFLOW
Storage Flow Name=AUTOTEST Storage Flow UPDATED
