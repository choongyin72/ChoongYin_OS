# Stream - All screen (CD.0007, class STREAM, view OV_STREAM) - Insert test data. Column =
# value, one per line, read at Insert time. Field labels are "Stream Code"/"Stream Name"
# (screen-prefixed, same convention as Berth/Royalty Objects) - confirmed via a fresh live scan
# 2026-08-24 (no prior automation/notes existed for this specific screen).
# Mandatory set confirmed live (yellow MandatoryCellStyle scan of objectForm): Stream Code,
# Stream Name, Start Date, Stream Type (dropdown), Alloc Period (dropdown) - THIS SCREEN HAS
# TWO EXTRA MANDATORY DROPDOWNS beyond the Bank/Berth 3-field baseline (per the skill's own
# caveat: "some have mandatory reference dropdowns... recon each form before building").
# Stream Type options (5, live-confirmed): Measured/Reference/Calculated/Quality/Derived.
# Alloc Period options (10, live-confirmed): Daily/Monthly/Yearly/... - literal option text used
# (not __FIRST__), since TC02 round-trips this same file against the live form (VAT Code gotcha:
# __FIRST__ fails that round-trip compare).
Stream Code=AUTOTEST_STREAM
Stream Name=AUTOTEST Stream
Start Date=2000-01-01
Stream Type=Measured
Alloc Period=Daily
