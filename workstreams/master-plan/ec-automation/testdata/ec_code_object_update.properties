# EC Code Object screen (OV_EC_CODE_OBJECT) - Update test data. Column = value, one per line, read
# at Update time. Only fields that actually exist in the updateAttributes form may be listed here -
# Object Code is read-only there and Start Date/End Date live only in objectdates, not
# updateAttributes (per ec_code_object_page.resource's own Update EC Code Object Name keyword,
# which only ever fills Name). Field set matches the already-proven Python driver's own
# UPDATE_FIELDS (py/ec_code_object_iud.py: Name only) - not an independent guess.
Name=AUTOTEST EC Code Object UPDATED
