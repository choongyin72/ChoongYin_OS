# Document Date Term screen (OV_DOC_DATE_TERM) - Verify test data: the expected CURRENT state of
# the test Document Date Term after TC02 Insert + TC03 Update, used by TC04 Find/Verify to
# round-trip check the form actually SHOWS what was saved - a live-DOM check, independent of (in
# addition to) the DB assertions run separately. Represents a MERGED state: Name comes from the
# update, Code is unchanged since insert (mirrors bank_form_verify.properties' own merged-state
# convention). Date Calculation Method/Offset are deliberately NOT listed here - they are not
# present as separate updateAttributes fields (only in objectForm at insert time), matching Bank's
# own convention of only verifying fields that actually live in updateAttributes.
Code=AUTOTEST_DOCUMENT_DATE_TERM
Name=AUTOTEST Document Date Term UPDATED
