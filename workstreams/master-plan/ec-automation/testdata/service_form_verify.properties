# Service screen (OV_SERVICE) - Verify test data (TC04 Find/Verify round-trip check): the
# expected CURRENT state of the test service after TC02 Insert + TC03 Update. Kept as its OWN
# file (not reused from service_insert.properties/service_update.properties) because it
# represents a MERGED state: Service Name comes from the update, Service Code is unchanged
# since insert.
Service Code=AUTOTEST_SERVICE
Service Name=Automation Test Service UPDATED
