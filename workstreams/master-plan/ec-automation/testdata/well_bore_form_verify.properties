# Well Bore screen (OV_WELL_BORE) - Verify test data (TC04 Find/Verify round-trip check): the
# expected CURRENT state of the test object after TC02 Insert + TC03 Update. Kept as its OWN
# file (not reused from well_bore_insert.properties/well_bore_update.properties) because it
# represents a MERGED state: Well Bore Name comes from the update, Well Bore Code is unchanged
# since insert. Start Date/the Well popup are deliberately excluded (same re-render/read-only
# caveat as every other converted screen's date/popup fields - only the two plain-text labels
# are round-tripped).
Well Bore Code=AUTOTEST_WB
Well Bore Name=Automation Test Well Bore UPDATED
