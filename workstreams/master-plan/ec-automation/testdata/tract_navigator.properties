# Tract (Configuration > Assets > Royalty Objects > Tract) OV-GM navigator.
# Tract's navigator has TWO separate groups (nav:form:G:0 = Date, nav:form:G:1 = Unit
# Agreement) - NOT the same-row/increasing-column single-group cascade shape most other
# Area-pattern screens use. Live DOM recon 2026-08-26 confirmed: G:0's Date field already
# carries a non-empty default on load (today's date) and needs NO fill; G:1's Unit Agreement
# dropdown is the ONLY mandatory-and-empty nav field, and it is addressed at column C:0 (not
# C:1, unlike Area/Well/etc.) - confirmed live via a DOM scan of nav:form:G:1:R:1:C:0:dd
# (class included "{mandatory:true} MandatoryCellStyle").
# Filled via "Apply Navigator From Properties    testdata/tract_navigator.properties
# group=1    start_col=0" (resources/manage_object.resource) - this targets ONLY G:1's C:0
# and leaves G:0 untouched, per the shared keyword's ${group}/${start_col} optional args
# added this session specifically to cover Tract's shape.
# CRITICAL (owner rule): this value MUST be the exact same value used in
# testdata/tract_insert.properties' "Unit Agreement" field - the inserted/updated Tract's
# Unit Agreement must equal the navigator's own scope or the row is invisible under this
# OV-GM filter.
Unit Agreement=Unit Agreement 1
