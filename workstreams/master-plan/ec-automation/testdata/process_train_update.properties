# Process Train screen (OV_PROCESS_TRAIN) - Update test data. Column = value, one per line,
# read at Update time. Only Process Train Name may be listed here - Process Train Code is
# read-only in updateAttributes and Start Date lives only in objectdates, not
# updateAttributes (same pattern as Bank/Berth), matching the already-proven Playwright
# driver py/process_train_iud.py (UPDATE_FIELDS: Process Train Name only).
Process Train Name=AUTOTEST Process Train UPDATED
