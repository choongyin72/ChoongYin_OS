# Production Separator screen (OV_PRODSEPARATOR) - Insert test data. Column = value, one per
# line, read at Insert time. Production Separator Code/Production Separator Name are SCREEN-
# PREFIXED labels (confirmed live 2026-08-26 objectForm label scan). Op Production Unit is
# MANDATORY on this form (the pre-existing driver, py/production_separator_iud.py, always fills
# it) - but it is a date-effective reference dropdown FILTERED BY THE FORM'S OWN START DATE
# (reference_ec_object_start_date_version.md: "ref dropdowns filter by form Start Date"). Live
# recon 2026-08-26 (before Start Date is filled) showed "AS1 EC Exploration Norway" as the
# alphabetically-first option; requesting that EXACT value here after Start Date=2000-01-01 is
# filled reproducibly timed out live (tmp/wt-prodsep-live/output.xml, TC02 x2) - the
# version-filtered list post-Start-Date differs from the pre-filter list. Using the shared T2's
# "__FIRST__" literal instead (Fill OV Dropdown By Label / Select First EC Dropdown Option),
# EXACTLY matching the pre-existing proven driver's own mechanism (insert_fields: {"label": "Op
# Production Unit", "value": "__FIRST__"}) - genuinely first-available-after-filter, not a fixed
# string. Op Area/Op Facility Class 1 also exist on this objectForm (confirmed live 2026-08-26)
# but the proven driver leaves them blank and still inserts successfully under this scope -
# trusting that proven behavior (owner standing rule: don't hunt for unstated requirements), NOT
# adding them here.
Production Separator Code=AUTOTEST_PSEP
Production Separator Name=Automation Test Production Separator
Start Date=2000-01-01
Op Production Unit=__FIRST__
