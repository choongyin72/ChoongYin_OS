# UOP Key screen (OV_FIN_UOP_DEPR_KEY) - Verify test data: the expected CURRENT state of the
# test uop_key after TC02 Insert + TC03 Update, used by TC04 Find/Verify to round-trip check the
# form actually SHOWS what was saved (live-DOM check, same convention as Bank's own
# bank_form_verify.properties). MERGED state: Name comes from the update, every other field is
# unchanged since insert. Company/Key Type deliberately excluded (see @{UOP_KEY_FORM_LABELS} in
# uop_key_page.resource) - both were filled via __FIRST__ and can re-render different display
# text after reload, same reason Bank excludes Country from its DB-compare (screen-compare only,
# not listed in the round-trip label set at all here since the resolved reference value is not
# stable/predictable across environments).
Code=UOP_AUTOTEST
Name=Autotest UOP Key UPDATED
Description=Autotest UOP depreciation key
Comment=Autotest comment
Key Number=UOP_AUTOTEST
