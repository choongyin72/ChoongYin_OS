# Cost Object Mapping screen (OV_FIN_COST_OBJECT) - Verify test data: the expected CURRENT
# state of the test record after TC02 Insert + TC03 Update, used by TC04 Find/Verify to
# round-trip check the grid + form actually SHOW what was saved. Kept as its OWN file (not
# reused from cost_object_mapping_insert.properties/_update.properties) because it represents a
# MERGED state: Name/Description come from the update, every other field is unchanged since
# insert. Start Date is included for completeness but NOT checked by
# ${COST_OBJECT_MAPPING_FORM_LABELS} (it lives only in objectdates, not updateAttributes - same
# convention as Bank/Customer).
Code=AUTOTEST_CMAP
Name=AUTOTEST Cost Object Mapping UPDATED
Start Date=2003-01-01
Description=AUTOTEST Cost Object Mapping Description UPDATED
Object Type=Cost Center
Cost Object=AA
Company=Acme Chemicals
Distribution Object Type=Country
