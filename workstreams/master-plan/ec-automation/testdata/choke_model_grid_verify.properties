# Choke Model screen (OV_CHOKE_MODEL) - Grid-level verify test data: values must appear as
# substrings in the grid row's rendered text (Verify Object Row Fields From Properties checks
# Should Contain against the whole row text, not a header-column match) - mirrors Bank/Payment
# Term's own grid_verify.properties convention. Post-update state (Name updated).
Choke Model Code=AUTOTEST_CHOKE_MODEL
Choke Model Name=AUTOTEST Choke Model UPDATED
Start Date=2000-01-01
