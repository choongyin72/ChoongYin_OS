# Meter screen (OV_METER) - Update test data. Column = value, one per line, read at Update time.
# Only Meter Name is listed - Meter Code is read-only in updateAttributes (confirmed live
# 2026-08-26, self-cleaning probe RECON_METER_PROBE), and Start Date is not present in
# updateAttributes (only in objectForm/objectdates), matching Area's/Chemical Stream's own
# update-file convention. Meter Type/Delivery Point Name ARE editable in updateAttributes but
# are deliberately not re-edited here - only Name is exercised, same as every other converted
# screen's TC03.
Meter Name=Automation Test Meter UPDATED
