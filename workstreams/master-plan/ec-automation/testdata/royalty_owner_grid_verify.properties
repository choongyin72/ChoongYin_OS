# Royalty Owner screen (OV_ROYALTY_OWNER) - Grid-level verify test data: the table grid only
# displays a subset of the record - Code, Name, Start Date (confirmed live 2026-08-23 via
# a real existing row's rendered grid text: "183_OMICRON  OMICRON PERSEI  2010-01-01") - same
# convention as bank_grid_verify.properties/state_grid_verify.properties. Name here reflects
# the POST-UPDATE state (used by TC04 Find/Verify, after TC03 Update runs). Keys here are dict
# lookups only (matched against raw row text, not live DOM labels), but named to match the
# real grid columns for clarity.
Royalty Owner Code=AUTOTEST_ROYALTY_OWNER
Royalty Owner Name=AUTOTEST Royalty Owner UPDATED
Start Date=2000-01-01
