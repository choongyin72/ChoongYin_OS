# Calendar screen (OV_CALENDAR) - Verify test data: the expected CURRENT state of the test
# calendar after TC02 Insert + TC03 Update, used by TC04 Find/Verify to round-trip check the
# grid + form actually SHOW what was saved. Kept as its OWN file (not reused from
# calendar_insert.properties or calendar_update.properties) because it represents a MERGED
# state: Calendar Name comes from the update, Calendar Code is unchanged since insert.
Calendar Code=AUTOTEST_CALENDAR
Calendar Name=AUTOTEST Calendar UPDATED
