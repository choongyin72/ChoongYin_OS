# Well Bore Interval screen (OV_WELL_BORE_INTERVAL) - Insert test data. Column = value, one per
# line, read at Insert time by the bespoke "Insert Well Bore Interval Record And Save" T3 keyword
# (well_bore_interval_page.resource). Labels are SCREEN-PREFIXED ("Well Bore Interval Code"/
# "Well Bore Interval Name"), reused verbatim from the pre-existing py/well_bore_interval_iud.py
# driver's insert_fields list.
# NOTE: the mandatory "Well Bore" POPUP field is DELIBERATELY NOT listed here - its list grid is
# `Objects:form:T_data` (not the generic `PopupList:form:T_data` the shared "Fill OV Field By
# Label Any Kind" auto-detector expects), so it is filled by a screen-local picker
# ("Pick Well Bore Popup") after this file's fields, reusing the SAME value as the navigator's
# G:6 (FIELD-REUSE RULE - see well_bore_interval_navigator.properties' "Well Bore" key).
Well Bore Interval Code=AUTOTEST_WBI
Well Bore Interval Name=Automation Test Well Bore Interval
Start Date=2020-01-01
