# Data Extract Setup screen (OV_SUMMARY_SETUP) - Verify test data: the expected CURRENT state
# of the test data_extract_setup after TC02 Insert + TC03 Update, used by TC04 Find/Verify to
# round-trip check the updateAttributes form actually SHOWS what was saved. Only Data Extract
# Setup Code/Name are listed - Data Extract Type is an Insert-only field (objectForm), not
# present in updateAttributes (same as Bank/Berth/Data Extract Set's Insert-only field
# exclusion pattern).
Data Extract Setup Code=AUTOTEST_DXS
Data Extract Setup Name=AUTOTEST Data Extract Setup UPDATED
