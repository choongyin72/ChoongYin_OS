# Meter screen (OV_METER) OV-GM navigator - single mandatory Business Unit dropdown at
# nav:form:G:0:R:1:C:1:dd, SAME single-dropdown shape as Area's own Production Unit navigator
# (nav:form:G:0:R:1:C:1:dd). Live DOM scan (2026-08-26, tests/_recon_meter2.robot, deleted before
# commit) confirmed a SECOND dropdown at nav:form:G:0:R:1:C:2:dd labelled "Delivery Point"
# (nav:form:G:0:R:0:C:2:la), but it is an OPTIONAL filter, not mandatory - GO succeeded with rows
# loading (1 row visible under a live production BU before this conversion's own test rows
# existed) after filling ONLY C:1 (Business Unit). Only ONE line is listed here so the shared T2
# "Apply Navigator From Properties" (resources/manage_object.resource) fills C:1 only, matching
# the confirmed-live requirement. "ECP Norway" is the SAME value the pre-existing meter_iud.robot
# (${NAV_BU}) already proved live (2026-06-13, Meter's original 4/4 build).
Business Unit=ECP Norway
