# Payment Term screen (OV_PAYMENT_TERM) - Update test data. Column = value, one per line, read at
# Update time. Only fields that actually exist in updateAttributes and that the proven driver
# actually changes may be listed here - Payment Term Code is read-only there, and Start
# Date/End Date live only in objectdates, not updateAttributes (confirmed live via
# evidence/pt_07_update_filled.png: Start Date/End Date sit in a separate row above the
# updateAttributes field block). Matches the already-proven Python driver's own Update step
# (only Name is changed - Date Calculation Method/Number Of Days are left as inserted).
Name=AUTOTEST Payment Term UPDATED
