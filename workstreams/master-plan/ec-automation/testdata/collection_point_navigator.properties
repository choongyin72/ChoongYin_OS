# Collection Point (CO.0205) OV-GM navigator - genuine 3-level SAME-ROW cascade (Production
# Unit -> Area -> Operator Route): nav:form:G:0:R:1:C:1:dd (PU) then C:2:dd (Area) then C:3:dd
# (Operator Route). Fits the shared T2 "Apply Navigator From Properties"
# (resources/manage_object.resource) supported shape - confirmed live 2026-08-26
# (tmp/recon_cp_navigator_cascade.py): C:1/C:2/C:3 all present, C:4 absent (no 4th level). Also
# confirmed live that the shared keyword's flat 0.7s sleep between levels is sufficient for this
# screen's redraw timing at BOTH the C1->C2 and C2->C3 transitions (no retry needed).
#
# VALUES: the SAME explicit values the pre-existing bespoke driver (collection_point_page.resource
# "Open Collection Point Screen", py/collection_point_iud.py) already used successfully -
# first-available broke a later level on this screen per that file's own documentation, so these
# are PROVEN explicit values (scripts/find_populated_scope.py), not re-invented.
Op Production Unit=P3 Production Unit
Op Area=P3 Area
Op Operator Route=Oper Route 1
