# Account screen (OV_FIN_ACCOUNT) - Update test data. Column = value, one per line, read at
# Update time. Only Name is listed here - Code is read-only in updateAttributes, Start Date lives
# only in objectdates (not updateAttributes), and Cost Object Type/GL Account are left unchanged
# from Insert (same pattern as Bank/State: only the field(s) actually being changed are listed).
Name=AUTOTEST Account UPDATED
