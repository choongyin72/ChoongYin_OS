# Price Object (CO.3016) OV-GM navigator - single-dropdown (C:1 only; the 2 sibling nav
# columns are unrelated optional filters, NOT cascade children - see price_object_page.resource
# documentation and docs/ec_screen_registry.md line 342).
# Value confirmed LIVE this session (2026-08-26) via a fresh read-only recon
# (tmp/recon_price_object_nav.py): the navigator's first-available Business Unit option in
# this sandbox resolves to "EC LNG Norway" (NOT "Royalty Canada" - that value belongs to
# sibling screens Property/Price Index/Division Order/Royalty Contract, confirmed NOT the
# same environment default here). The Insert form's own Business Unit dropdown (parent_dd)
# MUST be set to this SAME value or the inserted row is not grid-visible under this scope -
# this was the screen's real original defect (registry: "missing Business Unit scope binding").
Business Unit=EC LNG Norway
