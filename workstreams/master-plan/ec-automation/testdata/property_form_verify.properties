# Property screen (OV_PROPERTY) - Verify test data (TC04 Find/Verify round-trip check): the
# expected CURRENT state of the test object after TC02 Insert + TC03 Update. Kept as its OWN
# file (not reused from property_insert.properties/property_update.properties) because it
# represents a MERGED state: Property Name comes from the update, Property Code is unchanged
# since insert.
Property Code=AUTOTEST_PROPERTY
Property Name=Automation Test Property UPDATED
