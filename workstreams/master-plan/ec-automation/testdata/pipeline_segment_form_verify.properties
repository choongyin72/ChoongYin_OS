# Pipeline Segment screen (OV_PIPELINE_SEGMENT) - Verify test data (TC04 Find/Verify round-trip
# check): the expected CURRENT state of the test pipeline segment after TC02 Insert + TC03
# Update. Kept as its OWN file (not reused from pipeline_segment_insert.properties/
# pipeline_segment_update.properties) because it represents a MERGED state: Pipeline Segment Name
# comes from the update, Pipeline Segment Code is unchanged since insert. Start Date/Pipeline
# Name are deliberately excluded from this round-trip compare - Start Date lives only in
# objectdates (not updateAttributes), and Pipeline Name (dropdown) can re-render different
# display text after reload, same caveat as every other converted screen's dropdown fields.
Pipeline Segment Code=AUTOTEST_PIPELINE_SEGMENT
Pipeline Segment Name=Automation Test Pipeline Segment UPDATED
