# Calculation Context screen (OV_CALC_CONTEXT) - Grid-level verify test data: the table grid
# (summary data section) only displays a subset of the record - Calc Context Code, Calc Context
# Name, Start Date, End Date (live-recon 2026-08-23) - far fewer columns than the form record.
# Kept as its OWN file, separate from calculation_context_form_verify.properties (the full
# form-record field set). End Date is excluded here - it stays blank until deleted.
Calc Context Code=AUTOTEST_CALCCTX
Calc Context Name=Automation Test Calculation Context UPDATED
Start Date=2000-01-01
