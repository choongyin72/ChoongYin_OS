# Contract screen (OV_CONTRACT) - Update test data. Column = value, one per line, read at
# Update time. Only Contract Name is listed - Contract Code is read-only in updateAttributes
# (confirmed by the screen's own prior driver, contract_page.resource "Update Contract Name").
Contract Name=Automation Test Contract UPDATED
