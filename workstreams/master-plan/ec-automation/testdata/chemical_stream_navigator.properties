# Chemical Stream (CO.0258) OV-GM navigator - genuine 3-level cascade (Production Unit -> Area
# -> Facility Class 1), same row increasing column: nav:form:G:0:R:1:C:1:dd (PU) then
# nav:form:G:0:R:1:C:2:dd (Area) then nav:form:G:0:R:1:C:3:dd (Facility Class 1). Fits the shared
# T2 "Apply Navigator From Properties" (resources/manage_object.resource) supported shape -
# confirmed via chemical_stream_page.resource's own pre-existing "Apply Chemical Stream
# Navigator" keyword (lines 44/46/48 of the pre-conversion file): C:1/C:2/C:3 present, matching
# this screen's registry entry ("Production Unit -> Area -> Facility Class 1", docs/
# ec_screen_registry.md).
#
# VALUES: SPECIFIC P1 values, NOT first-available - owner screenshot (2026-07-30) proved the
# mandatory From Connection popup's source list is EMPTY under the first-available AS1 scope but
# populated under this P1 scope (Object Type CHEM_TANK, P1 CT001..CT014). Same values already
# proven live by py/chemical_stream_iud.py (NAV_PU/NAV_AREA/NAV_FC1) and the pre-conversion
# chemical_stream_page.resource (${CHS_NAV_PU}/${CHS_NAV_AREA}/${CHS_NAV_FC1}) - captured here as
# EXPLICIT values, not re-invented.
Op Production Unit=P1 Production Unit
Op Area=P1 Area
Op Facility Class 1=P1 Facility 1
