# Cost Centre screen (OV_FIN_COST_CENTER) - Grid-level verify test data: the table grid only
# displays a subset of the record - Code, Name, Start Date - same convention as
# bank_grid_verify.properties/state_grid_verify.properties. Name here reflects the POST-UPDATE
# state (used by TC04 Find/Verify, after TC03 Update runs). Keys here are dict lookups only
# (matched against raw row text, not live DOM labels).
Code=AUTOTEST_COST_CENTRE
Name=AUTOTEST Cost Centre UPDATED
Start Date=2000-01-01
