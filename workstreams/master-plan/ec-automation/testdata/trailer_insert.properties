# Trailer screen (OV_TRAILER) - Insert test data. Column = value, one per line, read at
# Insert time. Field labels are "Trailer Code"/"Trailer Name" (screen-prefixed) - confirmed
# via direct grep and the already-proven Playwright driver py/trailer_iud.py (insert_fields).
# Licence Plate No / Trailer Type / UOM / Transport Company are ALL filled here too, matching
# the proven driver exactly - live-confirmed necessary for Save to persist the record (Process
# Train Batch-9 lesson: a field can be de-facto mandatory even when not CSS-flagged; the
# unmodified proven driver already fills all of these and is the source of truth, not a static
# label/CSS scan).
Trailer Code=AUTOTEST_TRAILER
Trailer Name=AUTOTEST Trailer
Start Date=2000-01-01
Licence Plate No=AUTOTEST-TRL-001
Trailer Type=__FIRST__
UOM=__FIRST__
Transport Company=__FIRST__
