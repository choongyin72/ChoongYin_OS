# Carrier screen (OV_CARRIER) - Grid-level verify test data: the table grid only displays a
# subset of the record - Carrier Code, Carrier Name, Start Date - same convention as
# bank_grid_verify.properties/berth_grid_verify.properties/port_grid_verify.properties. Name here
# reflects the POST-UPDATE state (used by TC04 Find/Verify, after TC03 Update runs). Keys here are
# dict lookups only (matched against raw row text, not live DOM labels), but named to match the
# real grid columns for clarity.
Carrier Code=AUTOTEST_CARRIER
Carrier Name=AUTOTEST Carrier UPDATED
Start Date=2003-01-01
