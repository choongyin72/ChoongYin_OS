# Meter Run screen (OV_METER_RUN) - Update test data. Column = value, one per line, read at
# Update time. Only Meter Run Name is listed - Meter Run Code is read-only in updateAttributes
# and Start Date lives only in objectdates, not updateAttributes (same pattern as Bank/Berth),
# matching the already-proven Playwright driver py/meter_run_iud.py (UPDATE_FIELDS: Name only).
Meter Run Name=AUTOTEST Meter Run UPDATED
