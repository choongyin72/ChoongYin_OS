# Shift screen (OV_SHIFT) - Update test data. Column = value, one per line, read at Update time.
# Only Shift Name is listed - Shift Code is read-only in updateAttributes (confirmed live via
# the prior driver, py/shift_iud.py update_fields).
Shift Name=Automation Test Shift UPDATED
