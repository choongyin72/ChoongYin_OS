# Reservoir Formation screen (OV_RESV_FORMATION) - Grid-level verify test data: the table
# grid only displays a subset of the record - Reservoir Formation Code, Reservoir Formation
# Name, Start Date - same convention as berth_grid_verify.properties. Name here reflects the
# POST-UPDATE state (used by TC04 Find/Verify, after TC03 Update runs). Keys here are dict
# lookups only (matched against raw row text, not live DOM labels), but named to match the
# real grid columns for clarity.
Reservoir Formation Code=AUTOTEST_RESVF
Reservoir Formation Name=AUTOTEST Reservoir Formation UPDATED
Start Date=2000-01-01
