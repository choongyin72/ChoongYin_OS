# Port screen (OV_PORT) - Update test data. Column = value, one per line, read at Update
# time. Only Port Name may be listed here - Port Code is read-only in updateAttributes and
# Start Date lives only in objectdates, not updateAttributes (same pattern as Bank/Berth),
# matching the already-proven Playwright driver py/port_iud.py (UPDATE_FIELDS: Port Name only).
Port Name=AUTOTEST Port UPDATED
