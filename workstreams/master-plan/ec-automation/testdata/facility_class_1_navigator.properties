# Facility Class 1 (CO.0019) OV-GM navigator - genuine 2-level cascade (Production Unit -> Area),
# same row increasing column: nav:form:G:0:R:1:C:1:dd (PU) then nav:form:G:0:R:1:C:2:dd (Area).
# Fits the shared T2 "Apply Navigator From Properties" (resources/manage_object.resource) supported
# shape - confirmed live 2026-08-26 (tmp/recon_fc1_navigator_cascade.py): C:1 and C:2 both present,
# C:3 absent (no 3rd level), matching this screen's registry entry ("Production Unit -> Area
# cascade + GO", docs/ec_screen_registry.md).
#
# VALUES: same ones the existing driver's "Apply OV-GM Navigator First Available"
# (py/ec_object_iud.py apply_ovgm_navigator, currently-passing per docs/ec_screen_registry.md
# "verify_screen PASS 2026-07-30") already selects successfully - first-available picks the
# alphabetically-first option in each panel, which live recon confirmed is "AS1 EC Exploration
# Norway" at C:1 and "AS1_Area" at the resulting C:2 (only option once C:1 is that PU). Captured
# here as EXPLICIT values, not re-invented.
Op Production Unit=AS1 EC Exploration Norway
Op Area=AS1_Area
