# Storage Flow screen (OV_STORAGE_FLOW) - Grid-level verify test data: the table grid only
# displays a subset of the record - Storage Flow Code, Storage Flow Name, Start Date - same
# convention as bank_grid_verify.properties/berth_grid_verify.properties. Name here reflects
# the POST-UPDATE state (used by TC04 Find/Verify, after TC03 Update runs). Keys here are
# dict lookups only (matched against raw row text, not live DOM labels).
Storage Flow Code=AUTOTEST_STFLOW
Storage Flow Name=AUTOTEST Storage Flow UPDATED
Start Date=2000-01-01
