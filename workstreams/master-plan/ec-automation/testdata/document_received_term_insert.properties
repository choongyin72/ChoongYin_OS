# Document Received Term screen (OV_DOC_RECEIVED_TERM) - Insert test data. Column = value, one
# per line, read at Insert time. Field labels are GENERIC "Code"/"Name" (NOT screen-prefixed) -
# confirmed via a live objectForm field-inventory scan (2026-08-24): Code/Name/Start Date/Date
# Calculation Method/Offset are the mandatory fields; End Date/Description/Comments are optional
# and deliberately NOT filled here (IUD fills only needed fields). Date Calculation Method is a
# mandatory autocomplete dropdown - literal option 'Manual entry' used (matches the already-proven
# Playwright driver ec_iud_document_received_term.py, same convention as Document Date Term's
# sibling build). Offset is a mandatory numeric field, filled 0 (simplest valid value).
Code=AUTOTEST_DRT
Name=AUTOTEST Document Received Term
Start Date=2000-01-01
Date Calculation Method=Manual entry
Offset=0
