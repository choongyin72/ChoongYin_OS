# Lifting Account (CO.2004) OV-GM navigator - ONE group (nav:form:G:0, single fieldset), cascade
# rendered across TWO grid rows: R:1 C:1..C:3 (Production Unit/Area/Facility Class 1), then R:3
# C:0 (Storage). Confirmed live 2026-08-27: all four dd spans carry "{mandatory:true}
# MandatoryCellStyle" (genuinely empty) - not a "MULTIPLE independent groups" shape (that would be
# separate G:1/G:2 fieldsets, per Tract's precedent), so this still fits the Area-pattern single-
# navigator-fill convention, just with the shared keyword's new ${row2_from}/${row2}/
# ${row2_start_col} args (added this session) to span the row break.
# Storage is EMPTY under the first-available AS1 path (why this screen was originally hand-built/
# parked) - values below are the EXACT proven scope already confirmed live and shipped in the
# pre-existing driver (py/lifting_account_iud.py, commit 6e88e371): P1 Production Unit -> P1 Area
# -> P1 Facility 1 -> Storage P1_CRUDE_STOR. REUSED here verbatim per the field-reuse rule, not
# re-derived.
# Filled via "Apply Navigator From Properties    testdata/lifting_account_navigator.properties
# row2_from=3    row2=3    row2_start_col=0" (resources/manage_object.resource).
Production Unit=P1 Production Unit
Area=P1 Area
Facility Class 1=P1 Facility 1
Storage=P1_CRUDE_STOR
