# Calculation Group Context screen (OV_CALC_GRP_CONTEXT) - Update test data.
# Only fields that actually exist in the updateAttributes form may be listed here - Code is
# read-only there and Start Date/End Date live only in objectdates, not updateAttributes.
# Field set matches the already-proven Python driver's own UPDATE_FIELDS
# (py/calculation_group_context_iud.py: Calculation Group Context Name only).
Calculation Group Context Name=AUTOTEST Calculation Group Context Bank UPDATED
