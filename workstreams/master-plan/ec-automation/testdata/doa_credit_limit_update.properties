# DOA Credit Limit screen (OV_DOA_CREDIT_LIMIT) - Update test data. Column = value, one per
# line, read at Update time. Only fields that actually exist in the updateAttributes form may be
# listed here - "DOA Credit Limit Code" is read-only there (MandatoryCellStyleWhite class,
# confirmed live via tmp/recon_doa2.robot - deleted before final commit) and Start Date/End Date
# live only in objectdates, not updateAttributes. Only the Name is changed here, matching Bank/
# VAT Code's own TC03 convention (a single-field update is enough to prove the update mechanism).
DOA Credit Limit Name=AUTOTEST DOA Credit Limit UPDATED
