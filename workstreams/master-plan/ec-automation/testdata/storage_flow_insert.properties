# Storage Flow screen (OV_STORAGE_FLOW) - Insert test data. Column = value, one per line,
# read at Insert time. Field labels are "Storage Flow Code"/"Storage Flow Name"
# (screen-prefixed) - confirmed via direct grep of pageobjects/Configuration/Assets/
# Tank_and_Storage_Objects/storage_flow_page.resource and the already-proven Playwright
# driver py/storage_flow_iud.py (INSERT_FIELDS).
# Storage Flow Code/Storage Flow Name/Start Date are mandatory. Flow Direction and Storage
# are also filled here (__FIRST__ = pick the first available option) because the existing
# proven page object (Fill Storage Flow Mandatory Extras) and driver (INSERT_FIELDS) both
# already fill them alongside Code/Name/Date - per the Batch-9 Process Train lesson, trust
# the existing proven driver's field set over a static CSS/label mandatory scan.
Storage Flow Code=AUTOTEST_STFLOW
Storage Flow Name=AUTOTEST Storage Flow
Start Date=2000-01-01
Flow Direction=__FIRST__
Storage=__FIRST__
