# Other Split Key screen (CD.0046, class SPLIT_KEY, view OV_SPLIT_KEY, SPLIT_TYPE=SPLIT_ITEM_OTHER)
# - Insert test data. Column = value, one per line, read at Insert time. Field labels confirmed
# live via tmp/scripts/_recon_other_split_key3.py (2026-08-25): "Split Key Code"/"Split Key Name"
# (screen-prefixed, NOT the generic "Code"/"Name"). Only the 3 fields confirmed live as
# mandatory (yellow+empty) on the pristine New-Object form are listed here (IUD Fill Only
# Needed Fields convention) - Description/Comments/Value Method/Shares add to 100%/Rounding
# Rule/Number of Rounding Decimals/Number Display Format all confirmed NOT mandatory live
# (screenshot-verified white background, several already pre-defaulted), left unset.
# Test code AUTOTEST_SPLITKEY_OTHER is distinctly scoped from the 5 sibling Split Key screens
# (Product/Company/Field/Stream Item Category/Stream Item) being built in parallel - all 6
# share the SAME OV_SPLIT_KEY view, discriminated only by SPLIT_TYPE, so each build uses its
# own distinctly-scoped test code to avoid cross-screen collision.
Split Key Code=AUTOTEST_SPLITKEY_OTHER
Split Key Name=AUTOTEST Other Split Key
Start Date=2000-01-01
