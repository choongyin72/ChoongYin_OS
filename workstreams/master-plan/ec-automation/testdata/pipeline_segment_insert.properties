# Pipeline Segment screen (OV_PIPELINE_SEGMENT) - Insert test data. Column = value, one per line,
# read at Insert time and filled IN THIS FILE ORDER by the shared T2 "Insert Object From
# Properties" (resources/manage_object.resource) via "Fill OV Field By Label Any Kind" (auto-
# detects each field's kind - text/date/dropdown - from its live DOM). No popup involved on this
# screen's insert form (unlike Meter's Delivery Point Name), so file order is not load-bearing
# the way it is on Meter - kept in the SAME order the pre-existing pipeline_segment_page.resource
# already proved live (2026-06-12): Code, Name, Start Date, then the mandatory "Pipeline Name"
# dropdown.
#
# Field labels confirmed live (2026-08-26, tmp/_recon_pipeline_segment2.robot, deleted before
# commit): objectForm ECCell labels are screen-prefixed "Pipeline Segment Code"/"Pipeline Segment
# Name" (NOT the generic Bank/Object List "Code"/"Name"), matching Area's/Meter's own screen-
# prefixed convention. Confirmed MandatoryCellStyle (yellow) on the pristine insert form: Pipeline
# Segment Code, Pipeline Segment Name, Start Date, Pipeline Name (dropdown) - exactly the same
# mandatory set the pre-existing driver already documented, no new/extrapolated fields added.
# "TS5 Gas Pipeline" is the SAME Pipeline Name value the pre-existing pipeline_segment_iud.robot
# (${PARENT_VALUE}) already proved live and DB-verified (2026-06-12) under the TS5 BU navigator
# scope - reused, not reinvented.
Pipeline Segment Code=AUTOTEST_PIPELINE_SEGMENT
Pipeline Segment Name=Automation Test Pipeline Segment
Start Date=2020-01-01
Pipeline Name=TS5 Gas Pipeline
