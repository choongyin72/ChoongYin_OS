# Bank-pattern conversion — screen tracking checklist

_Created 2026-08-23. Tracks which EC screens (with an EXISTING IUD suite already built,
just on the older hardcoded-field-id pattern) have been converted to the label-driven,
properties-file-driven, T2-consolidated "Bank pattern" — so future sessions know exactly
which screens are done, which are queued, and which are out of scope, without redoing the
survey each time._

## Background

Bank/State/Object List/Account/Cost Centre/etc. were rebuilt (2026-08-22/23) from an older
per-screen hand-coded pattern (hardcoded `objectForm`/`updateAttributes` field ids, bespoke
per-screen keywords, no properties files) to a shared, label-driven, T2-consolidated pattern
(`resources/manage_object.resource` keywords: `Insert/Update/Find/Verify Object *`,
`Delete Object Via End Date`), including the explicit grid column-filter row-locate wiring
(see `docs/grid-filter-standardization-checklist.md`). A full-repo survey (2026-08-23)
found **129 OV/OV-GM screens still on the old pattern with an existing IUD suite** already
built, split into:

- **48 "Tier 0" screens** — genuinely old pattern (hardcoded field ids in
  `objectForm`/`updateAttributes`) — **these are the real candidates for conversion**,
  tracked below.
- **80 "Tier 1" screens** — already label-driven via a SEPARATE Playwright/generator
  pipeline (`verify_screen.py`-gated, built in an earlier session) — structurally modern
  already, just not the RF Bank-pattern shape. Lower priority / arguably optional; NOT
  tracked in this checklist (a different, already-adequate pattern).
- **56 OV-GM screens** (gated navigator) — bigger lift than a straight Bank-pattern
  conversion, needs a nav-cascade design decision first. NOT tracked here.

Of the 48 Tier-0 screens, a further live-verified filter (checking each screen's actual
documented navigator requirement in `docs/ec_screen_registry.md`, not just its OV/OV-GM
label) found:
- **23 screens are genuinely nav-free** (plain manage-object with no mandatory
  dropdown/date before GO, OR custom-URL with no navigator at all) — same shape as
  Bank/Account. **These are what this checklist tracks and batches.**
- **20 screens have a real navigator requirement** (mandatory single date+GO, or a
  mandatory dropdown/cascade) — SKIP per owner instruction ("if the ec screen is not same
  as bank... SKIP it for next target screen"). Listed in "Excluded" below for the record.
- **5 screens were unclear/not in the registry** at survey time — need live recon before
  they can be classified either way.

## Batch tracking (23 nav-free Tier-0 candidates)

| Screen | Folder | Status | PR |
|---|---|---|---|
| Country | Basic Objects | ✅ DONE (2026-08-23, Batch 2) | #428 |
| County | Basic Objects | ✅ DONE (2026-08-23, Batch 2) | #429 |
| Regulatory Permits | Basic Objects | ✅ DONE (2026-08-23, Batch 2) | #432 |
| Currency | Financial Objects | ✅ DONE (2026-08-23, Batch 2) | #430 |
| VAT Code | Financial Objects | ✅ DONE (2026-08-23, Batch 2) | #431 |
| Customer | Commercial Objects | ✅ DONE (2026-08-23, Batch 3) | #435 |
| Field Group | Commercial Objects | ✅ DONE (2026-08-23, Batch 3) | #434 |
| Licence | Commercial Objects | ✅ DONE (2026-08-23, Batch 3) | #438 |
| MMS Lease | Commercial Objects | ✅ DONE (2026-08-23, Batch 3) | #437 |
| Operator Lease | Commercial Objects | ✅ DONE (2026-08-23, Batch 3) | #436 |
| State Lease | Commercial Objects | ✅ DONE (2026-08-23, Batch 4) | #440 |
| Vendor | Commercial Objects | ✅ DONE (2026-08-23, Batch 4) | #439 |
| Cost Object Mapping | Financial Objects | ✅ DONE (2026-08-23, Batch 4) | #442 |
| DOA Credit Limit | Financial Objects | ✅ DONE (2026-08-23, Batch 4) | #443 |
| Product Description | Financial Objects | ✅ DONE (2026-08-23, Batch 4) | #441 |
| Sales Order | Financial Objects | ✅ DONE (2026-08-23, Batch 5) | #444 |
| Product Group | Royalty Objects | ✅ DONE (2026-08-23, Batch 5) | #445 |
| Royalty Depositor | Royalty Objects | ✅ DONE (2026-08-23, Batch 5) | #448 |
| Royalty Owner | Royalty Objects | ✅ DONE (2026-08-23, Batch 5) | #447 |
| Unit Agreement | Royalty Objects | ✅ DONE (2026-08-23, Batch 5) | #446 |
| Calendar Collection | Date Objects | ✅ DONE (2026-08-23, Batch 6) | #449 |
| Calendar | Date Objects | ✅ DONE (2026-08-23, Batch 6) | #451 |
| Account Mapping | Financial Objects | ✅ DONE (2026-08-23, Batch 6, FINAL) | #450 |

**23 of 23 done. ALL SCREENS IN THE ORIGINAL CANDIDATE POOL CONVERTED.** Batch 2
(Country #428, County #429, Regulatory Permits #432, Currency #430, VAT Code
#431), Batch 3 (Customer #435, Field Group #434, Licence #438, MMS Lease #437,
Operator Lease #436), Batch 4 (State Lease #440, Vendor #439, Cost Object
Mapping #442, DOA Credit Limit #443, Product Description #441), Batch 5 (Sales
Order #444, Product Group #445, Royalty Depositor #448, Royalty Owner #447,
Unit Agreement #446), and Batch 6 (Calendar Collection #449, Calendar #451,
Account Mapping #450) — all 23 nav-free Tier-0 screens are now on the
label-driven, properties-file-driven, T2-consolidated Bank pattern, with
explicit grid-filter wiring included. This checklist's "Batch tracking" table
is now complete; any future Bank-pattern conversion work (Tier 1/OV-GM
screens, or newly-registered screens) should get its own new tracking table
rather than reusing this one.

Note: once a screen from this table is converted, it should ALSO be added to
`docs/grid-filter-standardization-checklist.md`'s "done" table in the same PR (per the
owner's standing instruction to include the filter wiring from day one, not as a
follow-up pass).

## Excluded — has a real navigator requirement (per owner: SKIP if not same as Bank)

| Screen | Reason |
|---|---|

| Field, Contract Area, Delivery Point, Delivery Stream, Nomination Point, Pipeline Segment, Transport Zone | Mandatory Business Unit/Area dropdown + GO |
| Sub Area | Cascading PU→Area + GO |
| Equipment | 5-field cascading navigator |
| Analysis Point | 3-level cascade (PU→Area→Facility Class) + GO |
| Tract | Gated: mandatory date + mandatory Unit Agreement dropdown + GO |

## Unclear at survey time — needs live recon before batching either way

Sub Field, Pipeline, Meter, Transport System, Commercial Entity, Company Contact,
Carrier — not found in `docs/ec_screen_registry.md`'s navigator column at survey time,
or registry text was ambiguous (e.g. "optional" dropdown/date). Do NOT assume nav-free;
recon live before adding to a batch.

## Out of scope (Tier 1 — different, already-adequate pattern)

The 80 Tier-1 screens (generator-scaffolded, `verify_screen.py`-gated, Playwright-driven,
already label-driven via a different mechanism) are NOT tracked in this checklist. They
are not "old pattern" in the sense this doc cares about — converting them to the RF
Bank-pattern would be a consistency change, not a functional uplift, and is lower
priority. If ever prioritized, treat as a separate initiative, not an extension of this
checklist.

## Batch 7 additions (2026-08-23) — consolidated

_Section consolidated by the reviewer at the Batch 8 merge: the parallel per-screen
PRs each appended their own "Batch 7" section header; rows below are the union, each
screen exactly once, content unchanged._

| Screen | Explicit filter wired? / Status | Notes |
|---|---|---|
| Berth | ✅ DONE (2026-08-23, Batch 7) | Manage-object OV (Bank family), single-page grid (11 rows). Screen-prefixed labels "Berth Code"/"Berth Name" confirmed via ec-ui-knowledge/screens/berth.md + proven py/berth_iud.py (only Code/Name/Start Date mandatory, dropdowns optional). Rebuilt berth_page.resource/berth_iud.robot to mirror bank_page.resource/state_page.resource exactly: properties-file-driven insert ("Insert Object From Properties And Verify Code"), update, form/grid verify, explicit `Find/Clear Berth Row By Filter` wired into Update/Find/Verify-Found/Delete. Fixed test code `AUTOTEST_BERTH` (confirmed free live). Live 5/5, dryrun 753/753, filter fired 15x (output.xml grep), DB self-clean confirmed via fresh oracledb connection. No shared T1/T2 file changes. |
| Calculation Group Context (Configuration > Assets > Calculation_Objects, CO.0245) | ✅ DONE (2026-08-23) — brought to full Bank-pattern shape: `Insert/Update Object From Properties`, `Verify Object Insert Exists/Form Record/Found/Removed/Does Not Exist`, explicit `Find/Clear Calculation Group Context Row By Filter`, dedicated `CALCULATION_GROUP_CONTEXT_EC_USER/PASS`, fixed test code `AUTOTEST_CGC_BANK`. Live 5/5 RF, robocop parity with `bank_iud.robot`'s baseline, dryrun 753/753, DB self-clean (fresh connection) 0 residual, filter fired 23x per output.xml. See `docs/ec_screen_registry.md` and `docs/grid-filter-standardization-checklist.md` for full detail. |

## Batch 8 additions (2026-08-23) — consolidated

_Section consolidated by the reviewer at the Batch 8 merge: the parallel per-screen
PRs each appended their own "Batch 8" section header; rows below are the union, each
screen exactly once, content unchanged._

| Screen | Explicit filter wired? / Status | Notes |
|---|---|---|
| Inventory Area | ✅ DONE (2026-08-23, Batch 8) | Manage-object OV (Inventory Objects, CD.0115), single-page grid. Screen-prefixed labels "Inventory Area Code"/"Inventory Area Name" confirmed via the pre-existing page object + proven py/inventory_area_iud.py (only Code/Name/Start Date mandatory, no other fields), re-confirmed via a fresh live Playwright run 2026-08-23. Rebuilt inventory_area_page.resource/inventory_area_iud.robot to mirror bank_page.resource/berth_page.resource exactly: properties-file-driven insert ("Insert Object From Properties And Verify Code"), update, form/grid verify, explicit `Find/Clear Inventory Area Row By Filter` wired into Update/Find/Verify-Found/Delete. Fixed test code `AUTOTEST_INVA` (confirmed free live via fresh oracledb connection, before and after the live RF run). Live 5/5, dryrun 758/758, filter fired 11x (output.xml grep), DB self-clean confirmed via fresh oracledb connection. No shared T1/T2 file changes. |
| Meter Run (Configuration > Assets > Stream_Objects, CO.0091) | ✅ DONE (2026-08-23, Batch 8) | Manage-object OV (Bank family), single-page grid (20 rows). Screen-prefixed labels "Meter Run Code"/"Meter Run Name" confirmed via the proven py/meter_run_iud.py + the prior live-tested page object (only that driver, not a fresh guess). Rebuilt meter_run_page.resource/meter_run_iud.robot to mirror bank_page.resource/berth_page.resource exactly: properties-file-driven insert (`Insert Object From Properties And Verify Code`), update, form/grid verify, explicit `Find/Clear Meter Run Row By Filter` wired into Update/Find/Verify-Found/Delete. Fixed test code `AUTOTEST_METER_RUN` (confirmed free live via a fresh oracledb query). Mandatory set is WIDER than Bank/Berth - besides Code/Name/Start Date, Type of Taps/Pipe Material/Location of Taps dropdowns and Pipe Diameter/Diameter Meas Temp/All Calibration Factor are also mandatory (per the existing driver, unchanged from before this batch). Live 5/5, dryrun 758/758, filter fired 15x (output.xml grep), DB self-clean confirmed 0 residual via a fresh oracledb connection. No shared T1/T2 file changes. |
| Orifice Plate (Configuration > Assets > Stream_Objects, CO.0089) | ✅ DONE (2026-08-23, Batch 8) — brought to full Bank-pattern shape: rebuilt `orifice_plate_page.resource`/`orifice_plate_iud.robot` to mirror `bank_page.resource`/`berth_page.resource` exactly — properties-file-driven insert (`Insert Object From Properties And Verify Code`), update, form/grid verify, explicit `Find/Clear Orifice Plate Row By Filter` wired into Update/Find/Verify-Found/Delete, dedicated `ORIFICE_PLATE_EC_USER/PASS`, fixed test code `AUTOTEST_ORIFICE_PLATE` (confirmed free live), added TC04 Find (prior suite only had TC01-04 Verify/Insert/Update/Delete, no Find). Mandatory fields beyond Code/Name/Start Date: Material (dropdown), Diameter [mm], Measurement Temp [°R] — all confirmed mandatory via ec-ui-knowledge/screens/orifice_plate.md + proven py/orifice_plate_iud.py, included in the insert properties file. Live 5/5, dryrun 758/758, filter fired 13x (output.xml grep), DB self-clean confirmed via fresh oracledb connection. No shared T1/T2 file changes. This MODIFIES the screen's existing `docs/ec_screen_registry.md` / `docs/automation-scorecard.md` rows (from the 2026-07-26 generator-scaffolded build) — not a new row. |
| Chemical Transport Tank | ✅ DONE (2026-08-23, Batch 8) | Manage-object OV, single-page grid (Chemical Objects). Screen-prefixed labels "Transport Tank Code"/"Transport Tank Name" confirmed via the already-proven Playwright driver py/chemical_transport_tank_iud.py (only Code/Name/Start Date mandatory, dropdowns optional). Rebuilt chemical_transport_tank_page.resource/chemical_transport_tank_iud.robot to mirror bank_page.resource/berth_page.resource exactly: properties-file-driven insert (`Insert Object From Properties And Verify Code`), update, form/grid verify, explicit `Find/Clear Chemical Transport Tank Row By Filter`. Fixed test code `AUTOTEST_CTT` (confirmed free live via fresh oracledb connection). Live 5/5, dryrun 758/758, filter fired 7x (output.xml grep), DB self-clean confirmed via fresh oracledb connection. No shared T1/T2 file changes. |
| Deferment Group (Configuration > Assets > Facility_Objects, CO.0149) | ✅ DONE (build 2026-08-23 Batch 8, BLOCKED at that time, re-verified + merged 2026-08-23 after an owner-side access-gate fix) | Manage-object OV (Bank family), single-page grid. Screen-prefixed labels "Deferment Group Code"/"Deferment Group Name" confirmed via the already-proven Playwright driver `py/deferment_group_iud.py` (only Code/Name/Start Date mandatory, dropdowns optional). Rebuilt `deferment_group_page.resource`/`deferment_group_iud.robot` to mirror `bank_page.resource`/`berth_page.resource` exactly at Batch-8 build time: properties-file-driven insert (`Insert Object From Properties And Verify Code`), update, form/grid verify, explicit `Find/Clear Deferment Group Row By Filter`, dedicated `DEFERMENT_GROUP_EC_USER/PASS`, fixed test code `AUTOTEST_DEFERMENT_GROUP`. **Could not be run live at Batch-8 build time**: `TV_T_BASIS_ACCESS` had `LEVEL_ID=0` ("No access") for all 5 roles on this screen object, so the screen link was unreachable even via menu search. Left uncommitted in its own isolated clone (`Workplaces/deferment_group`) pending an access fix. Owner granted sysadmin-role access 2026-08-23; this pass re-synced the clone to master (fast-forward + one non-conflicting `credentials.py` merge conflict resolved), re-verified live access (login + menu search now finds and opens the screen), then ran the full gate: robocop clean on the page object (9 issues on the suite, 4 VAR02 + 5 DOC02, same accepted baseline as every other Batch 7-11 screen), dryrun 774/774, live 5/5, filter fired 15x (output.xml grep), DB self-clean confirmed 0 residual `AUTOTEST_DEFERMENT_GROUP` rows in `OV_DEFERMENT_GROUP` via a fresh independent oracledb connection. No shared T1/T2 file changes. |

## Batch 9 additions (2026-08-23) — consolidated

_Header pre-created via PR #464 per the Batch-8 lesson; two parallel PRs raced it
and self-added their own headers (honestly flagged in their bodies) - consolidated
by the reviewer at merge, rows unioned by key, content unchanged._

| Screen | Status | Notes |
|---|---|---|
| Port (Configuration > Assets > Transport_Objects, CO.2003) | ✅ DONE (2026-08-23, Batch 9) | Manage-object OV (Bank family), **paginated grid (2 pages)**. Screen-prefixed labels "Port Code"/"Port Name" confirmed via the pre-existing page object + proven `py/port_iud.py` (only Port Code/Port Name/Start Date mandatory - Country/Canal/Time Zone/Carrier dropdowns all optional). Rebuilt `port_page.resource`/`port_iud.robot` to mirror `bank_page.resource`/`berth_page.resource` exactly: properties-file-driven insert (`Insert Object From Properties And Verify Code`), update, form/grid verify, explicit `Find/Clear Port Row By Filter` wired into Update/Find/Verify-Found/Delete. Fixed test code `AUTOTEST_PORT` (confirmed free live via fresh oracledb connection, before and after the live RF run). **Paginated-grid confirmation**: the shared T2 filter/row-locate keywords walked all pages correctly live - no engine change needed. Live 5/5, dryrun 762/762, filter fired 15x (output.xml grep), DB self-clean confirmed 0 residual via a fresh oracledb connection. No shared T1/T2 file changes. |
| Reservoir Block (Configuration > Assets > Well_and_Reservoir_Objects, CO.0133) | ✅ DONE (2026-08-23, Batch 9) | Manage-object OV (Bank family), single-page grid. Screen-prefixed labels "Reservoir Block Code"/"Reservoir Block Name" confirmed via the already-proven Playwright driver py/reservoir_block_iud.py (only Code/Name/Start Date mandatory, no other fields on this screen's insert form) and the prior label-driven page object, not re-scanned live. Rebuilt reservoir_block_page.resource/reservoir_block_iud.robot to mirror bank_page.resource/berth_page.resource exactly: properties-file-driven insert (`Insert Object From Properties And Verify Code`), update, form/grid verify, explicit `Find/Clear Reservoir Block Row By Filter` wired into Update/Find/Verify-Found/Delete, dedicated `RESERVOIR_BLOCK_EC_USER/PASS`, fixed test code `AUTOTEST_RESVB` (confirmed free live via a fresh oracledb query). Live 5/5, robocop parity with the berth_iud.robot exemplar (same 9 baseline DOC02/VAR02 issues, no regression), dryrun 762/762, filter fired 13x (output.xml grep), DB self-clean confirmed 0 residual via a fresh oracledb connection. No shared T1/T2 file changes. This MODIFIES the screen's existing `docs/ec_screen_registry.md` / `docs/automation-scorecard.md` rows (from the 2026-07-26 generator-scaffolded build) — not a new row. NOTE: at clone time PR #464 (pre-created this section header) was still OPEN, not merged into master - this PR re-adds the identical header text verbatim so it converges cleanly with #464 whichever merges first; flag this to the reviewer. |
| Reservoir Formation | ✅ DONE (2026-08-23, Batch 9) — brought to full Bank-pattern shape: rebuilt `reservoir_formation_page.resource`/`reservoir_formation_iud.robot` to mirror `bank_page.resource`/`berth_page.resource` exactly — properties-file-driven insert (`Insert Object From Properties And Verify Code`), update, form/grid verify, explicit `Find/Clear Reservoir Formation Row By Filter` wired into Update/Find/Verify-Found/Delete, dedicated `RESERVOIR_FORMATION_EC_USER/PASS`, fixed test code `AUTOTEST_RESVF` (confirmed free live via fresh oracledb connection, before and after). Mandatory fields: Reservoir Formation Code/Reservoir Formation Name/Start Date only (no other mandatory fields, confirmed via ec-ui-knowledge/screens/reservoir_formation.md + proven py/reservoir_formation_iud.py). Live 5/5, dryrun 762/762, filter fired 7x (output.xml grep), DB self-clean confirmed via fresh oracledb connection. No shared T1/T2 file changes. This MODIFIES the screen's existing `docs/ec_screen_registry.md` / `docs/automation-scorecard.md` rows (from the 2026-07-26 generator-scaffolded build) — not a new row. |
| Report Area | ✅ DONE (2026-08-23, Batch 9) | Manage-object OV (Reporting > Report Area, RP.0017, top-level Reporting menu not Assets), simplest OV (no Description, no dropdowns). Screen-prefixed labels "Report Area Code"/"Report Area Name" confirmed via the pre-existing page object + proven py/report_area_iud.py. Date label is **"Start date"** (lowercase "date") - confirmed live: "Start Date" (capital D) fails the exact-match label lookup with a 30s timeout. Rebuilt report_area_page.resource/report_area_iud.robot to mirror bank_page.resource/berth_page.resource exactly: properties-file-driven insert (`Insert Object From Properties And Verify Code`), update, form/grid verify, explicit `Find/Clear Report Area Row By Filter` wired into Update/Find/Verify-Found/Delete. Fixed test code `AUTOTEST_RPTA` (confirmed free via fresh oracledb connection before and after the live run). Live 5/5, dryrun 762/762, filter fired 28x (output.xml grep), DB self-clean confirmed 0 residual `AUTOTEST_RPTA` rows via fresh oracledb connection. No shared T1/T2 file changes. |
| Process Train (Configuration > Assets > Facility_Objects, CO.0120) | ✅ DONE (2026-08-23, Batch 9) | Manage-object OV (Bank family), single-page grid. Screen-prefixed labels "Process Train Code"/"Process Train Name" confirmed via the pre-existing page object + proven `py/process_train_iud.py`. Rebuilt `process_train_page.resource`/`process_train_iud.robot` to mirror `bank_page.resource`/`berth_page.resource` exactly: properties-file-driven insert (`Insert Object From Properties And Verify Code`), update, form/grid verify, explicit `Find/Clear Process Train Row By Filter` wired into Update/Find/Verify-Found/Delete, dedicated `PROCESS_TRAIN_EC_USER/PASS`, fixed test code `AUTOTEST_PT` (confirmed free live via a fresh oracledb query, before and after the live run). **CORRECTION found live**: the screen's own KB doc claimed "no mandatory dropdowns", but a first live attempt with only Code/Name/Start Date clicked Save successfully yet the row never reached `OV_PROCESS_TRAIN`, leaving EC's own unsaved-changes confirmation modal open and stalling the suite (4/5 fail). Re-running the already-proven `py/process_train_iud.py` unmodified (fills **Production Facility Class 1** = `__FIRST__`) passed 7/7, so that dropdown was added to `testdata/process_train_insert.properties` (excluded from the round-trip form-label compare list - `__FIRST__` never matches the resolved literal text on reload); ec-ui-knowledge/screens/process_train.md corrected in the same PR (write-after). Live 5/5, dryrun 762/762, DB self-clean confirmed 0 residual via a fresh oracledb connection, filter keyword confirmed fired via output.xml grep. No shared T1/T2 file changes. This MODIFIES the screen's existing `docs/ec_screen_registry.md` / `docs/automation-scorecard.md` rows (from the 2026-07-26 generator-scaffolded build) — not a new row. |

## Batch 10 additions (pending) — Split Item Other, Storage Flow, Stream Item Category, Data Extract Set, Trailer

_Section header pre-created on master before fan-out. Each screen's PR appends
only its own ROW here — no per-PR section header._

| Screen | Explicit filter wired? / Status | Notes |
|---|---|---|
| Split Item Other (Configuration > Assets > Revenue_Split_Keys, CD.0017) | ✅ DONE (2026-08-23, Batch 10) | Manage-object OV (Bank family), single-page grid. Labels: "Split Item Code" (screen-prefixed) but GENERIC "Name" (not screen-prefixed) - confirmed via direct grep and the already-proven Playwright driver `py/split_item_other_iud.py` (only Split Item Code/Name/Start Date mandatory, no optional dropdowns). Rebuilt `split_item_other_page.resource`/`split_item_other_iud.robot` to mirror `bank_page.resource`/`berth_page.resource` exactly: properties-file-driven insert (`Insert Object From Properties And Verify Code`), update, form/grid verify, explicit `Find/Clear Split Item Other Row By Filter` wired into Update/Find/Verify-Found/Delete, dedicated `SPLIT_ITEM_OTHER_EC_USER/PASS`, fixed test code `AUTOTEST_SIO` (confirmed free live via a fresh oracledb query, before and after the live run). Live 5/5, robocop parity with the berth_iud.robot exemplar (same 9 baseline DOC02/VAR02 issues, no regression), dryrun 767/767, filter fired 30x (output.xml grep), DB self-clean confirmed 0 residual via a fresh oracledb connection. No shared T1/T2 file changes. This MODIFIES the screen's existing `docs/ec_screen_registry.md` / `docs/automation-scorecard.md` rows (from the 2026-07-26 generator-scaffolded build) — not a new row. |
| Storage Flow (Configuration > Assets > Tank_and_Storage_Objects, CO.2091) | ✅ DONE (2026-08-23, Batch 10) | Manage-object OV (Bank family), screen-prefixed labels "Storage Flow Code"/"Storage Flow Name" confirmed via direct grep of the pre-existing page object + proven `py/storage_flow_iud.py`. Rebuilt `storage_flow_page.resource`/`storage_flow_iud.robot` from the older unique-timestamped-code/single-login/no-filter pattern to mirror `bank_page.resource`/`berth_page.resource` exactly: properties-file-driven insert (`Insert Object From Properties And Verify Code`), update, form/grid verify, explicit `Find/Clear Storage Flow Row By Filter` wired into Update/Find/Verify-Found/Delete, dedicated `STORAGE_FLOW_EC_USER/PASS`, fixed test code `AUTOTEST_STFLOW` (confirmed free via a fresh oracledb query, before and after the live run). Kept the screen's existing mandatory dropdowns `Flow Direction`/`Storage` (`__FIRST__`) already used by the proven page object + driver (per the Batch-9 Process Train lesson - trust the existing proven field set over a static CSS scan), added to `testdata/storage_flow_insert.properties`, excluded from the round-trip form-label compare (`__FIRST__` never matches the resolved literal text on reload). Live 5/5, dryrun 767/767, filter fired 15x (output.xml grep), DB self-clean confirmed 0 residual `AUTOTEST` rows via fresh oracledb connection. No shared T1/T2 file changes. This MODIFIES the screen's existing `docs/ec_screen_registry.md` / `docs/automation-scorecard.md` rows (from the 2026-07-26 generator-scaffolded build) — not a new row. |
| Stream Item Category (Configuration > Assets > Stream_Objects, CD.0016) | ✅ DONE (2026-08-23, Batch 10) | Manage-object OV (Bank family), single-page grid. GENERIC "Code"/"Name" labels (NOT screen-prefixed - confirmed via direct grep per batch10 shared findings). Mandatory: Code/Name/Start Date only (no optional dropdowns), confirmed against the already-proven Playwright driver `py/stream_item_category_iud.py` (INSERT_FIELDS/UPDATE_FIELDS). Rebuilt `stream_item_category_page.resource`/`stream_item_category_iud.robot` from the older 4-TC no-filter/no-properties pattern to mirror `bank_page.resource`/`berth_page.resource` exactly: properties-file-driven insert (`Insert Object From Properties And Verify Code`), update, form/grid verify, explicit `Find/Clear Stream Item Category Row By Filter` wired into Update/Find/Verify-Found/Delete, dedicated `STREAM_ITEM_CATEGORY_EC_USER/PASS`, fixed test code `AUTOTEST_SIC` (confirmed free live via a fresh oracledb connection, before and after the live run). Two properties-file constants (`*_FORM_VERIFY_PROPERTIES`/`*_GRID_VERIFY_PROPERTIES`) hit robocop LEN32 (43/40 chars) - shortened to `*_FORM_VERIFY_PROPS`/`*_GRID_VERIFY_PROPS`. Live 5/5, dryrun 767/767, filter fired 24x / cleared 20x (output.xml grep), DB self-clean confirmed 0 residual `AUTOTEST%` rows via a fresh oracledb connection. No shared T1/T2 file changes. This MODIFIES the screen's existing `docs/ec_screen_registry.md` / `docs/automation-scorecard.md` rows (from the 2026-07-26 generator-scaffolded build) — not a new row. |
| Data Extract Set (Configuration > Assets > Data_Mapping_Objects, SP.0049) | ✅ DONE (2026-08-23, Batch 10) | Manage-object OV (Bank family), single-page grid. GENERIC "Code"/"Name" labels (NOT screen-prefixed) confirmed via ec-ui-knowledge/screens/data_extract_set.md + the pre-existing label-driven page object + proven `py/data_extract_set_iud.py`. Mandatory: Code/Name/Start Date + **Owner Class (dropdown, mandatory)** = `All` (confirmed via the screen's own SOW, `screens/.../Data_Extract_Set/data_extract_set_sow.md`: "mandatory extra beyond Code/Name/Start Date: Owner Class"). Owner Class is Insert-only (`objectForm`) - NOT present in `updateAttributes` (Update/Find/Verify-Found compare only Code/Name), same exclusion pattern as Bank's Start Date. Rebuilt `data_extract_set_page.resource`/`data_extract_set_iud.robot` to mirror `bank_page.resource`/`berth_page.resource` exactly: properties-file-driven insert (`Insert Object From Properties And Verify Code`), update, form/grid verify, explicit `Find/Clear Data Extract Set Row By Filter` wired into Update/Find/Verify-Found/Delete, dedicated `DATA_EXTRACT_SET_EC_USER/PASS`, fixed test code `AUTOTEST_DXT` (confirmed free via a fresh oracledb query, before and after the live run). Sibling screen `data_extract_setup_page.resource` (Data Extract Setup, SP.0043) deliberately NOT touched - different screen, out of scope. Live 5/5, dryrun 767/767, robocop parity with the `berth_iud.robot` exemplar (identical 5 DOC02 + 4 VAR02 baseline issues, no regression), filter fired 13x (output.xml grep), DB self-clean confirmed 0 residual `AUTOTEST%` rows in `OV_SUMMARY_SET` via a fresh oracledb connection both before and after. No shared T1/T2 file changes. This MODIFIES the screen's existing `docs/ec_screen_registry.md` / `docs/automation-scorecard.md` rows (from the 2026-07-26 generator-scaffolded build) — not a new row. |
| Trailer (Configuration > Assets > Transport_Objects, CO.0265) | ✅ DONE (2026-08-23, Batch 10) | Plain OV (Bank family), custom grid id `trailer_object:form:T_data` (NOT the shared `manage_object_nav_nav:form:T_data` constant most manage-object screens use - confirmed via the already-proven `py/trailer_iud.py`, kept unchanged from the pre-existing page object). Screen-prefixed labels "Trailer Code"/"Trailer Name" confirmed via direct grep. Rebuilt `trailer_page.resource`/`trailer_iud.robot` to mirror `bank_page.resource`/`berth_page.resource` exactly: properties-file-driven insert (`Insert Object From Properties And Verify Code`), update, form/grid verify, explicit `Find/Clear Trailer Row By Filter` wired into Update/Find/Verify-Found/Delete, dedicated `TRAILER_EC_USER/PASS`, fixed test code `AUTOTEST_TRAILER` (confirmed free live via a fresh oracledb query, before and after the live run). **Licence Plate No confirmed genuinely needed**: it was already part of the proven `py/trailer_iud.py` driver's insert-field set alongside the 3 first-available dropdowns (Trailer Type/UOM/Transport Company); kept it in `trailer_insert.properties` unchanged rather than dropping it, per the Process Train Batch-9 lesson (trust the proven driver's field set over a static scan). The 3 dropdowns are excluded from the round-trip form-label compare list (can re-render Description text instead of code after reload, same convention as Process Train's dropdown). Live 5/5, dryrun 767/767, filter keyword fired 23x (output.xml grep), DB self-clean confirmed 0 residual `AUTOTEST_TRAILER` rows via a fresh oracledb connection. No shared T1/T2 file changes. This MODIFIES the screen's existing `docs/ec_screen_registry.md` / `docs/automation-scorecard.md` rows (from the 2026-07-31 generator-scaffolded build) — not a new row. |

## Batch 11 additions (pending) — Bank Account, Carrier

_Section header pre-created on master before fan-out. Each screen's PR appends
only its own ROW here — no per-PR section header. Note: both screens are on the
OLD hardcoded (`Fill New Object Form`) pattern, a bigger lift than Batch 7-10's
label-driven upgrades; Carrier's nav-free status also needs live recon before
committing to a full conversion (see `tmp/batch11_shared_findings.md`)._

| Screen | Explicit filter wired? / Status | Notes |
|---|---|---|
| Carrier (Configuration > Assets > Cargo_Objects) | ✅ DONE (2026-08-23, Batch 11) | Manage-object OV (Bank family), single-page grid. Screen-prefixed labels "Carrier Code"/"Carrier Name" confirmed via the prior SOW recon (`screens/.../Carrier/carrier_sow.md`) and the proven Playwright driver `screens/.../Carrier/playwright/ec_iud_carrier.py`. Navigator RE-CONFIRMED NOT gated before converting (registry flagged this screen as "unclear at survey time" - live evidence already on file: optional date + GO only, no mandatory dropdown/cascade). Mandatory beyond Code/Name/Start Date: **Unit** (reference dropdown) - filled `__FIRST__` (throwaway record) and deliberately EXCLUDED from `@{CARRIER_FORM_LABELS}` so the known `__FIRST__`-fails-round-trip-verify gotcha (Batch 2 VAT Code) never applies. Rebuilt `carrier_page.resource`/`carrier_iud.robot` from the older hardcoded-field-id (`Fill New Object Form`) pattern to mirror `bank_page.resource`/`berth_page.resource`/`port_page.resource` exactly: properties-file-driven insert (`Insert Object From Properties And Verify Code`), update, form/grid verify, explicit `Find/Clear Carrier Row By Filter` wired into Update/Find/Verify-Found/Delete, dedicated `CARRIER_EC_USER/PASS`, fixed test code `AUTOTEST_CARRIER` (confirmed free via a fresh oracledb query on `OV_CARRIER.CODE`, before and after the live run). Live 5/5, dryrun 772/772, robocop parity with the `port_iud.robot` exemplar (identical 5 DOC02 + 1 VAR02 baseline issues, no regression - both exit 1), filter fired 14x (output.xml grep), DB self-clean confirmed 0 residual `AUTOTEST_CARRIER` rows via a fresh oracledb connection. No shared T1/T2 file changes. This MODIFIES the screen's existing `docs/ec_screen_registry.md` / `docs/automation-scorecard.md` rows (from the 2026-06-19 ec-object-iud-builder build) — not a new row. |
| Bank Account (Configuration > Assets > Financial_Objects) | ✅ DONE (2026-08-23, Batch 11, FINAL batch of the confirmed 23-screen candidate pool) | Manage-object OV (Bank family). STILL on the OLDER hardcoded-field-id pattern (`Fill New Object Form ${BANK_ACCOUNT_INS_CODE} ...`, confirmed via direct grep) - a bigger lift than Batch 7-10's label-driven upgrades, closer to an original Batch 2-6 full rebuild. Code label CONFIRMED live as SCREEN-PREFIXED "Bank Account Code" (30-field objectForm/updateAttributes ECCell dump, `tmp/recon_bankaccount_labels.robot` + `tmp/recon_bankaccount_upd.robot`, not committed). Mandatory beyond Code/Name/Start Date: Sort Code (text) + Bank/Customer/Currency (reference dropdowns) - kept from the screen's own already-proven Playwright driver (`screens/.../Bank_Account/playwright/ec_iud_bank_account.py`, live-PASS 2026-06-11) since Customer's mandatory-ness is a conditional business rule invisible to this pass's static scan (Bank/Currency showed `{mandatory:true}`, Customer showed `{mandatory:false}`) - same Process Train Batch-9 lesson (trust the proven driver's field set). A live Vendor dd (R22) is NOT mandatory and NOT in the proven driver - deliberately omitted (IUD fill only needed fields). Rebuilt `bank_account_page.resource`/`bank_account_iud.robot` from the old pattern to mirror `bank_page.resource`/`berth_page.resource` exactly: properties-file-driven insert (`Insert Object From Properties And Verify Code`), update, form/grid verify, explicit `Find/Clear Bank Account Row By Filter` wired into Update/Find/Verify-Found/Delete, dedicated `BANK_ACCOUNT_EC_USER/PASS`, fixed test code `AUTOTEST_BACC` (confirmed free via a fresh oracledb query, before and after the live run). Bank/Customer/Currency use `__FIRST__`, excluded from the round-trip form-label compare (Storage Flow Batch-10 precedent - a resolved reference value can re-render as different display text after reload). Live 5/5, robocop 7 issues (2 VAR02 + 5 DOC02, same baseline-noise class already accepted throughout this batch series, no regression), dryrun 772/772, filter fired 15x / cleared 15x (output.xml grep), DB self-clean confirmed 0 residual `AUTOTEST%` rows via a fresh oracledb connection. No shared T1/T2 file changes. This MODIFIES the screen's existing `docs/ec_screen_registry.md` row (from the 2026-06-11 Playwright-era build) and ADDS a new row to `docs/automation-scorecard.md` (no prior Bank Account entry there). |

## Phase 3 additions (pending) — Chemical Product, Product, Document Template

_Section header pre-created on master before fan-out. Phase 3 = first batch out
of the Phase 2 recon (19 reconciled unknown screens); these 3 are the only ones
confirmed genuinely Bank-shaped with no navigator-column entry (Commercial
Entity/Company Contact deferred — they have a Navigator-column entry even
though optional/insert-form-only, per owner direction). Chemical Product and
Product are genuinely new (no existing automation) — use
`ec-bank-pattern-new-screen`. Document Template already has existing
generator-scaffolded/Playwright automation (live 4/4 RF + 7/7 Playwright,
2026-07-26) — use `ec-bank-pattern-converter` to upgrade it, not a from-scratch
build. Each screen's PR appends only its own ROW here — no per-PR section
header._

| Screen | Explicit filter wired? / Status | Notes |
|---|---|---|
| Document Template (Configuration > Assets > Revenue_Document_Objects, CD.0013) | ✅ DONE (2026-08-24, Phase 3) | Manage-object OV (Bank family). Already had label-driven RF automation (live 4/4, 2026-07-26) but NOT the Bank-pattern shape (no `Insert Object From Properties`, no `Find <Screen> Row By Filter`) - classified PARTIAL label-driven, upgraded per `ec-bank-pattern-converter`. Mandatory beyond Code/Name/Start Date: Document Title (text) - de-facto mandatory per the already-proven Playwright driver `py/document_template_iud.py` INSERT_FIELDS, even though the screen note called dropdowns "optional" (trusted the proven driver over the static note, per the standing gotcha). Rebuilt `document_template_page.resource`/`document_template_iud.robot` to mirror `bank_page.resource` exactly: properties-file-driven insert (`Insert Object From Properties And Verify Code`), update, form/grid verify, explicit `Find/Clear Document Template Row By Filter` wired into Update/Find/Verify-Found/Delete, dedicated `DOCUMENT_TEMPLATE_EC_USER/PASS`, fixed test code `AUTOTEST_DOCUMENT_TEMPLATE` (confirmed free via a fresh oracledb query on `OV_DOC_TEMPLATE.CODE`, before and after the live run). Delete's End Date field id is resolved BY LABEL at runtime (`OV Field Id By Label`), not hardcoded, since no live objectdates row-shape scan was run this session. Live 5/5, robocop 7 issues (parity vs Bank's 13, no regression), full-tree dryrun 775/775, filter fired 15x (output.xml grep), DB self-clean confirmed 0 residual `AUTOTEST%` rows via a fresh oracledb connection. No shared T1/T2 file changes. This MODIFIES the screen's existing `docs/ec_screen_registry.md` and `docs/automation-scorecard.md` rows (from the 2026-07-26 generator-scaffolded build) - not a new row. |
| Product (Configuration > Assets > Hydrocarbon_Objects, CO.0007) | ✅ DONE (2026-08-24) | Brand-new build, zero prior automation - `ec-bank-pattern-new-screen`. Confirmed NOT the same class as already-automated siblings Product Description (CD.0012)/Product Group (RC.0053) - genuinely `PRODUCT_MAINTAIN`. DB metadata (`resolve_ec_screen.py`): CLASS_TYPE=OBJECT, TIME_SCOPE_CODE=VERSIONED, base `PRODUCT`, view `OV_PRODUCT`. Live scan (`scan_ec_screen.py`) confirmed plain manage-object shape: navigator = single OPTIONAL date field + GO, grid `manage_object_nav_nav:form:T_data` - no navigator-registry entry, eligible per the stricter zero-nav-entry bar. Only Product Code/Product Name/Start Date mandatory; Hydrocarbon Component/Product Group/Product Type/ERP Code are optional dropdowns, left unset. Field label SCREEN-PREFIXED "Product Code"/"Product Name" (not generic "Code"/"Name"), passed as `code_label` throughout. Product Type/ERP Code present on Insert's `objectForm` but absent from `updateAttributes` (confirmed live). Built `product_page.resource`/`product_iud.robot` mirroring `bank_page.resource` exactly: properties-file-driven insert/update/verify, explicit `Find/Clear Product Row By Filter` wired into Update/Find/Verify-Found/Delete, dedicated `PRODUCT_EC_USER/PASS`, fixed test code `AUTOTEST_PRODUCT` (confirmed free via a fresh oracledb query against `PRODUCT.OBJECT_CODE`, before and after the live run). Live 5/5 first attempt, dryrun 779/779, robocop 12 issues (7 VAR02 + 5 DOC02) - parity with Bank's own 13-issue baseline, no regression. Filter fired 15x (output.xml grep). DB self-clean confirmed 0 residual rows across `PRODUCT`/`PRODUCT_VERSION`/`OV_PRODUCT` via a fresh oracledb connection. No shared T1/T2 file changes. This ADDS new rows to `docs/ec_screen_registry.md` and `docs/automation-scorecard.md` (genuinely new screen, no prior entry in either doc). |

| Screen | Explicit filter wired? / Status | Notes |
|---|---|---|
| Chemical Product (Configuration > Assets > Chemical Objects, CO.0072) | ✅ DONE (2026-08-24) | Genuinely new screen — zero prior RF/Playwright automation, built from scratch via `ec-bank-pattern-new-screen`. Manage-object OV (Bank family), versioned, `OV_CHEM_PRODUCT`. Navigator confirmed live via `scan_ec_screen.py`: single optional date field + GO, no mandatory dropdown (`manage_object_nav_nav:form:T_data` grid). Screen-prefixed labels "Chemical Product Code"/"Chemical Product Name" confirmed live. Mandatory beyond Code/Name/Start Date: **Meas. Units** (reference dropdown, not a cascade — confirmed via a live dropdown-options dump, no "Dependent field" banner); literal option `Liter` used (not `__FIRST__`, matching the VAT Code/Sales Order precedent). Fixed test code `AUTOTEST_CHEMPROD` (confirmed free via a fresh oracledb query on `CHEM_PRODUCT.OBJECT_CODE`, before and after the live run). **DELETE — documented EC PRODUCT DEFECT, not an automation gap:** `ec-ui-knowledge/EC_KNOWN_ISSUES.md` ("OV_CHEM_PRODUCT... End=Start delete blocked by a child-FK dependency") already recorded that `CHEM_PRODUCT` auto-creates a 1:1 `CHEM_USAGE_REPORT_CONF` child row on insert with a NO-ACTION FK, which silently blocks the standard zero-length-window UI delete (Save clicks, no error, `OBJECT_END_DATE` stays NULL) — no UI screen exists to remove that child row. Applied that same doc's prescribed fix: a new screen-scoped library `libraries/ChemicalProductCleanup.py` (NOT `resources/manage_object.resource` or `libraries/DbVerify.py` — both left untouched) removes the child row via DB immediately before the normal UI End=Start Save, called from `Delete Chemical Product Record And Save`. This "unparks" the screen for full IUD automation (previously flagged PARKED for delete only; insert/update were already known-fine). Live 5/5 (TC01-05), dryrun 779/779 full-tree, robocop parity with `chemical_transport_tank_iud.robot` (11 issues both, all DOC02 baseline noise, no regression). DB self-clean confirmed via a fresh oracledb connection: 0 residual `AUTOTEST%` rows in `CHEM_PRODUCT`/`OV_CHEM_PRODUCT`, 0 orphaned `CHEM_USAGE_REPORT_CONF` rows. Filter fired 26x, child-cleanup keyword fired (output.xml grep). No shared T1/T2 file changes. This ADDS a new row to `docs/ec_screen_registry.md` and `docs/automation-scorecard.md` (no prior Chemical Product entry in either). |

## Disposition Type reclassification + conversion (2026-08-24)

| Screen | Explicit filter wired? / Status | Notes |
|---|---|---|
| Disposition Type (Configuration > Assets > Hydrocarbon_Objects, CO.0208) | ✅ DONE (2026-08-24) | **Correction to this doc's own "Excluded" table**: Disposition Type was previously grouped with Document Date Term/Payment Term/Choke/Choke Model as "mandatory single date + GO before grid loads". Live evidence (this pass) shows that's wrong for THIS screen - the navigator has NO date field at all, just a bare GO button (`Apply Navigator`/`css=[id="button:form:B"]`) to populate the grid (matches `ec-ui-knowledge/screens/disposition_type.md`: "Grid needs GO to load" — no mandatory date filter documented or found). Reclassified as genuinely Bank-shaped and converted; moved out of "Excluded" above. Was an OLD hardcoded-field-id build (`Fill EC Field` with raw `R:n:C:n` ids, unique `AUTOTEST_DISP_<timestamp>` code, one shared Suite Setup login, inline DB-verify calls in the test file, only 4 TCs/no Find) - rebuilt `disposition_type_page.resource`/`disposition_type_iud.robot` to mirror `bank_page.resource`/`berth_page.resource` exactly: properties-file-driven insert (`Insert Object From Properties And Verify Code`), update, form/grid verify, explicit `Find/Clear Disposition Type Row By Filter` wired into Update/Find/Verify-Found/Delete, dedicated `DISPOSITION_TYPE_EC_USER/PASS`, fixed test code `AUTOTEST_DISPOSITION_TYPE` (confirmed free via a fresh oracledb query on `OV_DISPOSITION_TYPE.CODE`, before and after the live run). Screen-prefixed labels "Disposition Code"/"Disposition Name" confirmed via the already-proven Playwright driver `py/disposition_type_iud.py` (left untouched, RF-only task) - `code_label=Disposition Code` threaded through every T2 call. Only Disposition Code/Disposition Name/Start Date/Description used (matches the proven driver's own INSERT_FIELDS/UPDATE_FIELDS - Sort Order/Product left out, fill-only-needed-fields convention). Removed the prior build's inline DB-verify calls (`Field Should Equal In View`, `Disposition Type Should Exist/Not Exist In DB`) from the test file entirely, per Bank's own documented PURE SCREEN verification convention. Added the missing TC04 Find test case (prior suite had only 4 TCs). Live 5/5, robocop 11 issues (parity vs Bank's 13-issue baseline, no regression), full-tree dryrun 793/793, filter fired 15x (output.xml grep), DB self-clean confirmed 0 residual `AUTOTEST%` rows in `OV_DISPOSITION_TYPE` via a fresh independent oracledb connection. No shared T1/T2 file changes. This MODIFIES the screen's existing `docs/ec_screen_registry.md` / repo-root `docs/automation-scorecard.md` rows (from the 2026-07-25 build) - not a new row. Playwright bundle (`py/disposition_type_iud.py`, `screens/.../Disposition_Type/`) deliberately left untouched - RF-only task. |

## Document Received Term reclassification + conversion (2026-08-24)

| Screen | Explicit filter wired? / Status | Notes |
|---|---|---|
| Document Received Term (Configuration > Assets > Date Objects, CD.0108) | ✅ DONE (2026-08-24) | **Note on this doc's "Excluded" table**: Document Received Term was never itself named in the "Excluded" table (only its sibling Document Date Term was, as part of the "Document Date Term, Payment Term, Choke, Choke Model" mandatory-date group) — but its own SOW documents it as an identical-shape sibling, so it was reasonable to assume the same exclusion applied. A live navigator scan (`tmp/scripts/scan_ec_screen.py`, 2026-08-24) shows that assumption does NOT hold for THIS screen: the navigator is a single date field with `mandatory=False` + a bare GO button — the standard OPTIONAL Bank-family navigator (same shape as Bank/Report Area/Port), not a genuine mandatory-date gate. Converted on that live evidence; Document Date Term itself is unaffected by this note (out of this screen's scope — see the sibling PR for that screen's own classification). Was an OLD hardcoded-field-id build (`Fill New Object Form` with raw `objectForm`/`updateAttributes` field ids, one shared Suite Setup login, no filter wiring, only 4 TCs/no Find) — rebuilt `document_received_term_page.resource`/`document_received_term_iud.robot` to mirror `bank_page.resource`/`berth_page.resource`/`port_page.resource` exactly: properties-file-driven insert (`Insert Object From Properties And Verify Code`), update, form/grid verify, explicit `Find/Clear Document Received Term Row By Filter` wired into Update/Find/Verify-Found/Delete, dedicated `DOCUMENT_RECEIVED_TERM_EC_USER/PASS`, fixed test code `AUTOTEST_DRT` (confirmed free via a fresh oracledb query on `OV_DOC_RECEIVED_TERM.CODE`, before and after the live run). GENERIC "Code"/"Name" labels (NOT screen-prefixed) confirmed via a live objectForm field-inventory scan. Mandatory beyond Code/Name/Start Date: **"Date Calculation Method"** autocomplete dropdown (test literal `Manual entry`) + numeric **Offset** field (test `0`) — both confirmed live and matching the already-proven Playwright driver `py/ec_iud_document_received_term.py`'s field set. Added the missing TC04 Find test case (prior suite had only 4 TCs). Live 5/5, robocop 9 issues (parity vs Bank/Berth's baseline DOC02/VAR02 noise class, no regression), full-tree dryrun 794/794, filter fired 23x (output.xml grep), DB self-clean confirmed 0 residual `AUTOTEST%` rows in `OV_DOC_RECEIVED_TERM` via a fresh independent oracledb connection. No shared T1/T2 file changes. This MODIFIES the screen's existing `docs/ec_screen_registry.md` row (from the 2026-06-28 build) — not a new row. Playwright bundle (`py/ec_iud_document_received_term.py`, `screens/.../Document_Received_Term/`) deliberately left untouched — RF-only task. |
## Payment Term reclassification + conversion (2026-08-24)

| Payment Term (Configuration > Assets > Date Objects, CD.0023) | ✅ DONE (2026-08-24) | **Correction to this doc's own "Excluded" table**: Payment Term was previously grouped with Document Date Term/Choke/Choke Model as "mandatory single date + GO before grid loads". Live evidence (this pass) shows that's wrong for THIS screen — the navigator is a single Date field pre-filled with today's date + GO, the SAME "date nav + GO to load" shape already documented for Berth/Port (`docs/ec_screen_registry.md`) and already proven NOT disqualifying for those two. Confirmed live via the screen's own proven Playwright driver (`screens/.../Payment_Term/playwright/ec_iud_payment_term.py`) and its evidence screenshots (`evidence/pt_01_loaded.png`/`pt_02_clean_state.png`): the grid is checked/acted on immediately after opening the screen, with NO Apply Navigator/GO click first — the grid auto-populates on open. Re-confirmed this session: the rebuilt RF suite's TC01 "Verify Clean State" (which also never calls Apply Navigator) passed live on the first attempt. Reclassified as genuinely Bank-shaped and converted; moved out of "Excluded" above (Document Date Term/Choke/Choke Model left untouched — no own evidence gathered for those this pass). Was an OLD hardcoded-field-id build (`Fill New Object Form` with raw `R:n:C:n` ids, one shared Suite Setup login, 4 TCs/no Find, inline `Payment Term Should Exist/Not Exist In DB` calls in the test file) — rebuilt `payment_term_page.resource`/`payment_term_iud.robot` to mirror `bank_page.resource`/`berth_page.resource`/`disposition_type_page.resource` exactly: properties-file-driven insert (`Insert Object From Properties And Verify Code`), update, form/grid verify, explicit `Find/Clear Payment Term Row By Filter` wired into Update/Find/Verify-Found/Delete, dedicated `PAYMENT_TERM_EC_USER/PASS`, fixed test code `AUTOTEST_PAYMENT_TERM` (confirmed free via a fresh oracledb query on `OV_PAYMENT_TERM.CODE`, before and after the live run). Real on-screen field labels confirmed via the proven driver's own evidence screenshots (`evidence/pt_04_insert_filled.png` insert form, `pt_07_update_filled.png` update form): "Payment Term Code" is screen-prefixed but "Name" is the GENERIC label (like Bank); the mandatory dropdown/number are labelled "Date Calculation Method"/"Number Of Days" on screen — NOT "Method"/"Offset"/"Day Value" as the driver's own internal variable names might suggest. Only Payment Term Code/Name/Start Date/Date Calculation Method/Number Of Days used (matches the proven driver's own field set — ERP Code/Description/Comments/Calculation left out, fill-only-needed-fields convention). Removed the prior build's inline DB-verify calls from the test file entirely, per Bank's own documented PURE SCREEN verification convention. Added the missing TC04 Find test case (prior suite had only 4 TCs). Live 5/5, robocop 9 issues (4 VAR02 + 5 DOC02, parity vs Disposition Type's 11-issue baseline, no regression), full-tree dryrun 794/794, filter fired 5x / cleared 5x (output.xml grep), DB self-clean confirmed 0 residual `AUTOTEST%` rows in `OV_PAYMENT_TERM`/`PAYMENT_TERM` via a fresh independent oracledb connection. No shared T1/T2 file changes. Playwright bundle (`screens/.../Payment_Term/playwright/ec_iud_payment_term.py`) deliberately left untouched — RF-only task. This MODIFIES the screen's existing `docs/ec_screen_registry.md` / repo-root `docs/automation-scorecard.md` rows (from the 2026-06-28 build) — not a new row. |
## Document Date Term reclassification + conversion (2026-08-24)

| Document Date Term (Configuration > Assets > Date_Objects, CD.0107) | ✅ DONE (2026-08-24) | **Correction to this doc's own "Excluded" table**: Document Date Term was previously grouped with Payment Term/Choke/Choke Model as "mandatory single date + GO before grid loads". Live evidence (this pass, `tmp_recon_ddt.py`, discarded after use) shows that's wrong for THIS screen - the grid AUTO-LOADS on screen open (20 rows visible BEFORE any navigator GO click, unchanged after GO), the same shape as Bank, not a GO-gated or mandatory-date-gated grid. Reclassified as genuinely Bank-shaped and converted; moved out of "Excluded" above. Was an OLD hardcoded-field-id build (`Fill New Object Form` with raw `R:n:C:n` ids, a generated unique `AUTOTEST_DDT_<timestamp>` code, one shared Suite Setup login, inline DB-verify calls in the test file, only 4 TCs/no Find) - rebuilt `document_date_term_page.resource`/`document_date_term_iud.robot` to mirror `bank_page.resource`/`berth_page.resource`/`disposition_type_page.resource` exactly: properties-file-driven insert (`Insert Object From Properties And Verify Code`), update, form/grid verify, explicit `Find/Clear Document Date Term Row By Filter` wired into Update/Find/Verify-Found/Delete, dedicated `DOCUMENT_DATE_TERM_EC_USER/PASS`, fixed test code `AUTOTEST_DOCUMENT_DATE_TERM` (confirmed free via a fresh oracledb query on `OV_DOC_DATE_TERM.CODE`, before and after the live run). Labels confirmed live 2026-08-24 (`tmp_recon_ddt.py`): Code/Name/Start Date are GENERIC (not screen-prefixed, unlike Disposition Type); the two screen-specific mandatory New-Object extras are **'Date Calculation Method'** (autocomplete dropdown) and **'Offset'** (mandatory numeric cell) — the prior SOW/page object's placeholder label "Method" was NOT the real live label, corrected here. `Insert Object From Properties`'s existing label-driven-any-kind field resolution handled both extras with zero special-case code (dropdown auto-detected via `dd_input`, Offset as a plain text field). Test values match the already-proven Playwright driver (`screens/.../Document_Date_Term/playwright/ec_iud_document_date_term.py`): `Date Calculation Method=Set Document Date manually`, `Offset=0`. Removed the prior build's inline DB-verify calls (`Document Date Term Should Exist/Not Exist In DB`, `Field Should Equal In View`) from the test file entirely, per Bank's own documented PURE SCREEN verification convention. Added the missing TC04 Find test case (prior suite had only 4 TCs). Live 5/5 (2 earlier attempts hit transient login/session timeouts unrelated to this build - same class of flakiness seen in the sandbox's own recon script; a leftover `AUTOTEST_DOCUMENT_DATE_TERM` row from one such transient TC05 failure was cleaned up via the suite's own TC05 keyword, then a clean full run passed 5/5), robocop 9 issues (parity/better than Disposition Type's own 11-issue baseline, no regression), full-tree dryrun 794/794, filter fired 14x (`Find Document Date Term Row By Filter`, output.xml grep). DB self-clean confirmed 0 residual `AUTOTEST%` rows in `OV_DOC_DATE_TERM` via a fresh independent oracledb connection. No shared T1/T2 file changes. This MODIFIES the screen's existing `docs/ec_screen_registry.md` row (from the 2026-06-28 pilot build) - not a new row. Playwright bundle (`playwright/ec_iud_document_date_term.py`, `screens/.../Document_Date_Term/`) and the SOW deliberately left untouched - RF-only task. |

## Choke Model reclassification + conversion (2026-08-24)

| Screen | Explicit filter wired? / Status | Notes |
|---|---|---|
| Choke Model (Configuration > Assets > Stream Objects, CO.0217) | ✅ DONE (2026-08-24) | **Correction to this doc's own "Excluded" table**: Choke Model was previously grouped with Document Date Term/Payment Term/Choke as "mandatory single date + GO before grid loads". Live evidence (this pass) shows that's wrong for THIS screen: (1) `tmp/scripts/scan_ec_screen.py` live scan shows the navigator is a single date field with `mandatory=False` + one GO button (`navigator: {'fields': [{'grp': 'G:0', 'kind': 'da_input', 'mandatory': False}], 'go': ['button:form:B']}`); (2) a dedicated grid-row-count recon (`tmp/recon_choke_model_grid.py`, discarded after use) showed 18 grid rows visible BEFORE any GO click, unchanged after GO - the grid AUTO-LOADS on screen open, the same shape as Bank/Document Date Term, not a genuine mandatory-date gate. DB confirms `CHOKE_MODEL` CLASS_TYPE=OBJECT/TIME_SCOPE_CODE=VERSIONED (OV, Bank family), view `OV_CHOKE_MODEL`. Reclassified as genuinely Bank-shaped and converted; moved out of "Excluded" above (Choke itself left untouched - no own evidence gathered for it this pass, per the owner's explicit instruction not to probe that sibling). Was an OLD hardcoded-field-id build (`Fill EC Field` with raw `R:n:C:n` ids, a generated unique `AUTOTEST_CHKM_<timestamp>` code, one shared Suite Setup login, inline `Choke Model Should Exist/Not Exist In DB`/`Field Should Equal In View` calls in the test file, only 4 TCs/no Find) - rebuilt `choke_model_page.resource`/`choke_model_iud.robot` to mirror `bank_page.resource`/`payment_term_page.resource`/`disposition_type_page.resource` exactly: properties-file-driven insert (`Insert Object From Properties And Verify Code`), update, form/grid verify, explicit `Find/Clear Choke Model Row By Filter` wired into Update/Find/Verify-Found/Delete, dedicated `CHOKE_MODEL_EC_USER/PASS`, fixed test code `AUTOTEST_CHOKE_MODEL` (confirmed free via a fresh oracledb query on `OV_CHOKE_MODEL.CODE`, before and after the live run). Labels confirmed live via a fresh objectForm field-inventory scan (`screens/.../Choke_Model/investigation/recon.py`, re-run this session): SCREEN-PREFIXED "Choke Model Code"/"Choke Model Name" (matches Disposition Type's style, not Payment Term's generic "Name"); mandatory beyond Code/Name/Start Date: NONE (`MANDATORY DROPDOWNS: NONE`, all 9 optional dropdowns - Parent Choke Model/Condition/Measurement Type/Loss Accounting/Op Production Unit/Op Area/Op Facility Class 1/Geo Area/Geo Field - left unset, fill-only-needed-fields convention). Update fields (Name + Description) reused unchanged from the prior build's own already-working `updateAttributes` field ids (R1:C1/R3:C1). Removed the prior build's inline DB-verify calls from the test file entirely, per Bank's own documented PURE SCREEN verification convention. Added the missing TC04 Find test case (prior suite had only 4 TCs). Live 5/5 first attempt, robocop 10 issues (5 VAR02 + 5 DOC02, parity vs Payment Term's 9-issue baseline, no regression), full-tree dryrun 798/798, filter fired 15x (output.xml grep), DB self-clean confirmed 0 residual `AUTOTEST%` rows in `OV_CHOKE_MODEL` via a fresh independent oracledb connection. No shared T1/T2 file changes. This MODIFIES the screen's existing `docs/ec_screen_registry.md` / repo-root `docs/automation-scorecard.md` rows (from the 2026-07-26 build, which had live 4/4 RF + 7/7 Playwright and a `verify_screen.py` OVERALL PASS) - not a new row. Playwright bundle (`py/choke_model_iud.py`, `screens/.../Choke_Model/`) deliberately left untouched - RF-only task. |
## Choke reclassification + conversion (2026-08-25)

| Choke (Configuration > Assets > Well and Reservoir Objects, CO.0185) | ✅ DONE (2026-08-25) | **Correction to this doc's own "Excluded" table — closes out the entire original 5-screen group.** Choke was originally grouped with Document Date Term/Payment Term/Choke Model as "mandatory single date + GO before grid loads". That grouping is now proven wrong for ALL 5 originally-grouped screens, individually re-verified: Disposition Type (GO-only, no date field, converted PR — see section above), Payment Term (date pre-filled, no real gate, converted), Document Date Term (grid auto-loads before GO, converted), Choke Model (grid auto-loads, 18 rows visible pre-GO, converted — PR #497, base branch `feature/choke-model-bank-pattern`, not yet merged at the time of this Choke pass), and now Choke itself (this entry) — the last unverified member. Live evidence gathered fresh this pass (`scan_ec_screen.py` + a dedicated grid-row-count recon, both discarded after use): navigator is a single date field with `mandatory=False` + one GO button (`{'fields': [{'grp': 'G:0', 'kind': 'da_input', 'mandatory': False}], 'go': ['button:form:B']}`); the grid AUTO-LOADS on screen open — real seed data ("P1 C001") visible BEFORE any GO click, unchanged after GO — the same shape as Bank/Choke Model/Document Date Term, not a genuine mandatory-date gate. DB confirms `CHOKE` CLASS_TYPE=OBJECT/TIME_SCOPE_CODE=VERSIONED (OV, Bank family), view `OV_CHOKE`; deep-dive note `DeepDiveLearnings/ec-screens/notes/CO.0185.md` corroborates the same binding. Reclassified as genuinely Bank-shaped and converted; the "Choke, Choke Model" row removed from "Excluded" above (both now resolved). Was an OLD hardcoded-field-id build (`Fill EC Field` with raw `R:n:C:n` ids, a generated unique `AUTOTEST_CHK_<timestamp>` code, one shared Suite Setup login, inline `Field Should Equal In View` calls in the test file, only 4 TCs/no Find) — rebuilt `choke_page.resource`/`choke_iud.robot` to mirror `bank_page.resource`/`choke_model_page.resource`/`payment_term_page.resource` exactly: properties-file-driven insert (`Insert Object From Properties And Verify Code`), update, form/grid verify, explicit `Find/Clear Choke Row By Filter` wired into Update/Find/Verify-Found/Delete, dedicated `CHOKE_EC_USER/PASS`, fixed test code `AUTOTEST_CHOKE` (confirmed free via a fresh oracledb query on `OV_CHOKE.CODE`, before and after the live run). Labels confirmed live via `scan_ec_screen.py`'s objectForm field-inventory dump: SCREEN-PREFIXED "Choke Code"/"Choke Name" (matches Choke Model/Disposition Type's style); mandatory beyond Code/Name/Start Date: NONE (Choke Type dropdown/Critical Opening/End Date all optional, confirmed `mandatory=False` live). Update fields (Choke Name + Comments) reused unchanged from the prior build's own already-working `updateAttributes` field ids and the already-proven Playwright driver's own `UPDATE_FIELDS` (`py/choke_iud.py`, left untouched). Removed the prior build's inline DB-verify calls from the test file entirely, per Bank's own documented PURE SCREEN verification convention. Added the missing TC04 Find test case (prior suite had only 4 TCs). Live 5/5 first attempt, robocop 10 issues (5 VAR02 + 5 DOC02, parity with Choke Model's own baseline, no regression), full-tree dryrun 798/798, filter fired 15x / cleared 15x (output.xml grep), DB self-clean confirmed 0 residual `AUTOTEST%` rows in `OV_CHOKE` via a fresh independent oracledb connection. No shared T1/T2 file changes. This MODIFIES the screen's existing `docs/ec_screen_registry.md` / repo-root `docs/automation-scorecard.md` rows (from the 2026-07-25 build, which had live 4/4 RF + 7/7 Playwright and a `verify_screen.py` OVERALL PASS) — not a new row. Playwright bundle (`py/choke_iud.py`) and the SOW (`screens/.../Choke/choke_sow.md`) deliberately left untouched — RF-only task. |

## EC Code Object conversion (2026-08-24)

| Screen | Explicit filter wired? / Status | Notes |
|---|---|---|
| EC Code Object (Configuration > Codes, CD.0135) | ✅ DONE (2026-08-24) | PARTIAL label-driven -> full Bank pattern (PR #494): properties-file-driven insert/update/verify, explicit `Find/Clear EC Code Object Row By Filter` wired into Update/Find/Verify-Found/Delete, fixed `AUTOTEST_EC_CODE_OBJECT`, per-TC Login/Logout, added missing TC04. De-facto-mandatory `EC Code Type`/`EC Code` dropdowns (`__FIRST__`, per the proven `py/ec_code_object_iud.py` driver - the registry's stale "optional dropdowns skipped" note corrected in the same PR), excluded from the form-label compare. Delete End Date resolved BY LABEL at runtime (single-date objectdates row, unlike Bank's packed row). Live 5/5, dryrun 797/797, robocop parity vs Disposition Type, DB self-clean 0 residual via fresh connection. No shared T1/T2 changes. _Row added by the reviewer at merge - PR omitted the checklist updates._ |

## Report Group conversion (2026-08-24)

| Screen | Explicit filter wired? / Status | Notes |
|---|---|---|
| Report Group (Configuration > Assets > Facility_Objects, CO.0158) | ✅ DONE (2026-08-24) | PARTIAL label-driven -> full Bank pattern: properties-file-driven insert/update/verify, explicit `Find/Clear Report Group Row By Filter` wired into Update/Find/Verify-Found/Delete, fixed test code `AUTOTEST_REPORT_GROUP` (confirmed free via a fresh oracledb query on `OV_REPORT_GROUP.CODE`, before and after the live run), per-TC Login/Logout, added the missing TC04 Find test case (prior suite had only 4 TCs). Screen-prefixed labels "Reporting Group Code"/"Reporting Group Name" confirmed via the prior partial build's own `Fill OV Field By Label` usage. Grid id `report_group_table:form:T_data` is Report Group's OWN PrimeFaces widget id, kept as a screen-local constant (NOT the shared `${OV_MANAGE_OBJECT_TABLE}` = `manage_object_nav_nav:form:T_data` that Bank/Disposition Type/County/Payment Term use) — confirmed live via the already-proven `py/report_group_iud.py` driver and the prior T3 build, not assumed from a sibling. De-facto-mandatory `Business Area` dropdown filled `__FIRST__` (per the proven driver), excluded from the form-label compare (same documented exclusion rule as every other `__FIRST__`-filled dropdown on this project). Removed the prior build's inline DB-verify calls (`Report Group Should Exist/Not Exist In DB`) from the test file entirely, per Bank's own documented PURE SCREEN verification convention. Live 5/5, robocop 10 issues (parity vs Disposition Type's 11-issue baseline, no regression), full-tree dryrun 798/798, filter fired 15x (output.xml grep). DB self-clean confirmed 0 residual `AUTOTEST%` rows in `OV_REPORT_GROUP` via a fresh independent oracledb connection. No shared T1/T2 file changes. This MODIFIES the screen's existing `docs/ec_screen_registry.md` row (from the 2026-07-31 build) - not a new row. Playwright bundle (`py/report_group_iud.py`) and the SOW deliberately left untouched - RF-only task. |

## Document Sequence conversion (2026-08-25)

| Screen | Explicit filter wired? / Status | Notes |
|---|---|---|
| Document Sequence (Configuration > Assets > Revenue_Document_Objects, CD.0109) | DONE (2026-08-25) | Custom-URL OV converted to full Bank pattern (PR #506) per the Report Context precedent: properties-file-driven insert/update/verify, explicit `Find/Clear Document Sequence Row By Filter`, fixed `AUTOTEST_DOCUMENT_SEQUENCE` (replacing the old timestamped code), per-TC Login/Logout, added TC04, zero inline DB-verify calls. Live 5/5, dryrun 811/811, robocop parity vs Report Context, self-clean 0 residual via fresh connection. Playwright driver unchanged (re-run PASS). _Row added by the reviewer at merge - PR omitted the checklist updates (R38)._ |

## Input List conversion (2026-08-25)

| Screen | Explicit filter wired? / Status | Notes |
|---|---|---|
| Input List (Configuration > Assets > Revenue_Lists, CD.0035) | ✅ DONE (2026-08-25) | Generated/label-driven build -> full Bank pattern. Prior 2026-07-26 suite deviated from Bank's convention on multiple counts: called DB-verify keywords (`Input List Should Exist In DB`/`Should Not Exist In DB`/`Field Should Equal In View`) directly from the .robot test cases (not pure-screen verification), used a generated unique `AUTOTEST_IL_<timestamp>` code, only 4 TCs (no Find), and one Suite-level login instead of per-TC. Rebuilt `input_list_page.resource`/`input_list_iud.robot` to mirror `bank_page.resource`/`object_list_page.resource` exactly: properties-file-driven insert/update/verify (`testdata/input_list_{insert,update,form_verify,grid_verify}.properties`), explicit `Find/Clear Input List Row By Filter` wired into Update/Find/Verify-Found/Delete, fixed test code `AUTOTEST_INPUTLIST` (confirmed free via a fresh oracledb query on `OV_STREAM_ITEM_COLLECTION.CODE` before the live run), per-TC Login/Logout, dedicated `INPUT_LIST_EC_USER`/`INPUT_LIST_EC_PASS` (additive to `resources/credentials.py`). Mandatory `List Category` dropdown (maps to `CATEGORY` column) filled with `INPUT` - confirmed a real, already-used domain value via a fresh DB query (`DISTINCT CATEGORY` includes `INPUT` alongside `ALL`/`OVERVIEW`/`DAY_INPUT`/`MONTH_INPUT`/`DAY_OVERVIEW`/`MONTH_OVERVIEW`) and cross-confirmed against the already-proven Playwright driver (`py/input_list_iud.py` INSERT_FIELDS), so excluded from the form-label round-trip compare like every other resolved dropdown value. Live 5/5 (TC01-05), robocop 10 issues (parity with `object_list_iud.robot`'s own 10-issue baseline, DOC02/VAR02 only, no regression), full-tree dryrun 842/842, filter fired 15x (output.xml grep). DB self-clean confirmed 0 residual `AUTOTEST%` rows in `OV_STREAM_ITEM_COLLECTION` via a fresh independent oracledb connection. No shared T1/T2 file changes. This MODIFIES the screen's existing `docs/ec_screen_registry.md` / repo-root `docs/automation-scorecard.md` rows (from the 2026-07-26 generator-scaffolded build, which had live 4/4 RF + 7/7 Playwright and a `verify_screen.py` OVERALL PASS) - not a new row. Playwright bundle (`py/input_list_iud.py`) deliberately left untouched - RF-only task. |
## UOP Key conversion (2026-08-25)

| UOP Key (Configuration > Assets > Revenue_Lists, CD.0099) | DONE (2026-08-25) | OLD generator-scaffolded build (2026-07-26) had the exact inline-DB-verify deviation from Issue #504's sweep - `UOP Key Should Exist/Not Exist In DB` and `Field Should Equal In View` all called DIRECTLY in `uop_key_iud.robot`'s TC02/TC03/TC04, plus dynamic `AUTOTEST_UOP_<timestamp>` codes, only 4 TCs (no Find), and single Suite Setup/Teardown login. Confirmed identity live: URL `manage_object_nav/CLASS_NAME/FIN_UOP_DEPR_KEY`, `class_cnfg` CLASS_TYPE=OBJECT/TIME_SCOPE_CODE=VERSIONED (same shape as `BANK`/`BANK_ACCOUNT`), view `OV_FIN_UOP_DEPR_KEY`; layout confirmed Bank-shaped via the existing deep-dive screenshot (plain Date+GO navigator, Code/Name/Start Date/End Date grid, New Version form). Rebuilt `uop_key_page.resource`/`uop_key_iud.robot` to mirror `bank_page.resource` exactly: properties-file-driven insert (`Insert Object From Properties And Verify Code`)/update/form-verify/grid-verify (4 new `testdata/uop_key_*.properties` files), explicit `Find/Clear UOP Key Row By Filter` wired into Update/Find/Verify-Found/Delete, dedicated `UOP_KEY_EC_USER/PASS` (additive-only in `resources/credentials.py`), fixed test code `UOP_AUTOTEST` (confirmed free via a fresh oracledb query on `OV_FIN_UOP_DEPR_KEY.CODE` before the live run), per-TC Login/Logout with `Test Teardown Ensure Logged Out From EC Application`, added the missing TC04 Find test case. Mandatory `Company`/`Key Type` dropdowns filled `__FIRST__` (carried over from the proven prior driver), excluded from the form-label compare set (`@{UOP_KEY_FORM_LABELS}` = Code/Name/Description/Comment/Key Number). DELETE End Date id resolved LIVE via `OV Field Id By Label` at runtime (NOT hardcoded like Bank's packed-row id - UOP Key's objectdates row has a different shape, proven live by the prior 2026-07-26 driver's own label-driven fill, not a fresh guess). Removed all 3 inline DB-verify calls from the test file entirely, per Bank's own documented PURE SCREEN verification convention. Live 5/5 (`tmp/live_uop_key/output.xml`), robocop 9 issues (5 VAR02 + 4 DOC02, fewer than Bank's own 13-issue baseline, no regression), full-tree dryrun 842/842, filter fired 15x / screen-specific wrapper fired 14x (output.xml grep). DB self-clean confirmed 0 residual `UOP_AUTOTEST`/`AUTOTEST%` rows in `OV_FIN_UOP_DEPR_KEY` via a fresh independent oracledb connection (dsn `localhost:1521/ORCL`). No shared T1/T2 file changes. This MODIFIES the screen's existing `docs/ec_screen_registry.md` row (from the 2026-07-26 build) and the repo-root `docs/automation-scorecard.md` row - not a new row. Playwright bundle (`py/uop_key_iud.py`) and the SOW deliberately left untouched - RF-only task. |
## Data Extract Setup conversion (2026-08-25)

| Data Extract Setup (Configuration > Assets > Data_Mapping_Objects, SP.0043) | ✅ DONE (2026-08-25) | Manage-object OV (Bank family), single-page grid. SCREEN-PREFIXED "Data Extract Setup Code"/"Data Extract Setup Name" labels (NOT generic "Code"/"Name" - confirmed via ec-ui-knowledge/screens/data_extract_setup.md + the New Object form screenshot DeepDiveLearnings/ec-screens/notes/SP.0043_shot_1.png; mirrors State's own screen-prefixed precedent, not a universal Bank-family convention). Mandatory: Data Extract Setup Code/Name/Start Date + **Data Extract Type (dropdown, mandatory)** = `Actual` (confirmed via ec-ui-knowledge/screens/data_extract_setup.md and DB recon: `SUMMARY_SETUP_VERSION.SUMMARY_TYPE` distinct = ACTUAL/FORECAST). Data Extract Type is Insert-only (`objectForm`) - NOT present in `updateAttributes` (Update/Find/Verify-Found compare only Code/Name), same exclusion pattern as Bank's Start Date / Data Extract Set's Owner Class. Rebuilt `data_extract_setup_page.resource`/`data_extract_setup_iud.robot` to mirror `bank_page.resource`/`state_page.resource`/`data_extract_set_page.resource` exactly: properties-file-driven insert (`Insert Object From Properties And Verify Code`), update, form/grid verify, explicit `Find/Clear Data Extract Setup Row By Filter` wired into Update/Find/Verify-Found/Delete, dedicated `DATA_EXTRACT_SETUP_EC_USER/PASS`, fixed test code `AUTOTEST_DXS` (confirmed free via a fresh oracledb query, before and after the live run), added the missing TC04 Find test case (old suite had only 4 TCs). REMOVED the old suite's inline `Field Should Equal In View OV_SUMMARY_SETUP` DB-verify call that sat directly in the .robot file (TC03) - the same deviation class already found/fixed on County/HCB System/Document Template/Document Sequence/Calculation Context/Royalty Depositor/Stream Item Category/Input List/UOP Key this project, missed by GitHub Issue #504's grep sweep because this page object was still on the OLD label-driven-no-properties pattern (not one already using `Insert Object From Properties`). Sibling screen `data_extract_set_page.resource` (Data Extract Set, SP.0049) deliberately NOT touched - different screen, already converted separately (Batch 10). Live 5/5, dryrun 842/842, robocop parity with the `data_extract_set_iud.robot` exemplar (identical 5 DOC02 + 4 VAR02 baseline issues, no regression), filter fired 13x (output.xml grep), DB self-clean confirmed 0 residual `AUTOTEST%` rows in `OV_SUMMARY_SETUP` via a fresh oracledb connection after the live run. No shared T1/T2 file changes. This MODIFIES the screen's existing `docs/ec_screen_registry.md` / `docs/automation-scorecard.md` rows (from the 2026-07-26 generator-scaffolded build) - not a new row. |

## HCB System conversion (2026-08-25)

| Screen | Explicit filter wired? / Status | Notes |
|---|---|---|
| HCB System (Configuration > Assets > Revenue_Lists, CD.0097) | DONE (2026-08-25) | Converted from label-driven-only to full Bank pattern (PR #517): properties-file-driven insert/update/verify, explicit filter wiring, fixed `AUTOTEST_HCB` (was timestamped), per-TC Login/Logout, inline DB-verify removed in the same pass. Live recon corrected the stale KB claim - `HCB Category` IS mandatory (MandatoryCellStyle scan), reflected in insert properties + `ec-ui-knowledge/screens/hcb_system.md`. Live 5/5, dryrun 842/842, self-clean 0 residual in `OV_BALANCE` via fresh connection. No shared T1/T2 changes. _Row added by the reviewer at merge - PR omitted both checklists (R38)._ |

## Area structural conversion - owner-directed OV-GM exception (2026-08-25)

| Screen | Explicit filter wired? / Status | Notes |
|---|---|---|
| Area (Configuration > Assets > Basic_Objects, OV-GM) | STRUCTURE ONLY (2026-08-25, owner-directed one-off) | **NOT an eligibility reclassification - Area is and remains OV-GM** (mandatory Production Unit navigator + GO), and the OV-GM exclusion for the rest of that family is unchanged. Per the owner's exact 5-TC skeleton, PR #521 converted the RF suite's STRUCTURE to the Bank shape (5 TCs incl. TC04 Find, per-TC Login/Logout, fixed `AUTOTEST_AREA`, properties-file-driven insert/update, explicit filter wiring, pure-screen verification) while keeping the real PU+GO gesture in every TC (`Open Area Screen With Navigator Values Populated`). Live 5/5, dryrun 846/846, self-clean 0 residual in `OV_AREA` via fresh connection. _Row added by the reviewer at merge to satisfy the R38 four-doc gate; the PR itself deliberately left this doc untouched per the owner directive, and this row records the exception without touching the OV-GM exclusion tables._ **2026-08-26 (PR #523):** navigator fill now delegates to the shared T2 `Apply Navigator From Properties` (`resources/manage_object.resource`, additive-only shared-file change with R12 canaries: well 4/4 + test_separator 4/4 live, dryrun 846/846), driven by `testdata/area_navigator.properties` - same id/value as the previous inline fill. |

## OV-GM Area-parity conversions (2026-08-26) - External Location, Field, Facility Class 1

| Screen | Explicit filter wired? / Status | Notes |
|---|---|---|
| External Location (Configuration > Assets > Facility_Objects, OV-GM) | STRUCTURE (2026-08-26, Area role-model rule) | **OV-GM, not a plain-Bank reclassification** - GO-only navigator (no mandatory scope; empty navigator properties file degrades to bare GO - the #524 edge case) kept as-is via the shared T2 `Apply Navigator From Properties`. Full Area-parity structural conversion (#528): 5 TCs incl. TC04 Find, per-TC Login/Logout, fixed `AUTOTEST_EXTERNAL_LOCATION`, properties-file-driven insert/update/verify, explicit filter wiring, zero inline DB-verify (grep-verified). Live 5/5, dryrun 847/847, robocop parity with Area (7 issues), self-clean 0 residual via fresh connection. _Row added by the reviewer at merge (R38); the owner 2026-08-26 standing rule makes Area the role model for navigator screens matching its layout._ |
| Field (Configuration > Assets > Commercial_Objects, OV-GM) | STRUCTURE (2026-08-26, Area role-model rule) | **OV-GM, not a plain-Bank reclassification** - mandatory single-dropdown Area navigator (value Offshore area via field_navigator.properties) kept as-is via the shared T2 `Apply Navigator From Properties`. Full Area-parity structural conversion (#529): 5 TCs incl. TC04 Find, per-TC Login/Logout, fixed `AUTOTEST_FIELD`, properties-file-driven insert/update/verify, explicit filter wiring, zero inline DB-verify (grep-verified). Live 5/5, dryrun 847/847, robocop parity with Area (7 issues), self-clean 0 residual via fresh connection. _Row added by the reviewer at merge (R38); the owner 2026-08-26 standing rule makes Area the role model for navigator screens matching its layout._ |
| Facility Class 1 (Configuration > Assets > Facility_Objects, OV-GM) | STRUCTURE (2026-08-26, Area role-model rule) | **OV-GM, not a plain-Bank reclassification** - mandatory 2-level Production Unit -> Area cascade (C:1/C:2 via facility_class_1_navigator.properties) kept as-is via the shared T2 `Apply Navigator From Properties`. Full Area-parity structural conversion (#530): 5 TCs incl. TC04 Find, per-TC Login/Logout, fixed `AUTOTEST_FC1`, properties-file-driven insert/update/verify, explicit filter wiring, zero inline DB-verify (grep-verified). Live 5/5, dryrun 847/847, robocop parity with Area (7 issues), self-clean 0 residual via fresh connection. _Row added by the reviewer at merge (R38); the owner 2026-08-26 standing rule makes Area the role model for navigator screens matching its layout._ |

## OV-GM Area-parity batch (2026-08-26) - 19 conversions, PRs #533-#552

| Screen | Explicit filter wired? / Status | Notes |
|---|---|---|
| Operator Route (OV-GM) | STRUCTURE (2026-08-26, Area role-model rule) | Full Area-parity conversion (#533): 5 TCs incl. TC04 Find, per-TC Login/Logout, fixed AUTOTEST code, properties-file-driven insert/update/verify, genuine navigator kept via shared `Apply Navigator From Properties`, zero inline DB-verify. Live 5/5 + fresh-connection self-clean + full-tree dryrun + robocop parity with Area per the PR body. NOT a plain-Bank reclassification - OV-GM stands. _Row added by the reviewer at merge (R38)._ |
| Price Rate (OV-GM) | STRUCTURE (2026-08-26, Area role-model rule) | Full Area-parity conversion (#534): 5 TCs incl. TC04 Find, per-TC Login/Logout, fixed AUTOTEST code, properties-file-driven insert/update/verify, genuine navigator kept via shared `Apply Navigator From Properties`, zero inline DB-verify. Live 5/5 + fresh-connection self-clean + full-tree dryrun + robocop parity with Area per the PR body. NOT a plain-Bank reclassification - OV-GM stands. _Row added by the reviewer at merge (R38)._ |
| Contract Capacity (OV-GM) | STRUCTURE (2026-08-26, Area role-model rule) | Full Area-parity conversion (#535): 5 TCs incl. TC04 Find, per-TC Login/Logout, fixed AUTOTEST code, properties-file-driven insert/update/verify, genuine navigator kept via shared `Apply Navigator From Properties`, zero inline DB-verify. Live 5/5 + fresh-connection self-clean + full-tree dryrun + robocop parity with Area per the PR body. NOT a plain-Bank reclassification - OV-GM stands. _Row added by the reviewer at merge (R38)._ |
| Price Object (OV-GM) | STRUCTURE (2026-08-26, Area role-model rule) | Full Area-parity conversion (#536): 5 TCs incl. TC04 Find, per-TC Login/Logout, fixed AUTOTEST code, properties-file-driven insert/update/verify, genuine navigator kept via shared `Apply Navigator From Properties`, zero inline DB-verify. Live 5/5 + fresh-connection self-clean + full-tree dryrun + robocop parity with Area per the PR body. NOT a plain-Bank reclassification - OV-GM stands. _Row added by the reviewer at merge (R38)._ |
| Storage (OV-GM) | STRUCTURE (2026-08-26, Area role-model rule) | Full Area-parity conversion (#537): 5 TCs incl. TC04 Find, per-TC Login/Logout, fixed AUTOTEST code, properties-file-driven insert/update/verify, genuine navigator kept via shared `Apply Navigator From Properties`, zero inline DB-verify. Live 5/5 + fresh-connection self-clean + full-tree dryrun + robocop parity with Area per the PR body. NOT a plain-Bank reclassification - OV-GM stands. _Row added by the reviewer at merge (R38)._ |
| Sub Area (OV-GM) | STRUCTURE (2026-08-26, Area role-model rule) | Full Area-parity conversion (#538): 5 TCs incl. TC04 Find, per-TC Login/Logout, fixed AUTOTEST code, properties-file-driven insert/update/verify, genuine navigator kept via shared `Apply Navigator From Properties`, zero inline DB-verify. Live 5/5 + fresh-connection self-clean + full-tree dryrun + robocop parity with Area per the PR body. NOT a plain-Bank reclassification - OV-GM stands. _Row added by the reviewer at merge (R38)._ |
| Well Hookup (OV-GM) | STRUCTURE (2026-08-26, Area role-model rule) | Full Area-parity conversion (#539): 5 TCs incl. TC04 Find, per-TC Login/Logout, fixed AUTOTEST code, properties-file-driven insert/update/verify, genuine navigator kept via shared `Apply Navigator From Properties`, zero inline DB-verify. Live 5/5 + fresh-connection self-clean + full-tree dryrun + robocop parity with Area per the PR body. NOT a plain-Bank reclassification - OV-GM stands. _Row added by the reviewer at merge (R38)._ |
| Well (OV-GM) | STRUCTURE (2026-08-26, Area role-model rule) | Full Area-parity conversion (#540): 5 TCs incl. TC04 Find, per-TC Login/Logout, fixed AUTOTEST code, properties-file-driven insert/update/verify, genuine navigator kept via shared `Apply Navigator From Properties`, zero inline DB-verify. Live 5/5 + fresh-connection self-clean + full-tree dryrun + robocop parity with Area per the PR body. NOT a plain-Bank reclassification - OV-GM stands. _Row added by the reviewer at merge (R38)._ |
| Collection Point (OV-GM) | STRUCTURE (2026-08-26, Area role-model rule) | Full Area-parity conversion (#541): 5 TCs incl. TC04 Find, per-TC Login/Logout, fixed AUTOTEST code, properties-file-driven insert/update/verify, genuine navigator kept via shared `Apply Navigator From Properties`, zero inline DB-verify. Live 5/5 + fresh-connection self-clean + full-tree dryrun + robocop parity with Area per the PR body. NOT a plain-Bank reclassification - OV-GM stands. _Row added by the reviewer at merge (R38)._ |
| Contract Area (OV-GM) | STRUCTURE (2026-08-26, Area role-model rule) | Full Area-parity conversion (#542): 5 TCs incl. TC04 Find, per-TC Login/Logout, fixed AUTOTEST code, properties-file-driven insert/update/verify, genuine navigator kept via shared `Apply Navigator From Properties`, zero inline DB-verify. Live 5/5 + fresh-connection self-clean + full-tree dryrun + robocop parity with Area per the PR body. NOT a plain-Bank reclassification - OV-GM stands. _Row added by the reviewer at merge (R38)._ |
| Well Hole (OV-GM) | STRUCTURE (2026-08-26, Area role-model rule) | Full Area-parity conversion (#543): 5 TCs incl. TC04 Find, per-TC Login/Logout, fixed AUTOTEST code, properties-file-driven insert/update/verify, genuine navigator kept via shared `Apply Navigator From Properties`, zero inline DB-verify. Live 5/5 + fresh-connection self-clean + full-tree dryrun + robocop parity with Area per the PR body. NOT a plain-Bank reclassification - OV-GM stands. _Row added by the reviewer at merge (R38)._ |
| Chemical Stream Hookup (OV-GM) | STRUCTURE (2026-08-26, Area role-model rule) | Full Area-parity conversion (#544): 5 TCs incl. TC04 Find, per-TC Login/Logout, fixed AUTOTEST code, properties-file-driven insert/update/verify, genuine navigator kept via shared `Apply Navigator From Properties`, zero inline DB-verify. Live 5/5 + fresh-connection self-clean + full-tree dryrun + robocop parity with Area per the PR body. NOT a plain-Bank reclassification - OV-GM stands. _Row added by the reviewer at merge (R38)._ |
| Chemical Stream (OV-GM) | STRUCTURE (2026-08-26, Area role-model rule) | Full Area-parity conversion (#545): 5 TCs incl. TC04 Find, per-TC Login/Logout, fixed AUTOTEST code, properties-file-driven insert/update/verify, genuine navigator kept via shared `Apply Navigator From Properties`, zero inline DB-verify. Live 5/5 + fresh-connection self-clean + full-tree dryrun + robocop parity with Area per the PR body. NOT a plain-Bank reclassification - OV-GM stands. _Row added by the reviewer at merge (R38)._ |
| Contract (OV-GM) | STRUCTURE (2026-08-26, Area role-model rule) | Full Area-parity conversion (#546): 5 TCs incl. TC04 Find, per-TC Login/Logout, fixed AUTOTEST code, properties-file-driven insert/update/verify, genuine navigator kept via shared `Apply Navigator From Properties`, zero inline DB-verify. Live 5/5 + fresh-connection self-clean + full-tree dryrun + robocop parity with Area per the PR body. NOT a plain-Bank reclassification - OV-GM stands. _Row added by the reviewer at merge (R38)._ |
| Shift (OV-GM) | STRUCTURE (2026-08-26, Area role-model rule) | Full Area-parity conversion (#547): 5 TCs incl. TC04 Find, per-TC Login/Logout, fixed AUTOTEST code, properties-file-driven insert/update/verify, genuine navigator kept via shared `Apply Navigator From Properties`, zero inline DB-verify. Live 5/5 + fresh-connection self-clean + full-tree dryrun + robocop parity with Area per the PR body. NOT a plain-Bank reclassification - OV-GM stands. _Row added by the reviewer at merge (R38)._ |
| Chemical Tank (OV-GM) | STRUCTURE (2026-08-26, Area role-model rule) | Full Area-parity conversion (#549): 5 TCs incl. TC04 Find, per-TC Login/Logout, fixed AUTOTEST code, properties-file-driven insert/update/verify, genuine navigator kept via shared `Apply Navigator From Properties`, zero inline DB-verify. Live 5/5 + fresh-connection self-clean + full-tree dryrun + robocop parity with Area per the PR body. NOT a plain-Bank reclassification - OV-GM stands. _Row added by the reviewer at merge (R38)._ |
| Chem Injection Point (Chemical Injection Point, OV-GM) | STRUCTURE (2026-08-26, Area role-model rule) | Full Area-parity conversion (#550): 5 TCs incl. TC04 Find, per-TC Login/Logout, fixed AUTOTEST code, properties-file-driven insert/update/verify, genuine navigator kept via shared `Apply Navigator From Properties`, zero inline DB-verify. Live 5/5 + fresh-connection self-clean + full-tree dryrun + robocop parity with Area per the PR body. NOT a plain-Bank reclassification - OV-GM stands. _Row added by the reviewer at merge (R38)._ |
| Production Separator (OV-GM) | STRUCTURE (2026-08-26, Area role-model rule) | Full Area-parity conversion (#551): 5 TCs incl. TC04 Find, per-TC Login/Logout, fixed AUTOTEST code, properties-file-driven insert/update/verify, genuine navigator kept via shared `Apply Navigator From Properties`, zero inline DB-verify. Live 5/5 + fresh-connection self-clean + full-tree dryrun + robocop parity with Area per the PR body. NOT a plain-Bank reclassification - OV-GM stands. _Row added by the reviewer at merge (R38)._ |
| Service (OV-GM) | STRUCTURE (2026-08-26, Area role-model rule) | Full Area-parity conversion (#552): 5 TCs incl. TC04 Find, per-TC Login/Logout, fixed AUTOTEST code, properties-file-driven insert/update/verify, genuine navigator kept via shared `Apply Navigator From Properties`, zero inline DB-verify. Live 5/5 + fresh-connection self-clean + full-tree dryrun + robocop parity with Area per the PR body. NOT a plain-Bank reclassification - OV-GM stands. _Row added by the reviewer at merge (R38)._ |

## Meter Area-parity conversion (2026-08-26)

| Screen | Explicit filter wired? / Status | Notes |
|---|---|---|
| Meter (Configuration > Assets > Dispatching_Objects, OV-GM + POPUP) | STRUCTURE (2026-08-26, Area role-model rule) | Full Area-parity conversion (PR #554) of the original golden-exemplar build: 5 TCs incl. TC04 Find, per-TC Login/Logout, fixed `AUTOTEST_METER`, properties-file-driven insert/update/verify, genuine Business Unit navigator via shared `Apply Navigator From Properties`, Delivery Point POPUP mechanism preserved as-is (generic T1 `Pick From EC Object Popup` - orthogonal to the navigator, the earlier "does not fit Area" note corrected on live evidence). Zero inline DB-verify. Live 5/5, dryrun 874/874, robocop parity with Area, probe + fixed-code self-clean 0 residual via fresh connections. _Row added by the reviewer at merge (R38)._ |

## Tract Area-parity conversion (2026-08-26 - corrects PR #555's original "does not fit" conclusion)

| Screen | Status | Notes |
|---|---|---|
| Tract (Configuration > Assets > Royalty_Objects, OV-GM, 2 nav groups) | STRUCTURE (2026-08-26, Area role-model rule) | PR #555 originally (wrongly) declined this conversion, reasoning the navigator's two separate `nav:form:G:` groups (G:0 Date, G:1 Unit Agreement) didn't fit Area's single-group cascade shape. Owner correction + live re-recon (2026-08-26) found G:0's Date field already carries a non-empty default on load (needs no fill at all) and G:1's Unit Agreement dropdown is the ONLY mandatory-and-empty nav field - structurally the SAME single-mandatory-dropdown shape as Area's own navigator, just at group 1/column 0 instead of group 0/column 1. Extended the shared T2 `Apply Navigator From Properties` (resources/manage_object.resource) with two new OPTIONAL, backward-compatible arguments (`${group}=0`, `${start_col}=1`) - confirmed via a full-tree dryrun (874/874 before and after) plus live 5/5 regression canaries on Area and Meter (both already callers of the keyword's old 2-arg form). Full 5-TC rebuild: per-TC Login/Logout, fixed `AUTOTEST_TRACT` (confirmed free live via a fresh oracledb connection), properties-file-driven insert/update/verify (`testdata/tract_{navigator,insert,update,form_verify,grid_verify}.properties` - Unit Agreement value identical in the navigator and insert files per the owner's explicit field-reuse rule), zero inline DB-verify. Re-confirmed live: Unit Agreement 1-4 all effective 2010-01-01 (fresh oracledb query on `OV_UNIT_AGR.OBJECT_START_DATE`), so Start Date >= that constraint holds; reused the prior driver's proven 2011-01-01. Live 5/5, dryrun 875/875, robocop parity with Area (7 issues each, same missing-test-doc type), DB self-clean 0 residual `AUTOTEST_TRACT` via a fresh connection. `docs/navigator-screens-not-matching-area.md`'s wrongly-added Tract row removed. |
## Property Area-parity conversion (2026-08-26)

| Screen | Explicit filter wired? / Status | Notes |
| Property (Configuration > Assets > Data_Mapping_Objects, SP.0059, OV-GM) | STRUCTURE (2026-08-26, Area role-model rule) | Full Area-parity conversion of the original 2026-08-02 build (4/4 RF + 8/8 Playwright): 5 TCs incl. TC04 Find, per-TC Login/Logout, fixed `AUTOTEST_PROPERTY` (confirmed free in `OV_PROPERTY` via fresh oracledb connection, before and after the live run), properties-file-driven insert/update/verify (`testdata/property_{insert,update,form_verify,grid_verify,navigator}.properties`). **Live recon (`Workplaces/property-area-pattern/recon_property_nav.py`, read-only, no Save/Insert/Delete)** found the navigator has 2 DOM groups - G:0 ("Date") and G:1 ("Business Unit") - but G:0's Date field already carries a non-empty default on load (`MandatoryCellStyleWhite`), so G:1's Business Unit dropdown at `C:0` (not the usual `C:1`) is the ONLY genuinely mandatory+empty field - exactly the single-group-needs-fill shape already proven to fit on Tract, so this screen fits Area's pattern despite the 2-group DOM shape. Business Unit dropdown confirmed live to hold 16 options incl. "Royalty Canada" (matches the prior driver's proven value). Because the mandatory dropdown lives at `G:1/C:0` rather than the shared keyword's assumed `G:0/C:1`, the shared T2 `Apply Navigator From Properties` (resources/manage_object.resource) was extended ADDITIVELY with optional `${group}=0`/`${start_col}=1` arguments (default values preserve every existing caller's behavior unchanged - Area/Well/Test Separator/Chemical Tank/Price Object/Meter all still omit them); Property calls it with `group=1 start_col=0`. Shared-file-change safety protocol followed: file backed up first, robocop parity confirmed (same issue categories, only the touched keyword's own LEN01 line-length count grew from 44/40 to 50/40), full-tree dryrun 878/878 (zero collisions), and a live regression canary re-run of Price Object's own suite (5/5 PASS, confirming no regression on the keyword's default-argument path). Zero inline DB-verify calls in property_iud.robot (grep-verified). Live 5/5, DB self-clean confirmed 0 residual `AUTOTEST%` rows via a fresh independent oracledb connection, filter keyword (`Find Property Row By Filter`) fired 15x via output.xml grep. Playwright driver `py/property_iud.py` left untouched (RF-only conversion). |
## Pipeline Segment Area-parity conversion (2026-08-26)

| Pipeline Segment (Configuration > Assets > Dispatching_Objects, OV-GM) | STRUCTURE (2026-08-26, Area role-model rule) | Full Area-parity conversion of the original 4/4 build: 5 TCs incl. TC04 Find, per-TC Login/Logout, fixed `AUTOTEST_PIPELINE_SEGMENT` (confirmed free in `OV_PIPELINE_SEGMENT` via fresh oracledb connection), properties-file-driven insert/update/verify, genuine single-dropdown Business Unit navigator kept via shared `Apply Navigator From Properties` (re-confirmed live: single mandatory dropdown at `nav:form:G:0:R:1:C:1:dd`, same shape as Area's own Production Unit). Zero inline DB-verify. Live 5/5, dryrun 878/878, robocop parity with Area (7 issues, same VAR02+DOC02 kind/count), filter fired 15x via output.xml grep, DB self-clean 0 residual `AUTOTEST%` rows via a fresh independent connection. |

## Contract Inventory Area-parity conversion (2026-08-26)

| Contract Inventory (Configuration > Assets > Contract_Objects, CO.2054, OV-GM) | STRUCTURE (2026-08-26, Area role-model rule) | Full Area-parity conversion of the original 2026-08-02 build: 5 TCs incl. TC04 Find, per-TC Login/Logout, fixed `AUTOTEST_CONTRACT_INVENTORY` (confirmed free in `OV_CONTRACT_INVENTORY` via fresh oracledb connection, before and after the live run), properties-file-driven insert/update/verify (`testdata/contract_inventory_{insert,update,form_verify,grid_verify,navigator}.properties`), navigator kept via shared T2 `Apply Navigator From Properties` with ZERO shared-file changes. **Live recon correction**: the pre-existing registry note "Business Unit -> Contract Area cascade" reads as 2 mandatory levels; live mandatory-yellow DOM checks (`Workplaces/contract-inventory-area-pattern/recon_contract_inventory_mandatory_2026_08_26.py`, `recon_contract_inventory_cascade_2026_08_26.py`) found ONLY Business Unit (C:1) is genuinely mandatory-yellow+empty - Contract Area (C:2) and Contract (C:3) stay white/optional even after C:1 is filled. Kept both Business Unit and Contract Area filled anyway for behavioral parity with the already-proven prior scope (same-row, increasing-column shape, no shared-file change needed). Zero inline DB-verify calls (grep-verified). Live 5/5, full-tree dryrun 878/878 (zero collisions), robocop 7 issues (2 VAR02 + 5 DOC02) - exact parity with Facility Class 1's own baseline, DB self-clean confirmed 0 residual `AUTOTEST%` rows via a fresh independent oracledb connection, filter keyword fired 26x via output.xml grep. Playwright driver `py/contract_inventory_iud.py` left untouched (RF-only conversion). |


| Property (Configuration > Assets > Data_Mapping_Objects, SP.0059, OV-GM) | STRUCTURE (2026-08-26, Area role-model rule) | Full Area-parity conversion of the original 2026-08-02 build (4/4 RF + 8/8 Playwright): 5 TCs incl. TC04 Find, per-TC Login/Logout, fixed `AUTOTEST_PROPERTY` (confirmed free in `OV_PROPERTY` via fresh oracledb connection, before and after the live run), properties-file-driven insert/update/verify (`testdata/property_{insert,update,form_verify,grid_verify,navigator}.properties`). **Live recon (`Workplaces/property-area-pattern/recon_property_nav.py`, read-only, no Save/Insert/Delete)** found the navigator has 2 DOM groups - G:0 ("Date") and G:1 ("Business Unit") - but G:0's Date field already carries a non-empty default on load (`MandatoryCellStyleWhite`), so G:1's Business Unit dropdown at `C:0` (not the usual `C:1`) is the ONLY genuinely mandatory+empty field - exactly the single-group-needs-fill shape already proven to fit on Tract, so this screen fits Area's pattern despite the 2-group DOM shape. Business Unit dropdown confirmed live to hold 16 options incl. "Royalty Canada" (matches the prior driver's proven value). Because the mandatory dropdown lives at `G:1/C:0` rather than the shared keyword's assumed `G:0/C:1`, the shared T2 `Apply Navigator From Properties` (resources/manage_object.resource) was extended ADDITIVELY with optional `${group}=0`/`${start_col}=1` arguments (default values preserve every existing caller's behavior unchanged - Area/Well/Test Separator/Chemical Tank/Price Object/Meter/Pipeline Segment/Contract Inventory all still omit them); Property calls it with `group=1 start_col=0`. Shared-file-change safety protocol followed: file backed up first (`Workplaces/property-area-pattern/manage_object.resource.bak_before_property`), robocop parity confirmed (same issue categories, only the touched keyword's own LEN01 line-length count grew from 44/40 to 50/40), full-tree dryrun 878/878 (zero collisions), and a live regression canary re-run of Price Object's own suite (5/5 PASS, confirming no regression on the keyword's default-argument path). Zero inline DB-verify calls in property_iud.robot (grep-verified). Live 5/5, DB self-clean confirmed 0 residual `AUTOTEST%` rows via a fresh independent oracledb connection, filter keyword (`Find Property Row By Filter`) fired 15x via output.xml grep. Playwright driver `py/property_iud.py` left untouched (RF-only conversion). |
## Transport Zone Area-parity conversion (2026-08-26)

| Transport Zone (Configuration > Assets > Dispatching_Objects, OV-GM) | STRUCTURE (2026-08-26, Area role-model rule) | Full Area-parity conversion of the older 4-TC/suite-login/generated-code/inline-DB-verify build: 5 TCs incl. TC04 Find, per-TC Login/Logout, fixed `AUTOTEST_TRANSPORT_ZONE` (confirmed free via a fresh oracledb connection), properties-file-driven insert/update/verify, genuine Business Unit navigator (nav:form:G:0, C:1) via shared `Apply Navigator From Properties` - live recon confirmed C:0 (Date) is already defaulted, C:2 is an optional non-mandatory 2nd dropdown, only C:1 is genuinely mandatory+empty (single-dropdown/same-row-cascade shape matching Area). Zero inline DB-verify. Live 5/5, dryrun 875/875, robocop parity with Area (10 issues: 7 on changed screen files = exact parity + 3 pre-existing unrelated credentials.py findings), DB self-clean 0 residual via a fresh independent oracledb connection. |

## Lifting Account Area-parity conversion (2026-08-27)

| Lifting Account (Configuration > Assets > Transport_Objects, CO.2004, OV-GM, 4-level nav) | STRUCTURE (2026-08-27, Area role-model rule) | An earlier UNVERIFIED chat-level classification flagged this screen's navigator as "two separate rows" (disqualifying). Live per-field DOM recon (this session, read-only, no Save/Insert/Delete) confirmed the navigator is actually ONE group (`nav:form:G:0`, a single fieldset) - the mandatory cascade is rendered across TWO grid rows within that one group: R:1 C:1..C:3 (Production Unit/Area/Facility Class 1) then R:3 C:0 (Storage); all 4 dd spans carry `{mandatory:true} MandatoryCellStyle` (genuinely empty). This is NOT the "multiple independent groups" shape (that would be separate G:1/G:2 fieldsets, per Tract's precedent) - it fits Area's pattern. Full 5-TC rebuild: per-TC Login/Logout (`LIFTING_ACCOUNT_EC_USER`/`PASS`), fixed test code `AUTOTEST_LA_001` (reused from the pre-existing driver, confirmed free via a fresh oracledb connection on `OV_LIFTING_ACCOUNT` before AND after the live run), properties-file-driven insert/update/verify (`testdata/lifting_account_{navigator,insert,update,form_verify,grid_verify}.properties` - nav scope and Storage Name value reused VERBATIM from the pre-existing driver, `py/lifting_account_iud.py` commit 6e88e371: P1 Production Unit -> P1 Area -> P1 Facility 1 -> Storage P1_CRUDE_STOR), zero inline DB-verify. Extended the shared T2 `Apply Navigator From Properties` (resources/manage_object.resource) with three new OPTIONAL, backward-compatible arguments (`${row2_from}=${EMPTY}`, `${row2}=1`, `${row2_start_col}=1`) to span the row break within the single group in ONE call + ONE final GO - the first extension of this keyword for a MULTI-ROW single-group shape (distinct from Tract/Property's single-row-different-group-index shape). Live 5/5 (one transient autocomplete-panel pointer-event flake on the first attempt at TC03, re-ran clean 5/5 - not a suite defect), full-tree dryrun 881/881 (zero collisions), robocop parity with Area's own 7-issue baseline on the touched `.robot`/page-object files (`manage_object.resource` gained new ARG07/LEN07/LEN01 instances on the SAME already-flagged `Apply Navigator From Properties` keyword - baseline already had LEN07/ARG07 on sibling keywords + LEN01 on this keyword at 65/40, not a new issue category). DB self-clean confirmed 0 residual `AUTOTEST%` rows via a fresh independent oracledb connection. Playwright driver `py/lifting_account_iud.py` left untouched (RF-only structural conversion). |


## Pilot Area-parity conversion (2026-08-26)

| Pilot (Configuration > Assets > Transport_Objects, CO.2079, OV-GM) | STRUCTURE (2026-08-26, Area role-model rule) | Full Area-parity conversion (feature/pilot-area-pattern, PR #560) of the existing 2026-07-31 build: 5 TCs incl. TC04 Find, per-TC Login/Logout, fixed `AUTOTEST_PILOT` (confirmed free in OV_PILOT via a fresh oracledb connection), properties-file-driven insert/update/verify, genuine 3-level Production Unit -> Area -> Facility Class 1 navigator cascade confirmed live as ONE group (`nav:form:G:0`, same-row C:1/C:2/C:3, all MandatoryCellStyle - not a per-field-groups non-fit) via shared `Apply Navigator From Properties`. Op Production Unit on the insert form kept as `__FIRST__` (independent value domain from the navigator's Production Unit, per the pre-existing driver's own comment) - a documented exception to the generic field-reuse rule for this pair. Zero inline DB-verify calls. Live 5/5, full-tree dryrun 875/875 (isolated worktree), robocop parity with Area (7 issues, same kind/count), fresh-connection self-clean 0 residual `AUTOTEST_PILOT` rows, filter keyword fired 15x per output.xml. |

## How to update this doc

When a new batch of screens from the "Batch tracking" table gets converted, flip each to
"✅ DONE (date, BatchN)" with its PR number. If a screen turns out to have an
undocumented navigator requirement once recon'd live (contradicts its "nav-free" premise
here), move it to the "Excluded" table instead of force-fitting it — this happened for
zero screens in Batch 2, but is a real possibility for any future batch. Append-only in
spirit for the "Excluded"/"Unclear" tables; the "Batch tracking" table's rows should only
ever flip status, not be deleted.

