# Universal Screen Engine — Open Items Tracker

Companion to `universal_screen_engine_design.md`. Phases 1–4 of that design doc's phased plan
(section 7) are complete and merged — this file tracks the specific, named items that were
deliberately documented as still-open rather than fixed, so they don't get lost or silently
re-discovered from scratch later.

**Convention:** each row's Status starts `[ ] Not started`. Update to `[x] Done` only once the
action column has actually been carried out and verified — never pre-marked, per this project's
own no-guessing standard (see `CLAUDE.md`).

| # | Item | Defect | What needs to be done | Status |
|---|---|---|---|---|
| 1 | `open_screen()` navigation-timing flakiness | ~~Calling `open_screen()` a 2nd+ time in one page session intermittently times out waiting for the menu search box (resolves as `hidden`).~~ **ROOT CAUSE FOUND (self-inflicted, not a race):** `open_screen()`'s own trailing action clicks `screenToolbar:form:minmaxMenu` ("expand to full page"), which collapses `#ec-menu-container_0` - the very panel the search box lives inside - at the END of every call. A 2nd call then finds the box genuinely `display:none` (confirmed via live DOM instrumentation: `offsetParent` null, ancestor class `hidden`), not covered by a stray panel. | Fixed: at the START of `open_screen()`, if the search box isn't visible, click the same `minmaxMenu` toggle to re-expand the panel before proceeding. **Test evidence:** (1) the exact 2-screen sequence that failed repeatedly this session (Property → Project Properties) now passes; (2) a harder 5-screen chain in one session (Property → Project Properties → Property → Report Reference → Bank) - all 5 `open_screen()` calls succeed, fields resolve correctly on each. PR: fix/engine-open-screen-timing. | [x] Done |
| 2 | Reviewer's low-priority advisory — label-shadowing ordering flip | The `_refresh_field_map()` fix (Issue #361 item 1) incidentally changed form-vs-form (`objectForm` vs `updateAttributes`) label-collision ordering from last-wins to first-wins. Currently believed safe (both forms are never simultaneously present in one DOM scan today) but never positively re-confirmed with a full live cycle. | **Verified live, full cycle:** ran Insert → Update → Delete on Bank through the engine (`AUTOTEST_BANK_I2`) - all 3 Save actions succeeded (Insert OK, row re-selected + Update OK, row re-selected + End Date=Start Date Delete OK). **DB-verified final state:** `OV_BANK` = 0 rows for the code (deleted), and no residual `BANK_VERSION` row at all (fully removed, not just a zero-window row) - 0 residual. Confirms the label-shadowing fix's ordering change has NOT regressed Bank's baseline I-U-D behavior. Branch: test/engine-bank-full-iud-verify. | [x] Done |
| 3 | Deferment Group screen unreachable via menu search | ~~Initially suspected an EC environment/product defect~~ - **ACTUAL ROOT CAUSE (owner-supplied screenshot + DB-confirmed): role-based access control, not a bug.** `TV_T_BASIS_ACCESS` shows the screen object (`OBJECT_ID=1087`, `/com.ec.frmw.co.screens/manage_object_nav/CLASS_NAME/DEFERMENT_GROUP`) has `LEVEL_ID=0` ("No access") for ALL 5 roles configured against it - matching the owner's Object Maintenance screenshot exactly. That's why it's invisible via BOTH menu search AND treeview browsing regardless of which role `sysadmin` maps to: role access, not a search-index/treeview-registration problem. Class config (`CLASS_CNFG`) and treeview registration (`disabled: false`) were both checked and are fine - the missing layer was role access, which was never checked before this. | Not a code fix - this is a live-sandbox permissions/security config, out of scope to change without explicit authorization (a role-access grant is a real, consequential change to the local sandbox's security config, not a read-only investigation). Decision needed from the owner: is this screen SUPPOSED to be accessible (in which case grant an appropriate role access via the Object Maintenance screen), or is "No access" intentional (in which case this was never actually a defect at all - correctly configured, just not meant to be used)? | [x] Done (investigation) |
| 4 | 3 structural capability gaps in the generators | None of the 3 generators (`gen_ov_iud_bundle.py`, `gen_ovgm_iud_bundle.py`, `gen_tv_iud_bundle.py`) structurally model: (a) fields that behave as server-side type-to-search autocompletes rather than a full-list dropdown or a true popup dialog, (b) screens whose valid Insert values depend on other master-data screens being populated first (multi-level FK-scoped dependency chains), (c) Save-time validation rules spanning multiple fields that aren't visible as a simple per-field mandatory-yellow flag. This is exactly why pilot-3-shaped screens (Phase 4, section 25) are a named exception to the engine's default-path status, not a covered case. | **PARTIAL - gap (a) implemented, live proof-of-concept blocked on EC domain knowledge, not a code defect.** `Engine.select()` now unifies plain full-list dropdowns and type-to-search autocompletes under one call (click first, fall back to typing the search value if no option ever renders) - regression-checked clean against normal dropdowns (Bank Country, Property Business Unit, both `__FIRST__`, unaffected). Live proof-of-concept against the actual target field (PDMS Target Property, real existing data `SS1_CA`/"SS1 CA") FAILED - typing "SS1" still found no match, for reasons not understood (the field's `PopupDependency` scoping logic - date/dataset context, minimum search length, code-vs-name matching - is EC domain knowledge not yet mastered). **Owner instruction: stop retrying blindly on this specific screen - not a coding defect, a data-dependency/EC-knowledge gap. Revisit once EC mastery on this screen's scoping rules improves.** Gaps (b) and (c) not started at all. **Gap (b) groundwork done 2026-08-16, then explicitly parked** - live DB investigation (real queries, not guesses) confirmed `CLASS_REL_CNFG` (filtered to `RELATION_TYPE='OBJECT'`) gives a clean, structured parent-class dependency list, and `CLASS_ATTR_PROPERTY_CNFG`'s `PopupDependency` property gives the cross-field scoping rule on top - both verified to exactly reproduce the real Pilot 3 chain (`COST_MAPPING` -> `CONTRACT_AREA`/`PROJECT`/`REPORT_REFERENCE`) when queried directly. "Which screen builds class X" is separately already solvable via the existing `DeepDiveLearnings/ec-screens/notes/*.md` corpus (grep by class name in the URL). **Deliberately NOT built into a generator feature yet** - only ONE real screen (Pilot 3) has ever been traced end-to-end with this approach; building a generic detector off a single validated case would be exactly the "generalize from one instance" mistake this project's own standard warns against (see the Chemical Stream item 6 correction). Revisit once 2-3 more real cross-screen dependency chains have been traced this same way, to confirm the pattern actually generalizes rather than assuming it does. Also confirmed as a new tool for this kind of investigation: the EC app server's own log is reachable via `docker logs` on the local sandbox's `ec-app` container (never cloud-hosted envs - see memory `feedback_docker_server_log_local_only`). **Gap (c) CLOSED 2026-08-16 (owner classification, not built) - split into two distinct items rather than left as one ambiguous "not started" line:** (1) **EXCLUDED MATTER** - the 2 concrete instances found ("Either Project or Property must be chosen" and the Mapping-Type-conditional Reference requirement on Project Data Mapping Setup, both confirmed live in `CLASS_TRIGGER_ACTN_CNFG.DB_SQL_SYNTAX` as real PL/SQL `RAISE_APPLICATION_ERROR` triggers) are this SCREEN's own business rules, not an EC framework/automation gap - any change to how they behave is a product enhancement request for the EC product team, out of this engine's scope entirely. (2) **EC FRAMEWORK DEFECT (real, tracked, not built)** - the underlying general capability - the engine/generators having no way to detect or model a cross-field Save-time validation rule ahead of time, only discover it by hitting the Save error - is a legitimate framework-level limitation, owner-confirmed as worth tracking as its own item rather than closed silently. Real evidence a detection mechanism is at least feasible: `CLASS_TRIGGER_ACTN_CNFG` stores the literal trigger PL/SQL (including the exact error message text) per class, queryable the same way gap (b)'s `CLASS_REL_CNFG`/`CLASS_ATTR_PROPERTY_CNFG` approach worked - but this has had exactly ONE real screen's rules inspected so far (same "don't generalize from one instance" caution as gap (b)), so it is NOT built, only identified as a real, feasible-looking gap for a future pass. | [x] Gap (a) done; gap (b) parked (see notes); gap (c) closed - excluded matter + framework defect item recorded, not built |
| 5 | Click-stall bug (Price Object / Service / Contract Capacity / Chemical Stream) | ~~Dropdown-panel click stalls specifically in the row-select-derived `updateAttributes` form context~~ - **REAL ROOT CAUSE (owner-supplied domain knowledge): a self-inflicted test-pacing artifact, not an EC defect, CONFIRMED INDIVIDUALLY ON ALL 3 SCREENS THAT REPRODUCED IT.** Different dropdown fields query different underlying tables at genuinely different speeds. Every prior investigation script clicked each dropdown then moved to the next after only ~100ms, interrupting a still-loading field's query. Owner explicitly required real per-screen verification, not inference from a similar symptom - each screen below was independently traced (real `OV_*` row → FK chain, per owner's guided method, never a guessed navigator combination) and tested both ways. | **Price Object**: rushed pacing 9/11 stalled → proper pacing (~1.5s buffer) 0/11 stalled. **Service**: 8/9 → 0/9. **Contract Capacity**: 3/5 → 0/5 (needed the FULL 3-level chain BU→Contract Area→Contract; an initial single-level guess found an empty grid). **3 of 4 screens: CONFIRMED, same pacing-artifact cause, real before/after evidence each.** **Chemical Stream: DID NOT reproduce** - 0/21 stalled even under the same rushed pacing that broke the other 3; the proper-pacing re-run then hit an unrelated leftover confirmation-modal blocking the next click (a different, separate script-state issue, not tested to a clean result). Left honestly as inconclusive rather than forced to match the other 3 - closing this item on the 3 confirmed screens; Chemical Stream would need its own separate investigation if revisited (not urgent - the underlying methodology lesson is already learned and saved to memory `feedback_buffer_time_field_by_field`). | [x] Done - 3/4 screens confirmed pacing artifact; Chemical Stream inconclusive (separate, unrelated modal issue) |
| 6 | Chemical Stream - click-stall inconclusive + unrelated re-navigation modal bug (split out from item 5, per Issue #376 item 4) | Two loose, unresolved threads specific to Chemical Stream that item 5 closed the OTHER 3 screens on but left this one open: (a) the pacing-artifact hypothesis proven on Price Object/Service/Contract Capacity did NOT reproduce here (0/21 stalled even under rushed pacing) - the real cause of Chemical Stream's original stall symptom (if it ever genuinely had one, vs. a mis-observation) is still unknown; (b) a SEPARATE, unrelated bug was hit while re-testing: `#confirmationForm:confirmation_modal` intercepts pointer events on a row-click retry after re-navigating to the screen without resetting some dirty-form state - never traced to a root cause. | **Closed 2026-08-16.** (a) Re-tested via the real production `Engine.select()` (already properly paced) on a fresh page load, all 21 dropdowns on the `updateAttributes` form (structural recon first corrected a stale KB fact: 36 fields/21 dropdowns, not 2) - **0/21 stalled**, confirms and reinforces the original finding via the real engine. No stall exists on this screen under any tested condition. (b) Root-caused: EC's own genuine "Unsaved Changes" dialog (buttons `confirmationForm:yesbtn/nobtn/cancelbtn` = YES/NO/CANCEL), triggered when a dirty form is abandoned via `open_screen()` OR `select_row()` (confirmed live it fires on both, not just navigation - a first fix inside `open_screen()` alone proved insufficient). **Fixed** by centralizing dismissal inside `universal_classifier.ajax()` (called by nearly every state-changing action), always clicking NO per owner decision (default stays "always Save before switching screens"; NO only exercised when a caller deliberately abandons unsaved state). `engine_canary.py` re-run after the change - Bank + Language both ALL PASS, no regression. Full writeup: `screens/Configuration/Assets/Chemical_Objects/Chemical_Stream/JOURNAL.md` (2026-08-16 entry). | [x] Done |
| 7 | Pilot 3 (Project Data Mapping Setup) has no packaging decision recorded (Issue #385 item 2) | PR #379 correctly held Financial Item Definition/Template's packaging in scope while deferring Pilot 3 (Project Data Mapping Setup) "for a separate scope decision" - but that decision was never recorded anywhere as its own tracked item. Section 24 already proved the screen's hardest blocker has a working fix: the UPDATE-on-popup-fields issue (`Reference`/`REPORT_REF_ID` blanking on row-select) is resolved by a known, DB-verified workaround (re-supply the field's value before Save, same gesture as Insert) - so the screen is NOT permanently blocked, just never packaged. | **Closed 2026-08-16 - owner decision (a): packaged.** Full engine-only bundle built under `screens/Configuration/Assets/Data_Mapping_Objects/Project_Data_Mapping_Setup/` (SOW/README/JOURNAL/CHECKLIST/VERIFY-REPORT/playwright delegator/investigation/evidence), registry + scorecard rows added. Closed the gap the original Pilot 3 session left open: UPDATE was never proven there - this pass proves it live, with the section 24 Reference-field fix applied explicitly. 2 real date-effectivity bugs found and fixed via real DB queries during packaging (Target Property and Reference both scope by the row's own Start Date against each dependency's own effective date - not previously documented at this level of detail). Also produced a genuine engine improvement along the way (lazy dropdown-vs-popup classification, shipped as its own separate PR since `engine.py` is shared code) - not bundled into this screen's packaging. | [x] Done |
