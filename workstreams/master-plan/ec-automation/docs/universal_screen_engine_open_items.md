# Universal Screen Engine — Open Items Tracker

Companion to `universal_screen_engine_design.md`. Phases 1–4 of that design doc's phased plan
(section 7) are complete and merged — this file tracks the specific, named items that were
deliberately documented as still-open rather than fixed, so they don't get lost or silently
re-discovered from scratch later.

**Convention:** each row's Status starts `[ ] Not started`. Update to `[x] Done` only once the
action column has actually been carried out and verified — never pre-marked, per this project's
own no-guessing standard (see `CLAUDE.md`).

| # | Item | Defect | What needs to be done | Status |
|---|---|---|---|---|
| 1 | `open_screen()` navigation-timing flakiness | ~~Calling `open_screen()` a 2nd+ time in one page session intermittently times out waiting for the menu search box (resolves as `hidden`).~~ **ROOT CAUSE FOUND (self-inflicted, not a race):** `open_screen()`'s own trailing action clicks `screenToolbar:form:minmaxMenu` ("expand to full page"), which collapses `#ec-menu-container_0` - the very panel the search box lives inside - at the END of every call. A 2nd call then finds the box genuinely `display:none` (confirmed via live DOM instrumentation: `offsetParent` null, ancestor class `hidden`), not covered by a stray panel. | Fixed: at the START of `open_screen()`, if the search box isn't visible, click the same `minmaxMenu` toggle to re-expand the panel before proceeding. **Test evidence:** (1) the exact 2-screen sequence that failed repeatedly this session (Property → Project Properties) now passes; (2) a harder 5-screen chain in one session (Property → Project Properties → Property → Report Reference → Bank) - all 5 `open_screen()` calls succeed, fields resolve correctly on each. PR: fix/engine-open-screen-timing. | [x] Done |
| 2 | Reviewer's low-priority advisory — label-shadowing ordering flip | The `_refresh_field_map()` fix (Issue #361 item 1) incidentally changed form-vs-form (`objectForm` vs `updateAttributes`) label-collision ordering from last-wins to first-wins. Currently believed safe (both forms are never simultaneously present in one DOM scan today) but never positively re-confirmed with a full live cycle. | **Verified live, full cycle:** ran Insert → Update → Delete on Bank through the engine (`AUTOTEST_BANK_I2`) - all 3 Save actions succeeded (Insert OK, row re-selected + Update OK, row re-selected + End Date=Start Date Delete OK). **DB-verified final state:** `OV_BANK` = 0 rows for the code (deleted), and no residual `BANK_VERSION` row at all (fully removed, not just a zero-window row) - 0 residual. Confirms the label-shadowing fix's ordering change has NOT regressed Bank's baseline I-U-D behavior. Branch: test/engine-bank-full-iud-verify. | [x] Done |
| 3 | Deferment Group screen unreachable via menu search | ~~Initially suspected an EC environment/product defect~~ - **ACTUAL ROOT CAUSE (owner-supplied screenshot + DB-confirmed): role-based access control, not a bug.** `TV_T_BASIS_ACCESS` shows the screen object (`OBJECT_ID=1087`, `/com.ec.frmw.co.screens/manage_object_nav/CLASS_NAME/DEFERMENT_GROUP`) has `LEVEL_ID=0` ("No access") for ALL 5 roles configured against it - matching the owner's Object Maintenance screenshot exactly. That's why it's invisible via BOTH menu search AND treeview browsing regardless of which role `sysadmin` maps to: role access, not a search-index/treeview-registration problem. Class config (`CLASS_CNFG`) and treeview registration (`disabled: false`) were both checked and are fine - the missing layer was role access, which was never checked before this. | Not a code fix - this is a live-sandbox permissions/security config, out of scope to change without explicit authorization (a role-access grant is a real, consequential change to the local sandbox's security config, not a read-only investigation). Decision needed from the owner: is this screen SUPPOSED to be accessible (in which case grant an appropriate role access via the Object Maintenance screen), or is "No access" intentional (in which case this was never actually a defect at all - correctly configured, just not meant to be used)? | [x] Done (investigation) |
| 4 | 3 structural capability gaps in the generators | None of the 3 generators (`gen_ov_iud_bundle.py`, `gen_ovgm_iud_bundle.py`, `gen_tv_iud_bundle.py`) structurally model: (a) fields that behave as server-side type-to-search autocompletes rather than a full-list dropdown or a true popup dialog, (b) screens whose valid Insert values depend on other master-data screens being populated first (multi-level FK-scoped dependency chains), (c) Save-time validation rules spanning multiple fields that aren't visible as a simple per-field mandatory-yellow flag. This is exactly why pilot-3-shaped screens (Phase 4, section 25) are a named exception to the engine's default-path status, not a covered case. | **PARTIAL - gap (a) implemented, live proof-of-concept blocked on EC domain knowledge, not a code defect.** `Engine.select()` now unifies plain full-list dropdowns and type-to-search autocompletes under one call (click first, fall back to typing the search value if no option ever renders) - regression-checked clean against normal dropdowns (Bank Country, Property Business Unit, both `__FIRST__`, unaffected). Live proof-of-concept against the actual target field (PDMS Target Property, real existing data `SS1_CA`/"SS1 CA") FAILED - typing "SS1" still found no match, for reasons not understood (the field's `PopupDependency` scoping logic - date/dataset context, minimum search length, code-vs-name matching - is EC domain knowledge not yet mastered). **Owner instruction: stop retrying blindly on this specific screen - not a coding defect, a data-dependency/EC-knowledge gap. Revisit once EC mastery on this screen's scoping rules improves.** Gaps (b) and (c) not started at all. | [ ] Partial - see notes |
| 5 | Click-stall bug (Price Object / Service / Contract Capacity / Chemical Stream) | ~~Dropdown-panel click stalls specifically in the row-select-derived `updateAttributes` form context~~ - **REAL ROOT CAUSE FOUND (owner-supplied domain knowledge): a self-inflicted test-pacing artifact, not an EC defect.** Different dropdown fields query different underlying tables at genuinely different speeds - some simple/fast, some complex/slow. My own investigation scripts clicked each dropdown then pressed Escape after only ~100ms before moving to the next field, assuming uniform speed - interrupting a still-loading field's query and leaving the page in a state that then blocked every subsequent click. Live-reproduced BOTH ways on Price Object using a guaranteed-valid data chain (`SS1 BU` → `SS1 CA` → `SS1_Contract_A`, traced top-down from a real `OV_PRICE_OBJECT` row per owner's guidance, not guessed navigator combinations): with the rushed ~100ms pacing, **9/11 dropdowns stalled** (exact same symptom as the 4 prior "unexplained" occurrences); with a proper ~1.5s buffer per field allowing each query to actually finish, **0/11 stalled** - same screen, same data, same fields, pacing was the only variable changed. | **Confirmed on Price Object with a controlled before/after comparison** (9/11 → 0/11, single variable changed). Service, Contract Capacity, and Chemical Stream were never individually re-tested with proper pacing - very likely the same root cause given the identical symptom, but not yet re-verified screen-by-screen. If revisited: any live automation sweeping multiple dropdown fields back-to-back must wait for each field's own query to genuinely finish (not a fixed short delay) before moving to the next - see `feedback_buffer_time_field_by_field` memory. | [x] Done (Price Object); other 3 screens not individually re-verified |
